# SECRETS SCAN allowlist (scripts/check-secrets.mjs, plan item 38).
# One line per known false positive: <rule> <fingerprint> <path>  # reason (required).
# The fingerprint is a 12-hex sha256 prefix of the match, printed by a failing scan; the value never is.
# Rotate a real credential; never allowlist one.
generic-assigned-secret b90951f82c90 scripts/line-history/pinnacle_capture.py  # Pinnacle's public guest key (guest.api.arcadia.pinnacle.com), shipped in pinnacle.com's own web client; not a personal credential
generic-assigned-secret 5ca53493e48f server/services/book-feeds.js  # same public Pinnacle guest key as above
