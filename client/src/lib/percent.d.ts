/** Types for `percent.js`. Hand-written: strict is on and allowJs is off. */
export declare function percentText(
  value: number | null | undefined,
  precision?: number,
  options?: { neverCertain?: boolean }
): string | null;
