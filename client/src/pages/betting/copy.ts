/**
 * Shared copy for the NFL betting desk.
 *
 * One fact — the model hasn't beaten the real betting lines yet, so nothing
 * here risks real money — used to get restated in four different vocabularies
 * across the desk (the hero banner, the Board sidebar, the Engine Gates tab,
 * and the Engine Diagnostics tab), which read as four different claims to
 * anyone comparing them. This is the one canonical sentence; every one of
 * those four spots renders it verbatim, with its own state-specific technical
 * detail directly underneath, not replacing it.
 */
export const NOT_PROVEN_MESSAGE =
  "This model hasn't beaten the real betting lines yet, so no real money is at risk — everything below is practice, tracked so we'll know the moment that changes.";
