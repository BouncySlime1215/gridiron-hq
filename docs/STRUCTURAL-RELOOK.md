# Structural relook — earlier builds vs later evidence — 2026-09-18

Read-only audit of everything built since 2026-09-15, checked against the evidence that landed since (the model-chain audit, the provenance audit, the manager-data pipeline, the play-chance rebuild, the Sleeper history crawl). Rule it serves: section 00 part A2, "constantly be auditing... if you build something but later notice something, properly go back." Ranked by cost to Nick in the next 3 weeks. Actions are routed into the step named in the last column; none of this reopens a step already closed without a named action.

Audit complete. All paths below are absolute.

## Structural relook — builds since 09-15 vs. what we have learned since

Ranked by how much it costs Nick in the next 3 weeks.

| # | Item (built when) | What later evidence says | Verdict | Action → step |
|---|---|---|---|---|
| 1 | **Weekly ingestion assumed by every in-season model** — ROS (`491acc8`), early-week blend (`5757dc4`), play-chance role layer (`49174d4`, `f60c0d2`), talk-vs-model (`4b76a5d`), refresh loop (`986449c`, `cf6ae18`) | Nothing ingests a finalized week. `nfl_model_growth` (usage+snaps) and `ffopportunity` (xFP) are scheduler-only jobs (`/Users/nick_matta/Documents/GitHub/gridiron-hq/server/services/scheduler.js:934,1012`) and are **not** in `FANTASY_LIVE_JOBS` (`/Users/nick_matta/Documents/GitHub/gridiron-hq/scripts/refresh-live-data.mjs:42-58`), while the server runs `SCHEDULER_DISABLED=1`. DB: `player_week_usage` max week 2026 = **1**, snaps = 1, xFP = 1 (338 rows), `sync_log` last `ffopportunity` 09-17 18:59. The role layer's "missed last game" gap is counted from `player_week_usage` team games (`/Users/nick_matta/Documents/GitHub/gridiron-hq/server/services/contingency.js:320-333`), so **in week 3 a player who sits out week 2 reads gap 0 = healthy starter (~0.95)** — the #1 lever in TARGET-SPEC §6 silently inverts. Settlement is already stuck: `weekly_prediction_snapshots` 1,183 rows, 0 settled | **re-architect** | Put `nfl_model_growth` + `ffopportunity` on the refresh-loop allowlist with a loud staleness gate ("latest completed NFL week not ingested → ERROR, and the asset universe says so"). **WA integration, before Tuesday** |
| 2 | **Bluff detector / declaration credibility** (`4b76a5d`, `b0addba`, 09-17) and TARGET-SPEC's "Raj reversed 5 of 5" (`/Users/nick_matta/Documents/GitHub/gridiron-hq/docs/TARGET-SPEC.md:142`) | The construct is mis-specified. Jev's `own_roster` and `open_to_trade` are **message-level** questions ("Does the last message signal willingness to make or consider a trade?", `/Users/nick_matta/Documents/GitHub/gridiron-hq/scripts/news-line/jev_league_chat.mts:68-91`) joined to `mentioned_player`, which has no ownership check. Recomputed from the chat DB: Raj's 5 declarations are Achane ×3, Chase Brown, Nabers — **Achane and Chase Brown are on roster 5 (Nick)**; Rami's single one is Achane; 2 of Lars's 6 are not his. Current code scores Raj **5/5 hard reversals → credibility 0.29 → stance "ignore"** on his players. Revealed preference contradicts the chat read anyway: Raj himself proposed Nabers to Nick on 07-30, 08-01 and 08-20; Lars offered Jeanty on 09-11, the day before his "untouchable" | **re-architect** | Scope declarations to players on the speaker's roster at message time; make a reversal a **transaction** event (`league_transactions_raw` TRADE_PROPOSAL items), not a generic open-to-trade mention; treat chat as the trigger, transactions as the test. **WA (Trade Brain T1/T2), before proposals ship** |
| 3 | **Negotiation profiles** (`ecbdec8`, `457fa91`, built 09-18 05:21-05:54, Sonnet 5, $2.26) | Every input is stale or defective. `managerSignalsFor(4)` reads production `manager_signals`, still the **09-17 22:04 one-off** (Raj `tx_decisions_made` 0, `chat_night_share` 0.199 UTC — the Eastern fix makes it ~0.03); `statsPacket`'s `transactions_last_3_days` is an **unfiltered all-time raw-row count** (`/Users/nick_matta/Documents/GitHub/gridiron-hq/scripts/build-negotiation-profiles.mjs:297-298`) — exactly the rows the manager-data-pipeline proved miscount accepts/proposals; `declaration_record` is populated for only 3 of 9 (and misattributed, row 2), null-by-construction for `ME` (`s.name <> 'ME'`); roster reads cite **one-game** xFP gaps ("Hurts +10.1", "Jameson Williams −10.79") — 2026 xFP has week 1 only. Builder is hard-coded `LEAGUE_ID = 4` and still carries a second copy of the schema. B6 would anchor incremental updates on these profiles | **re-test → full rebuild** | Rebuild after WA's signals go live, with fixed inputs; add a content validator (every `roster_read` name must be on that roster; every cited number must appear in the packet); shrink xFP gaps n/(n+k); only then enable B6 incremental. **WA T1 + WB B6** |
| 4 | **Receptiveness / counterparty pricing** (`05fdacd`, 09-17) | Accept rates enter a score where **0.5 is neutral** (`/Users/nick_matta/Documents/GitHub/gridiron-hq/server/services/counterparty-pricing.js:~70-80`), but the real base rate is **6 accepts / 50 decided (12%)** across leagues (L4 5/21, L3 1/16, L2 0/7). So Lars at 0.38 (3× base) reads *unreceptive* (1.17→1.04) and Raj at 0.20 (≈base) is penalised. In leagues 1/2/3/5 chat weight is 0, so this term is the whole signal once signals go live | **re-architect (formula)** | Centre on the league/base accept rate with a beta-binomial shrink; publish a sensitivity table until the ~50 decided proposals can fit a real P(accept). **WA T3** |
| 5 | **Waiver "+3.45pp" finding** (`11a63da`, 09-17 20:37) and the live `chooseClaimCut` (`79db e1`/`11ab55c`) | Measured against teams that **never touch the wire** (`/Users/nick_matta/Documents/GitHub/gridiron-hq/study/replay/engine.mjs:272-294`): churners claim the best FA by trailing average (n≥1 — the Coker trap the ROS model later fixed), one add/week, only 3 of 10 competing, no priority cost, no K/DEF. In real leagues that comparator barely exists: of 1,548 Sleeper team-seasons with transaction data, **87% make adds (avg 22.6 in weeks 1-13)**; Nick's league 3 already has 8/8 teams active in 2026. The harness that set `churnMask` is **not committed** (only `engine.mjs:277` references it), so the number is unreproducible. The live rule ranks immediate claims by **this week's** lineup gain — a different objective from the one tested and from title odds | **re-test** | Re-run the replay with realistic churn (all teams active, Sleeper-calibrated rates), the live rule vs old rule vs trailing-average, priority modelled; commit the harness. Until then the Coach must not quote +3.45pp as Nick's edge (`/Users/nick_matta/Documents/GitHub/gridiron-hq/docs/FANTASY-ENGINE-MASTER-PLAN.md:241,310`). **WO (H1/O5) + C0 wording** |
| 6 | **Fantasy coordinator applied on the ensemble** (fit id 6, 09-17 19:00, pre-shrinkage) | Fit on actual − structural, served on the ensemble (`/Users/nick_matta/Documents/GitHub/gridiron-hq/server/services/trade-engine.js:269`). Latent only while weeks 2-4 serve structural-only (fit-2). **It goes live-wrong at week 5** (~Oct 8), and it double-counts game script against the Vegas lift | **re-test (hard deadline)** | O6's coordinator refit/basis fix must land before week 5, or switch it off until it does. **WO O6** |
| 7 | **Posture `SPREAD_SCALE` 1.63 + swap sigma 14.5 + coverage band** (`11ab55c`, 09-18) | Re-fit under the new chance-to-play gives **1.45**, outside the pre-registered 0.10 tolerance (play-chance-live TDD §5); the dataset was built 6 min before the availability fit and recorded no version. Sigma 14.5 was fit on the unlifted weeks 5-17 blend but is served in weeks 2-4 on the structural head. The posture fit used synthetic lineups **without K/DEF**, while Nick's five leagues all roster K and DEF; real team-week SDs are now on hand (Sleeper `sh_team_weeks`: 20.2 for 10-team PPR, 26.0 for 8-team PPR, 22,374 team-weeks) | **re-test + re-architect** | Refit both on the live basis; make posture and season sim v2 share **one** fitted team-week variance model (real spreads + our projection error), instead of two. Move from WD to **WO O3/O6** |
| 8 | **Season sim / Team Outlook prerequisites** (season-sim untouched since 08-28; D1-D2 written 09-18) | Three unbuilt dependencies: (a) `players.sleeper_id` is populated for **0 of 8,640** players — D2's "every real team gets our projected strength" has no crosswalk; (b) the plan says Nick has **12 completed league-seasons** (lines 133/142/160/313) — the DB has **7** (L1 2023-25, L2 2023-25, L3 2025); the "1,652 team-weeks" figure sums all rows including **590 unplayed 2026 schedule rows**; (c) fit-1 was trained through 2025 W18, so any 2024/25 "held-out" calibration that centres on the live blend is partly in-sample | **re-architect (scope)** | Add a Sleeper↔our-id crosswalk to H1 phase 1; correct N in D1/D2/E1; require walk-forward weights for every historical validation. **WO H1/O3/O4** |
| 9 | **Team injury-dialect k** (`scripts/fit-availability.mjs`) | Confirmed look-ahead: k is chosen by minimising log loss on `TEST_SEASON = 2025` (`/Users/nick_matta/Documents/GitHub/gridiron-hq/scripts/fit-availability.mjs:140-151`), and the role layer prices Questionable as role cell × that team ratio — so the 2025 Q-row validation (G1 guard, G2 cells) is partly in-sample. 2025 has now been scored 3× for this family; k=2 vs k=5 tie within 0.0002 | **re-test (cheap)** | Re-pick k on ≤2024, re-run G1/G2 once, report; until then the Q numbers carry provenance class "partly". **WD (or O2 if O2 touches availability)** |
| 10 | **Opportunity redistribution, OFF** (`eb163d9`, 09-17 20:15) | The null's own explanation — "the ensemble already weights season-to-date 0.40 / last-3 0.15" — **no longer holds in weeks 2-4**, which now serve the structural head alone (fit-2, `5757dc4`). It was also graded only on 2025 weeks 5-17, on points MAE, with the absorption table fit on 2021-**2025** (in-sample), a row-level (not player-clustered) bootstrap, and the pre-fitted-K head | **re-test** | Re-test inside O1 with: weeks 2-4 included, fitted-K head, absorption fit excluding the graded season, teammate **P(out)** from the role layer instead of realized absence, graded on opportunity + start/sit pairs. **WO O1** |
| 11 | **Three untracked modules** `coach-qb-context.js`, `efficiency-features.js`, `td-features.js` (09-17 21:04-21:05) + feature-store study (`9a7a809`) | They are O1's candidate families, and the study already graded most of them null on points: man/zone × opponent 0.00 weight, O-line + pressure 0.00, deviation transforms 0.00; only the `apm_`+`mz_` pair was positive and is called "a hypothesis to pre-register". Worse for live use: the man/zone source has **no 2026 rows** (`pbp_participation` max game-id year 2025), and nothing refreshes the nflverse satellite (last write 09-16 23:19) — so that family fails O1's "must exist live in 2026" rule today | **re-test / partly obsolete** | Move the decision from WD housekeeping into **O1 discovery**: wire `td-features`/`coach-qb-context` as O1 candidates graded on opportunity, park man/zone until 2026 participation exists, delete what O1 does not take. **WO O1** |
| 12 | **UI teardown** (`60f5f5d`) | Bigger orphan surface than the inventory recorded: the ⌘K palette still lists **7 deleted betting routes** plus `/edge` (`/Users/nick_matta/Documents/GitHub/gridiron-hq/client/src/navigation.ts:62-73`) and a `/betting/nfl/wong` note. Two deleted pages held capabilities the new home needs: `Home.tsx` was the **only** reader/resolver of `/decision-inbox` (where the action plan is to be stored) and the only freshness panel; `DataHealth.tsx` was the **only** `sync_log`/`/dev/sources` view — and WA infra has just added sync_log rows (league_chat PARTIAL, manager_signals, roster_snapshots, league_tx ERROR) that no page shows. `LeagueBrain.tsx` was the only writer of `manager_profiles` tiers, so the 0.55 "hard" path in `findTrades` is dead code on a 0-row table | **re-architect (small)** | Prune the palette; B4 must include a data-health strip and decision-inbox resolve; decide `Model.tsx` (unrouted, has Accuracy/Championship-odds/Availability tabs) as the home of the accuracy scoreboard and live self-grading; retire the tier path or give it a Settings control. **WC + WB B4** |
| 13 | **Talk-vs-model "hype window"** (`4b76a5d`) | Fires only at `games >= 2` with a 3.0 pts/game gap (`/Users/nick_matta/Documents/GitHub/gridiron-hq/server/services/talk-vs-model.js:38,80`) — at 2-3 games that is the same week-1 noise the ROS model shrinks with k=4; and with xFP frozen at week 1 it cannot fire at all right now | **re-test** | Shrink the gap n/(n+k); gate the tactic on n; depends on item 1. **WA T2** |
| 14 | **Trade horizon + value-giveaway λ** (`9ceb8d0`, `483d11b`) | `playoffOdds` default 0.5 is documented as "a 10-team league with four playoff spots" (`/Users/nick_matta/Documents/GitHub/gridiron-hq/server/services/trade-horizon.js:75-81`) — Nick's 10-team leagues take **6** and his 8-team leagues take 4; `PLAYOFF_IMPORTANCE = 4` is borrowed. λ 0.9 charges **market value that is not being refreshed** (`dynasty_values` 207 rows, last fetched 09-17 20:15; no job on the loop) | **re-test** | Pass real odds from O4; measure the exchange rate in the replay; include λ in the trade-value backtest (if realized ROS is the target, λ should be near zero except as an acceptance/veto proxy); add a value-refresh job. **WO O5 + WA integration** |
| 15 | **Season-ending news flag** (`e2ba4c4`, `c4e38c4`) | Released-then-signed players stay flagged for 45 days unless they are on a **fantasy** roster (`/Users/nick_matta/Documents/GitHub/gridiron-hq/server/services/player-availability.js:167-177` reads only league payloads) — yet `roster_players` (refreshed by the on-loop `espn_rosters` job) already shows Jaydon Blue on team 26 and Joe Milton III on team 9. A free agent in that state is invisible to waivers for 45 days | **re-test (small)** | Add the `roster_players` override (NFL row fetched after the story, different team) to the existing WD backtest. **WD / WB B3** |
| 16 | **Chat pipeline** (`da5e75f`, `6f8a80d`, `e5e809b`) | A single question's failure discards **every** label for that message: msg 191359 (Raj, 09-15, "Olave has injury risk and Tyson comes back" — inside the Achane negotiation) and 185708 ("Send me an offer") have **no rows at all** in `jev_chat_signals`. Coverage is thin by design: only 284 of 15,587 messages carry a matched player, first-match only. The night-share fix (0.227→0.030) is still not in production `manager_signals` | **re-test** | Persist per-question results, retry the 18 `ok=0` rows once, log a `sync_log` row. **WD (+ inputs for WB B6)** |
| 17 | **Replay study findings — early QB / robust RB / Zero RB** (`b5c1bc6`) | Still internally sound (gate 15/15, look-ahead fixed), but conditional in ways the later work sharpens: priced on FantasyPros ECR (the doc's own §3 caveat), run only in 10-team formats while **leagues 1 and 3 are 8-team**, the "10-team half-PPR 2-flex" format matches **none** of Nick's 2026 leagues (all five are full PPR, 4-pt pass TD; league 4 is PPR 2-flex), no K/DEF, and every team churning under the trailing-average rule | **holds as measured, re-test before use** | Keep as a draft-time reference only; before the 2027 draft re-run with 8-team PPR, an ESPN-ADP-priced field and a VOR baseline agent. **WD / Later** |
| 18 | **Week-2 accuracy baseline** (`cf6ae18`, weekly learning on the loop) | The only pregame snapshot is 2026 W2, captured 09-17 18:56 under the **pre-role-layer** engine; the slate has started, so it cannot be recaptured, and settlement is blocked by item 1. Grading it will grade an engine Nick never saw | **re-test (labelling)** | Label week 2 as "old engine" in the scoreboard or start the live self-grading at week 3. **WD scoreboard / WB B4** |

## Checked and holding — no action needed

- **Volume shrinkage fit #1** (`7b4f2d6`): cutoff-safe, weekly-engine-only by construction (`activeKVectorFor` withholds volume k from season-long callers), and fit-2 was graded on top of it. Still valid.
- **Weekly ensemble fit-1** (`06f05ff`): the re-fit under the new head failed its own gate and production correctly kept fit-1; the weight surface is flat (4.292→4.298, 4.330→4.333). Valid — with the standing caveat that it is trained through 2025 (item 8c).
- **Early-week fit-2** (`5757dc4`): graded after the shrinkage promotion, weeks 5-18 byte-identical (0 of 15,175 contexts moved), and the retrain-drops-early-buckets hole was closed in review-fixes.
- **ROS model** (`491acc8`): gate 12/12; its structural prior and its fit share one basis. The two known gaps (no availability term, unproven past week 10) are already queued with deadlines.
- **Play-chance role layer + ESPN designation** (`49174d4`, `f60c0d2`): gates G1-G4 pass, live examples check out — for the **current** week, which is all it claims (items 1 and 9 are about future weeks and the dialect k).
- **Fake floors** (`947d66c`): G2 93→0 and D1 all favourable; zero live effect until week 5.
- **Decision leftovers** (`79dbeb1`): IR never started, teamless FAs excluded, home factor retired, card total equals Start/Sit — all re-verified on a production copy.
- **`lineupSpread` quadrature, weekly level, matchup no-signal** (`11ab55c` + retroactive tests): every gated rule now fails under mutation; nothing later contradicts them.
- **Manager-data-pipeline** (`e3b9f53`, `fe1b81a`): the transaction reader fix, identity-confirmation carry-forward and single application of the tier are correct and independently verified.
- **LLM plumbing** (`76b9da3`, `91c0780`): prices verified live, caching opt-in, budgets enforced, historical costs corrected with a backup.
- **Infra essentials** (`bdfafda`…`c2e2bad`): roster snapshots with player-level pregame ESPN status (the missing history the plan called for), launcher node-path fix, honest `league_tx` label.
- **Sleeper crawler** (`6b7e504`): privacy-by-construction, resumable, 215 leagues / 22,374 team-weeks so far — good data; only its *use* is blocked (crosswalk, and it stores no `waiver_type`, which matters because **all five of Nick's leagues are traditional waivers with no FAAB** — so the planned FAAB bid guidance in B5 is obsolete for him).
- **Review fixes** (`6303356`): route auth, asset-cache fingerprint, silent ROS failures, pinned gate baselines, `gate-verdicts.js`, the 375px lineup card.
- **Luck panel maths**: filters to fully scored weeks, so the luck numbers are sound even though the headline "12 league-seasons / 1,652 team-weeks" count is not.
