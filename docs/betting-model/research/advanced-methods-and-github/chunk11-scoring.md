# Chunk 11/21 scoring notes — verified against fantasy-football-dashboard (read-only)

## Verification performed
- Confirmed all cited files exist: server/services/{correlation,nfl-drive-sim,nfl-live,nfl-espn-pbp,nfl-pbp,nfl-policy,nfl-rookies,nfl-specialists,season-sim}.js, server/migrations/027_decision_tape.js, server/betting/nfl/strategy/{teaser-staking,teaser-leg-rates}.js, client/src/App.tsx, client/src/pages/betting/Edges.tsx.
- server/services/nfl-live.js: confirmed it IS a score-only random-walk model exactly as N07-C1 describes — its own header says "it has no possession, down-and-distance, or timeout awareness." liveWinProbability(lead, secondsLeft, pregameSpread) at line 66, liveGames() at line 105. This is real, matches the candidate's premise precisely.
- server/services/nfl-espn-pbp.js: pollLiveGames() at line 213, liveModelValidation() at line 508 (candidate said ~507 — accurate), simulateRemainder import/usage confirmed at lines 32, 280-290, 608.
- client/src/App.tsx: grepped the full route table — confirmed NO /live or /gameday route exists today (N07-C2's premise is accurate). Edges.tsx's 'live' tab (line 79-156) is the only WP UI, inside /betting.
- server/services/nfl-pbp.js: wpa field confirmed at lines 81,109,295,376,402,486-487,632 — all season-aggregate accumulation, never a live per-play computation. Matches N07-C3/C7 premise.
- server/services/nfl-drive-sim.js: PLAY_MODEL_VERSION = 'pbp-drive-v3-cutoff-calibrated' confirmed at line 49 (candidate said line 41 — close, off by 8 lines, immaterial).
- server/migrations/027_decision_tape.js: nfl_decision_runs / nfl_decision_events tables confirmed in schema. **But queried the live DB (server/data.sqlite, read-only) and both tables have 0 rows.** server/services/nfl-decision-tape.js exists and presumably writes to them, but nothing has been captured yet — same cold-start problem as the fantasy weekly-learning loop flagged in tonight's verified findings. This meaningfully weakens N06-N3's claim that Gridiron "already has the ideal raw material" — the schema exists, the data does not yet.
- server/services/trade-engine.js: bestLineup() confirmed at line 323, greedy sort as claimed.
- server/services/correlation.js: correlationMatrix() (158), correlatedSampler() (181), correlationTable() (198) all confirmed exported.
- server/services/season-sim.js: correlatedSampler() call confirmed at line 228 exactly as cited.
- Searched the whole repo (server + client, node_modules excluded) for DraftKings/FanDuel/salary_cap/DFS: every single hit is sportsbook teaser/spread betting (teaser-staking.js, teaser-scan.js, teaser-season.js, margin-distribution.js) referencing DraftKings as a **sportsbook** for odds, never as a salary-cap DFS contest platform. **No dfs_slates, dfs_salaries, or any salary-cap contest infrastructure exists anywhere in the codebase.** No memory file or code path suggests Nick plays DK/FD salary-cap DFS contests — his fantasy footprint (draft-night setup, live draft assistant, trade engine, ESPN sync) is entirely season-long redraft/dynasty league management. This is a serious applicability gap for the entire N08 DFS series.
- server/services/nfl-rookies.js: measureRookiePriors() confirmed real, joins nfl_rookie_evidence (draft capital) to player_week_usage (>=4 games) exactly as described. **Ran the actual join against the live read-only DB: n=301, not the candidate's claimed n=75.** The file's own caveat comment ("Thin late-round bands are still collapsed into R4+ and shrunk hard downstream") supports that per-band samples are still small even though the total pool is ~4x larger than N09-C1 states — the small-n framing is directionally right but the specific number is wrong.
- server/services/nfl-specialists.js: ridgeFit() confirmed at line 99, used by nfl-rookies.js path as claimed.

## Scoring rationale summary
- N07-C1/C2/C6/C7 are the strongest items in this chunk: each cites a real, verified file/line, addresses either tonight's already-confirmed drive-sim physics bugs (C6) or an already-confirmed score-only random-walk model with a genuine gap (C1), and C2/C7 are near-zero-cost structural correctness/product moves. All four: build.
- N07-C3/C4 are cheap, correctly-sequenced follow-ons once C1 exists: build (contingent on C1 landing first, which is implicit in "hours once N07-C1 exists").
- N07-C5 (fantasy garbage-time badge) is self-labeled weak evidence and unverified attachment point (no live player-context surface was located to confirm the attachment); cheap enough to prototype: test-first.
- N06-N2 (bandit sizing) admits in its own writeup that ticket volume (15-33/season) is too low for online learning to reliably beat the already-recommended static Quarter-Kelly rule — this is a self-undermining case for weeks of engineering effort on a betting-secondary-priority feature: reject.
- N06-N3 (RL OPE harness) has strong conceptual backing (PBO/CSCV) but its core justification — "ideal raw material already exists" — is weakened by the decision tape being schema-only with 0 real rows today, and it exists to gate N1/N2, one of which (N2) is itself rejected here: later.
- N08 series (DFS): rejected across the board. Real academic grounding for the underlying papers (ILP lineup construction, Dirichlet ownership models) but zero evidence Gridiron or Nick operates in the DFS salary-cap contest space — no data tables, no code path, no memory-file signal. Building this would be a new, disconnected product line, not an extension of anything Gridiron does today, and directly cuts against Nick's stated "fantasy [redraft/dynasty] over betting" priority (DFS is neither).
- N09-C1 (rookie augmentation): real attachment points confirmed, but the "n=75" premise is off — actual usable rookie-seasons after the same join is n=301. The small-n problem is real only at the band level (confirmed by the module's own caveat), not at the aggregate level the candidate cites. Worth a bounded pilot with the proposed walk-forward validation, not a blind build: test-first.
