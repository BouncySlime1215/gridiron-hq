# GRIDIRON HQ HANDOFF, 2026-09-22

> STATUS NOTE (18:2xZ, handoff worker, stopped by coordinator order mid-second-pass): COMPLETE with thread-supplied text folded in: sections 1, 2, 3, 5, and 6.1-6.6 (Release, Wiring map, UI, Fantasy plan, Scheduler, Feature audit). SKELETON (first pass from coordinator memory only, not yet updated with the 15 thread files under /mnt/project-files/handoff/): sections 4, 6.7-6.17 (Trade Ledger, Opportunity, Google sign-in, Chat sync, Model evidence audit, Coach, Explorer, Planner, Independent Auditor, Evidence Auditor, coordinator), 7, 8 (R62-R64 missing), 9, 10, 11 (Auditor five-thread list missing), 12 (post-17:59Z events missing). Where a skeleton entry disagrees with a file in /mnt/project-files/handoff/, the thread file wins.

Written by the handoff worker at 18:00Z (coordinator clock) on the request Nick made at 17:57Z: "put all the updates and work into a MASSIVE handoff doc and save all work." Everything here was read from the coordinator memory tree (`/tmp/claude/memory/team/silo/`), the project files (`/mnt/project-files/`), and the repository at `/home/claude/gridiron-hq` after `git fetch origin main` (origin/main `c90d2834b827f32e62117d20ae5e63ff6774b264`). Sources were read only; no memory file was modified. `gh` is not installed in this environment, so GitHub state is quoted from the coordinator's board files and the PR numbers they carry, not re-fetched.

Conventions in this file: no secret value appears anywhere, credentials are named only; every figure carries the tree or commit it was measured on; withdrawn numbers live only under "NEVER QUOTE THESE" in section 7; message ids, session ids and trigger ids appear in code spans or links, never in prose; thread sections in section 6 that were supplied by the thread itself are marked "(thread's own words)".

---

## 1. READ THIS FIRST

1. origin/main was `c90d2834` (PR #129, 17:53Z) when the sources for this file were read; two merges landed while it was being written: #124 as `9f0b5b66` (18:06Z, main run CANCELLED by the next push, never verified on its own) and #92 as `6e722719` (18:08Z, main push run `35765300584` SUCCESS 18:17:59Z, all steps, 8m20s). Main is green at `6e722719`. The DEPLOYED tree is `c5ee3b54` = main at #99 (image `deployment-01M3517MWXX3HF7CEPSWB8ZK0M`, machine `84ed41eae1dd68`, 17:05Z). #113, #115, #117, #119, #129, #124 and #92 are NOT in the live image (`git merge-base --is-ancestor bf359c6 c5ee3b5` is false; memory's "#113 deployed" line was wrong). Rollback image `deployment-01M2VZ9JRYSXVHCRWJ83V360QH`, brake first, Nick's word only.
2. Nick's last unanswered question (17:53:19Z, `cmsg_01YAsw8AnFv4ioRMQw8dfPmTB66DzF2jsF3iMn2pcMTCiB`): "how did we use our entire weekly budget and we didnt get shit done lol". Coordinator's answer (`cmsg_01YAsw8AnFv4ioRMQw8dfPmTQAa88dq5aCwzT7uNZojG8d`): 1 = five threads on Plan items 1-3 plus two auditors (recommended); 2 = keep 15; 3 = stop after held PRs merge. WAITING ON HIS NUMBER. Until then every thread lands what is in flight and starts NO new unit. Section 11.5 has both five-thread lists (coordinator's and the Independent Auditor's).
3. Nick decisions owed, beyond the number: (a) DEPLOY BUTTON BRAKE. #127's `deploy.yml` re-sets `SCHEDULER_DISABLED=1` on every deploy, as the brief and `docs/runbooks/deploy-654ff93.md` ordered, but the brake has been OFF since 17:08Z on his word with the scheduler live and proven. Each press of the button would turn the live scheduler off until he runs `fly secrets unset SCHEDULER_DISABLED --app gridiron-hq`. He decides before the first press whether the button keeps the brake step (safe, needs an unset after every deploy) or drops it (a new unit with a RED test). (b) `FLY_API_TOKEN` after #127 merges (section 11.7). (c) keep/drop nflverse/nfldata feeds with no licence. (d) the Explorer production read, five commands on the Fly console (section 11.7). (e) Coach's corpus run on his Mac (section 11.7). (f) key rotation, declined 15:38Z, stays listed.
4. Standing rule since 15:45Z (`cmsg_01YAsw8AnFv4ioRMQw8dfPmTBEhtChMmuvMJ7aNSnbMRAP`): push everything truly ready and truly tested, manage the threads, keep the doc. Merge rule since 16:44Z (his "1", `cmsg_01YAsw8AnFv4ioRMQw8dfPmTWPhFDXgc4Mw5WZc7QiFACJ`): CI green on the exact head plus REAL; docs-only on green alone; deploy his word. At 18:06Z the gate changed shape: threads squash-merge their own PRs on CI green on the exact head plus a four-part self-check block in the body (guard command and exit code; RED/GREEN shas per R52.2; the five questions; one liveness proof per behaviour change, R63). The Evidence Auditor now body-reads before merge and keeps the ledger; it no longer re-runs suites. Model, projection, trade-logic and lineup code still needs the Independent Auditor.
5. 30-minute update in the shape of the 17:56Z post (`cmsg_01YAsw8AnFv4ioRMQw8dfPmTGC9ZtY8aG2DJaFuN8DKx1f`), trigger `trig_01XsQzN1SGcFPKMFetyoBQCy` (18:26Z, then +30). MLB goes out of the app (17:24Z); Scheduler's #128 (head `8e2667bc`) is the removal.
6. Held by the Independent Auditor after R62/R64: #121 (R57.3 three text items plus R62.5 k-vector line); #130 (R58, fourth surface, now fixed on `5b8a345`, guard pending); #135 reach-ladder (R62: numbers reproduce, HELD only on the call-site mutant at `reach-ladder.mjs:266` surviving); charting revision 2 CLEARED with three text conditions (no further auditor round); #132 CLEARED to merge (R64). R54.4 Scheduler scope trace still open.
7. Never delete `/data/data.sqlite.pre-migration-*.bak` on the Fly volume. Only rollback of row data; no Fly backup. Tight disk: `fly volumes extend`. Never touch `LOOP_WATCHDOG_*`; they are not the brake.
8. Three key exposures, five credential names, zero rotations (section 10, names only, never values).
9. The 25 plan items in section 4 are verbatim from Nick's PART 5 post (`cmsg_01YAsw8AnFv4ioRMQw8dfPmTRwtoUfvgWYLwXAcxCgATNx`) and v2 (`cmsg_01YAsw8AnFv4ioRMQw8dfPmTEADG`). Phase board 17:56Z: Foundation 72/86, A 0/50, B 0, C 0, D 0/10. #92 and #124 merging since moves nothing on the board's item map.
10. Clocks: thread-reported times run up to an hour ahead of the coordinator clock. Use message ids for identity, coordinator clock for ordering. `switch_model` returning success does NOT mean the model switched: opus 5.5 was rejected fleet-wide except Release and the Independent Auditor; read `last_served_model` via `get_session`.

The 17:57:04Z request that produced this file: `cmsg_01YAsw8AnFv4ioRMQw8dfPmT9UECH71Pn5PerAQK5Lp9ip` ("put all the updates and work into a MASSIVE handoff doc and save all work pls").

---

## 2. NICK'S STANDING RULES

Every rule Nick gave, most recent first. Message ids are the chat message ids; times are UTC on 2026-09-22 unless another date is stated. Prefix for all message ids below is `cmsg_01YAsw8AnFv4ioRMQw8dfPmT` followed by the suffix shown.

### 2.1 Rules from today, most recent first

| Time | Id | Rule (his words where quoted) |
|---|---|---|
| 17:53:19Z | `...B66DzF2jsF3iMn2pcMTCiB` | "how did we use our entire weekly budget and we didnt get shit done lol". Open question. Answered with options 1/2/3; his number is pending. Constraint until he answers: land in-flight work, start no new unit. |
| 17:24:48Z | `...V63S4omowfQdXAX2JAeEU3` | "just manage the things and then gimme that update every 30 min / and then ping me with the copy paste if u need me / good work". Coordinator manages; 30-minute updates; a copy-paste request when his hands are needed. |
| 17:24:12Z | `...JB98aj3bzRdHsQPCME5PUm` | "i like these updates - gimme this evry 30 min pls : get rid of MLB btw". Update cadence is 30 minutes in the phase-board shape; MLB is to be removed from the app (Scheduler owns the removal; Wiring map contributes the map commit). |
| 17:14:17Z | `...FbbMhCVZwYE5DYPwFx87b3` | Nick is away studying. Chat is reserved for his word or a milestone. |
| 17:07:02Z | `...94Bra9yknAoEUEkApzZCSU` | "bro use opus 5.5 ACross everything pls". Resolution recorded 17:13Z: 5.5 was actually served only on Release and Independent Auditor; sonnet-served threads were moved to claude-opus-5; no more retries; new threads start on 5.5; check `last_served_model` before claiming a model. This suspends the earlier model-by-weight rule. |
| 16:55:19Z | `...Vhciquw8xe1QbpsRU8SRs6` | R&D updates for Nick: he wants the R&D findings in the updates, not only build progress. |
| 16:44:57Z | `...WPhFDXgc4Mw5WZc7QiFACJ` | "1": merge as they go. CI green plus REAL from the Evidence Auditor merges; docs-only merges on green alone; deploy remains his word. |
| 16:23:11Z | `...9Ew6TGiS4ZnGZHoQHgXwH6` | "monitor waiting on u for me always". Every update carries a Waiting-on-you line listing what only he can do. |
| 15:45:33Z | `...BEhtChMmuvMJ7aNSnbMRAP` | STANDING RULE: "PUSH EVERYTHING IF ITS TRULY READY AND TRULY TESTED ... MANAGE THE THREADS ... KEEP THE DOC". |
| 15:42:28Z | `...CYnTcbgi6G6B8CBk4p3hX7` | General resume after the 13:02Z pause. |
| 15:38:21Z | `...1VqJVcvi7GEAaoU9cm8F2z` | NO key rotation now. The exposures stay open; section 10 lists them by name. |
| 15:29:23Z | `...DMgw8u2y7RiCyGrUtuTLyT` | "If u think it can merge then u merge". Coordinator has merge authority under the gate. |
| 15:24:45Z | `...H6hcj4fXG3Mg6FzgpHzPpc` | "Yes / It's public so free now". Repo is public; CI is free; he re-enabled Actions at 14:52Z (workflow `357164314` active). |
| 15:17:45Z | `...BoVHdg5JWizuu1LexraDUR` | Verify once, then model by weight (heavy model on judgement work, lighter on mechanical). Model-by-weight was later suspended by the 16:37Z and 17:07Z opus 5.5 orders. "Verify once" stands. |
| 15:02:27Z | `...GnxHramhy7xUkX48YsHKk3` | Resume instruction, second message of the pair. |
| 15:02:16Z | `...VyfLF9E4GmsypJtAfcB75z` | Resume instruction, first message of the pair. |
| 14:35:57Z | `...CkdCsHHVwVc6Z32bVvbrSt` | #95 was not to merge without the test rundown; the migration 064 number collision was to be named. Both since handled (#95 merged `eb861fe` after the rundown). |
| 13:02:32Z | `...ChJCHDt33Bs4w8XuVeCTt4` | Pause. |
| 09:42:21Z | `...D3JPA3bFwucA53toZFtsdx` | Full stop. |
| 08:53:44Z | `...BMsrZp6gjx1wnUd9TFf4fR` | Pause. |
| 08:31:13Z | `...7QKHpo8LFpS1PVn4cf2wqn` | Goodnight; auditors keep running; one message per unit overnight. |
| 08:25:21Z | `...Rm2YBUbGEoT371ioCdkPm3` | Overnight R&D loop: Explorer and Planner run the explore/plan/build loop unattended. |
| 08:23:45Z | `...4dNPmXRkGax9VPqjSVn5E7` | Always answer a thread that says ready-for-review; nothing sits unanswered. |
| 08:13:11Z | `...QCHFbWsG3vzEVgu4BBtseq` | Follow-on to the R&D loop order (scope of what the loop may build). |
| 08:07:25Z | `...CXAo4ZsR87wYou7x94bzbr` | R&D explore/plan/build loop established: Explorer measures, Planner integrates, findings go through the Independent Auditor. |
| 07:34:09Z | `...5cqaWo187zNcbUvaVH2gam` | Independent Auditor and both R&D threads run 24/7. |
| 07:14:26Z | `...3KiCiFCNRVdLzR7VGhZ9Ty` | Auditor rule: three gates (method, evidence, fit against what production does); the auditor can kill or redirect a unit; a fixed submission format. |
| 07:13:54Z | `...Pi3t2Db9J94yUvt4gJxNNo` | Push authority restored to threads after the overnight hold. |
| 04:09:07Z | `...CpykNc7JFn3mpbKt8M9pc8` | "dont flag shit just worl bruj". Do the work; do not raise every finding to him. |
| 03:53:38Z | `...Ehe6e4aum5My5N1kUagRNd` | Missing-data rule and free-data-first rule: when a source is absent, log it and find a free source; nothing paid, ever, in R&D. |
| 03:28:38Z | `...DGjnLfcy54XP63pupq9ZSX` | Phase-transition plan, no stop: when a phase ends, plan the next and keep going. |
| 01:26Z | `...BqgHcvtRg3MpKxYaSVKdQz` | Stop/resume protocol: a stop is a stop ledger entry; resume replays the ledger. |
| 01:25Z | `...8hKXRheXe6eCXwGuAPPEAb` | GO on the plan. |

### 2.2 Rules from before today

- 2026-09-21T19:58:42Z, `cmsg_01YAsw8AnFv4ioRMQw8dfPmTRwtoUfvgWYLwXAcxCgATNx`: the PART 5 plan post that section 4 quotes verbatim. The v2 update is `cmsg_01YAsw8AnFv4ioRMQw8dfPmTEADG`.
- 2026-09-20T01:31Z: no-PR-noise rule. No comment on a PR that does not change its state; no "still working" comments; every PR comment carries evidence.
- 2026-09-20T01:21Z: five-questions rule. Before a thread asks Nick anything, it answers the five questions itself (what is it, what does production do, what did I measure, on what tree, what do I want him to decide). If the coordinator can answer, Nick is not asked.

### 2.3 The never-build list (NOT APPROVED, from the PART 5 post)

Multi-platform league import. Offseason product. Monetization model. Banning narrative features. None of these is built, planned, or scaffolded. If a thread proposes one, the answer is no without asking Nick.

### 2.4 Scope rules

- Gridiron HQ is a fantasy platform ONLY. Betting code exists on disk but is not a product surface; nothing new goes into it (nine betting files and their bugs are listed in section 7 as known and unfixed).
- Navigation is eight tabs. Deleted pages are never rebuilt.
- Nothing paid in R&D, ever. Licence check before any measurement on a new source (rule file `gridiron-licence-before-measurement-rule.md`).
- Threads report to the coordinator only, never to Nick directly. Chat is reserved for his word or a milestone.
- One editor per file. The file allocation (section 6) is the law; a thread that needs a line in another thread's file gets a written grant for that line.
- Every figure carries the tree it was measured on; a figure without a tree is not quoted.

### 2.5 CLAUDE.md security constraints, verbatim

From `/home/claude/gridiron-hq/CLAUDE.md` on origin/main `c90d2834`, section 2:

> **Secrets never appear in the repo, a commit, chat, or a screenshot.** Environment variables only. A key that has been shown anywhere is burned and must be rotated, and the rotation confirmed. Read a value's presence, never its content, into a log or a message.

> **Errors are handled or they throw.** No bare `catch {}` that swallows a fault. This project has shipped two real bugs of exactly that shape, where a silent catch deleted a whole data layer and the page kept printing numbers as if nothing had happened. If a layer goes inert, the surface must say so.

> **Parameterised queries only.** No string-built SQL.

> **Commits.** `<type>: <description>` (feat, fix, refactor, docs, test, chore, perf, ci). Attribution footers stay on.

> **A failing test is never "a flake" until proven, and "no API key" is not a cause.** Name the failure and its real cause before calling it environmental.

> **Run `npm ci` before trusting any suite number.**

Note on CLAUDE.md drift: the copy on origin/main differs from the local checkout by 17 added / 9 removed lines. On origin/main, `TASKS.md` IS on main (the local copy says it is not), and the CI paragraph reads that CI has run seven times with run 252 succeeding on 2026-09-19 and workflow `357164314` active. The local, older text says CI had never completed on main. Trust origin/main.


### 2.6 Three more of Nick's briefs, verbatim as the threads recorded them

- 04:10:24Z, the R&D charter (Planner's record): "have the data techniques and RD be exploring and working non stop, it should alwys be adding to the plan, build another task to clean its work up... tell data techniques and R&D to move in a circle and cpnstantly be moving and exploring."
- The Wiring map brief (Phase 0 item 5): "take full inventory of everything built so far, every model, every pipeline, every thread's work, what's actually wired and working, what's half-done, what's dead or stale, what's silently broken. Be brutally honest; if something is decoration or doesn't pull real data, say so."
- The Feature audit scope limits, still in force: "do not deploy to the live app, do not write to league data, and do not touch the betting side."
- 15:17Z: one clean run with the full rundown, a second run only if something actually changed (the verify2x guard still runs twice; Fantasy plan measured the second run catching nothing all day).
- 17:59Z (Scheduler's record): the MLB reads left without a writer also go: `mlbMarketMovement()`, `mlbIntelligence()`, `bookHold`'s `sport` option, `GET /hold`'s MLB comparison, `parlay-api.js`.

### 2.6 Three more of Nick's briefs, verbatim as the threads recorded them

- 04:10:24Z, the R&D charter (Planner's record): "have the data techniques and RD be exploring and working non stop, it should alwys be adding to the plan, build another task to clean its work up... tell data techniques and R&D to move in a circle and cpnstantly be moving and exploring."
- The Wiring map brief (Phase 0 item 5): "take full inventory of everything built so far, every model, every pipeline, every thread's work, what's actually wired and working, what's half-done, what's dead or stale, what's silently broken. Be brutally honest; if something is decoration or doesn't pull real data, say so."
- The Feature audit scope limits, still in force: "do not deploy to the live app, do not write to league data, and do not touch the betting side."
- 15:17Z: one clean run with the full rundown, a second run only if something actually changed (the verify2x guard still runs twice; Fantasy plan measured the second run catching nothing all day).
- 17:59Z (Scheduler's record): the MLB reads left without a writer also go: `mlbMarketMovement()`, `mlbIntelligence()`, `bookHold`'s `sport` option, `GET /hold`'s MLB comparison, `parlay-api.js`.

---

## 3. STATE OF THE CODE

### 3.1 Trees and images

| Thing | Value | Source |
|---|---|---|
| origin/main | `c90d2834b827f32e62117d20ae5e63ff6774b264` (tree `63ebac8a`, byte-identical to tested head `6432a768`) | `git fetch origin main` at 17:59Z; state 1279 |
| Deployed | commit `c5ee3b54` = main at #99 (nothing merged after it is live: not #113, #115, #117, #119, #129, #124, #92), image `deployment-01M3517MWXX3HF7CEPSWB8ZK0M`, machine `84ed41eae1dd68`, app `gridiron-hq`, deployed 17:05Z | post-deploy read, state ~1260 |
| Rollback image | `deployment-01M2VZ9JRYSXVHCRWJ83V360QH` (the image that ran before 17:05Z; rolling back is Nick's word) | release-train-deploy-facts |
| Live URL | `https://gridiron-hq.fly.dev` (coordinator egress to fly.dev returns 403; only Nick and the Release thread with his Fly token can read it) | live-environment-notes |
| Repo | `github.com/BouncySlime1215/gridiron-hq`, PUBLIC, no branch protection, no LICENSE file at root | repo listing 17:59Z |

Today's main chain, oldest to newest (each line is the squash-merge commit and the PR it landed): `e3e7602` #91 → `1a13614` #89 → `ac31922` #111 → `140d436` #102 → `0877ea4` #114 → `26a5002` #68 → `eb861fe` #95 → `7eca9a8` #86 → `a1e661f` #112 → `4ff3048` #109 → `0c5b7be` #87 → `ab01efa` #110 → `eb19f47` #108 → `c5ee3b5` #99 (DEPLOYED) → `bf359c6` #113 → `144b722` #115 → `92654e2` #117 → `f620a12` #119 → `c90d283` #129.

Since the sources were read: #124 → `9f0b5b66` (18:06Z, main run cancelled by the next push) and #92 → `6e722719` (18:08Z, main run `35765300584` SUCCESS 18:17:59Z). Main went RED at `144b722` / `eb19f475` from wiring-gate false positives between 17:04Z and 17:53Z; #129 (`c90d283`) turned it green again (R60 closed R53.1). Squash merges orphan the branch shas; `refs/pull/N/head` survives and is what to fetch when a merged PR's tested head is needed.

### 3.2 Scheduler and the brake

- Merge-burst finding (Evidence Auditor, 18:18Z): CI's concurrency group cancels main's in-flight run when the next push lands, so only the last merge of a burst gets a real main signal. Main runs for #117, #115, #113, #99, #110, #87, #109 and #124 were all cancelled. Verified mains today: `c90d2834` (run `35763448618`) and `6e722719` (run `35765300584`). If a later main is red, the bisect range is the whole burst.
- `SCHEDULER_DISABLED=1` is an app-level secret (set 17:03Z ahead of the deploy, confirmed present on the machine at 15:35Z earlier in the day for the previous cycle). The brake was LIFTED at 17:08Z; the scheduler has been ON live since then.
- MLB has three jobs on the request thread; the 1.5-second blocking line is real (rule file `gridiron-mlb-*`, Scheduler thread measurement on `654ff93`). Nick ordered MLB out at 17:24Z; Scheduler owns the removal (11 files, 2,342 lines, draft not yet pushed).
- The scheduler was cleared of the delete accusation (`gridiron-scheduler-cleared-of-delete.md`): the data-layer deletion earlier in the project was a silent catch, not a scheduler job.
- Do NOT put nflverse depth on a timer: the 51 MB depth CSV OOMs the 2 GB machine (fly-cold-start / live-environment-notes).

### 3.3 Production data reads (live, 15:37Z read on the deployed tree, two-copy rule R49)

| Table | Rows | Note |
|---|---|---|
| `nfl_depth` | 0 | inert (R29 Condition B) |
| `nfl_snaps` | 128,146 | |
| `nfl_injuries` | 28,411 | |
| `nfl_qbr_weekly` | 574 | |
| promoted weekly fits | 0 | frozen-2023 fit is what is served |
| ESPN cookies | present for all five leagues | presence only, never content |
| DB size | ~0.90 GB main + WAL | |
| `news_items` importance | all 914 rows are 2 | the importance=3 predicate is dead (section 7) |

### 3.4 Migrations

- Main tops at `062` (there are TWO `062_*` files) plus `064_league_history_tables.js`; #94 adds `067_outcome_ledgers.js` (Trade Ledger) and #92 (merged `6e722719`) adds migration `070` widening `nfl_play_formations` (`time_to_throw`, `was_pressure`, `defense_man_zone_type`, `defense_coverage_type`; NULL on every pre-existing row until re-ingest). Migration `066` provenance is unresolved (section 7). `league_transactions_raw` is created by a script, not the schema, so a fresh clone does not have it (Coach).
- The runner keys on the `name` export, never on the filename number. Never renumber a migration. Collisions are resolved by leaving both files and confirming both `name` exports differ (`gridiron-migration-number-collisions.md`, `gridiron-migration-runner.md`).
- Boot takes a pre-migration snapshot: `assertRoomForSnapshot` needs DB size + 2 GB free or the app refuses to boot (`gridiron-pre-migration-snapshot.md`). Each `.bak` is ~445 MB.

### 3.5 The backup file never to delete

`/data/data.sqlite.pre-migration-*.bak` on the Fly volume. There is no Fly volume backup (`gridiron-fly-volume-has-no-backup.md`). The `.bak` files are the only row-level rollback. `scripts/nightly-backup.sh` has a prune pattern that would match `.bak` files if it were ever pointed at `/data`; it is not, and must not be. If the volume fills, run `fly volumes extend`.

### 3.6 CI

- Workflow id `357164314`, active (Nick re-enabled Actions 14:52Z; repo public so minutes are free, his 15:24Z word).
- `.github/workflows/ci.yml:70` runs `check:wiring` BEFORE tests, and CI calls each npm script directly: it never invokes `npm run check`, so removing the CI step would remove the gate from CI even though `check` contains it (Wiring map).
- CI builds `merge(head, base)`, never the bare branch: a main-side gate reaches every open PR at once without a rebase (that is how #108 turned the whole fleet red), and a local run on your own head cannot reproduce a main-side failure until main is merged in. A red that finishes in about one minute is the wiring gate failing and means the tests did not run at all. Read the job log before calling a red a test failure.
- `timeout-minutes: 20` is the budget; PR #7 brought the runtime inside it. No coverage floor, no E2E tier (CLAUDE.md section 3).
- Deploy is `fly deploy` by hand from Nick's machine. PR #127 (Release, head `43116ab`) adds a `workflow_dispatch` deploy button and a `.dockerignore` (build context 491 MB → 12 MB); after it merges Nick must create a deploy token and paste it as `FLY_API_TOKEN` (section 11 has the exact commands).

### 3.7 The wiring gate and `npm run check`

`package.json` on `c90d2834`: `"check": "npm run typecheck && npm run lint && npm run check:wiring && npm test && npm run build && npm run start:smoke"`. Six steps. `check:wiring` = `node scripts/wiring-map.mjs --check` and has been part of `check` since #129. Before #129 the local gate omitted the wiring step, which is why main could go red on a tree that had passed `check` locally (rule file `gridiron-check-script-omits-wiring-gate.md`, now superseded by `gridiron-local-gate-includes-wiring-rule.md`). Map size on `c90d2834`: 1053 files, 333 tables, 765 surfaces, 2520 findings; GATING is exactly two rules (`column-read-never-written`, `producer-with-no-caller`); accept list `docs/wiring/annotations.json` holds 23 `accepted_missing_feeds` and 50 `accepted_orphan_modules`, each with an owner and a RETIRES WHEN. The `cascade-grade.js` stale-entry line is a deliberate pre-registration for #72: never delete it. The Wiring map thread's baseline after #129 is 19 unresolved receivers; the ratchet (fail on any new unresolved receiver) is its next unit.

Run tests with the suite's flags: `--experimental-test-module-mocks` and `NODE_OPTIONS='--import ./test/offline-guard.mjs'`; a bare `node --test` fails with `mock.module is not a function`, which is not a regression. The fleet guard form: isolated worktree with its own `npm ci` (or hard-linked `node_modules`), `git status --porcelain` empty and `git write-tree` identical before and after, `node_modules` mtime unchanged, log outside the repo, `find -newermt` sweep; porcelain is the load-bearing half because `write-tree` hashes the index.

`docs/tdd/` on origin/main holds 91 evidence files; every merged feature PR today has one (RED commit, GREEN commit, evidence file).

---

## 4. THE PLAN

### 4.1 Phase board as of the 17:56Z post (`cmsg_01YAsw8AnFv4ioRMQw8dfPmTGC9ZtY8aG2DJaFuN8DKx1f`)

| Phase | Merged | Weighted | Basis |
|---|---|---|---|
| Foundation (items 1-7) | 72% | 86% | 1 done (report); 2 open #98; 3 merged #86 (+ #96/#104 open); 4 open #94 (+ #103/#120); 5 merged #99 + #108 (+ #116 open); 6 merged #102; 7 done (deployed `c5ee3b54`) |
| A (items 8-10, "Fantasy model") | 0% | 50% | item 8 ← #93 draft (Fantasy plan); item 9 ← #68 merged + #88/#106/#121/#122 open; item 10 ← #90/#82 + Coach suspension |
| B | 0% | 0% | not started |
| C | 0% | 0% | not started |
| D | 0% | 10% | item 21 partial |

The PHASE-BOARD file at 17:50Z said 71/86 for Foundation; the 17:56Z post said 72/86 after #129 merged. Use the post.

Execution order: Foundation → A → B → C → D. Phase-transition rule: no stop between phases; plan the next and keep going.

### 4.2 The 25 plan items, verbatim

Source: `/mnt/project-files/PLAN-ITEMS-1-25.md` (9,576 B), which is the same text as memory parts `gridiron-plan-items-1-25-verbatim-part1/2/3.md`, transcribed from Nick's PART 5 post and the v2 update.

**FOUNDATION**

1. **Full test rundown.** Run the entire suite, every test, and report what is green, what is red, and why, with the real cause named for every red. No "flake". No "no API key".
2. **Wiring map.** A machine-checkable map of every route, every job, every script, and every table each one reads or writes. It fails the build when a receiver is unresolved.
3. **UI truth.** Every number on every page traces to a table and a row. Where the page prints a number the table cannot support, the page says so instead of printing.
4. **Trade brain grounded.** The trade engine reads the same tables the projections read, prices counterparties from real manager behaviour, and never invents a player.
5. **Opportunity model.** Reach ladder for every table: not reachable, reachable-data-absent, or predicate dead. Write-side axis for each: own trigger, piggyback, hand-run, or none.
6. **Chat sync and league history.** Every league's transaction and chat history lands in tables with a migration, and the manager archetypes are built from it.
7. **Deploy.** The tested tree runs on Fly, with the scheduler brake in place until the post-deploy read is clean.

**A. FANTASY MODEL**

8. **Fantasy plan with results.** A plan for the projection model that starts from what the production model does today, measured on held-out seasons, and names the ceiling before naming the improvement.
9. **Model evidence audit.** Every claim the model makes on a page traces to a measurement on a named tree. Claims that cannot be traced are removed from the page.
10. **Coach grounded.** The coach reads projections, availability, and matchups from tables, never from prose, and every recommendation carries its basis.

**B. LEAGUE INTELLIGENCE**

11. **Manager archetypes served.** The archetypes built in item 6 drive counterparty pricing and waiver competition.
12. **Waiver brain.** Waiver recommendations priced against the league's actual claim behaviour.
13. **Lineup optimiser with ceilings.** Ceiling-lineup uses the same configuration as the projections; the divergence is a bug, not a feature.
14. **Matchup context.** Game-level multipliers are additive arms on the projection, measured against the baseline before they ship.
15. **News grounding.** News items feed availability and role changes with a measured effect or not at all.

**C. PLATFORM**

16. **Google sign-in.** Account linking that survives ESPN cookie rotation; credentials per user, not a global slot.
17. **Multi-user.** Every table with league data carries an owner; no global Anthropic key; encryption helper for stored credentials.
18. **Data freshness surface.** Every page shows when its data was last written and by which job.
19. **Attribution.** Every data source with a licence is attributed where its data is shown; sources with no licence are dropped or their keep is Nick's word.
20. **Release train.** Deploy from CI on Nick's word; rollback in one command; post-deploy read automated.

**D. RESEARCH**

21. **Data and techniques R&D.** Explore free sources; measure on held-out seasons; nothing paid; licence before measurement; findings go through the Independent Auditor before the Planner integrates.
22. **R&D integration and cleanup.** The Planner turns cleared findings into pre-registered units with a RED test, a GREEN commit and evidence.
23. **Research corpus.** Papers and write-ups live under `research/`; every one carries the source and licence.
24. **Model routing.** Which model serves which thread is written down and checked against `last_served_model`.
25. **Handoff.** The state of the project is written so a cold reader can run it. (This document.)

**GLOBAL RULES (from the same post):** TDD with a written record; every figure carries its tree; no secrets anywhere; parameterised queries only; errors handled or thrown; one editor per file; threads report to the coordinator only.

**NOT APPROVED:** multi-platform league import; offseason product; monetization model; banning narrative features.

---

## 5. PR BOARD

State as of the thread handoffs (18:01Z-18:14Z) plus the Evidence Auditor's 18:06Z-18:18Z merge pass. Heads are what each thread reported for its own PRs; where the Evidence Auditor read a different head, both are given. `gh` could not be run here. "REAL" = the Evidence Auditor's verdict on the named head; it does not travel to a rebased head. "dirty" = GitHub reports a conflict against the current base.

### 5.1 Merged today (21)

| PR | Merge sha | What landed | Thread |
|---|---|---|---|
| #91 | `e3e76025` | Every read on the trade path says what measured it | Trade Ledger |
| #89 | `1a136145` | League history as-of rebase (migration 064) | Chat sync |
| #111 | `ac31922d` | A failed archetype read is reported by asking, not by being thrown at (fixed the #89/#91 contract-coupling regression that had blocked the fleet) | Trade Ledger |
| #102 | `140d436` | CLAUDE.md CI-history correction (7 runs, run 252 first to complete) plus PR board batch 3 | Release |
| #114 | `0877ea4` | Feature audit evidence (docs) | Feature audit |
| #68 | `26a5002a` | Target-share prior graded, default-off behind `sharePrior` | Model evidence audit |
| #95 | `eb861feb` | Live-tier jobs off the request thread | Scheduler |
| #86 | `7eca9a8b` | Data freshness that reads the rows; `unknown` freshness status | UI |
| #112 | `a1e661fc` | Depth-chart zero-row guard, pinned | Scheduler (memory had it as Chat sync; Scheduler's own record claims it) |
| #109 | `4ff3048b` | `build-negotiation-profiles.mjs` refuses to run without an explicit paid-run opt-in | Coach (drove the merge) |
| #87 | `0c5b7be4` | Feed-zero sentinel plays guarded in v1, v2 and `participation()` | Feature audit (from Planner's routed finding) |
| #110 | `ab01efa8` | Off-thread test temp-dir cleanup survives ENOTEMPTY race (branch `claude/project-thread-sytruo-offthread-cleanup`; the board's stale "unassigned" entry for that branch is this PR) | Chat sync |
| #108 | `eb19f475` | The wiring gate turned on over existing debt; 881-row inventory; accept list with owner and retirement per entry. Main went RED here. | Wiring map |
| #99 | `c5ee3b54` | Two reach graders `scripts/reach-grade.mjs`, `scripts/symbol-reach.mjs`. DEPLOYED tree. Its tree is not the tree Opportunity tested (`5c6cce90` vs `1da962d3`): a squash lands on main as it is, not as the branch was. | Opportunity |
| #113 | `bf359c68` | Count an unowned ESPN slot instead of dropping it silently (hole fixed by #131) | Chat sync |
| #115 | `144b722` | `availabilityPicture()` says "data gap" instead of "close to healthy" when `nfl_injuries` is empty for the team | Feature audit |
| #117 | `92654e20` | `build-manager-archetypes.mjs --jev` paid-run guard | Coach (drove the merge) |
| #119 | `f620a120` | A growth cycle whose download threw stops reporting ok (its "retries next cycle" note is FALSE for optional sources; fix owed) | Scheduler |
| #129 | `c90d2834` | Three false positives in the gate fixed, `check:wiring` folded into `npm run check`; main green (R60) | Wiring map |
| #124 | `9f0b5b66` | `liveDraft()` fails closed with the failure named (was fail-open on the cookie-collision gate). Merged 18:06Z on head `8c66d93e`; main run cancelled, covered only as an ancestor of `6e722719`. | Chat sync |
| #92 | `6e722719` | Participation season gate deleted (2024-2025 had been silently skipped for two years), `chartingSummary` binds its season and branches on HTTP status, `who-plays.js` reads the defensive snap column, migration 070, red-zone tiers, `scripts/paid-run-optin.mjs`, `nfl_route_splits` WITHDRAWN on its own evidence. Merged 18:08Z on head `23b4ddd`, main run SUCCESS. | Planner |

Evidence Auditor verdict heads (what REAL was measured on, for the record): #111 `670feac9`; #108 `1161c165` then `a333f449`; #86 `85caa0d5`; #95 `3f3ddbaa`; #112 `fa84d59f`; #68 `29b148c7`; #109 `5b27a540`; #110 `311e84cc`; #87 `d372fd3c`; #113 `8fd27b20`; #99 `605ab3f6`; #115 `6d637d3d`; #117 `38f6f4c5`; #119 `225f5ec7`; #103 `08c729e1`; #94 `00319229` (run on `951c70f1`); #120 `a1164dd5` (tree `9fad5221`, five-step gate only); #116 `6dfc123d` (run on `93870a92`); #92 `28ff7ae5` (superseded by `23b4ddd`); #129 `6432a768` (tree `63ebac8a` = main's tree). #102 and #114 were docs-only and did not pass the gate.

### 5.2 Closed today

| PR | Reason |
|---|---|
| #36 | Superseded by #108 (Wiring map). Branch kept. |
| #54 | The routes it labelled no longer exist on main (`routes/model.js` routes deleted 2026-09-20). Branch `claude/wiring-map-8f96ur-availability-basis` at `207f7640` kept; `availabilityBasis()` is already exported from `contingency.js`. (Memory's board still listed #54 as open keep-or-close; Wiring map's own record says closed.) |
| #105 | Superseded by #106. |
| #35 | Release train done elsewhere: #63 `2206383` (carries #56, #59, #61), #52 `9075e33`, #49 `654ff93`. |

### 5.3 Open PRs by thread, with what is left

**Trade Ledger (Trade Brain), merge stack in order: #94 → #103 → #120 → #100**
- #94 outcome ledger: rebased head `ff38f5a2` (local, gate started 17:59:52Z, NOT pushed; remote still `00319229`, REAL there, does not travel). Adds `trade_outcomes` (migration 067) and `trade-outcomes.js`; no observed row exists yet. Left: gate exit, push with `--force-with-lease=<branch>:00319229`, Auditor on the new head, squash. Evidence Auditor body-read: compliant, liveness proof 33 rows / 32 killed.
- #103 bare-catch sweep: head `08c729e1`, base `ac31922d` (predates #108, so no wiring gate on its tree), CI green, REAL there, dirty. Left: rebase after #94, six-step gate, delete the false paragraph at body line 95, resubmit.
- #120 crashed read stops telling Nick to send the offer: head `a1164dd5`, stacked on #103, REAL on tree `9fad5221`. Body thin on liveness proof (no failure count against unfixed code). Left: rebase after #103, both gates, resubmit.
- #100 two reads that said "absent" when they meant "could not look": head `29c825cf` pushed, base `f620a120`; last gate CHECK_EXIT 0, 3539/3498/0/41, tree `7a0ad270`. Left: rebase after #120, gate, Auditor, squash.
- #70 (head `10daad70`) and #41 (head `12223841`): evidence-only, figures from 2026-09-20, bodies corrected; rebase and fresh run before quoting.
- #136 jev/4b port snapshot: head `7e57a9ba`, 103 commits off `654ff933`, conflicts with main. NEVER merge, NEVER rebase. Re-derive the three or four surviving pieces as small PRs (listed in its body).

**Opportunity (w45mur)**
- #135 reach-ladder command: head `1d5076fe`, base `c90d2834`, draft, RED `5f2433dc` GREEN `1d5076fe`. R62: numbers reproduce exactly; HELD only on the call-site mutant at `reach-ladder.mjs:266` surviving (14/0); closes with a test that kills it. Pre-rebase `83b67b82` gate exit 0 (3547/3506/0/41) does not transfer.
- #116 symbol-reach namespace imports: head `6dfc123d`, base `f620a120` stale, REAL. Left: merge main in, one guarded run, merge (queue position 2).
- #85 one vocabulary for what priced a chance to play: head `69909edd`, base stale, verified. MERGE main in, NEVER rebase (the branch carries two reverts of the CONTRACT.md #99 landed; replaying deletes a live document; foreign author).
- #72 teammate-absence multiplier: head `ffe83637`, base `791b131f`, zero CI runs ever; body's false "CI disabled" claim retracted 17:56Z under a visible heading. Left: merge main in, push, first CI signal, then Independent Auditor. Until it lands the `cascade-grade.js` accept-list line warns on every branch.
- Archive refs, pushed for preservation, NEVER merge, no PR: `archive/w45mur-data-completeness-check` (`06f7b4e7`), `archive/w45mur-wiring-map-5ab50c8` (`5ab50c85`); diffs against main are 90,529 and 50,858 line deletions.

**Chat sync (sytruo)**
- #125 feature-store-state: head `9ed12b95` (`0f442fa` + main), gate exit 0 on tree `c6c9ddb3`, 107/107; guard on merged head still running at save. Conflict pre-resolved at `4d2c505` per the Evidence Auditor; lands after #131.
- #131 history-state (fixes #113's hole): head `3fddc87f` (`665cbb2` + main), gate exit 0 on tree `eac38a40`, 110/110, RED `eedeb64` GREEN `665cbb2`. Body-read compliant. Next in the merge pass.
- #134 empty-payload: head `a0bb416`, base `f620a12`, NOT merged with `c90d2834`. RED `fa5e1fb` GREEN `a0bb416`, 9/9. Left: merge main, one guard, replace the gate figures in the body (a warning block says so), then Auditor.

**Release**
- #127 deploy button + `.dockerignore`: head `883f4bf` (rebased; body still cites `43116ab`), base `c90d283`, draft. RED `62816a9`, GREEN `ba97ee8`, 8 tests, 22/22 mutants killed. `deploy.yml` runs on `workflow_dispatch` only, stages the brake, fails unless the new image logs "Scheduler disabled", never contains `secrets unset` (test 4). `.dockerignore`: `data`, `analysis`, `test`, `**/node_modules`, `**/*.sqlite*`, `**/*.bak`. Left: gate on `883f4bf`, CI green, Auditor, merge; then Nick's token; then Nick's brake decision (section 1 item 3).
- #133 nflverse CC BY 4.0 descriptor: head `617fd7f`, base `c90d283`, draft. `NFLVERSE_SOURCE` at `server/services/nflverse.js:25-35`; `sources` on `GET /api/data-freshness`; 4 tests, sweep 10/10. R59(3): cleared as a machine-readable step, not compliance; the visible credit line is UI's, open. Left: gate, CI, Auditor, merge.

**Wiring map**
- #137 CONTRACT.md reach-grade column: head `3a770c6`, docs-only, gate exit 0. Prints measured cells only (172 low end, both columns sum to 321), pinned to `c90d2834` and subtree hashes `server/services` `2c900fff`, `server/modeling` `6bbd8e6a`. The headline bracket at `CONTRACT.md:126-135` is deliberately untouched until R62's mutant closes. Left: CI green, merge (docs-only skips the Evidence Auditor).

**UI (xiezr0)**
- #132 ceiling-recency: head `929432cc`, base `c90d2834` merged in, tree `cca2dfa9`. `npm run check` exit 0: 3562/3521/0/41, 485.4 s; RED 6/9 failing, GREEN 9/9; sweep 10 rows, 7 killed, 0 survivors, 3 controls. R64: CLEARED to merge. Claim: `ceiling-lineup.js:62` built projections under seasonDecay 0.35 while the weekly engine used 0.05; now passes `WEEKLY_ROLE_RECENCY`. Incumbent example: HOM QB1 2026 wk5 attempts 29.126/carries 0.221 vs weekly 23.973/0.369 (21% gap). No held-out split, no accuracy claim.
- #118 position-applicability: head `de5aa1a9` (main merged in; local until the gate returns); earlier CI reds were main's three findings.
- Older, untouched: #78, #76, #75, #73 (stacked design-system chain; bodies carry the false CI paragraph), #60, #58, #53, #46, #43 (`b7d88fcf`, bare `claude/project-thread-xiezr0`).
- #140, #141, #142, #143 on `claude/project-thread-xiezr0-mergeproof-*`: snapshots, DO NOT MERGE, CI expected red, close once read (like #65/#69).

**Scheduler (o3wt2p)**
- #128 MLB removal: head `8e2667bc`, draft, full gate running; `check:wiring` exit 0 on it. Removes `routes/mlb.js` (28 endpoints), the mount, eight services (1,898 lines), five jobs, `scripts/bootstrap-mlb.mjs`, the NRFI test, and the writer-less reads Nick named 17:59Z. No table dropped, no row deleted (`test/mlb-removed.test.js` asserts the eleven `mlb_*` tables stay and nothing DROPs). Left for Wiring map, one commit on this branch: `wiring-map.mjs:2164-2165` (`BETTING_TABLE` matching `mlb_` MUST stay) and CONTRACT.md's mlb column (`:126`, `:162`, `:182`, `:199`, `:205`). Expect `wired-mlb-only` files to become `unreached`.
- #126 refresh-lastline: head `6b4d9ee2`; gate exit 0 on `018c1ac9` (3550/3509/0/41); needs a re-run on merged main.
- #77 mainthread-holds: head `3902ba78`, retargeted to main 17:47Z, dirty; three conflicts analysed: take main on `scheduler.js` and `test/abandoned-run-backoff.test.js`; `test/health-route-single.test.js` needs a decision after reading current wiring-map output. Body missing all four self-check parts, zero check runs.
- #84 scheduled-ingests: head `8709ec66`, stacked on #77 (legitimate). Re-check after #77.
- #96 servedtables: head `a24692d3`; its merge gate is lifted (the `sql` rule shape consumer landed with #86). Rebase and re-run.
- #104 freshness-evaluator: head `e3a86764`, stacked on #96; false CI paragraph to remove.
- #101 epoch-fallback-loud: head `8d7312dc`; re-run against current main.
- #98, #97: old; rebase or close.

**Feature audit (5f9c3y)**
- #130 evidence fault not no-record: head `5b8a345` (Evidence Auditor read `8047ba7`; a docs commit above the guarded `5b8a345`), base `c90d2834`. Four surfaces fixed incl. `RiskStrip.tsx:5-6`; RED/GREEN triples `77ccbbc`/`6412283`, `cc97eb2`/`8b522d1`, `e7d2a84`/`389c2ac`; server 11/19 fail pre-fix → 19/19, client 5/5. Guard on `5b8a345` running at save. Left: guard exit, body updated to the new shas, CI, Auditor R50 final.
- #122 (head `0cac36d`) and #123 (head `a36c69c`): docs-only, rebased, CI green needed only.
- #55 `ae28a81`, #62 `457aac5`, #67 `305c612` (stacked on #62), #74 `b5f3996`: 2026-09-20 heads, bodies corrected, no guard on current heads. #62 and #74 need the Independent Auditor before merge.
- #138 draftboard-health-hold (`759f6cf`) and #139 trend-exploits-usage-blindspot (`bc03222`): rescued unsaved work, opened as drafts, no guard run. #138 overlaps #79 (merges `claude/project-thread-f921do-preseason-layers-hold`); #138 and #139 both edit `trend-exploits.js`; #139's evidence file wrongly says "not pushed".

**Fantasy plan (f921do)**
- #106 shrinkage-efficiency-weighting: head `af60024`, base `c90d2834` (merged at `fe6d73d`), 16 files +2,937/−23, draft. `check:wiring` exit 0 on tree `669436af`; full guard in flight. CI has never reached this branch's test phase (gate was red first). RED `60dc4f0` (18/2) GREEN `d01aac8` (20/20) for the committed-fixture guard. Left: guard, CI, Auditor REAL, and the Auditor's call on whether any of R35 still holds it.
- #66, #79, #80, #83: stale drafts, zero check runs, bodies corrected, deliberately not pushed. Keep-or-close decision owed.

**Coach (4l8hno)**
- #90 the whole of Coach: head `4b7f49ec`, main merged at `67ebb0ef`, 72 files +16,077/−35, 82 commits, draft. `check:wiring` exit 0 (was 19 findings; ten were a local named `db` holding the chat corpus, renamed `chatDb`). Full guard running; previous head 3768/3723/4 fail, all since fixed. Body's "19 blocking findings" paragraph stale. Left: guard, Evidence Auditor, ready-for-review, squash. Then close #82.
- #82: head `3ba9ec4a`, base `791b131f`, dirty, wholly contained in #90; do not resolve; close when #90 merges.

**Model evidence audit (w0gpjt)**
- #121 coupled-grade pre-registration: head `91ed5f1a`, tree `f5969717`, base `c90d2834`. `npm run check` exit 0: 3553/3512/0/41; `check:wiring` exit 0 run explicitly too. Three files, no behaviour change. R57.3 items fixed in `5fe195bf`; R62.5 adds one line (where the fitted k vector comes from). Left: that line, Independent Auditor clearance, Evidence Auditor REAL.

**Google sign-in (n4052e)**: #48, #71, #81, bodies corrected by Fantasy plan at the coordinator's request; the thread has finished (no handoff section supplied).

**Planner (2oztzw)**: nothing open; #92 merged; branch spent.

### 5.4 Merge pass order (Evidence Auditor, 18:06Z)

#94 first; then anything green in any order; except #103, #120, #100 follow #94 and #84 follows #77. Green and queued after the two merges: #131 `3fddc87f`, #125 (after #131), #121 `91ed5f1a` (needs Auditor too), #130 `8047ba7`, #132 `929432cc`, #137 `3a770c6`, #90 `4b7f49ec` (CI pending). Not in the pass: #38, #40, #44, #43, #60, #15 (old, paused threads); #72, #62, #74 need the Independent Auditor first.

Stale-head mismatches the body-read found (a quoted exit code is not a statement about the tree that merges): #94 `8406ecbd` vs `00319229`; #116 `28b801b8` vs `6dfc123d`; #125 `0f442fa` vs `9ed12b95`; #100 `eb813b4d` vs `29c825cf`; #127 `43116ab` vs `883f4bff`; #130 `5b8a345` vs `8047ba76`; #131 `665cbb2` vs `3fddc87f`; #103 `a830c2b` vs `08c729e1`; #132 `8c04565` vs `19510ce2`; #124 no sha at all. Only #121, #120, #84 quoted a run on their current head.

### 5.5 The thirty old PRs with false "CI cannot run" text

`/mnt/project-files/audit-lapsed-hold-reasons-2026-09-22.md`: #35, #41, #47, #48, #55, #57, #66, #67, #70, #71, #72, #73, #74, #75, #76, #77, #78, #79, #80, #81, #82, #83, #84, #96, #97, #98, #101, #103, #104, #107. Written when Actions was disabled; CI has run since 14:52Z. Many bodies were corrected in place today (body edits, no push, no CI spent): #47, #55, #57, #67, #74, #70, #41, #72, #82, #66, #79, #80, #83, #48, #71, #81. Still to strip on next push: #103 (line 95), #104, #73, #75, #76, #78. The true reason those heads show no check runs is that they have not been pushed since 2026-09-20.

### 5.6 Local-only things that die with containers

Trade Ledger backup refs under `backup/` (`ledger-00319229`, `datakey-0cfd96d4`, `datakey-2ef11634`, `ledger-f0fa58d7`, `ledger-8406ecbd`, `tactics-81f5b09e`, `enginefault-89de8326`); Release scratch branches `rehearsal`, `verify-stack-six`, `release-train-scratch` (`1b7e55b`), all superseded; guard scripts `/tmp/claude-0/repro/verify1x-local.sh` (Scheduler) and `/mnt/project-files/verify2x-v4.sh` (shared, runs the suite twice; a task to make the second run conditional was completed at handoff time); gate logs named in each thread's section 6 entry. Everything that matters is on a pushed branch.

---

## 6. THREADS

Fifteen threads plus two auditors. Every entry below except Google sign-in was folded in from that thread's own file under `/mnt/project-files/handoff/` (written 18:01Z-18:14Z) and is marked "(thread's own words)"; the text is condensed but nothing was added that the thread did not say. Google sign-in supplied nothing and is from the coordinator's record. Message-id prefix throughout: `cmsg_01YAsw8AnFv4ioRMQw8dfPmT`. Full files: `/mnt/project-files/handoff/<name>-2026-09-22.md`.

### 6.1 Release (thread's own words)
- Thread: [Release](#cmsg_01YAsw8AnFv4ioRMQw8dfPmTLtvcKX5oSjZz6x2L5FEXHk). Session `cse_016rykKAHmB43LAZp6eqwedG`. Model claude-opus-5-5. Reports to the coordinator, never to Nick.
- Branches: designated `claude/release-train-2yv3x6` (PR #35, closed as done; nothing on it is live); unit branches `claude/deploy-workflow-gh-actions` (#127), `claude/nflverse-attribution` (#133).
- Owns: `.github/workflows/deploy.yml`, `docs/runbooks/deploy-workflow.md`, `test/deploy-workflow.test.js`, `docs/tdd/deploy-workflow.tdd.md`, `docs/tdd/sweeps/deploy-workflow-mutations.mjs`, `.dockerignore`; the `NFLVERSE_SOURCE` grant in `server/services/nflverse.js`; the `sources` field in `server/routes/data-freshness.js`; `test/nflverse-attribution.test.js` and its evidence. Not `ci.yml` (Scheduler's), not client code (UI's).
- Shipped: #102 `140d436` (CLAUDE.md CI-history correction; board batch 3). Closed #35 as done (#63 `2206383` carrying #56/#59/#61, #52 `9075e33`, #49 `654ff93`). The 17:05Z deploy of `c5ee3b54` = main at #99; #113/#115/#117/#119/#129 NOT in the live image.
- Open: #127 head `883f4bf` (gate on `43116ab` before rebase: check rc 0, 3500 pass; isolated gate on `883f4bf` running at handoff, result `/tmp/claude-0/gates-c90/pr127/summary.txt`, container only). #133 head `617fd7f` (gate queued after #127's). Left on each: gate result, CI green, Auditor REAL, merge under Nick's "1".
- Blocked on Nick: (1) `FLY_API_TOKEN` after #127 merges, and the brake decision before the first press (section 1 item 3; not yet proven that a deploy token may `secrets set`, `secrets list` and `logs`; if step 2 fails nothing was deployed). (2) Read-only check that the live image carries no local database: `fly ssh console --app gridiron-hq -C "find /app -name '*.sqlite*' -o -name '*.bak'"`; until #127, main's `.dockerignore` excludes only `server/data.sqlite*`. (3) `nflverse/nfldata` licence not established: `LICENSE`/`LICENSE.md` 404 on master, main, gh-pages; README (master, 1,017 B) names none; consumers `gamescript.js:23`, `nfl-opening-lines.js:31`, `nfl-profitability.js:19` (betting), `nfl-coaches.js:31`, `nfl-officials.js:25` (fantasy). (4) Repo has no LICENSE; observation only, do not add one. (5) Key rotation: 3 exposures, 0 rotations, declined 15:38Z.
- Findings not built: visible CC BY credit line (UI; must render regardless of freshness because `DataFreshnessBanner.tsx:132` returns null when fresh or dismissed); `Model.tsx:31` fetches `/model/status` which does not exist (`model.js:538`), so the "players linked to nflverse" strip never renders (UI); `FFOPPORTUNITY_SOURCE` (`ffopportunity.js:17-23`) lacks `license_url`, `creator`, `modified`, pinned verbatim by #133 test 4 so change both together; `node_modules` mtime moves during `npm run check` (Vite writes `.vite-temp`), guard on `node_modules/.package-lock.json` mtime instead.
- Rules: deploy order is brake, deploy, `/api/health` plus the log line `Scheduler disabled via SCHEDULER_DISABLED=1` (`scheduler.js:2130`), unset only after three proofs and only by Nick. Rollback: re-set the brake first, then `deployment-01M2VZ9JRYSXVHCRWJ83V360QH`, Nick's word. `deploy.yml` must never contain `secrets unset`. Rebase onto current `origin/main` before every merge; never use local `main` (`git checkout main -- .` once staged 214 old files). Docker in the sandbox needs the intercepting CA in a bench copy of the Dockerfile; never change the repo's. Public repo: Actions logs are public; never print a secret, the secrets list or app logs.
- Next three: (1) `git fetch origin`; confirm #127/#133 heads are still `883f4bf`/`617fd7f` on current main, rebase if moved and update evidence shas in the same push. (2) Guarded gate per head in its own worktree, then the mutation sweep; send head and exit code to the coordinator and Evidence Auditor. (3) Merge on green plus REAL (squash); after #127 lands give Nick the two token steps. Never press Deploy; the button is his word.

### 6.2 Wiring map (thread's own words)
- Thread: [Wiring map](#cmsg_01YAsw8AnFv4ioRMQw8dfPmTFsHkhHKFJUzXHr2RdRZRWK). Session `cse_01CQKUgynAHZALCtMfvu7ieE`. Phase 0 item 5, the honest inventory. Product is a gate that fails a build when something new lands unwired, plus the inventory and contract that explain it.
- Branches: designated `claude/wiring-map-8f96ur` frozen at `603080a` (PR #36's head, kept; the Stop hook reports phantom unpushed commits against it, verify with `git rev-list --count @{u}..HEAD`). Live: `-contract-column` `3a770c6` (#137), `-gate-fix` `6432a768` (#129 merged), `-availability-basis` `207f7640` (#54 closed, kept), `-inventory-hold` `a333f449` (#108's head), `-census-hold`, `-route-gate-hold` `23ed6daf`, `-usage-coverage-hold` `2626712b`, `-usage-coverage-setup-hold` `a44f53ca` (parked units).
- Owns: `routes/model.js`, `routes/accolades.js`, `scripts/wiring-map.mjs`, `scripts/inventory.mjs`, `docs/wiring/annotations.json`, `docs/inventory/CONTRACT.md`, `server/services/gridiron-model.js`, `scripts/run-purged-evaluation.mjs`, `scripts/run-historical-leaderboard.mjs`, `scripts/freeze-baseline.mjs`, `scripts/joint-score-report.mjs`, `scripts/lib/evidence-report.mjs`, `test/decision-inbox.test.js`, `test/health-endpoint.test.js`. Grant on `package.json` (one line, 17:01Z) is spent.
- Shipped: #108 `eb19f475`, #129 `c90d2834`. Main's own push run on `c90d2834` is `35763448618`, success. What #129 fixed: (1) `td-features.js:188` queries the nflverse DB as `nflDb.prepare(...)` and `handleFor` fell through to `'app'`; an unrecognised explicit receiver is now reported as itself (24 candidate sites, exactly 2 tables move); (2) `producer-with-no-caller` counted `name(` only, so `league_rosters: { run: refreshLeagueRosters }` at `scheduler.js:1299` read as uncalled; `callSites()` now counts a registration; (3) a pinned test asserted the wrong thing against its own preamble; reversed with the reason written in.
- Open: #137 (section 5.3). Headline bracket `CONTRACT.md:126-135` untouched until the Auditor's ladder ruling.
- Blocked: the MLB commit waits on Scheduler's branch (now #128, `claude/project-thread-o3wt2p-remove-mlb`): two edits, `wiring-map.mjs:2164-2165` and the `mlb` column, one commit on Scheduler's branch, both gates on the combined head. The §2b low-end correction waits on the Auditor (R62 now: mutant only).
- Findings not built: (1) the stale-entry report cannot tell a deliberate pre-registration (`cascade-grade.js`, `_PERMANENT_ORPHAN_REASONS`, arrives with #72) from a rotted entry; fix `staleOrphanEntries` to read the `PRE-REGISTERED` marker; owner this thread, with the ratchet. (2) 19 queries on a receiver the resolver cannot identify (`td-features.js` 7 sites, `scripts/run-historical-leaderboard.mjs` 8, `test/model-registry-persistence.test.js` 4); the ratchet baselines 19 and fails on growth. (3) Six grandfathered producers open; worst is `syncEspnMarket()` (`espn-market.js:18`), sole writer of `espn_player_market`, read at zero hops by `routes/aggregates.js:226` and four fantasy modules: a live fantasy surface reading a table nothing fills; owner Feature audit, Scheduler registers the job. Also `backfillNewsEntities()` (`routes/espn.js:210`, unassigned) and four betting-scope ones. (4) An availability figure with no basis reads as a low chance of playing for a kicker (was #54); only matters if those payloads are rebuilt. (5) Two out-of-order `accepted_orphan_modules` pairs, cosmetic.
- Rules: `GATING` is two rules; everything else reports. It runs at `ci.yml:70` AND in `package.json:20`; CI never invokes `npm run check`. The accept list is a ratchet, not approval: every entry has an owner and a RETIRES WHEN that is a result, not a date. `NEVER_BASELINE` holds `producer-with-no-caller`. Report-never-gate on stale entries is deliberate. A gate baselined at one tip and landed at another is the failure mode: rebase and re-run immediately before merging a gate change. The recurring defect class is a confident default standing in for a question nobody asked (`boot:server/index.js`, `CLOSE_HOPS`, `'app'`, `name(`): when the resolver does not know it must say so. Naming an unknown receiver trades a visible false positive for an invisible false negative, which is why `unresolvedReceivers` exists. Cite the population subtree hashes (`2c900fff`, `6bbd8e6a`), not the repo write-tree. Every published `reach-grade.mjs` figure is request+job; the request-only end has only ever come from a hand-written driver (205 → 196 → 172 → 178). The 172/228 pair on tree `500bab36` commit `b0c1616d` is reachable from nobody's repository.
- Next three: (1) get #137 merged (docs-only, CI green). (2) Do not delete the `cascade-grade.js` line; teach the report to say which case it is. (3) Build the unresolved-receiver ratchet. Then the MLB commit when Scheduler names the branch, and the §2b correction when the Auditor rules.

### 6.3 UI (thread's own words, relayed by the coordinator)
- Thread: [UI](#cmsg_01YAsw8AnFv4ioRMQw8dfPmTYDSUNJhXhq5i9wxu6ZASvP). Session `cse_012mJNcQKskfZmyq4qTmFqDe`. Designated `claude/project-thread-xiezr0` (bare name carries #43 at `b7d88fcf`); units on `-<topic>`. Everything is on the remote.
- Owns: all client pages, `lineup-brain`, `lineup-posture`, `draft-assist`, `news-fantasy-impact`, `routes/nfldata`, `stats`, `accolades`, `news`, `analysis`, `players`, `trend-watch`, `ceiling-lineup.js`, `client/src/components/AdvancedStatsPanel.tsx`. Not: `server/index.js`, `source-registry.js`, `routes/model.js`, `trade-engine.js`, `projections.js`, `nfl-weekly-feature-store.js`, `weekly-weight-store.js`, `shrinkage-fit.js`, `player-week-engine.js`, `routes/trades.js`, `manager-archetypes.js`, `CLAUDE.md`. For #130 only, Feature audit holds `RiskStrip.tsx` floorOf (`:5-6`, `:31-32`) and `types.ts` trade-risk lines (`:41`, `:44-50`).
- Shipped: #86 `7eca9a8` (16:59Z), introduced the `unknown` freshness status.
- Open: #132 `929432cc` (R64 cleared), #118 `de5aa1a9`; the old chain and the four mergeproof snapshots (section 5.3).
- Blocked: `shrinkage-fit.js:507` one-line correction (lists ceiling-lineup as season-long, now wrong; Fantasy plan's file). Nothing else.
- Findings not built: `Model.tsx` and `Edge.tsx` imported by nothing (measured on `f620a120` with a passing control: TeamDetail `App.tsx:17`, MyTeam `LeagueHub.tsx:4`, Leagues `LeagueHub.tsx:3`, Drafts `DraftHub.tsx:3`); `routes/model.js:1-26` records 18 routes deleted 2026-09-20 and says not to re-add (Nick's nav decision). Four unverified client paths: pairing-code, pairing-info, `/drafts`, `/drafts/live/link`. `shrinkage-fit.js:500-515` names four more season-long callers (preseason-model, season-sim, draft-assist, week-postmortem), each deserving the #132 question; owner unassigned. R45(1) self-referential ceiling target (`effectiveTarget = target ?? r2(naiveScore.ceiling)` at `ceiling-lineup.js:211`; `ceiling = r2(q(0.90))` at `:132`), named in #132's docstring, not fixed. Stray remote branch `tdd/xiezr0-hosted` should be deleted. Credit-line unit needs #133's shape. Coach panel importing `coach.types.ts`: file lives only on #90.
- Rules: nav is 8 tabs, never rebuild a deleted page. A page-reachability scan without a passing control is worthless (first scan missed `lazy()` and hub imports). Branches cut from a squash-merged PR head hit add/add on every file that PR added; take your side only after `git diff origin/main <head-you-cut-from> -- <file>` is empty. An absence must say which absence, from a field not a sentence (unknown vs fresh; not_measured vs zero; not_applicable vs not_measured; table_absent vs empty). An assertion on a word or a constant compared to itself is not an assertion on behaviour (five survivors were this shape). A gap is not an answer. `mock.module` takes `namedExports` not `exports`; `assert.doesNotMatch` not `notMatch`. Every mutation spec needs a designed surviving control and a not-applied control; `mutation-runner.py` decides APPLIED by `count(old)==1`. Never `git add -A`; check `merge-base --is-ancestor` before every push. Biggest saving: one guarded `npm run check` per push with the evidence in the PR body; second, get main green first because every branch pays for a red main.
- Next three: (1) check CI on #132 and #118, read the job log first if red. (2) Merge #132, then #118. (3) R45(1).

### 6.4 Fantasy plan (thread's own words)
- Thread: [Fantasy plan](#cmsg_01YAsw8AnFv4ioRMQw8dfPmTHJkQrWhT9rQXMaMsRp8sgy). Session `cse_01U2PQK2qw4VrXdytNqpq5an` (the Plan 01 / R25 / shrinkage unit). Designated harness branch `claude/project-thread-f921do`; live branch `shrinkage-efficiency-weighting` (#106); stale `-season-list` #66, `-arm-align-hold` #79, `-memo-key-hold` #80, `-weekly-scores-hold` #83. The `n4052e` prefix (#48, #71, #81) is Google sign-in's, finished.
- Owns: `nfl-blind-audit.js` (live bug at `:263`), `weekly-backtest.js`, `scripts/verify-qbr-integration.mjs` (claims production at `:12-13`), `scripts/fit-weekly-coverage.mjs` (`:227` runs `RUNS=300` vs production's 2,000 draws), `level-information-decomposition.js`, `scripts/r25-level-vs-information.mjs`, `backtest-significance.js` for the one additive `observed_diff` unit only, `nfl-player-context.js` and `shrinkage-fit.js` for #106. One-time grant on `docs/wiring/annotations.json` applied at `af60024` and spent. NOT `projections.js`; the struck paragraph at `:264-270` is never to be edited; a QBR re-run is a new gate.
- Shipped: nothing merged. Any merge sha attributed to this thread should be checked, not believed.
- Open: #106 in four parts: (1) efficiency specs weight by RECENCY not raw opportunity count (`c8939a8`); (2) Plan 01 graded availability `fitGradedAvailability` + `gradedAvailabilityMultiplier` with the R19.6 conditioning fix (population from each entity's EARLIEST `nfl_feature_revisions` row, because `nfl_injuries` UPSERTs in place and reading it for a Sunday-lock decision is a look-ahead leak); (3) the R25/R36 level-vs-information decomposition, pre-registered then run; (4) the R40 kill switch `GRADED_AVAILABILITY_ENABLED = false` at `nfl-player-context.js:537`, zero call sites, a default rather than a hard block because three Plan 01 evidence tests grade the enabled behaviour and opt in explicitly; default-on may happen only at the coupled grade's named call site, enforced by a scan that fails the build if a caller passes a 6th argument.
- Blocked: nothing technical; no new unit until Nick's fleet decision.
- Findings not built: (1) LEVEL correction on the incumbent (R46): 62% of control's apparent 2024 advantage is a calibration constant capturable with `m0` at `weekly-ensemble.js:69-77`; pre-register first, configuration B; highest-value unbuilt item. (2) `observed_diff` in `pairedBootstrapDiff` (`backtest-significance.js:114` returns the mean over bootstrap iterations, never the full-sample difference; 48 importers, 45 callers, additive only; five tests planned). (3) Sign convention: `weekly-ensemble.js:85-88` is predicted − actual, the R25 module is actual − predicted; state it before any signed error leaves a run. (4) `fit-weekly-coverage.mjs:227` draws question, a follow-up gate after the level correction. (5) The `production()` named-configuration-marker unit (`fit-weekly-coverage.mjs:72-75` promoted to one exported definition; eight call sites deliberately off on the axis they fit; `nfl-blind-audit.js:263` fixed in the same commit; `verify-qbr-integration.mjs:12-13` switches to the marker; 24-row reference `ROUTING-TABLE-REPLAY-CONFIG-2026-09-22.md`). (6) Two out-of-order annotations entries (Wiring map's).
- The R25 result, not to get backwards: control's vector vs shipped `WEEKLY_ENSEMBLE_WEIGHTS`, method locked before the run: 2024 (in-sample for control) n 4,419 raw 0.0728 significant, debiased 0.0274 NOT significant CI [−0.0570, 0.0037]; 2021 n 4,341 raw 0.0441, debiased 0.0128 not significant; 2022 n 4,359 raw 0.0015 not significant, debiased −0.0572 significant REVERSED, shipped beats control, under all three anchors. Control NOT promoted. Do not rebuild a promotion case on pooled MAE without explaining 2022. Every rig result carries the R37 rider: "this grades the replay predictor, whose equivalence to production's is unestablished."
- Committed-fixture RED form (R54.3): a guard test's scanner is a function over `{path, source}` records; cases are inline string fixtures; a separate test pins the real tree clean; strip comments and string bodies before scanning; commit must-NOT-flag fixtures; the binding resolver must find exactly one local name in the declaring file. The two cases that failed RED were an aliased import and a destructured dynamic import renamed on the way out.
- Guard results: `d4c6e49` (tree `93b22df5`) 3135/3094/0/41 clean; `771bf64` (tree `afb20a79`) 3145/3104/0/41, runs valid but the before/after window was broken (a kill did not take and a merge landed in the primary repo during it; recorded, not hidden); `af60024` (tree `669436af`) in flight, `check:wiring` exit 0. Guard: `bash /mnt/project-files/verify2x-v4.sh <commit-ish> [tag]`; nothing may touch git in the primary repo while its window is open.
- Next three: (1) read the `af60024` guard log (`<scratchpad>/guard-runs/run-r54merge.log`) or re-run; quote both exit codes against tree `669436af` in `docs/tdd/graded-availability-kill-switch.tdd.md` and the body; send to the coordinator. (2) Keep-or-close on #66, #79, #80, #83. (3) Pre-register and price the level correction.

### 6.5 Scheduler (thread's own words)
- Thread: [Scheduler](#cmsg_01YAsw8AnFv4ioRMQw8dfPmTTWgBAMfiBF9jyPkKJpFNGs). Session `cse_01DrNvRmumDqr2e83XuG7Qr1`. Prefix `claude/project-thread-o3wt2p-*`, thirty-four branches on the remote; the ones that matter are in section 5.3.
- Owns: `server/services/scheduler.js`, `source-registry.js`, `server/platform/loop-watchdog.js`, `fly.toml`, `server/index.js`, `test/scheduler-off-thread.test.js`, `weekly-weight-store.js` (epoch-fallback unit), and, recorded as unallocated and taken, `nfl-model-growth.js` and `nfl-advanced.js`. Not: `wiring-map.mjs`, `CONTRACT.md`, `routes/nfl-betting.js:1130`, the five weekly-fit promotion gates (`minSettled` 250 at `:224`, the four-way AND at `:310-311`).
- Shipped: #95 `eb861feb`, #112 `a1e661fc`, #119 `f620a120` (carries a defect found after merge).
- Open: #128 MLB removal head `8e2667bc`, #126, #77, #84, #96, #104, #101 (section 5.3).
- Blocked: Wiring map owes one commit on the MLB branch; #77's third conflict needs a five-minute read of the wiring-map output. Nothing on a person.
- Findings not built: (1) #119's `ingest_error` note promises a retry that does not happen for an optional source (`nfl-model-growth.js` gate `:222`/`:227`, ingest `:230-237`); not live (deployed image predates #119) but on main; first unit. (2) Optional sources never trigger their own sync: `nfl_snaps`, `nfl_ngs`, `nfl_pfr_adv`, `nfl_depth` sync only inside `runNflModelGrowthCycle()` with `force=false` and `:185` syncs them only when `finalized_week > 0 && (force || coreLag)`, `coreLag` counting required sources only; GATED on a fantasy-route call-reach trace (R54.4); do NOT flip them to `required: true`. (3) Two snap ingests, two tables, nothing reconciling: `nfl-advanced.js:174 syncSnaps` → `nfl_snaps` (name-keyed, keeps defence/ST, NOT scheduled) and `nflverse.js:270 syncSnapCounts` → `player_week_snaps` (player_id-keyed, offence only, scheduled); defensive and ST snap share exist only in the unscheduled table. (4) Do not put nflverse depth on a timer (`scheduler.js:1156-1162` header: 51 MB CSV, 2 GB machine, "how the OOM kills come back"); `scheduler.js:1164-1167` calls `syncDepthChart` from `routes/nfldata.js:217`, which writes `roster_players.depth_slot` from ESPN, not `nfl_depth`. (5) A third cause for `nfl_depth = 0`: on the rebuilt rig `syncDepthCharts` stored 0 rows because `game_lines` was empty ("Sync the schedule first"); read the recorded error text. (6) `saveAndVerifyWeeklyFit` (`weekly-weight-store.js:175`, demotes on failed re-read at `:184`) has zero callers; `runWeeklyLearningCycle → weekly-learning.js:319` calls raw `saveWeeklyFit`, so a promoted fit that fails re-read stays promoted; third unit. (7) `model.js:598-599` `correlations_fitted`/`gamescript_fitted` are bare `COUNT(*)` with no `fitted_at` predicate (Model thread). (8) Three refresh children still `process.exit` without flushing (`collect-league-transactions.mjs`, `collect-roster-snapshots.mjs`, `build-manager-signals.mjs`). (9) `ON_REQUEST_THREAD` holds 22 entries mixing "cannot move" with "nobody moved it"; the four 3-minute betting jobs are excused on an unmeasured worker cost.
- Rules: the brake is `SCHEDULER_DISABLED=1` as an app-level Fly secret; `fly secrets unset SCHEDULER_DISABLED` lifts it; never touch `LOOP_WATCHDOG_*`. Main is nine PRs ahead of the deployed image. `npm run check` runs the wiring gate BEFORE tests, so a red wiring step means "0 tests" is not a passing tree. Tiers: `resolveOffThread(job, override)` reads allow-list, override, then `job.offThread ?? job.tier === 'heavy'`; only heavy goes off-thread by default. `runIfStale` on a name not in `JOBS` returns `{ error: 'unknown job' }` and does not throw; two route handlers fire-and-forget `refreshInBackground`, so removing a job without its callers is silent; `test/mlb-removed.test.js` asserts every name `BOOT_JOBS` and `refreshInBackground` can carry exists. `node:sqlite` is fully synchronous: a job on the request thread blocks every HTTP request; "a slow job" and "an outage" are the same event. Measure on the tree you name (the primary worktree was 204 tracked files behind main). When you report a negative, say what you searched (`docs/inventory/CONTRACT.md` exists, 400+ lines). Verify a mutation applied by md5, never by grep. Run one full check at a time (ENOTEMPTY worker race). Kill a gate run on a stale tree. `MLB` is also Middle Linebacker (`FormationView.tsx`, five slot maps in `routes/nfldata.js`), pinned.
- Tooling (container-only): `/tmp/claude-0/repro/verify1x-local.sh <commit-ish> <tag>` (one clean run, both exit codes on their own lines); `/tmp/claude-0/repro/run-nowiring.sh`; `/mnt/project-files/POSTDEPLOY-READ-2026-09-22.sh` (paste-able, read-only, ten sections: migrations, scheduler runs, depth charts, freshness registry, growth runs, news importance, injuries, table presence, `league_transactions_raw`, `nfl_capture_triggers`).
- Next three: (1) finish the MLB removal: read `/tmp/claude-0/verify1x-nomlb4.out` or re-run, push `-remove-mlb`, rewrite #128's body from census to removal, send head and both exit codes. (2) Resolve #77 (main's side on two files; read wiring output for the third), merge `c90d2834` in, guard, push, re-check #84. (3) Fix #119's note with a RED that pins the optional case.

### 6.6 Feature audit (thread's own words)
- Thread: [Feature audit](#cmsg_01YAsw8AnFv4ioRMQw8dfPmT1e6VjvMZtgk12UfXZf7BhA). Session `cse_01XL5WQkomfhtJ925G1wZ9yr`. The fantasy-side sense-check. Designated `claude/project-thread-5f9c3y`, ten unit branches carry work: `evidence-fault-not-no-record` (#130), `trade-engine-crash-correction` (#122), `roster-risk-waiver-brain-audit` (#123), `consensus-season` (#55 `ae28a81`), `waiver-kdef` (#62 `457aac5`), `byerisk-honest` (#67 `305c612`), `window-honest` (#74 `b5f3996`), `draft-chain-hold` (`f6f4d79`), `week-callers` (`1b66a80`), `draftboard-health-hold` (#138 `759f6cf`), `trend-exploits-usage-blindspot` (#139 `bc03222`). The last two had no remote branch before this sweep.
- Owns: `nfl-weekly-feature-store.js`, `football-context.js`, `trade-engine.js` for the `evidence_unreadable` work; grant for #130 only on `RiskStrip.tsx` and the trade-risk lines of `types.ts` (17:51Z, UI told; hand back when #130 merges). Tests `football-context-injury-availability`, `trade-engine-evidence-fault`, `trade-risk-strip-unreadable`, evidence tests in `trade-evidence.test.js`. Evidence files `football-context-injury-data-gap.tdd.md`, `trade-engine-evidence-fault-not-no-record.tdd.md`, `roster-risk-waiver-brain-silent-default-audit.md`.
- Shipped: #87 `0c5b7be`, #115 `144b722` (on main; its own file says "in the deployed tree or later", which is wrong for #115: it merged after `c5ee3b5`).
- Open: #130, #122, #123, the 09-20 four, #138, #139 (section 5.3). #138 overlaps #79; #138 and #139 both edit `trend-exploits.js`; #139's evidence file says "not pushed", now false; `trend-exploits.js:245` `rosterTeams.has(team)` suppresses "avoid" reads for any rival on a team where you own any player, deliberately left.
- Blocked: nothing on a person. Standing risk not this thread's: five credentials exposed, zero rotations, declined 15:38Z.
- Findings not built: (1) `pass_rushers` guard sequencing: #87 parked it; #92 (now merged) writes `was_pressure` only on dropbacks (`nfl-formations.js:122-127`) and `nfl_play_charting` shares the `(game_id, play_id)` PK; THE TRAP: pre-070 rows get those columns only on re-ingest (`:86-88`), so a `was_pressure IS NOT NULL` gate before a participation backfill nulls essentially the whole history. Correct order: merge #92 → re-ingest participation 2016-2025 → then gate. (2) The rig's table inventory: free-CSV rig populates only `player_week_usage` (11,851 rows, 2023+2024) and `players` (738, every `espn_id` NULL); empty `leagues`, `roster_players`, `nfl_snaps`, `nfl_injuries`, `nfl_qbr_weekly`, `nfl_depth`, `player_week_snaps`, `nfl_team_week_features`, `nfl_player_week_features`, `nfl_play_formations`, `nfl_play_charting`, `dynasty_values`, `player_season_stats`, `nfl_teams`, `game_lines`; `nfl_games` does not exist; no 2026 data; larger than the "six blindnesses" shorthand. (3) The post-deploy `nfl_injuries` probe handed over has not come back; #115 is verified only offline.
- Rules: the cache-fingerprint hazard is a live trap: `findTrades` caches on `manager_profiles` COUNT and `MAX(updated_at)` (`tradeIdeasFingerprint` `trade-engine.js:1478`, `compute-cache.js:48-68`); upserting `tradeability` alone moves neither, so a second `findTrades` returns the same object; two of this thread's own e2e tests passed against unfixed code; fix is `reSearch()` writing a strictly increasing explicit `updated_at` and asserting reference inequality. The silent-inert class is the codebase's signature bug, found four times today (empty table read as healthy; thrown query read as "never played" three places; thrown query read as "no record" on the trade card); when you find an absence, ask what sentence the UI prints for it. `assetUniverse`/`loadRosters`/`freeAgents` do not crash (every caller validates first; #122 retracts). A test can hold the right reasoning and the wrong assertion (`trade-evidence.test.js:173`; own first draft of R2 in `trade-risk-strip-unreadable`); re-run RED after every assertion change. "Nothing is left uncovered" was a claim and was wrong; corrected in place naming the sentence. Report negatives (#123).
- Next three: (1) read `/tmp/claude-0/verify-5b8a345.out` or re-run `bash /mnt/project-files/verify2x-v4.sh 5b8a345 <tag>` and kill RUN 2; update #130's body to the new commit table; send head plus exit code. (2) Land #122 and #123 on CI green. (3) No new unit; `pass_rushers` is queued, not assigned, and not before #92 (now landed, so it is unblocked but still needs the backfill first).

### 6.7 onward: coordinator-record entries (skeleton). Thread-supplied text for Trade Ledger, Opportunity, Chat sync, Model evidence audit, Coach, Explorer, Planner, Independent Auditor and Evidence Auditor exists in /mnt/project-files/handoff/*.md and was read but not yet folded in; those files are the authority over the entries below. Google sign-in supplied no file.

### 6.7 Trade Brain
- Thread: [Trade Brain](#cmsg_01YAsw8AnFv4ioRMQw8dfPmTWTNUkdBSZxWaPcUAWCz5d8). Session `cse_01M65mgrWTebkf9zqNR3gFnL`. Trigger `trig_01Sp27doa6JEqMtBTg91jYc7` (enabled:false).
- Branches: `claude/project-thread-3xqh5l-*`, plus `engine-fault` and `jev-4b-port` branches.
- Owns: `server/trade/league-brain.js`, `counterparty-pricing.js`, `trade-acceptance.js`, `trade-tactics.js`, `routes/trades.js`, `server/league/manager-signals.js`.
- Shipped: #91 `e3e7602`, #111 `ac31922`.
- Open: #94 `00319229` (queue head, rebase first), #100 `eb813b4d`, #103 `08c729e1`, #120 `9fad5221`.
- Known: `tradeWeekContext` missed wire-up (section 7).
- Blocked on: the gate; nothing from Nick.
- Next three: (1) rebase #94 onto `c90d283` and re-run check; (2) rebase #103/#120; (3) `tradeWeekContext` wire-up with a RED test.

### 6.8 Opportunity
- Thread: [Opportunity](#cmsg_01YAsw8AnFv4ioRMQw8dfPmTPBrwJmDV2B1faqr1XGaJM4). Session `cse_01BvuTs792dFTGix8BixBJWv`.
- Branches: `claude/project-thread-w45mur-*` (`-reach-ladder`: RED `e2f2674b`, GREEN `83b67b82`).
- Owns: new files under `server/opportunity/**`, `scripts/reach-partition.mjs`, `server/model/contingency.js`.
- Shipped: #99 `c5ee3b5` (DEPLOYED tree), #108 `eb19f47`.
- Open: #116 `6dfc123d` (REAL, queue position 2), #85 `69909edd`, #72; the reach-ladder branch (R59.1: not ruled until pushed to origin).
- Blocked on: R54 partition prereg (six conditions) requires the partition script before the call graph.
- Next three: (1) push the reach-ladder branch and open the PR; (2) partition script unit; (3) call graph unit.

### 6.9 Google sign-in
- Thread: [Google sign-in](#cmsg_01YAsw8AnFv4ioRMQw8dfPmT8YsGjX4GZdwhGVZ82qyDvf). Session `cse_01Fvh3EsJ4qTYUfpkB1ArgBM`. IDLE.
- Branches: `claude/project-thread-n4052e-*`.
- Owns: `server/routes/espn-connect.js`, `espn-draft.js`, `server/data/espn.js`, `server/auth/account-link.js`, `routes/leagues.js`, `server/platform/espn-credentials.js`.
- Shipped: nothing merged today.
- Open: #48, #71, #81 (old, lapsed-hold text).
- Blocked on: Phase C has not started; the thread is a candidate to close under option 1.
- Next three (if kept): (1) rebase #48; (2) per-user credential slot design with the multi-user gaps in section 7; (3) encryption helper.

### 6.10 Chat sync
- Thread: [Chat sync](#cmsg_01YAsw8AnFv4ioRMQw8dfPmTKYXEhjbYKXZq8jgBSxswWF). Session `cse_01XTraU7NJBSDtpxoVYe2oWR`.
- Branches: `claude/project-thread-sytruo-*`.
- Owns: `server/league/league-history.js`, `server/league/manager-archetypes.js`, `scripts/build-manager-archetypes.mjs` build side shared with Coach by grant, migrations 064+.
- Shipped: #89 `1a13614`, #102 `140d436`, #112 `a1e661f`, #110 `ab01efa`, #113 `bf359c6`.
- Open: #124 `db6b001`, #125 `0f442fa`, #131 `665cbb2`.
- Four-item queue, in order and not to slip: (2) `league-history.js:144` and `:114`; (3) `:1449` basis `'draft'`; (4) `:196-214` `priorSeasonFantasyCv`; (5) `:116` `?? 14` must not land before (4).
- Blocked on: the gate only.
- Next three: (1) #124/#125 through the gate; (2) item (2) of the queue; (3) item (3).

### 6.11 Model evidence audit
- Thread: [Model evidence audit](#cmsg_01YAsw8AnFv4ioRMQw8dfPmT7SXDS8LvnSNmvMsRPdPPrw). Session `cse_01RaKeP3tXctv8SXVFaMRZdd`.
- Branches: `claude/project-thread-w0gpjt-*`.
- Owns: `server/model/projections.js`.
- Shipped: #68 `26a5002` (target-share prior, default-off, R35/R40).
- Open: #121 `57c21a0f` HELD by R57 (ceiling RED accepted; back once).
- Known: feed-zero at `projections.js:492` is live, with supports at `:395` and `shrinkage-fit.js:330` (R39 folds the three into one coupled grade).
- Credential exposure: the 08:23Z env grep (section 10).
- Blocked on: R57.
- Next three: (1) #121 resubmission once; (2) feed-zero coupled-grade unit; (3) page-claim trace for item 9.

### 6.12 Coach
- Thread: [Coach](#cmsg_01YAsw8AnFv4ioRMQw8dfPmTS3usSxcPEMGfCMXmNcuFcN). Session `cse_016PjGEhxy64vLJRHjfRmZAH`.
- Branches: `claude/coach-grounded-4l8hno`.
- Owns: `server/coach/**`, `routes/coach.js`; build side of `scripts/build-manager-archetypes.mjs` by grant.
- Shipped: #109 `4ff3048`, #117 `92654e2`.
- Open: #90 (RED `03da7fd6`, GREEN `9e1a931b`), #82; the suspension finding for item 10.
- Credential exposure: the 03:35Z env dump (section 10).
- Blocked on: the gate.
- Next three: (1) #90 through the gate; (2) #82 rebase; (3) suspension basis unit.

### 6.13 Data and techniques R&D (Explorer)
- Thread: [Explorer](#cmsg_01YAsw8AnFv4ioRMQw8dfPmT4vudwP2HRu3f6Jh6t6Wnw5). Session `cse_01MZWAai2grAYofLf1AFcQTf`. Trigger `trig_01TmmVb3SUDjmL1ytumBjCMV` (ENABLED per 15:47Z record; R&D circle).
- Branches: none; writes no repo files. Output is PACKAGE-*.md (11 files) under `/mnt/project-files/` and the READ-B bundle (md5 `2a0bd577715b961677edc101dd97443a`, 49,888 B).
- Shipped: the findings in section 7 marked Explorer; the offline rig rebuild (`gridiron-rig-rebuildable.md`, `gridiron-offline-rig.md`).
- Open: PACKAGE 9 correction; charting REVISION 1 supports the Planner's prereg.
- Blocked on: "explorer paused" state (`gridiron-explorer-paused.md`) pending Nick's option number.
- Next three: (1) stay paused; (2) on resume, only measurement requested by a cleared prereg; (3) licence check on any new source before a byte is read.

### 6.14 R&D integration and cleanup (Planner)
- Thread: [Planner](#cmsg_01YAsw8AnFv4ioRMQw8dfPmTEemiXC9TrLjvH3yHwUXYiK). Session `cse_01AkWVQyBHMgzzCWthGU6PNw`. Trigger `trig_01Q9tHR6BwGBFKQW7CW3nt1K` (ENABLED).
- Branches: `claude/project-thread-2oztzw`.
- Owns: `server/data/who-plays.js`, `server/data/offseason-data.js` (build side), `server/data/nfl-pbp.js`, one additive arm in `server/model/matchups.js`.
- Shipped: #92 `4a8085c` opened (matchups baseline 2025 MAE 4.333 / Spearman 0.6789 on that head).
- Open: PREREG-070 charting team-week REVISION 1 at the Independent Auditor (R56 chose 059 over 070 and held three blocking items; R59.2 back once); `/mnt/project-files/PREREG-070-CHARTING-TEAM-WEEK-2026-09-22.md` (31,203 B).
- Blocked on: R56/R59.2.
- Next three: (1) REVISION 1 answers to the three blocking items; (2) #92 through the gate; (3) gameMultiplier arm as its own PR with the baseline in the evidence file.

### 6.15 Independent Auditor
- Thread: [Independent Auditor](#cmsg_01YAsw8AnFv4ioRMQw8dfPmT3KiCiFCNRVdLzR7VGhZ9Ty). Session `cse_01Q2FHgt4RMnqaJ2LRECwpSV`. Model opus 5.5. Branch `claude/project-thread-naclrx` (rulings file only).
- Owns: `/mnt/project-files/audit-unitA-and-ceiling-bracket-2026-09-22.md` (290,656 B, 3,515 lines), rulings R1-R61 (section 8).
- Open gates it holds: R54.4, R56/R59.2, R57, R58, R59.1, #132.
- Runs 24/7 by Nick's 07:34Z order; its heartbeat trigger `trig_015rcBfjwLNXrfEnCMHV2aH3` no longer exists (it runs on messages).

### 6.16 Evidence Auditor
- Thread: [Evidence Auditor](#cmsg_01YAsw8AnFv4ioRMQw8dfPmT3qmhahWyZnNu4SCfUGc16M). Session `cse_012mT7CaxQcWsGgGqaj6PCqF`.
- Role: the serial merge gate. Runs `npm run check` on each pushed head, reports REAL or not; docs-only PRs skip it.
- State: its context compacted at 17:04Z. When re-supplying it, send the queue heads from section 5.3 explicitly; do not assume it remembers them.
- Its hourly trigger `trig_01F3XCyBXT3bLDW8DQD8uTaQ` was disabled 08:56Z and later reported as no longer existing; it runs on messages from the coordinator.

### 6.17 The coordinator (for completeness)
- Memory tree `/tmp/claude/memory/team/silo/`; `MEMORY.md` is single-writer (`gridiron-memory-md-single-writer.md`); state files `gridiron-state-NNNN-2026-09-22.md` reached 1279; index parts 1-45; PR board parts 2-7. A background "Memory writer" agent was running at handoff time for the 17:56Z-18:01Z batch.
- Triggers: `trig_01XsQzN1SGcFPKMFetyoBQCy` (30-minute update, next 18:26Z); `trig_016cw1uZrkMtLJu6RyfNNBAf` disabled 17:56Z; `trig_014tYBA5uqmgAE81Ffj72BXY` gone; `trig_012RN55qCnW2TAZnS3VTgmcL` (resume, spent 11:12Z); `trig_01491GKGxdgwFw3tkt68dbyA` one-shot spent.

---

## 7. FINDINGS HANDED OFF AND NOT YET BUILT

Each line: finding, owner, tree measured on. These are cleared or recorded findings with no merged fix.

### 7.1 Data pipeline
1. Charting never reaches `nfl_team_week_features`. Explorer on `654ff93`; corrections on `4a8085c`: `writeTeamWeeks` at `nfl-pbp.js:563`, 91 suffixes at `:447-559`, six single-consumer columns. Planner's PREREG-070 (REVISION 1 at auditor, R56 chose 059 over 070).
2. Optional sources with `required:false` never trigger. Scheduler on `654ff93` and `f620a120`. Four tables at full force: `nfl_snaps`, `nfl_ngs`, `nfl_pfr_adv`, `nfl_depth`. The #119 retry note is FALSE and needs a docs fix (Scheduler).
3. `snap_counts` CSV is ingested twice: `syncSnaps` at `nfl-advanced.js:174` → `nfl_snaps`; `syncSnapCounts` at `nflverse.js:270` → `player_week_snaps`. Scheduler, `654ff93`. Not deduplicated.
4. `saveAndVerifyWeeklyFit` has zero callers (`weekly-weight-store.js:175`, `654ff93`). Promoted weekly fits = 0 in production; frozen-2023 served. Scheduler owns the caller unit; the weekly-fit promotion gate rule (`gridiron-weekly-fit-promotion-gate.md`) governs when a fit may be promoted.
5. 070 participation cannot serve 2026: `pbp_participation_2026.csv` returns 404. Explorer. Any 2026 use of participation is reachable-data-absent.
6. `nfl_depth` is inert (0 rows, R29 Condition B). Do not put it on a timer (51 MB CSV OOMs the 2 GB machine).
7. `collect-league-transactions` is hand-run only (write-side axis "hand-run"). Chat sync.
8. `news_items` importance=3 predicate is dead: all 914 rows are 2 (live read). Whoever owns news grounding (item 15, unassigned) builds it.
9. `NFL_SEASON` is not set in `fly.toml`; the app derives the season. Release. Recorded, no change ordered.
10. Migration 066 provenance unresolved (which thread wrote it, and its `name` export). Chat sync / coordinator.
11. Replay reads only 4 tables while live reads 23 (R37, R41). Fantasy plan. The replay is not the production model and figures from it carry that label.

### 7.2 Model
12. Structural head has a 0.175 MAE gap between the two intended configurations (R44 provisional accept, weekly config). Fantasy plan.
13. Ceiling-lineup configuration diverges from projections; R42 ruled it a BUG; fix is #132 (UI).
14. `matchups.js:365` gameMultiplier seam; baseline on `4a8085c`: 2025 MAE 4.333, Spearman 0.6789. Planner builds the additive arm; must beat the baseline on held-out before it ships.
15. Plan 01 graded availability is built but ungraded (R19.5); refit on as-of status (R19.6 redirect). Fantasy plan. Graded availability clears at −0.0180 / −0.0164 (`gridiron-graded-availability-clears.md`, tree in that file).
16. Plan 01 1b canonical: INFORMATION share 14.6% (R11, four re-runs). Ceilings canonical 0.4405 / 0.6265 (R12). Weekly ceiling 0.3854, model at 82.7% of it, headroom +0.0668 (R2 order, `gridiron-weekly-ceiling.md` correction).
17. Target-share prior 1.90 → 1.80: held-out 2025 MAE improvement, #68 merged default-off (R35 accept, do not ship alone; R40 may merge).
18. Production read 15:37Z on the deployed tree: control minus shipped pooled −0.0632 [−0.0847, −0.0414], n=4419 (R21 one blocking change, R25 ADD TO PLAN). R46: control NOT promoted; next unit is the LEVEL correction at `weekly-ensemble.js:69-77`.
19. Committee reallocation: small, real (`gridiron-committee-reallocation-real.md`).
20. Injury report captures 21% of ceiling (`gridiron-injury-report-forecasts-snaps.md`).
21. Snap-share Simpson paradox (`gridiron-snap-share-simpson-paradox.md`): pooled snap share reverses within-position; never pool across positions.
22. Depth rank negative (`gridiron-depth-rank-rejected.md`); TE pressure null (`gridiron-te-pressure-null.md`); BDB AYEI rejected (`gridiron-bdb-ayei-rejected.md`); Sharp Football source refused (licence).
23. Conformal beats the interval layer (`gridiron-conformal-beats-interval-layer.md`). Not built.
24. `K.yards_per = 34` stands (R31, R34: K evidence accepted, remedy redirected to fit-against-served-prior; burned seasons rule).
25. effW: correctness only, no accuracy claim (#106, R51 default-off).
26. Feed-zero at `projections.js:492` live; supports `:395` and `shrinkage-fit.js:330`; R39 coupled grade. Model evidence audit.
27. Trend-exploits usage blindspot fixed locally at `bc03222`, not on origin (`gridiron-trend-exploits-usage-blindspot.md`).
28. Participation is not target rate (`gridiron-participation-not-target-rate.md`); N is not raw opportunities (`gridiron-n-is-not-raw-opportunities.md`); charting box contamination present, offense pct not contaminated.
29. Weekly lineup ordering changes (`gridiron-lineup-ordering-changes.md`): R45 withdrew the rates; the finding stands as "ordering changes under the named configuration marker".

### 7.3 Code and surfaces
30. Reach bracket on `500bab36` is reproducible by the partition script (R54): request-only 172/65/6/18/11/47; request+job 228/55/3/2/10/21; 214 route / 14 script-only. On main `02096ca5`: 169... / 225.... The ladder as a whole is NOT reproducible by command (R51, `gridiron-ladder-not-reproducible-by-command.md`).
31. `docs/wiring/CONTRACT.md:160-166` prints 178 summing to 325. Wiring map fix pending (line 172).
32. `stale_findings` reported nowhere (`nfl-candidate-findings.js:345-349`, betting side, not edited).
33. Cache-fingerprint test hazard at `trade-engine.js:1478` (`gridiron-cache-fingerprint-test-hazard.md`). Feature audit.
34. `Model.tsx:31` calls dead `/model/status`. UI.
35. `tradeWeekContext` missed wire-up. Trade Brain.
36. MLB three jobs on the request thread, 1.5 s blocking line real. Scheduler removal in draft.
37. nflverse CC BY 4.0: zero attribution until #133. nflverse/nfldata (coaches, officials) has NO licence; keep-or-drop is Nick's word.
38. Repo has no LICENSE file and is public.
39. Betting bugs unfixed (`gridiron-betting-bugs-unfixed.md`): `nfl_prop_calibration` throws and 2026 scores never land; nine betting files (R27), seven files (R32). Not a product surface; not fixed.
40. Multi-user gaps (`gridiron-multi-user-gaps.md`): ESPN cookie is a global slot; no encryption helper; global Anthropic key. Phase C.
41. Explorer's `gridiron-package9-correction.md`, `gridiron-feed-zero-class-closed.md`, `gridiron-opponent-defence-closed.md`, `gridiron-effw-gate-closed.md` mark lines of inquiry CLOSED; do not reopen without a new prereg.

### 7.4 NEVER QUOTE THESE

Withdrawn or superseded numbers. They appear in old state files and old PR text. Do not carry any of them into an update, a PR, or a page.

- −4.6 pts/week (withdrawn R43).
- 80% / 88.29% lineup ordering rates (withdrawn R45).
- 98.9% ceiling / +0.0037; 70.1% / +0.1362; 0.4548 as a ceiling (superseded by 0.4405 / 0.6265 canonical, R12).
- 116 / 53 / 67 / 183 and 178 as "reproducible" ladder counts (only the `500bab36` partition numbers in 7.3 item 30 are reproducible; the ladder is "not reproducible by command").
- 21.02% / 11.2% (superseded by 14.6% / 7.6%).
- 60 / 50 / 40 / 20 as the structural head (it is the history head).
- 4.514 rig MAE.
- "add-absences mandatory".
- K ypt 64 / 70; ypc 115 / 134.
- 2978/2 fail suite figure; 3233 (#100 suite figure); 112/123.
- 205-233 bracket.
- "over-predicts by 15%".
- "five feeds stale forever" wording (the correct statement is item 2 above).
- 7.55-7.89 cap; 8.13 / 8.62.
- 989 weekly ceiling (corrected to 0.3854 in `gridiron-weekly-ceiling.md`).

---

## 8. AUDITOR RULINGS R1-R61

From `/mnt/project-files/audit-unitA-and-ceiling-bracket-2026-09-22.md`. One line each.

- R1: #16 void.
- R2: order of the ceiling bracket work; weekly ceiling and headroom figures set.
- R3: option (c) chosen.
- R4: standing note on evidence form.
- R5: ceiling conditions discharged.
- R6: #20 re-reports.
- R7: price, don't classify.
- R8: closures of earlier items.
- R9, R10: #21 restatements.
- R11: four re-runs; INFORMATION share 14.6% canonical.
- R12: pooling is NULL; ceilings 0.4405 / 0.6265 canonical; power check required.
- R13: 17b closes.
- R14: ceiling derivation; MAE-flat is a standing finding.
- R15: CRPS re-draw band.
- R16: scripts option (c), default reversed.
- R17: import reach is via the scheduler.
- R18: mlb category.
- R19: method confirmed; Plan 01 built-ungraded (R19.5); R19.6 redirect to as-of refit.
- R20: CRPS band; stratified read.
- R21: production read: one blocking change.
- R22: 109 / 178 / 228 ladder.
- R23: VACUUM INTO for the snapshot.
- R24: 183 carries after reconciliation.
- R25: production read: ADD TO PLAN.
- R26: "script-only" is misleading as a label.
- R27: nine betting files named.
- R28: range has two standards.
- R29: Condition B; nfl_depth inert.
- R30: split 2/7.
- R31: K evidence accepted, remedy redirected; burned seasons rule.
- R32: seven files.
- R33: fix the tool now; bespoke-tool cross-check rule.
- R34: K withdrawn as a change; fit-against-served-prior.
- R35: target-share accept, don't ship alone.
- R36: prereg two changes.
- R37: 4-vs-23 tables replay vs live.
- R38: coherence settled.
- R39: three supports fold into one coupled grade.
- R40: #68 may merge.
- R41: replay vs production distinction stands.
- R42: ceiling-lineup divergence is a BUG.
- R43: −4.6 withdrawn.
- R44: provisional accept; weekly config.
- R45: rate withdrawn; named configuration marker required.
- R46: control not promoted; LEVEL correction next.
- R47: ceiling RED accepted; #121 held.
- R48: local-gate gap scope.
- R49: two-copy production read rule (R49.2 premise amended by R61).
- R50: Feature audit rewrite, six checks.
- R51: #106 default-off accepted; ladder not reproducible by command; call-graph prereg cleared.
- R52: reversed assertion, five checks; evidence-citation rule.
- R53: gate fix provisional; optional-sources confirmed; write-side axis adopted (R53.1 closed by R60).
- R54: 172 reproducible; partition prereg six conditions; committed-fixture RED; scope open (R54.4); name-the-table rule.
- R55: contradiction rule adopted with correction.
- R56: 059 over 070; held three blocking items.
- R57: #121 back once.
- R58: #130 held, fourth surface.
- R59: reach-ladder not ruled (not on origin) (R59.1); charting REVISION 1 back once (R59.2); nflverse attribution cleared as a machine-readable step; predicate-injection lesson adopted.
- R60: #129 merged; R53.1 closed.
- R61: R49.2 premise amended.

Open gates: R54.4 (Scheduler scope trace); R56 / R59.2 (Planner charting, back once); R57 (#121, back once); R58 (#130, back once); R59.1 (reach-ladder awaiting push); #106 committed-fixture on the gate sha; #132 (UI) at the auditor; #133 cleared as a step, awaiting the Evidence Auditor.

---

## 9. FLEET RULES AND LESSONS

One paragraph per rule file in `/tmp/claude/memory/team/silo/`. Format: what the rule is, why it exists, how to apply it.

**247-threads-rule.** Threads run around the clock; a thread that is idle says so in its state rather than going silent. Exists because silent threads were mistaken for working ones. Apply: every thread state file has a "state" line; idle is a state.

**30min-update-rule.** Every 30 minutes Nick gets the phase-board post: percentages, merged since last, queue head, Waiting-on-you, R&D line. Exists by his 17:24Z order. Apply: trigger `trig_01XsQzN1SGcFPKMFetyoBQCy`; shape is section 11.2.

**audit-the-build-standing-rule.** Every build unit goes through the Independent Auditor's three gates before it is called done. Exists by his 07:14Z auditor rule. Apply: submission format is the auditor's; kill/redirect is the auditor's call.

**authorisation-rule.** PLACEHOLDER. The original was overwritten at 02:37Z and lost. Its substance is reconstructed from 07:13Z (push authority restored) and 15:29Z (coordinator may merge). Apply: treat this document's section 2 as the authority list until the file is rewritten.

**bespoke-tool-cross-check-rule.** Any bespoke measurement tool is cross-checked against an independent computation before its figure is quoted (R33). Exists because a bespoke tool produced a wrong count that was quoted for hours. Apply: two computations, one figure.

**burned-seasons-rule.** A season used to fit is burned for evaluation; held-out means never touched by the fit (R31). Apply: name the held-out season in every figure.

**contradiction-test-rule.** When two sources disagree, the contradiction is written down and one is tested, not averaged (R55). Apply: a contradiction line in the state file until resolved.

**evidence-citation-rule.** Every claim in a PR or ruling cites a file and line on a named tree (R52). Apply: `path:line@sha`.

**fit-against-what-production-does-rule.** Improvements are measured against the served model, not the replay (R34, R41). Apply: name whether the baseline is production or replay.

**five-questions-rule.** Before asking Nick, answer the five questions (section 2.2). Apply: the coordinator answers if it can.

**free-data-first-rule.** Nothing paid; a free source is found or the gap is logged (03:53Z). Apply: `gridiron-missing-data-log.md`.

**gate-evidence-before-nick-rule.** Nothing is reported to Nick as done until the gate said REAL. Apply: "merged" in an update means REAL and merged.

**licence-before-measurement-rule.** Licence is read before a byte of a new source is measured. Exists because of Sharp Football (refused) and nflverse (CC BY, zero attribution). Apply: licence line in the prereg.

**local-gate-includes-wiring-rule.** `npm run check` includes `check:wiring` since #129; a local pass before that did not cover the gate. Apply: run the full six steps; compare CI's job order.

**mid-verification-hold-rule.** A thread mid-verification is not interrupted; the hold waits for the run to finish. Apply: check the state file for "verifying" before sending a redirect.

**missing-data-workaround-rule.** A missing source gets a logged workaround, never a silent default. Apply: log entry plus a surface message.

**name-the-table-rule.** Every reach claim names the table (R54). Apply: no "the data" without a table name.

**name-the-tree-rule.** Every figure names the commit it was measured on. Apply: `@sha` on every number (section 7 does this).

**no-pr-noise-rule.** No PR comment without a state change and evidence (2026-09-20). Apply: fix descriptions when the PR is next touched.

**phase-transition-plan-no-stop-rule.** No stop between phases (03:28Z). Apply: the next phase's brief is written before the current one closes.

**predicate-injection-test-rule.** A predicate's reach is tested by injecting a row that satisfies it (R59, the news_items importance=3 case). Apply: RED test that injects, GREEN that reads it back.

**production-read-two-copy-rule.** A production read is done on two copies (live and snapshot) and the two agree (R49, R61 amended premise). Apply: both counts in the state file.

**rnd-updates-for-nick-rule.** R&D findings go in the 30-minute update (16:55Z). Apply: one R&D line per update.

**suite-figure-rule.** A suite figure (N pass / M fail) is only quoted from a run on a named tree after `npm ci`. Exists because 2978/2 and 3233 were quoted from unnamed trees. Apply: tree plus `npm ci` line.

**waiting-on-you-line-rule.** Every update ends with the list of things only Nick can do (16:23Z). Apply: section 11.2.

**comment-right-assert-wrong-lesson.** A test whose comment described the right behaviour asserted the wrong one and passed. Apply: read the assertion, not the comment.

**empty-rebase-range-lesson.** A rebase that reports an empty range means the branch is already contained; do not force-push. Apply: `git log main..branch` before rebasing.

**rebase-before-merge-lesson.** A REAL check on a head that is behind main is not REAL after main moves (#94). Apply: rebase, re-check, then merge.

**tree-hash-rule-correction.** The tree hash, not the commit hash, proves byte identity across a squash (`63ebac8a` = `6432a768` tested head). Apply: `git rev-parse sha^{tree}`.

**verify-once-and-model-by-weight.** Verify a claim once, not repeatedly (stands); model-by-weight (suspended by the opus 5.5 orders). Apply: `last_served_model` check.

**always-answer-ready-for-review.** A ready-for-review is answered within the next coordinator turn (08:23Z).

**chat-reserved-for-his-word-or-milestone.** Chat with Nick carries only his orders and milestones (17:14Z).

**table-reach-taxonomy.** Not reachable / reachable-data-absent / predicate dead, plus write-side axis own trigger / piggyback / hand-run / none (R53). Apply: every table in the wiring map carries both.

**check-script-omits-wiring-gate.** Historical (pre-#129); superseded by local-gate-includes-wiring-rule. Keep for reading old state files.

**cache-fingerprint-test-hazard.** `trade-engine.js:1478` fingerprint can make a stale cache pass a test. Apply: clear the cache in the test's setup.

**claude-md-tasks-md-claim-stale.** The local CLAUDE.md says TASKS.md is not on main; origin/main has it. Apply: trust origin.

**matchup-multiplier-seam.** `matchups.js:365` is the one seam for game multipliers; additive arm only.

**sharp-football-source-refused.** Licence forbids; never measured.

**k-yards-per-heldout.** K.yards_per=34 stands on held-out.

**weekly-fit-promotion-gate.** A weekly fit is promoted only after coverage and held-out checks; today 0 promoted.

**lineup-ordering-changes.** Finding stands without rates (R45).

**weekly-ceiling.** 0.3854 canonical; 989 was a transcription error, withdrawn.

**memory-md-single-writer.** Only the coordinator writes MEMORY.md; threads write their own state files.

**no-thread-has-live-db-access.** Only Release (with the Fly token) can read the live DB; all others use the snapshot or rig.

**goodnight-one-message-per-unit.** Overnight, one message per completed unit, nothing else.

**missing-data-log.** The log of absent sources and their workarounds.

**fantasy-plan-results.** The item 8 document's results table and what each figure was measured on.

**model-routing.** Which model serves which thread (item 24); check `last_served_model`.

**stop-ledger / stop-resume-protocol.** Stops are ledgered; resume replays the ledger (01:26Z).

**nick-morning-list (+part2).** The list Nick was to read on waking (superseded by this document).

**denominator-correction.** The share denominators corrected (14.6% / 7.6% over 21.02% / 11.2%).

**explorer-paused / rd-explorer-planner-loop.** Explorer paused pending option number; the loop's shape.

**audit-findings-ledger.** Findings by R-number and status.

**fly-login-token.** The token exists under the name `GRIDIRON_FLY_TOKEN`; presence only.

**kaggle-open-access.** Kaggle datasets read are open-access; licence recorded.

**tdd-defect-injection / evidence-file-form.** RED must fail for the injected defect; evidence file shape under `docs/tdd/`.

**scheduler-cleared-of-delete.** The delete was a silent catch, not a job.

**ladder-not-reproducible-by-command.** The ladder is history; only the partition on `500bab36` reproduces.

**n-is-not-raw-opportunities / participation-not-target-rate / charting-box-contamination / offense-pct-not-contaminated.** Measurement definitions from Explorer (section 7).

**rig-rebuildable / offline-rig.** The offline rig rebuilds from the snapshot by script; no thread needs the live DB.

**package9-correction / feed-zero-class-closed / committee-reallocation-real / yards-per-34-stands / effw-gate-closed / graded-availability-clears / opponent-defence-closed / depth-rank-rejected / te-pressure-null / snap-share-simpson-paradox / injury-report-forecasts-snaps / conformal-beats-interval-layer / trend-exploits-usage-blindspot / bdb-ayei-rejected.** Explorer finding closures; section 7 has the substance.

**fantasy-audit-open-items / betting-bugs-unfixed / multi-user-gaps / no-betting-ui.** Open item lists by area.

**research-corpus-home.** `research/` is the corpus home (item 23).

**migration-runner / migration-number-collisions.** Section 3.4.

**fly-cold-start / scheduler-disabled-brake / release-train-deploy-facts / post-deploy-scheduler-read / post-deploy-chain / live-read-2026-09-22 / live-data-state.** Section 3.1-3.3.

**file-allocation-history.** Who owned what, when; the current allocation is section 6.

**projection-model-items / model-items-what-is-buildable.** Which model findings can become units now (section 7.2).

**Plan bundle files (go-plan, plan-v2-detail, auditor-charter-0714, authority-lessons + part2, overnight-0825, overnight-usage-0831, deploy-step, optional-sources-never-trigger, charting-never-reaches-team-week, nflverse-cc-by-attribution, live-environment-notes, fly-volume-has-no-backup, pre-migration-snapshot, espn-credentials-not-on-main, never-grep-env-for-values, data-loops-answer-0719).** Sections 4, 7, 10. `never-grep-env-for-values` is the one to read first: two exposures came from threads grepping env; presence only, never content.

---

## 10. OPEN RISKS

1. **Key exposures, unrotated.** Three exposures, five credential names, zero rotations. Nick declined rotation at 15:38Z. Names only: `AI_GATEWAY_API_KEY` (screenshot, 2026-09-18); `GRIDIRON_FLY_TOKEN` and `GRIDIRON_ANTHROPIC_API_KEY` (Coach env dump 03:35Z; Model evidence audit env grep 08:23Z). Per CLAUDE.md these are burned and must be rotated and the rotation confirmed. Waiting-on-you line carries this every update until he does it.
2. **Repo is public with no LICENSE file.** Anyone may read; nobody has a licence to use. Nick's call on which licence.
3. **nflverse / nfldata has no licence** (coaches, officials). Keep-or-drop is Nick's word; until then the data stays and is not shown with attribution because there is nothing to attribute to.
4. **nflverse CC BY 4.0 attribution absent** until #133 merges.
5. **Disk on the Fly volume.** Each pre-migration `.bak` is ~445 MB; `assertRoomForSnapshot` refuses boot at less than DB + 2 GB free. No scheduled backup of `/data`. Fix tight disk with `fly volumes extend`, never by deleting `.bak`.
6. **2 GB machine OOMs on the 51 MB depth CSV.** `nfl_depth` stays hand-run and inert.
7. **No thread has live DB access** except Release; coordinator egress to fly.dev is 403. Live reads are rare and go through Release.
8. **Scheduler is ON live** since 17:08Z with MLB jobs still present until the removal merges (1.5 s blocking line on the request thread).
9. **Multi-user gaps.** ESPN cookie global slot, no encryption helper, global Anthropic key. Single-user today; Phase C.
10. **Evidence Auditor context compacted 17:04Z.** Queue heads must be re-supplied.
11. **`gridiron-authorisation-rule.md` is a PLACEHOLDER**; the lost text is reconstructed in section 2.
12. **Clock skew** between thread-reported times and the coordinator clock (~1 hour). Use message ids for ordering when it matters.
13. **Thirty old PRs carry false "CI cannot run" text** (section 5.5).
14. **Betting side unfixed** (`nfl_prop_calibration` throws; 2026 scores never land). Not a product surface; do not build on it.
15. **Budget.** Nick's 17:53Z question is about spend; option 1 (five threads) is the recommended answer and is not yet chosen.

---

## 11. HOW TO RESTART

### 11.1 Memory files to read, in order

1. `/tmp/claude/memory/team/silo/MEMORY.md` (authority line, standing rules, dangers).
2. This file, `/mnt/project-files/HANDOFF-2026-09-22.md`.
3. `/mnt/project-files/PHASE-BOARD-2026-09-22.md` and `/mnt/project-files/PLAN-ITEMS-1-25.md`.
4. The PR board: `gridiron-pr-board-2026-09-22.md` and parts 2-7; `gridiron-pr-board-github-list-merged-caveat.md`.
5. `gridiron-file-allocation.md`, `gridiron-threads-directory.md`, `gridiron-open-risks.md`.
6. The latest index part (45) and the latest state files (1275-1279).
7. `gridiron-never-grep-env-for-values.md` and `gridiron-memory-md-single-writer.md` before touching anything.
8. `/mnt/project-files/audit-unitA-and-ceiling-bracket-2026-09-22.md` headings for the open R-gates.
9. `/mnt/project-files/WORKLOG-2026-09-22.md` for the narrative 15:02Z to 17:56Z.

### 11.2 The 30-minute update shape

Post to Nick at :26 and :56 (trigger `trig_01XsQzN1SGcFPKMFetyoBQCy`). Shape of the 17:56Z post:

```
Phase board HH:MMZ
Foundation NN% merged / NN% weighted | A n/n | B n/n | C n/n | D n/n
Merged since last: #N (sha), ...
Gate: queue head #N (head sha), N behind it
Held: #N (Rnn), ...
R&D: one line
Waiting on you:
- your 1/2/3 number
- keep/drop nflverse/nfldata (no licence)
- after #127 merges: FLY_API_TOKEN (commands below)
- key rotation (declined 15:38Z)
```

Rules: figures carry trees; no ids in prose; no secrets; five items visible per list; end with one thing he can do in under two minutes.

### 11.3 Spawning a thread, brief format

```
Title: <one noun phrase>
Plan item: <number and verbatim text>
Files owned: <paths>; grants: <path:lines from whom>
Branch prefix: claude/project-thread-<slug>-
Model: opus 5.5 (check last_served_model)
Deliverable: RED commit, GREEN commit, docs/tdd/<name>.tdd.md, PR against main
Gate: npm run check on the pushed head; Evidence Auditor says REAL; Independent Auditor for any measured claim
Report to: coordinator only; never Nick
Rules: no secrets; name the tree; name the table; licence before measurement; no PR noise
Stop when: <the unit's done condition>
```

### 11.4 Merge gate protocol

1. Thread pushes head; opens PR against `main` with the evidence file.
2. Coordinator sends the head sha to the Evidence Auditor with the queue position.
3. Evidence Auditor runs `npm ci && npm run check` on that head; posts REAL (or the failing step and its cause).
4. If main moved since the head was cut: rebase, re-push, back to step 2 (rebase-before-merge lesson).
5. Any measured claim in the PR also needs the Independent Auditor's ruling.
6. CI green on the PR (workflow `357164314`; a ~1-minute red is the wiring gate).
7. Coordinator merges (squash). Docs-only PRs merge on CI green alone. Record merge sha in the PR board.
8. Deploy is never part of a merge; deploy is Nick's word.

### 11.5 Recommended five-thread configuration for Plan items 1-3 (option 1)

1. **Full test rundown (item 1)** kept as a standing check inside the Evidence Auditor rather than a thread; reason: the rundown is the gate's own output on every head.
2. **Wiring map (item 2)**: keep the Wiring map thread; reason: it owns the gate that just turned main green and the ratchet is the last piece.
3. **UI truth (item 3)**: keep the UI thread; reason: #132 fixes an auditor-ruled bug and #118 is one rebase from the gate.
4. **Scheduler**: keep; reason: MLB removal is Nick's direct order and it owns the optional-sources note fix and the zero-caller weekly fit.
5. **Chat sync**: keep; reason: five merges today, three PRs in queue, a four-item ordered queue that only it can sequence.
Plus the two auditors. Threads to pause under option 1: Trade Brain (after #94 lands), Feature audit (after #130 resolves), Fantasy plan, Model evidence audit (after #121), Coach, Opportunity (after #116 and the reach-ladder push), Google sign-in (idle), Explorer (already paused), Planner (after REVISION 1), Release (after #127/#133 and the token).

### 11.6 Routine triggers and their state

| Trigger | Purpose | State |
|---|---|---|
| `trig_01XsQzN1SGcFPKMFetyoBQCy` | 30-minute update | active; next 18:26Z |
| `trig_016cw1uZrkMtLJu6RyfNNBAf` | previous update trigger | disabled 17:56Z |
| `trig_014tYBA5uqmgAE81Ffj72BXY` | earlier update trigger | gone |
| `trig_012RN55qCnW2TAZnS3VTgmcL` | resume | one-shot, spent 11:12Z |
| `trig_01F3XCyBXT3bLDW8DQD8uTaQ` | Evidence Auditor hourly | disabled 08:56Z; later reported no longer existing |
| `trig_01TmmVb3SUDjmL1ytumBjCMV` | Explorer R&D circle | ENABLED per 15:47Z record; Explorer itself is paused |
| `trig_015rcBfjwLNXrfEnCMHV2aH3` | Independent Auditor heartbeat | no longer exists |
| `trig_01Q9tHR6BwGBFKQW7CW3nt1K` | Planner | ENABLED |
| `trig_01Sp27doa6JEqMtBTg91jYc7` | Trade Brain | enabled:false |
| `trig_01491GKGxdgwFw3tkt68dbyA` | one-shot | spent |

### 11.7 Things only Nick can do (the standing Waiting-on-you list)

1. Answer 1 / 2 / 3 on the thread configuration.
2. Keep or drop nflverse/nfldata feeds (coaches, officials), which have no licence.
3. After #127 merges: run `fly tokens create deploy --app gridiron-hq --name github-actions-deploy --expiry 8760h` and paste the result as `FLY_API_TOKEN` at `https://github.com/BouncySlime1215/gridiron-hq/settings/secrets/actions/new`.
4. Say the word for the next deploy (and for any rollback to `deployment-01M2VZ9JRYSXVHCRWJ83V360QH`).
5. Rotate the three exposed credentials (declined 15:38Z; stays on the list).
6. Pick a repo LICENSE.

---

## 12. TIMELINE OF 2026-09-22

One line per state file, from index parts 1-45 (coordinator clock; thread-reported times may read up to an hour later). Where a range of state files carries one event the range is given.

- 0001-0010, 00:00Z-01:20Z: plan review; five-questions and no-PR-noise rules carried from 09-20; PART 5 plan (09-21 19:58Z) taken as the plan of record.
- 0011, 01:25Z: GO (`...8hKXRheXe6eCXwGuAPPEAb`).
- 0012, 01:26Z: stop/resume protocol.
- 0013-0030, 01:30Z-02:30Z: thread spawns (Wiring map, UI, Trade Brain, Opportunity, Chat sync, Scheduler, Feature audit); file allocation written.
- 0031, 02:37Z: `gridiron-authorisation-rule.md` overwritten (placeholder from here on).
- 0032-0050, 02:40Z-03:25Z: first PRs opened; first RED/GREEN pairs.
- 0051, 03:28Z: phase-transition plan no stop.
- 0052-0056, 03:30Z-03:40Z: Coach env dump at 03:35Z exposes `GRIDIRON_FLY_TOKEN` and `GRIDIRON_ANTHROPIC_API_KEY` (names only).
- 0057, 03:53Z: missing-data / free-data-first rule.
- 0058-0070, 03:55Z-04:08Z: Explorer and Planner spawned; licence-before-measurement.
- 0071, 04:09Z: "dont flag shit just worl bruj".
- 0072-0150, 04:10Z-07:00Z: overnight units; Explorer PACKAGE 1-5; ceiling bracket work; R1-R10.
- 0151, 07:13Z: push authority restored.
- 0152, 07:14Z: auditor rule; Independent Auditor charter.
- 0153-0160, 07:15Z-07:33Z: auditor spawn; R11-R14 (14.6% canonical; ceilings canonical).
- 0161, 07:34Z: auditors and R&D 24/7.
- 0162-0200, 07:35Z-08:05Z: R15-R20; data-loops answer 07:19Z; overnight R&D loop shape.
- 0201, 08:07Z: R&D explore/plan/build loop.
- 0202, 08:13Z: loop scope follow-on.
- 0203, 08:23Z: always answer ready-for-review; Model evidence audit env grep at 08:23Z (second exposure of the same two names).
- 0204, 08:25Z: overnight R&D loop order; 0825 overnight plan.
- 0205, 08:31Z: goodnight; usage note 0831.
- 0206-0230, 08:35Z-08:55Z: R21-R25 (production read, ADD TO PLAN); Evidence Auditor hourly trigger disabled 08:56Z.
- 0231, 08:53Z: pause.
- 0232-0300, 09:00Z-09:40Z: held work; R26-R30.
- 0301, 09:42Z: full stop.
- 0302-0400, 09:45Z-11:10Z: stop ledger; auditors continue R31-R38; K evidence; burned seasons.
- 0401, 11:12Z: resume trigger fires (`trig_012RN55qCnW2TAZnS3VTgmcL`).
- 0402-0600, 11:15Z-13:00Z: resume; R39-R42 (feed-zero coupled grade, #68 may merge, replay vs production, ceiling-lineup BUG); first merges prepared.
- 0601, 13:02Z: pause.
- 0602-0700, 13:05Z-14:30Z: paused; auditors R43-R46 (−4.6 withdrawn; provisional accept; rates withdrawn; control not promoted).
- 0701, 14:35Z: #95 hold, test rundown demanded, 064 collision named.
- 0702-0720, 14:36Z-14:52Z: rundown run; Actions re-enabled by Nick 14:52Z.
- 0721-0760, 14:55Z-15:02Z: CI first runs; runbook `deploy-654ff93.md`.
- 0761, 15:02Z: resume pair.
- 0762-0790, 15:03Z-15:17Z: merges #91 `e3e7602`, #89 `1a13614`, #111 `ac31922`, #102 `140d436`.
- 0791, 15:17Z: verify once and model by weight.
- 0792-0800, 15:18Z-15:24Z: #114 `0877ea4`; "It's public so free now".
- 0801, 15:29Z: "If u think it can merge then u merge".
- 0802-0830, 15:30Z-15:37Z: #68 `26a5002`; brake confirmed present 15:35Z; production read 15:37Z (−0.0632 pooled).
- 0831, 15:38Z: NO key rotation.
- 0832, 15:42Z: general resume.
- 0833, 15:45Z: STANDING RULE push/manage/keep the doc.
- 0834-0900, 15:46Z-16:20Z: #95 `eb861fe`, #86 `7eca9a8`, #112 `a1e661f`, #109 `4ff3048`, #87 `0c5b7be`; Explorer trigger enabled 15:47Z; R47-R50.
- 0901, 16:23Z: Waiting-on-you line rule.
- 0902-0940, 16:24Z-16:44Z: #110 `ab01efa`; lapsed-hold audit written; opus 5.5 order first form 16:37Z.
- 0941, 16:44Z: "1" merge as they go.
- 0942-0960, 16:45Z-16:55Z: #108 `eb19f47`; R51-R52.
- 0961, 16:55Z: R&D updates for Nick.
- 0962-1000, 16:56Z-17:03Z: #99 `c5ee3b5` merged; `SCHEDULER_DISABLED=1` set 17:03Z; R53.
- 1001-1020, 17:04Z-17:05Z: Evidence Auditor context compacted 17:04Z; main goes RED on the wiring gate at `144b722`/`eb19f475`; deploy of `c5ee3b54` at 17:05Z (image `deployment-01M3517MWXX3HF7CEPSWB8ZK0M`).
- 1021, 17:07Z: "use opus 5.5 ACross everything".
- 1022-1040, 17:08Z-17:13Z: brake lifted 17:08Z; post-deploy read; model resolution 17:13Z.
- 1041, 17:14Z: Nick away studying.
- 1042-1100, 17:15Z-17:24Z: #113 `bf359c6`, #115 `144b722`; R54-R55.
- 1101-1102, 17:24Z: "gimme this evry 30 min : get rid of MLB"; "just manage the things".
- 1103-1180, 17:25Z-17:45Z: #117 `92654e2`, #119 `f620a12`; MLB census #128; Scheduler removal draft; R56-R58 (#130 held, #121 back once); #94 REAL on `00319229`.
- 1181-1240, 17:46Z-17:52Z: R59 (reach-ladder not ruled; charting REVISION 1; attribution cleared); #133 opened; PHASE-BOARD file 17:50Z (71/86).
- 1241-1260, 17:53Z: #129 `c90d283` merged, main green; R60; Nick's budget question 17:53:19Z; coordinator's three options.
- 1261-1275, 17:54Z-17:56Z: 30-minute post (72/86); trigger swap (`trig_016cw1uZrkMtLJu6RyfNNBAf` disabled, `trig_01XsQzN1SGcFPKMFetyoBQCy` created); R61.
- 1276-1279, 17:56Z-17:59Z: Nick asks for the handoff doc 17:57Z; `/mnt/project-files/handoff/` created 17:59Z; this file started.

End of first pass. Section 6 entries will be replaced by thread-supplied sections where `/mnt/project-files/handoff/*.md` provides them.
