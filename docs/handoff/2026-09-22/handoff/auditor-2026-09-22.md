# Independent Auditor — handoff, 2026-09-22 (written ~18:05Z)

Session cse_01Q2FHgt4RMnqaJ2LRECwpSV, thread suffix …3KiCiFCNRVdLzR7VGhZ9Ty. Full record, every ruling in full with file:line:
`/mnt/project-files/audit-unitA-and-ceiling-bracket-2026-09-22.md` (A.1–B.8, then R1–R61, ~3,530 lines). **This file is the index. The record is the authority.** Where they differ, the record wins.

## 1. Charter (Nick, 07:14:26Z)
One dedicated auditor owns the audit queue. It is independent (no stake in any build), reports only to the coordinator (never to builders, never to Nick directly), and holds kill/redirect authority **on evidence quality, not on build scope**. Every gated unit (models, trade logic, projections, plus random spot checks) gets three gates: acceptance criteria at kickoff, a mid-point wiring/evidence check, and a final evidence review. Cheap checks (test rundowns, wiring maps, branch hygiene) stay with the coordinator and the Evidence Auditor. Opus time goes to statistical validity, architecture coherence, and real-vs-decoration. Submission format: **(a) claim; (b) file:line on a reachable tree; (c) command, metric, n, held-out split; (d) incumbent to beat.** Missing (c) or (d) is an automatic redirect. I cannot rule on commits that are not on origin. "The finding and the remedy are separate rulings." Model: claude-opus-5-5 (Nick 17:07:02Z).

## 2. Every ruling, one line each
Format: **ruling**: decided / held, on whom.

**Morning (Unit A and ceiling bracket, ~11:30Z)**
- **A.1–A.8** Unit A (Unit 18 closure): ACCEPTED on mechanism (claim 3). Plan 05's sharp prediction REFUTED and recorded as my failed prediction. My A.3 arithmetic test and A.6 sign conflict withdrawn. The predA list binds. Rule plus correction, no build.
- **B.1–B.8** Ceiling MAE-floor gate: met. Gaussian branch closed by measurement. +0.4258 accepted **as an estimate**; the **bracket** is what may be cited. BINDING: the CI is not the uncertainty (B.5). Two conditions before §3 shares are quoted (B.7).
- **R1** #16 ACCEPTED AS VOID. A second (level) channel exists, so my one-channel rule was too coarse (Explorer).
- **R2** Order confirmed: fix the published defect (#20 write-up) before re-running voided units. Mark VOID now.
- **R3** Claim 1 vs 8 was not a sign conflict. I compared a parameter to a performance difference; my A.6 withdrawn.
- **R4** Standing note: no kill on a null, or on harm from a prediction-lowering term, without stating what the instrument could have shown.
- **R5** Ceiling conditions DISCHARGED (178+344 = 522 rows, same population). Shares clear with two labels.
- **R6** #20 re-reports ACCEPTED. One claim unverifiable on the reachable tree (R6.1); one attribution refused.
- **R7** Direction rule: my R1 matrix SUPERSEDED (m0 = 0.910, predA 1.085, crossing). The new rule: **price, don't classify.** "#13/#14 lower bounds" withdrawn.
- **R8** R6 closures verified directly (cp-test.mjs week-stripping defect is real). Mark the defect on the file.
- **R9** #21 restatement 1 ACCEPTED, and stronger: W = 6 gives 0.2559, below the claimed 0.27–0.28 limit.
- **R10** #21 restatement 2: n-ratio is a **trajectory** (0.197 at week 5 to 0.307 at season end). Quote the week.
- **R11** Four re-runs ACCEPTED. Level/information decomposition exact (#13 info 14.55%, #14 7.58%). **Plan 01 1b restated to the INFORMATION share.**
- **R12** Partial-pooling NULL accepted. Ceiling discrepancy resolved against me (0.4405/0.6265 canonical; mine were back-computations).
- **R13** Unit 17b GATE MET on the real `fitAllK`. Closes with three write-up conditions.
- **R14** Ceiling derivation accepted; early-week stand-down accepted. A four-unit pattern escalated as a standing finding.
- **R15** CRPS-primary units: the PIT band does not govern CRPS, but the frozen-simulation bootstrap alone is not enough. A re-draw band is required.
- **R16** Package.json scripts: (c) documented-per-(b), default reversed. A header is not a criterion.
- **R17** Scheduler import-reach: the brake flag is EXCLUDED from the criterion. My R16 bucket 3 widened ("only reader is its own writer").
- **R18** R17 grades confirmed. The `routes/mlb.js` correction has consequences beyond the one file.
- **R19** Inventory set-operation method CONFIRMED. stale_findings ruled. Plan 01 accepted as built-ungraded with a look-ahead redirect.
- **R20** CRPS re-draw band accepted. Position read STRATIFIED. The 60/50/40/20 withdrawal was itself half-wrong (R20.4 narrows it).
- **R21** Production read GOES TO NICK with one blocking command change (read-only snapshot) and five reading changes. "QB reverses" withdrawn.
- **R22** 109/178/228 allowed with the question named and 109 tagged unchecked. 190 is not a ceiling. I withdrew my R19.4 edit order.
- **R23** VACUUM INTO satisfies R21.2; my remedy withdrawn. db/index.js writes at import registered as a finding.
- **R24** 183 carries after one reconciliation. The fresh-twenty check is not the right check. "Floor" is the wrong word.
- **R25** Production read ADDED TO PLAN as a measured result. One gate before it moves weights, plus one discriminating check.
- **R26** Sample B 10/10 → 2/10 is the definition boundary, not a labeller failure.
- **R27** R24(1) cleared 183. Nine of ten promoted files are betting-named, and that is the live question.
- **R28** A range is fine, but not one whose ends are measured to different standards. 112 does not reconcile.
- **R29** Condition B check ACCEPTED by execution (a fifth rig blindness: `players.espn_id` all NULL). `nfl_depth` CONFIRMED inert.
- **R30** Seven of nine files come out on **call reach**, verified on main. No per-branch model needed.
- **R31** Kicker/efficiency K: evidence accepted, remedy redirected. **Superseded by R34.**
- **R32** My prediction (bottom below 112) was wrong: it came out at 114. `server/data.sqlite` is the default dev DB.
- **R33** Fix the tool now. The paragraph is written once, on post-fix numbers. (Later found in R51.3: I accepted a "tool-produced" 183 without running it.)
- **R34** R31 SUPERSEDED. The k finding does not survive its intervals, and it was fitted against a prior production does not use. "Self-refutation" credit withdrawn.
- **R35** Target-share prior evidence ACCEPTED. **Do not ship alone**: it and Plan 01 are one finding (the accidental availability hedge).
- **R36** Fantasy plan's pre-registration acknowledged with two required changes. Run after them.
- **R37** 4-vs-23 question registered, narrowed to one comparison. Retro-correction to R25.3. Taxonomy: touching a table is not reading its signal. **R37.6: the "production read" measures the replay predictor on production rows.**
- **R38** Coherence point settled against me (fixed reference reproduces exactly at n = 140).
- **R39** Three supports in one estimator (observation incl. zeros, prior > 0, k fit > 0): fold into the coupled grade.
- **R40** R35's hold meant the code's **behaviour**. #68 may merge as opt-in with zero behaviour change.

**Afternoon (configuration axes, ceiling-lineup, merges)**
- **R41** Explorer gate 1 (replay vs production role recency) CONFIRMED. My R41.1 "trap" wording later withdrawn.
- **R42** Explorer's correction accepted: the `activeKVectorFor` coupling is documented design, and the effect is joint. **ceiling-lineup's missing roleRecency is a BUG.**
- **R43** The production read ran bare. My exemption WITHDRAWN (it contradicted my own R37.6). **−4.6 pts/week withdrawn; correction owed to Nick** (delivered 17:02Z/17:05Z).
- **R44** ceiling-lineup ordering: PROVISIONAL accept. Fix confirmed, lands first. **R44.1: gate evidence reaches the Auditor before Nick hears a result.**
- **R45** Lineup-ordering submission ACCEPTED in format and finding, but the RATE does not describe the product. **No lineup rate to Nick.** `production()` is a named configuration marker.
- **R46** Level/information decomposition: control NOT promoted. Fantasy plan's next unit = LEVEL correction on the incumbent (weekly-ensemble.js LEVEL block). State the sign convention first.
- **R47** Ceiling RED accepted with the boundary explicit. Coupled-grade prereg (#121) HELD on configuration B and units. R46 record gate closed. **R47.2: the call site binds the build.**
- **R48** Local-gate gap: `npm run check` omits wiring. Gate = `check && check:wiring`.
- **R49** My R45.3 mechanism came off a stale docstring (corrected). **Pristine two-copy production read** (premise amended in R61).
- **R50** Feature audit's rewrite of "a throwing source is swallowed" legitimate in principle. Six conditions (a)–(e) for the pushed head.
- **R51** #106 kill-switch shape ACCEPTED. 0.0003 closed. **Inventory ladder NOT reproducible by command** (shared blame). Call-graph prereg cleared with two additions.
- **R52** Wiring map's reversed assertion legitimate in principle. **Citation rule: #N + subject + sha, RED output inline, ancestry checked** (squash hole closed).
- **R53** Wiring map provisionally passes five checks. Explorer's optional-sources package accepted on mechanism. **#119 put a false "retries next cycle" promise on main for optional feeds.**
- **R54** Bracket low end reproducible **as 172** from the committed grader. CONTRACT.md's column total is false by 6. Partition prereg (Opportunity) cleared with six conditions. **Guard RED must use committed inline fixtures.** My R53.2 scope line withdrawn. **Name the table AND the writer function.**
- **R55** Contradiction-test rule ADOPTED (a zero/empty/success from a bespoke check needs one known-nonzero case). The fourth instance is misplaced.
- **R56** Planner's charting prereg: 059 over 070 YES (070 only as a crosswalk). HELD on scope, values, incumbent.
- **R57** #121 @ 57c21a0f: configuration B CLEARED, units CLEARED (option ii beat mine). **Back once:** name the call site, the decisionAt rule, and the observation support per arm. The QBR strike's stated reason is false.
- **R58** #121 R47(B) cleared (as R57.1). **#130 @ ea03d88:** R50 (a)(b)(c)(e) cleared. **HELD on a fourth surface (RiskStrip Floor cell) my own R50(d) list missed.** The cache-fingerprint find is confirmed and does not reach main.
- **R59** Reach-ladder not ruled (not on origin), pre-ruling given. Charting revision 1 back once (the named site misses the trade path; five-rates-to-one form and persistence rule are free; configuration B for the incumbent). nflverse attribution cleared **as an API step, not compliance.** Predicate lesson adopted plus call-site mutants.
- **R60** #129 merged (c90d2834). R53.1 CLOSED. **check:wiring exits 0 on main**, so any wiring exit 1 on a rebased branch is now that branch's own.
- **R61** R49.2 premise AMENDED: import runs only `000_legacy_schema` when its marker is absent, and numbered migrations run via `runMigrations`. The two-copy rule stands. A "no such column" on an import-built DB is never production evidence.

## 3. OPEN gates, and what closes each
1. **#121 coupled grade** (claude/project-thread-w0gpjt, 57c21a0f). Three text items close it (R57.3): (a) the call site as file:line on a stated tree plus what it multiplies; (b) pre-kickoff `decisionAt` at the site plus a revised-after-kickoff control; (c) one observation-support row per arm. R57.4 also: replace the QBR strike's reason (configuration, not aggregation). Rebase onto c90d2834 before the final wiring read. Then the held three-support confirming check may run.
2. **#130 trade-engine evidence fault** (…5f9c3y-evidence-fault-not-no-record, ea03d88). Closes when RiskStrip.tsx floorOf shows the shortfall, the floorBetter colour is suppressed when unreadable > 0, and types.ts carries `unreadable`/'unknown', with a RED first (option i, granted to Feature audit), or the "Nothing is left uncovered" sentence goes. Non-blocking: a reference notEqual in the offseason test, the "numbers stay null" comment, and a findTrades-cache sentence.
3. **Charting pre-registration revision 2** (Planner, PREREG-070-CHARTING-TEAM-WEEK). Closes on R59.2: one function both matchups.js:365 and :419 read; the graded form declared (points at trade-engine.js:359 vs volume in the sampler); five-rates-to-one form and fit; a persistence decision rule with n and an interval; the table matching the SQL; N for per-target rates; configuration B for the incumbent re-run, with a stated tolerance.
4. **Reach-ladder command** (Opportunity). Closes on a pushed head: RED/GREEN ancestry, my re-run on b0c1616d/500bab36, M1 killed, and naming the 02096ca5 tree (it is not main; main is tree 6c00c129). **Then** Wiring map corrects CONTRACT.md:126-135 separately (low end 172; 178 column total withdrawn).
5. **nflverse attribution #133** (Release, 25052404). Closes when the PR body stops calling it compliance and names the visible credit line as open (Explorer §4.2). Non-blocking: complete FFOPPORTUNITY_SOURCE.
6. **UI ceiling-lineup fix** (claude/project-thread-xiezr0-ceiling-recency, RED 6247820 / GREEN 81d0fa8). Final evidence review not yet done: exact equality to the weekly path; the boundary in the PR body; the shrinkage-fit.js:507 comment half; the ceiling-lineup.js:141-142 stale docstring; check:wiring.
7. **#106 kill switch** (d4c6e49). R51.1 conditions plus a fixture-form guard test (R54.3).
8. **Explorer READ-B and the optional-sources prereg.** Pristine-copy protocol (R61), a sub-mechanism prediction, a brake anchor, and an empty-`nfl_depth` outcome.
9. **Opportunity partition script** (six R54.2 conditions) and **call-graph unit** (two R51 additions).
10. **Scheduler optional-sources unit**: conditional on a fantasy-route **call-reach** trace (R54.4). #119's false "retries next cycle" note stands on main until fixed.
11. **Fantasy plan LEVEL correction** (R46): sign convention stated before any signed error leaves a run.

## 4. Fleet rules I originated or ratified
- Submission format (a)–(d); a missing (c)/(d) means auto-redirect. A zero or null is never a kill without an instrument statement (R4).
- The CI is not the uncertainty. Cite brackets, not points, where the rig can move them (B.5).
- **Price, don't classify:** decompose into level vs information. A level win is free via m0 (R7, R11).
- **Configuration B:** any replay grade declares `roleRecency: WEEKLY_ROLE_RECENCY` explicitly, omits `kOverride`, and runs a k control that stops at `K.share = 6` (R42, R43, R57.1, R59.2).
- **Named configuration marker** over a required argument: silence and ambiguity are both invalid inputs (R45).
- **Condition A:** orderings travel between rigs, magnitudes don't. **Condition B:** state rig blindness (R29, R61).
- **Gate evidence before Nick** (R44.1). No lineup rate or rig magnitude reaches Nick (R45, R61).
- **The call site binds the build** (R47.2, R57.3).
- **Call reach, not import reach.** Touching a table is not reading its signal (R30, R37.1). **Name the table AND the writer function** (R54.5).
- **Pristine two-copy production read:** the pristine copy is touched by no repo code (R49, R61).
- **Citation:** #N + subject + sha, RED failing output inline, ancestry checked on the reviewed head (R52.2).
- **"Tool-produced" means the Auditor ran it.** Bespoke-tool cross-check; a cross-check covering a delta does not certify a total (R33, R51.3).
- **Contradiction test:** a zero/empty/success from a bespoke check needs a known-nonzero case (R55).
- **A test pinning a defect is wrong** (CLAUDE.md §2): the rewrite keeps the safety half and splits decision from text (R50).
- **Guard REDs use committed inline fixtures**, including alias and namespace cases (R54.3).
- **Predicate rule:** pin the injected predicate directly, and the mutation set includes call-site mutants (R59.4).
- A range's two ends must be measured to one standard (R28). Every count carries its question and its tree (R22).
- Local gate = `npm run check && npm run check:wiring` (R48).
- Ratified: the licence check is a gate before any measurement of external data; nothing paid.

## 5. Withdrawn numbers. A new session must never quote these as findings
- **−4.6 fantasy points/week** over nine starters (R43), and **any lineup-ordering rate, incl. 80%** (R45). Corrected to Nick.
- **Inventory 183 / 116 / 53 / 67 / 178, and 112, 109 (unchecked), 190 (not a ceiling):** not reproducible by command. The reproducible bracket is **172–228 on b0c1616d, tree 500bab36**. CONTRACT.md's 178-column total "319" is false (the column sums to 325).
- **n-ratio 0.307** as a single figure (a trajectory: 0.197 at week 5) and the **0.27–0.28 convergence** (R9, R10).
- **Ceilings 0.4410 / 0.6277** (mine; canonical 0.4405 / 0.6265) (R12). **41.6%** non-portability (measured 30.8% / 32.4%) (R11). **+0.3141** (B). "#13/#14 survive as lower bounds" (R7).
- **R31's k finding**, EFFICIENCY-K-SPEC's **115**, and the ypc "self-refutation" (R34).
- **"QB reverses"** (R21). The 60/50/40/20 statement only as R20.4 narrows it.
- **+0.1273 and +0.0569 as observed differences:** they are bootstrap `mean_diff` (backtest-significance.js:114). The observed figure for #121's is +0.1269 (R51.2, R57.5).
- **QBR 4.749 vs 4.751:** UNJUDGED (bare replay), not a verdict either way (R57.4).
- #119's **"the scheduler retries it on the next cycle"** for optional feeds: false on main (R53).
- **Any magnitude from either measurement rig** (old n = 4,343, rebuilt n = 4,361). Direction only (R61).

## 6. Recommendation: five threads on Plan items 1-3
**Caveat first:** Foundation is at 71% and the plan order says nothing jumps ahead, so the five have to carry what Foundation still has in flight rather than abandon it. **My five:**
1. **Model evidence audit.** It owns the one grading harness that every item-2 feature must pass: configuration B, the k control, and the call-site binding. It finishes #121 first. The #121 pre-registration is the best-formed of the series, and item 2's "every feature must prove lift" bar is worthless without a harness that grades the configuration the app serves. Today's biggest recurring defect was grading the wrong configuration (R42, R43, R57.4, R59.2).
2. **Data & techniques R&D (Explorer).** Item 2 features from free nflverse data, plus the pristine production census. It produced today's most important corrections, including several of mine (R41–R43, R49, R61), and it rebuilt the rig from free data.
3. **R&D integration & cleanup (Planner).** Item 2's pressure/charting feature (the nearest thing to "OL-vs-DL") through the matchup seam, once revision 2 clears. It re-pointed the unit rather than defending it, which is the behaviour to keep.
4. **Feature audit.** Item 1, league-config auto-ingest and the scoring-verification check. Its #130 work was the best TDD discipline I audited: it found a cache that made two of its own tests vacuous and proved liveness by reference. Item 1 is mostly "prove the projection used the right scoring", which is exactly that skill.
5. **Opportunity.** It closes Foundation's inventory work in flight (reach-ladder, partition script), then item 3 (beat-reporter source map). Item 3's "whose reports predicted outcomes" is a scoring and calibration problem that needs the same self-falsifying habit it showed on M1.
**Land and stop:** UI (ceiling-lineup fix), Release (#133), Wiring map (the CONTRACT.md correction after R59.1), Scheduler (MLB removal, Nick's word). Fantasy plan's LEVEL unit folds into Model evidence audit's harness. Trade Brain, Coach, Chat sync and Google sign-in have no item 1-3 work in my queue. **I have not audited UI, Trade Brain, Coach, Chat sync or Google sign-in in substance**, so leaving them out is not a judgement of their quality.

## 7. Updates after the handoff was written
- **R62 (18:10Z).** #135 reach-ladder @ 1d5076fe: numbers reproduce exactly (172–228 / 214 / 14 on b0c1616d; 169–225 / 211 / 14 on main, services subtree 2c900fff). My R59.1 "branch files" reading was wrong and is withdrawn. **HELD on one item:** the call-site mutant at `reach-ladder.mjs:266` survives (14/0). It closes with a test that kills it. Charting revision 2: **CLEARED with three text conditions**, confirmable by line: walk-forward `z`, a joint points+volume ship rule, and a k control that expects production's resolved k from the pristine census. The persistence check may run now.
- **R62.5.** #121 must say where its fitted k vector comes from (an active fit in the rig DB, or an explicit non-null kOverride). Otherwise its own control stops it at `K.share = 6`. This rides R57.3's round.
- Open-gate changes: gate 3 is now "three text conditions, no Auditor round". Gate 4 is now "the call-site mutant only". Gate 1 adds the R62.5 line.
- **R63.** Scope cut (Nick 18:05Z): the Auditor gates statistical claims only (model, projection, trade valuation, lineup). Ordinary code goes through gridiron-merge-gate-v2. The #130, #135 and #133 holds became advisory.
- **R64.** The UI ceiling-lineup fix (#132 @ 19510ce2) is CLEARED: the test is exact, and my two mutants were killed. #121 @ 91ed5f1a is CLEARED to merge (docs and comments only). Before its grade runs: change `:625`→`:605`, and fix the arm (a) k label and the k source (R62.5).
- **R65.** Merge bar = gridiron-merge-gate-v2: one live proof per behaviour change, plus call-site mutants.
- **R66.** `buildImporterGraph` misses bare `import '…'` (12 on c90d2834). This needs its own PR, with an inline fixture and a superset invariant. #72 @ c1655b25 is CLEARED: the null multiplier has no arithmetic consumer, and the threshold of 9 is post hoc and labelled as a rule. The grade json was not re-run.
- **R67.** #62 @ 01f7273 and #74 @ 7a55f75 are CLEARED: tests are live on unfixed source, and the #74 mutant was killed. #67 and #55 are ordinary code.
- **R67.1/R67.2.** Wiring map's 9 was a missed comment-tail case, since fixed to 12. "169–225 wired" is exact on c90d2834 only. 228 on 500bab36 keeps its "≥". The hand-run/unreached cells (10/26 vs 11/25) are still open.
- **18:42Z HARD STOP (Nick, usage).** Nothing is pending with the Auditor. The next session starts from this file, §3 and §5 first.
