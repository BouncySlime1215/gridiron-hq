# Handoff: Release thread, 2026-09-22 (written 18:0xZ)

## THREAD

- **Thread:** "release train". Reports to the coordinator, never to Nick directly.
- **Designated session branch:** `claude/release-train-2yv3x6`. It is PR #35, closed today as done; nothing on it is live.
- **Unit branches (one per PR):**
  - `claude/deploy-workflow-gh-actions` (#127)
  - `claude/nflverse-attribution` (#133)
- **Model:** claude-opus-5-5, per Nick at 17:07Z.

## SHIPPED

**Merged by this thread today:** #102, squash `140d436`. It carried:
- the CLAUDE.md §3 CI-history correction: 7 runs on main; run 252 on 2026-09-19 was the first to complete; the workflow is active, not disabled;
- Batch 3 of the PR board, `docs/board/pr-board-2026-09-22-0800Z.md`.

**Closed as done today:** #35. Everything it ordered is on main:
- #63 `2206383`, which carries #56, #59 and #61;
- #52 `9075e33`;
- #49 `654ff93`.

**Today's merges on main, oldest first:**

| PR | sha | PR | sha | PR | sha |
|---|---|---|---|---|---|
| #91 | `e3e7602` | #95 | `eb861fe` | #108 | `eb19f47` |
| #89 | `1a13614` | #86 | `7eca9a8` | #99 | `c5ee3b5` |
| #111 | `ac31922` | #112 | `a1e661f` | #113 | `bf359c6` |
| #102 | `140d436` | #109 | `4ff3048` | #115 | `144b722` |
| #114 | `0877ea4` | #87 | `0c5b7be` | #117 | `92654e2` |
| #68 | `26a5002` | #110 | `ab01efa` | #119 | `f620a12` |
| | | | | #129 | `c90d283` |

**Deploy.** `c5ee3b54` was deployed at 17:05Z. It is main at #99.
- **#113, #115, #117, #119 and #129 are NOT in the live image.** They merged after `c5ee3b5`; checked with `git merge-base --is-ancestor bf359c6 c5ee3b5`, which is false.
- Memory's line "#113 … deployed in c5ee3b54" is wrong.
- The next deploy is Nick's word.

## OPEN

### #127: Deploy button + `.dockerignore`

- **Head / base:** head `883f4bf`, base `c90d283`, draft.
- **Commits:**
  - RED `62816a9` "test: RED - deploy workflow and build-context guards"
  - GREEN `ba97ee8` "feat: GREEN - a Deploy button that re-asserts the brake first and proves it held"
  - evidence `d6e086b`, `408300b`, `883f4bf`
- **What it adds:**
  - `.github/workflows/deploy.yml`, which runs from the button only (`workflow_dispatch`);
  - `.dockerignore`: `data`, `analysis`, `test`, `**/node_modules`, `**/*.sqlite*`, `**/*.bak`;
  - `test/deploy-workflow.test.js` (8 tests);
  - `docs/runbooks/deploy-workflow.md`;
  - `docs/tdd/deploy-workflow.tdd.md`;
  - `docs/tdd/sweeps/deploy-workflow-mutations.mjs` (22 of 22 killed).
- **Gate history:**
  - On `43116ab`, before the rebase: check rc 0, 3500 pass, 0 fail. Wiring rc 1, from main's own three findings at the time.
  - On `883f4bf`: an isolated guarded gate was running at handoff. Result: `/tmp/claude-0/gates-c90/pr127/summary.txt` (this container only).
- **Left before merge:**
  1. Gate result on `883f4bf`.
  2. CI green on `883f4bf`.
  3. Evidence Auditor REAL. It is eighth in the queue.
  4. Merge under Nick's "1".

### #133: nflverse CC BY 4.0 descriptor

- **Head / base:** head `617fd7f`, base `c90d283`, draft.
- **Commits:**
  - RED `74c4449` "test: RED - nflverse data carries no CC BY 4.0 source descriptor"
  - GREEN `901c2fd` "feat: GREEN - nflverse data carries its CC BY 4.0 descriptor on the freshness report"
  - test fix `1ff383c`
  - evidence `3199874`, `617fd7f`
- **What it adds:**
  - `NFLVERSE_SOURCE` at `server/services/nflverse.js:25-35` (closing `});` on :35);
  - `sources: [NFLVERSE_SOURCE, FFOPPORTUNITY_SOURCE]` on `GET /api/data-freshness`, in `server/routes/data-freshness.js`;
  - `test/nflverse-attribution.test.js` (4 tests; sweep 10 of 10).
- **Auditor R59(3):** cleared as a machine-readable step only. The body now says this is not compliance and names the visible credit line as open.
- **Gate:** an isolated guarded gate was queued after #127's at handoff. Result: `/tmp/claude-0/gates-c90/pr133/summary.txt`.
- **Left before merge:** gate result, CI green, Auditor REAL, then merge.

**Local-only scratch branches, deliberately not pushed.** All are superseded; the PRs they rehearsed are merged or closed.
- `rehearsal` and `verify-stack-six`: rehearsal merge commits for the 09-19 six-PR stack.
- `release-train-scratch` (`1b7e55b`): scratch fixes for #14 and #26, both now closed.

## BLOCKED

1. **FLY_API_TOKEN (Nick, after #127 merges).**
   - Run on the Mac: `fly tokens create deploy --app gridiron-hq --name github-actions-deploy --expiry 8760h`.
   - Copy the whole `FlyV1 …` line.
   - Paste it as the repository secret `FLY_API_TOKEN` at https://github.com/BouncySlime1215/gridiron-hq/settings/secrets/actions/new (Settings → Secrets and variables → Actions → New repository secret).
   - The Deploy button appears only once `deploy.yml` is on main.
   - Not yet proven: that a deploy token may `secrets set`, `secrets list` and `logs`. The first press settles it; if step 2 fails, nothing was deployed.
   - **Decision owed before the first press.** The button re-sets `SCHEDULER_DISABLED=1` on every deploy, as the brief and deploy-654ff93.md ordered. But the brake has been OFF since 17:08Z. So each press turns the live scheduler off until Nick runs `fly secrets unset SCHEDULER_DISABLED --app gridiron-hq` again.
     - If that is no longer wanted now that the scheduler is proven, the brake step needs a change. That would be a new unit with a RED test.
2. **Live image may carry a local database (Nick's hands, read-only).**
   - Run: `fly ssh console --app gridiron-hq -C "find /app -name '*.sqlite*' -o -name '*.bak'"`.
   - Until #127 merges, main's `.dockerignore` excludes only `server/data.sqlite*`. Any local `fly deploy` from main can still bake a local database under `server/` into the image.
3. **`nflverse/nfldata` licence is not established.** Every probe leg failed, so do not measure or ship anything new from this source.
   - `LICENSE` and `LICENSE.md` return 404 on master, main and gh-pages.
   - README.md returns 200 on master only (1,017 bytes, sha256 `42035b4e…f88a`) and names no licence.
   - Consumers: `gamescript.js:23`, `nfl-opening-lines.js:31`, `nfl-profitability.js:19` (betting side); `nfl-coaches.js:31`, `nfl-officials.js:25` (fantasy side).
4. **The repository has no `LICENSE` file.** It is public. This is Nick's call, as an observation only; do not add one.
5. **Key rotation:** 3 exposures, 0 rotations. Nick said no for now at 15:38Z.

## FINDINGS handed off, not built

- **Visible CC BY credit line.** Owner: UI. Proposed text is in the #133 body. It must render regardless of freshness or dismissal, because `DataFreshnessBanner.tsx:132` returns null when fresh or dismissed.
- **`Model.tsx:31` fetches `/model/status`, which does not exist** (`model.js:538` says so). The "players linked to nflverse" strip never renders. Owner: UI.
- **`FFOPPORTUNITY_SOURCE` (`ffopportunity.js:17-23`) lacks `license_url`, `creator` and `modified`.** Left as is. Test 4 of #133 pins it verbatim, so change both together.
- **`node_modules` mtime moves during `npm run check`.** Vite writes `node_modules/.vite-temp`. Guard on `node_modules/.package-lock.json` mtime instead of the directory's.

## RULES AND LESSONS

- **Deploy runbook:** `docs/runbooks/deploy-654ff93.md`. The order is:
  1. brake `SCHEDULER_DISABLED=1` before;
  2. deploy;
  3. `/api/health` plus the log line `Scheduler disabled via SCHEDULER_DISABLED=1` (`scheduler.js:2130`);
  4. unset only after three proofs, and only by Nick.
- **Brake status:** it has been OFF since 17:08Z on Nick's word, with the scheduler live. Nick said "get rid of MLB" at 17:24Z; the Scheduler thread is removing those jobs.
- **Rollback:** re-set the brake first, then the previous image `deployment-01M2VZ9JRYSXVHCRWJ83V360QH`, on Nick's word only.
- **Never delete `/data/data.sqlite.pre-migration-*.bak`.**
- **`deploy.yml` must never contain `secrets unset`.** Test 4 in `test/deploy-workflow.test.js` enforces this.
- **Merge authority:** Nick's "1" at 16:44:57Z. A PR merges when CI is green on its exact head AND the Evidence Auditor says REAL; docs-only PRs need CI green only. The deploy is still his word.
- **Rebase onto current `origin/main` before every merge.** Never use local `main`, which is stale: `git checkout main -- .` once staged 214 old files. Update cited shas in the same push that rewrites them (R52.2), and cite RED/GREEN as `#N`, subject and sha, with the RED assertion message inline.
- **The local gate is now `npm run check`.** Since `c90d283` it includes `check:wiring`. Run it under one guard: `npm ci` first, porcelain empty before and after, `git write-tree` unchanged, lockfile mtime unchanged. Use a dedicated worktree with its own `npm ci`.
- **Run tests with the suite's flags.** Use `--experimental-test-module-mocks` and `NODE_OPTIONS='--import ./test/offline-guard.mjs'`. Without the flag, `mock.module is not a function` looks like a regression and is not one.
- **Docker in this sandbox** needs the intercepting CA in a bench copy of the Dockerfile. Never change the repo's Dockerfile for that.
- **Public repo:** Actions logs are public. Never print a secret, the secrets list or app logs.

## FILES this thread owns or holds grants on

- `.github/workflows/deploy.yml`, `docs/runbooks/deploy-workflow.md`, `test/deploy-workflow.test.js`, `docs/tdd/deploy-workflow.tdd.md`, `docs/tdd/sweeps/deploy-workflow-mutations.mjs`, and `.dockerignore` (#127).
- `server/services/nflverse.js` (the `NFLVERSE_SOURCE` grant), `server/routes/data-freshness.js` (the `sources` field; no open PR touches it), `test/nflverse-attribution.test.js`, `docs/tdd/nflverse-attribution.tdd.md`, and `docs/tdd/sweeps/nflverse-attribution-mutations.mjs` (#133).
- Not `ci.yml`, which is Scheduler's. Not client code, which is UI's.

## NEXT THREE STEPS, if picked up cold

1. `git fetch origin`. For #127 (`claude/deploy-workflow-gh-actions`) and #133 (`claude/nflverse-attribution`), confirm the head is still `883f4bf` / `617fd7f` and still based on current main. Rebase if main moved, and update the evidence-file shas in the same push.
2. Run the guarded gate on each head in its own worktree: `npm ci && npm run check`, then the PR's mutation sweep. Send each head and exit code to the coordinator and the Evidence Auditor.
3. When CI is green and the Auditor says REAL, merge (squash, as #102 was). After #127 lands, the coordinator gives Nick the two FLY_API_TOKEN steps above. Do not press Deploy yourself; the button is his word.

## ADDENDUM 18:3xZ: both PRs landed; thread paused

- **#127 merged as `9d262ad`** (squash; head `7c60529` contained main `6e72271`).
  - Gate: `npm run check` rc 0, with 3604 tests: 3563 pass, 0 fail, 41 skipped, 0 wiring findings. CI run 35766254717 green.
  - The Deploy button is on main. **Do not press it.** The brake question is with Nick, and the FLY_API_TOKEN steps above still apply.
- **#133 merged as `9f121bc`** (squash; head `657dba5`).
  - Gate: `npm run check` rc 0, with 3600 tests: 3559 pass, 0 fail. CI run 35766252802 green.
  - It is machine-readable only. The visible credit line is open for UI.
- **Both sweeps now carry merge-gate-v2 controls:** a designed survivor and a designed not-applied control. Result 22/22 + 2/2 for #127, 10/10 + 2/2 for #133.
- **OPEN from this thread:** none.
- **BLOCKED and FINDINGS:** unchanged from the sections above.
- **Lesson:** main moved 3 times while a guard was running. Merge `origin/main` in, don't rebase, so the cited RED/GREEN shas stay valid. Before merging, check that the new main commits don't touch your files with `git diff --name-only <contained-main> origin/main`.
