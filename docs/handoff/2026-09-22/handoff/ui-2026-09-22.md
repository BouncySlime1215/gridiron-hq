# UI thread handoff, 2026-09-22 18:10Z (relayed by the coordinator; UI's session cannot write this path)

PART 1: #132 ceiling-recency PUSHED, gate exit 0. branch claude/project-thread-xiezr0-ceiling-recency, head 929432cc349edb5558284fd8108035b9b09dd628, base main at c90d2834 (merged in), tree cca2dfa9327c29adf4dd6c67698e92d1bbff5ee8. npm run check exit 0: tests 3562 / pass 3521 / fail 0 / skipped 41 / 485.4 s; smoke passed. porcelain empty before/after, write-tree stable, node_modules mtime unchanged, 25 files written all under client/dist/. Wiring inside that exit 0. PR body in merge-gate shape plus the project's five questions. Auditor R64: CLEARED to merge after main is in.
#118 position-applicability: merged onto c90d2834, head de5aa1a9ad7bcdbe42837cf6035f40cdc4c38778 (local until gate returns).
Both earlier CI failures (jobs 106866265489, 106862767392) were main's three wiring findings + stale cascade-grade entry; fixed by #129.
#132 Auditor submission: claim = ceiling lineup built projections under roleRecency seasonDecay 0.35 while the weekly engine used 0.05; now passes WEEKLY_ROLE_RECENCY. file:line server/services/ceiling-lineup.js:62 on 929432cc; incumbent same line on f620a120. Evidence: node --experimental-test-module-mocks --test --test-concurrency=1 test/ceiling-lineup-weekly-agreement.test.js test/ceiling-lineup-recency.test.js, RED 6/9 failing, GREEN 9/9; suite 3562/3521/0/41; mutation sweep 10 rows, 7 killed, 0 survivors, 3 controls; n = one seeded fixture; NO held-out split, no accuracy claim. Incumbent behaviour: HOM QB 1, 2026 week 5, attempts 29.126 / carries 0.221 vs weekly engine 23.973 / 0.369 (21% gap). Related, not in PR: shrinkage-fit.js:507 lists ceiling-lineup as season-long, now wrong (Fantasy plan's file).
PART 2 HANDOFF. THREAD: UI, designated branch claude/project-thread-xiezr0 (bare name carries PR #43 at b7d88fcf); each unit on claude/project-thread-xiezr0-<topic>. Everything is on the remote.
SHIPPED: #86 "Data freshness that reads the rows, and two numbers that stop overclaiming", merged 2026-09-22 16:59Z, squash 7eca9a8; introduced the `unknown` freshness status.
OPEN: #132, #118 as above. Older untouched: #78 deep-dive (base xiezr0-stat-block), #76 stat-block (base xiezr0-basis-chip), #75 basis-chip (base xiezr0-design-system), #73 design-system (base main), #60 lineup-week (base 5f9c3y-trade-week), #58 odds-basis (base f921do-sim-basis), #53 freshness (main), #46 hosted (main), #43 bare xiezr0 (main, b7d88fcf). #73/#75/#76/#78 bodies carry the false "CI cannot run" paragraph; strip on next push. NEW snapshots #140 #141 #142 #143 on claude/project-thread-xiezr0-mergeproof-*: DO NOT MERGE, CI expected red, no main, close once read like #65/#69; unsubscribed.
BLOCKED: shrinkage-fit.js:507 one-line correction (Fantasy plan). Nothing else.
FINDINGS NOT BUILT: Model.tsx and Edge.tsx imported by nothing (measured on f620a120 with a passing control: TeamDetail App.tsx:17, MyTeam LeagueHub.tsx:4, Leagues LeagueHub.tsx:3, Drafts DraftHub.tsx:3); routes/model.js:1-26 records 18 routes deleted 2026-09-20 and says not to re-add; deletion is Nick's nav decision. Four unverified client paths: pairing-code, pairing-info, /drafts, /drafts/live/link. shrinkage-fit.js:500-515 names four more season-long callers (preseason-model, season-sim, draft-assist, week-postmortem), each deserves the #132 question; owner unassigned. R45(1) self-referential ceiling target (effectiveTarget = target ?? r2(naiveScore.ceiling) at ceiling-lineup.js:211; ceiling = r2(q(0.90)) at :132), named in #132 docstring, not fixed. Stray remote branch tdd/xiezr0-hosted should be deleted. Credit-line unit: needs #133's shape. Coach panel importing coach.types.ts: file not on main (lives on Coach's branch #90).
RULES AND LESSONS: 1 nav is 8 tabs, never rebuild a deleted page. 2 a page-reachability scan without a passing control is worthless (first scan missed lazy() and hub imports). 3 add/add squash lesson: branches cut from a squash-merged PR head hit add/add on every file that PR added; take your side only after `git diff origin/main <head-you-cut-from> -- <file>` is empty. 4 an absence must say which absence, from a field not a sentence (unknown vs fresh; not_measured vs zero; not_applicable vs not_measured; table_absent vs empty). 5 an assertion on a word or a constant compared to itself is not an assertion on behaviour (five survivors were this shape). 6 a gap is not an answer. 7 mock.module takes namedExports not exports; assert.doesNotMatch not notMatch. 8 the guard block: porcelain + write-tree + node_modules mtime before/after, log outside the repo, find -newermt; porcelain is the load-bearing half. 9 as of #129 npm run check includes wiring. 10 every mutation spec needs a designed surviving control and a not-applied control; mutation-runner.py decides APPLIED by count(old)==1. 11 never git add -A; check merge-base --is-ancestor before every push.
FILES OWNED: all client pages, lineup-brain, lineup-posture, draft-assist, news-fantasy-impact, routes/nfldata, stats, accolades, news, analysis, players, trend-watch, ceiling-lineup.js, client/src/components/AdvancedStatsPanel.tsx. NOT mine: server/index.js, source-registry.js, routes/model.js, trade-engine.js, projections.js, nfl-weekly-feature-store.js, weekly-weight-store.js, shrinkage-fit.js, player-week-engine.js, routes/trades.js, manager-archetypes.js, CLAUDE.md. For #130 only Feature audit holds RiskStrip.tsx floorOf (:5-6, :31-32) and types.ts trade-risk lines (:41, :44-50).
NEXT THREE STEPS: 1 check CI on #132 (929432c) and #118, read the job log first if red. 2 merge #132 (Auditor cleared), then #118. 3 R45(1).
PART 3: biggest saving was one guarded npm run check per push with the evidence in the PR body; second, get main green first because every branch pays for a red main.

## ADDENDUM, 18:42Z, written at the hard stop

#132 is MERGED, squash sha 94531b2 (landed 18:27Z by Nick's hand). The ceiling lineup now passes roleRecency: WEEKLY_ROLE_RECENCY; the My Team ceiling tool will pick different players than before, which is the fix.

#118 is READY AND UNMERGED. It needs one click and nothing else. Branch claude/project-thread-xiezr0-position-applicability, head cf60f5e3f2c206ce17a165bb1c8effcc9e48b2f4 (pushed), base main at 9d262adf merged in (not rebased), no conflicts, no file it touches was touched by anything main gained. Tree 78bb115e00f1b963499e6012638ca891be7581c7 = HEAD^{tree}. Local gate npm run check exit 0: 3649 tests, 3608 pass, 0 fail, 41 skipped, 494.7 s, smoke passed on an isolated database. Guard: git status --porcelain empty before and after; git write-tree 78bb115e00f1 both sides; node_modules mtime 1790094971 both sides; nothing written outside client/dist/. PR is out of draft; body carries the full v2 merge gate sections 1 to 5 including the RED re-run against unfixed code (7 of 14 failing, first failure quoted) and the reachability chain with its control. CI was in_progress on that head at the stop: run 35768419597, job 106883720010, started 18:37:44Z. Only remaining step: confirm that check went green, then squash-merge.

Gap left deliberately: docs/tdd/player-advanced-stats-position.tdd.md carries the figures but not the RED's failing assertion verbatim (predates the rule; the PR body has it). Fold into whatever next touches that file.

Subscriptions and triggers: clean (unsubscribed #118 at the stop; #132, #86, #140-#143 already unsubscribed; no triggers owned).

#140-#143 are preservation snapshots. NEVER MERGE. CI on them is expected red. Close them once read.

Three things a fresh session should not rediscover: (1) mcp__github__pull_request_read with method "get" ignores fields and returns the whole body (~7k tokens); use git fetch && git log --oneline for shas and method "get_check_runs" for CI. (2) When main moves under you, kill any guard run in flight; a green on a superseded tree is worse than no number. (3) Both gates can run at once: scripts/start-smoke.mjs:11 takes its port from the pid, so two runs do not collide.

Next three steps, cold: (1) confirm CI on cf60f5e3 and squash-merge #118. (2) The credit-line unit on the freshness page: verify `sources` on GET /api/data-freshness and DataFreshnessBanner.tsx:100 against the tree first; everything known about them is second-hand. (3) R45(1), the self-referential ceiling target, named in #132's docstring at ceiling-lineup.js:141-142, not fixed.
