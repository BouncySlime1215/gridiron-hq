---
name: gridiron-consensus-weights-acceptance
description: The heldBecause fix proved by six mutations another thread specified — all six red, at alternation-hold 9393762.
metadata:
  type: project
---

2026-09-20. The `consensus-weights.js` fused-cause fix (one boolean and one
"A or B" sentence for three distinct states) was proved against mutations
**written by the fantasy plan thread, not by its author**. A mutation table
written by the author of a fix proves the fix against the author's imagination.

Their four survive on `main` at **zero fail** — base `d1f5eabd828e` (`791b131`),
W1 `2f299a59c65e`, W2 `e709991898c3`, W3 `e85f72a040e6`, W4 `c36ce7b8931b`.
So the old gate was not weakly pinned, it was **unpinned**: either half of the
condition deletable, or either cause reportable exclusively, in silence.

Against the fix, base `a76bfca38f4d`, each applied alone and restored between
rows, hashed before and after: **six of six red.**

| row | after | pass/fail |
|---|---|---|
| W1 drop the row-count arm | `0cc6d80c84b2` | 14/4 |
| W2 drop the variance arm | `5d380bc6f484` | 16/2 |
| W3 report only the row-count cause | `9a5414bf20ec` | 16/2 |
| W4 report only the variance cause | `78e680b0521d` | 14/4 |
| W5 delete the zero-rows cause | `d3f185dacc2e` | 17/1 |
| W6 return only the first cause | `608dc5c42c33` | 17/1 |

NO-OP control `e5d4c7a1d228` 18/0; kill control `4f47d62dcd17` 12/6.
Evidence file: `docs/tdd/consensus-weights-held-because.tdd.md` at
`claude/project-thread-5f9c3y-alternation-hold` **9393762**.

**W2 and W3 redden the same two tests by different routes** — W2 lets a flat
panel fit at all, W3 keeps it held and mislabels the cause. They look like
restatements of each other and are not. Worth remembering when writing a
mutation set: two rows that hit the same tests can still be two real holes.

**Why the distinction is load-bearing** (their account, better than mine): too
few rows is a coverage problem more seasons fix; no measurable variance means a
source's errors are identical across the panel, which in that file almost
certainly means a broken measurement. Same action, different cause, one of them
a bug. **Why nobody noticed:** holding at the hand-set weights is the correct
action in both cases, so the output was right and only the reason was wrong.

**The third state** completes the diagnosis: with no rows at all `variance` is
`null`, `null > 0` is false, and both causes report through one branch for a
reason that is neither. That is why three states wore one sentence.
