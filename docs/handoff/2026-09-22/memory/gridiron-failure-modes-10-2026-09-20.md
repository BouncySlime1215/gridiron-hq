---
name: gridiron-failure-modes-10-2026-09-20
description: Page 10 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 07:35Z 2026-09-20 on that did not fit on page 9.
metadata:
  type: project
---

Continues [[gridiron-failure-modes-9-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-8-2026-09-20]]).

**Catalogue (cont.):** (74) a guard written to catch the instance just found is shaped like that instance and missed the most consequential number in the file by one line of syntax (feature audit 2692006: the SCORE_WEIGHTS guard read only lines beginning "score:" and missed the bare 1.15 tiebreak at trend-exploits.js:135; fixed 88e86a6 RED / c6df372 GREEN; coordinator's numbering 74; rule: a guard scans the whole file, and its RED includes a shape the first instance did not have); (75) a too-broad search fragment reported 26 dials on a route with none, a verdict-stopping false keep (route-verdicts.json GET /:leagueId/trends "dials: 26" from the fragment "/trades"; precise search 0; rule: every fragment is checked to match exactly one route before its count is served); (76) two publishers with no reader and a dead link, orphaned by a teardown, kept writing (the Decision Inbox: waiver-brain.js:303 and trade-engine.js:2865 publish, no client reader, rows link '/brain' which is NotFound at App.tsx:156; rule: when a surface is retired, its publishers are retired or re-pointed in the same sweep); (77) an unexpectedly empty diff before a fetch read as "no change" (scheduler's pair check nearly reported the wiring map's health-test edit absent; rule: fetch the ref first; an empty diff is a missing object until proven otherwise); (78) a query on a column that never existed, thrown on every run, swallowed by a bare catch and explained in a comment as a handled edge case (chat sync: SELECT MAX(sent_at) where the column is ts_utc, extract_league_chat.py:84; newest_message null on every corpus ever; 07:36Z, coordinator's numbering 78; rule: a catch that produces null names the error it saw, and a "never happens" comment is a test target); (79) two timestamp formats in one column do not sort ('T' 0x54 vs space 0x20), so MAX returns the newest new-format row whatever its date (rule: normalise before any watermark read; incremental writers get a normalisation pass); (80) a branch nothing can reach written as a state (Trade Brain's "no path configured": chatDbPath() always resolves; path_source replaces it; rule: a branch nothing can reach is a comment, not a state); (81) asserting on a field while claiming a guarantee about a sentence, three times in one thread's mutation sweep (Trade Brain's five first-pass survivors); (82) a chip that vanishes when the server vocabulary changes is indistinguishable from a page that forgot to check (UI d4685f2 keeps the old constants key mapped; rule: a consumer keeps the deployed vocabulary mapped until the new one is live, with a test accepting either).

Continues: [[gridiron-failure-modes-11-2026-09-20]] (83-86, from 07:38Z).
