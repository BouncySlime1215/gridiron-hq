---
name: gridiron-failure-modes-11-2026-09-20
description: Page 11 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 07:38Z 2026-09-20 on that did not fit on page 10.
metadata:
  type: project
---

Continues [[gridiron-failure-modes-10-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-9-2026-09-20]]).

**Catalogue (cont.):** (83) the coordinator restated a route deletion from memory of a report as if read from the tree, and a thread nearly deleted the only writers of a table on it (the Decision Inbox: server/routes/decision-inbox.js is still mounted on main at index.js:46/:129 and has no row in dead-routes-corrected.md at 8307d09; feature audit held the order of operations, 07:38Z; coordinator's numbering 83; rule: a deletion that depends on another thread's change cites the commit or the verdict row, never a summary); (84) a tombstone naming a successor that has since been retired lies to every caller (the /brain/plan 410 said the weekly plan was being rebuilt on the Decision Inbox; corrected at 3f7a82d; rule: grep every tombstone's served string on each retirement); (85) a fully enumerated file still missed a zero-caller route (GET /api/trades/:leagueId/inbox at trades.js:579, absent from the nineteen rows for that file; the second enumeration gap; rule: an enumeration is checked against the router's own declaration list, count to count); (86) a background test run killed mid-stream leaves "ok" lines and no failures, which reads as green (feature audit's re-run died at ok 1633 with no summary; rule: no summary block, no number); (87) a suite run across a branch switch exits 0 for a tree that no longer existed (feature audit's c6df372 re-run VOID; 07:43Z, coordinator's numbering 87; rule: never run a suite across a branch switch; one suite at a time); (88) output piped through tail is empty until exit, so an empty run and a dead run look identical (the earlier truncated run, probably killed by contention from a second suite; rule: read the summary block, not the tail); (89) RED proofs measured on a tree behind the cited head and a mutation cited by ordinal (wiring-map-route-deletions @ 8307d09 says 62/62 with "test 62 fails; 61 pass" while the file at that commit has 64 tests; "test 61" is untieable to a named test; Model audit 07:44Z; rule: RED proofs are re-run at the cited head and every row names its test by title); (90) a retired module named in a served string, a registry entry or a tombstone message, none an import, so nothing breaks and the name keeps being served (three cases tonight: the trend-watch note, the /brain/plan tombstone naming the Decision Inbox, gridiron-model.js:164 `module: 'weekly-trends + trend-exploits'`; Trade Brain 07:48Z, coordinator's numbering 90; rule: on every retirement grep the module's NAME in served strings and registry entries, not only its symbols; wiring map builds a report-only check for literals naming a basename absent from the tree, working once it reproduces the three cases); (91) a reviewer graded an evidence section before finishing it (three tables; stopped after the first, a historical before-state), Model audit on Google sign-in's c986b80 (07:50Z self-correction: the killing test exists since 65ac359, `not ok 16` on the mutated source, hashes byte-identical); the file's present-tense verdict made it easy, and the reword stays as a good change, but the misreading is the reviewer's (coordinator's numbering 91, rewritten 07:52Z).

Continues: [[gridiron-failure-modes-12-2026-09-20]] (92 on, from 07:51Z).
