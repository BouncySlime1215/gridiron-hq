---
name: gridiron-failure-modes-12-2026-09-20
description: Page 12 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 07:51Z 2026-09-20 on that did not fit on page 11.
metadata:
  type: project
---

Continues [[gridiron-failure-modes-11-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-10-2026-09-20]]).

**Catalogue (cont.):** (92) a fallback tone identical to a real state's tone makes the unknown render as the healthy case (TradeCard.tsx:229 `FAIRNESS_TONE[deal.fairness] ?? 'text-[var(--muted)]'`, the same tone as 'even money' at line 41; trade-engine.js:1327 already serves a sixth label 'unpriced' with no tone entry, so on the deployed app a deal where nothing could be priced displays as an even-money deal; UI 07:51Z, coordinator's numbering 92; rule: an unknown-string fallback gets its own tone distinct from every real state, and every served label has an entry with plain-English copy beside it); (93) a guard sized against the longer array passes its mirror image, groups sized to the shorter array, and a comment above the guard reads as if the class is closed; bare catches keep the trigger invisible (offseason-model.js :1143-1153 and :1465-1471 pool model arms of different lengths and compare them by index when fitGbm throws on a subset of test seasons, `catch { gbmModel = null; }`; pairedBootstrapDiff's n = the shorter array, so backtest-significance.js:80's strict guard passes and the clustered path resamples by index, a clean interval on a comparison of nothing; gbm = true by default at :1050 and :1434; Model audit 07:52Z on origin/main 791b131, coordinator's numbering 93; rule: arms are pooled by row identity or the comparison is refused, and the refusal is SERVED as a field naming arms and row counts; a bare catch becomes caught-and-recorded); (94) when an edit is structural rather than logical, a targeted suite is not evidence; only the full run is (fantasy plan's accessor edit replaced text between two anchors and deleted memoKBasis; both targeted suites passed while 54 tests in other files failed "memoKBasis is not defined"; restored from b1ee48d; 07:54Z, coordinator's numbering 94); (95) a rebuilt test file carrying forward assertions that pin behaviour another thread is removing by decision (the wiring map's rebuilt test/decision-inbox.test.js still imports waiverUpgrades at :42 and asserts reco.sourceModel === 'waiver-brain' at :324, so the feature audit's publisher stop fails it; caught by the feature audit reading the tree, not by any check; 08:01Z, coordinator's numbering 95; rule: the file's one editor drops the assertions in its own commit and the dependent thread's evidence states the dependency); (96) a mutation table that only pushes one way (break the check so it misses a lie) leaves the false-positive guard unproven: no row killed test 6 of setup-status-usage-coverage at 52a55cc, the only guard against a false "reports success but holds no rows" banner on a healthy feed; the file itself says "a test that no mutation can fail proves nothing" without applying it; Model audit 08:02Z, coordinator's numbering 96; rule (seventh part): an unkilled test is the mirror of an unkilled mutation and gets a killing row or a stated reason. (97) a cycle stamped from the trigger's fire time when the session resumed hours later (the "08:15Z cycle" fired 08:15:21Z but the session and every thread were paused from about 08:16Z to 11:01Z; the post went out at 11:01Z; coordinator's numbering 97; rule: stamp from the send_later 'now' or another tool clock at post time, not the trigger's fire time, and compare the two).

Continues: [[gridiron-failure-modes-13-2026-09-20]] (98 on, from 11:04Z).
