---
name: gridiron-failure-modes-13-2026-09-20
description: Page 13 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 11:04Z 2026-09-20 (after the 08:16Z-11:01Z pause) on.
metadata:
  type: project
---

Continues [[gridiron-failure-modes-12-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-11-2026-09-20]]).

**Catalogue (cont.):** (98) a mutation table without a per-row hash is an assertion, not a reading (fantasy plan's weekly-scores re-run at 6b77382 found two rows recorded caught were SURVIVING; coordinator's numbering 98; the hash rule joins the standard; AMENDED 11:23Z: a NO-OP is also a pattern that matched something ELSE, e.g. snap-guard's S4 pattern present twice in the file, the usage loop and the snap loop); (99) a long-running shell script edited while running dies mid-file because bash reads by offset (restart-health-log.tsv stopped 09:54:26Z, "line 54: t: command not found"; rule: launch long-running scripts from a copy nothing edits); (100) a role-arm assertion without snap rows passes for the wrong reason (the branch is skipped silently unless role_rates rows, a 2026 wk1 appearance with snaps and useRole are all present; Opportunity e53ff1a); (101) a rollup's as_of read as the corpus age: two states in one sentence (the block's MAX(last_msg) is what the rollup has seen, lagging the corpus; chat sync 572e838 splits age from rollup: current | behind | unknown | missing); (102) an in-progress week served as a final score, and a missing side read as 0 so a null-score row is indistinguishable from a shutout (weekly-scores 6b77382; two tests added). (103) a union merge is a silent revert when the base already carries the key one side deleted (scheduler's merge-base check, 11:06Z; rule: read the base before taking a union; coordinator's numbering 103); (104) CONFIRMED 11:12Z: a hole reported from one source file when the consumer (restart-count.sh) merges two (Trade Brain 11:01Z, withdrawn 11:09Z; two loggers ran from 06:55Z, coverage continuous; the 09:54Z stop was a row budget, not an edit; catalogue 99 keeps only the mechanism); (105) a `?? 0` on an absent count prints the opposite claim (UI's fairnessNote 11:08Z: a missing unpriced count would read as "nothing is unpriced"; print an indefinite sentence instead); (106) an evidence file carried unchanged onto a new head with its old full-check line (memo-fit at 9fce773 byte-identical to b1ee48d's, 2,966/2,925 against the head's 2,975/2,934; Model audit 11:08Z); (107) two changes each covering the tested case let a mutation of either survive; the separating fixture (a mixed league) did not exist until built (Trade Brain Part 9, 11:09Z).

Continues: [[gridiron-failure-modes-14-2026-09-20]] (108 on).
