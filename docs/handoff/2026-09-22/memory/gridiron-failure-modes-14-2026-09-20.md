---
name: gridiron-failure-modes-14-2026-09-20
description: Page 14 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 11:12Z 2026-09-20 on (108 onward).
metadata:
  type: project
---

Continues [[gridiron-failure-modes-13-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-12-2026-09-20]]).

**Catalogue (cont.):** (108) line numbers relayed across trees: three of three wrong on the receiving branch (counterparty-pricing.js:144 was the as-of block, the gate is :167; manager-signals.js:271 was last_week_margin, archetypeIndex is :288; trades.js:1055 did not exist, the sentence is at :929); rule 11:12Z: every relayed cite carries the head it was read on and the receiver re-locates by string (coordinator's numbering 108); (109) a third reading that runs only the targeted suite cannot verify a structural-edit claim (memo-fit M5's 54 cross-file failures; Opportunity 11:09Z), so cross-file counts need the full run recorded with one named outside test; (110) a heuristic duplicated in a report and a gate lets the two disagree silently, and the gate drops rows with no trace (routeLiteralAbsent hid three routes, 382 → 385 in scope; fixed by one exported routePattern() pinned by a test; wiring map 11:12Z); (111) stopping a task wrapper does not kill the detached process (two full suites plus three orphaned node --test processes ran through the pause until 11:05Z; scheduler); (112) a slow check this morning may be contention, not the tree: do not read timing as evidence (same cause). Also 11:14Z, a coordinator rule: name whose reading before citing it (the 52a55cc table was Model audit's, misattributed to Opportunity). (113) a thread's context compaction dropped a coordinator instruction (Coach lost the 11:10Z control/SHA instruction and recovered it from line 597 of the conversations doc, 11:19Z); the ONE DOC works as shared memory only while section 3's rulings stay complete and dated (coordinator's numbering 113). (114) a NO-OP that matched something else: a pattern present twice in the file lands on the other site (snap-guard S4; a sweep that predates the hash column is an assertion until re-run; coordinator's numbering 114); (115) a canonical file with its own part 5 unmet, credited on the strength of its mutation numbers (uncalled-surface-audit §6 had no numbers at da8ec48; Model audit's reading 7); (116) a value-equal pair of constants makes a wrong import invisible to value assertions (DEFAULT_ACTIVE_PROBABILITY and DEFAULT_DURABILITY_PRIOR both 0.92): pin the import line; (117) a figure quoted as measured on the commit that prints it is impossible unless docs/ is outside the check: the full-check line names the parent of the printing commit (Google sign-in 511eed8); (118) two branches each green alone, both required for a retirement, red together via a third test file neither owned (test/lineup-diff-urgency.test.js pinned the Decision Inbox publish; 4389a7a measured without the stop): the branch-pair sweep catches conflicts, not combined-suite reds; run the suite on the combined tree before a retirement; (119) a time is a measurement: a number must be read, not recalled, and a clock is a number; a stamp extrapolated from the last one seen drifts ahead and can reorder two threads' claims about one file (scheduler's own correction 11:27Z; second instance Coach 11:31Z, stamp 11:34Z ahead of the queue; coordinator instance: rulings stamped 11:36Z and 11:38Z by estimate before a 11:35:43Z clock reading, restamped 11:35Z; third instance Coach 11:39Z stamped 11:47Z). (120) mutation rows credited to the wrong test when written from memory rather than from the runner's output (chat sync 22bbd45: M4/M5 credited to each other's tests, M1/M2 part-credited to an ISO test that fails under neither); caught only by generating the table from the run (coordinator's numbering 120).

Continues: [[gridiron-failure-modes-15-2026-09-20]] (121 on).
