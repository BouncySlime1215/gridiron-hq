---
name: gridiron-failure-modes-15-2026-09-20
description: Page 15 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 11:38Z 2026-09-20 on (121 onward).
metadata:
  type: project
---

Continues [[gridiron-failure-modes-14-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-13-2026-09-20]]).

**Catalogue (cont.):** (121) a core-looking table created only by a backfill script (league_season_teams, scripts/backfill-league-history.mjs:48): three readers in manager-archetypes.js (:243, :819, :831) throw "no such table" where the backfill never ran, a bare catch in routes/trades.js:338 collapsed that into "no archetype", and a hand-compiled no-migration list (Coach's eleven) missed it; rules: readers report absence with a null naming its kind, and the no-migration list is generated by the wiring map's parser, never compiled by hand (Trade Brain 11:33Z; coordinator's numbering 121). (122) a mutation killed incidentally by an unrelated guard proves nothing about the property it named (Coach's M54 died on the thin-sample floor; above the floor, message text would have leaked with nothing red); caught only by re-aiming above the guard's floor (11:39Z; coordinator's numbering 122). (123) a regex or slice pattern satisfied by a neighbour: /<BasisChip/ also matched <BasisChipX, a slice on a comment rule occurring eleven times took the wrong one, a guard matched anywhere in the file while the render satisfied it (UI 11:40Z; anchor the pattern to the one site and prove the mutation lands there); (124) a mutation runner killed by a tail pipe (`| head -3`) leaves the mutation in the tree and a stale results file that reads as a result (UI 11:40Z; caught by the full check); extends the rule that tail-piped output is empty until exit: never pipe a runner that edits the tree (coordinator's numbering 123, 124). (125) a full-check line correct when written and aged wrong when later doc commits moved the parent (Google sign-in: §6 named 1171d66 two doc commits behind at 1e8dddd); the maintenance half of part five: a later commit touching the report re-measures and re-points the figure, or it is a claim about a tree that no longer exists (11:41Z; coordinator's numbering 125). (126) a filter that can never match reads as a deliberate decision (the impact report's survivors() excluded test/ paths from a grep that never included them; wiring map 11:41Z); prove a filter can fire before crediting it; (127) a symbol's name in a test string or assertion message counted as a caller (league-brain.test.js:57, route-deletion-impact.test.js:64, fixture text), under-reporting a deletion's blast: eleven of fifty symbols had a sentence for a caller; blank string and regex literals before the walk (coordinator's numbering 126, 127). (128) a control whose hash is real but unrecoverable from the page: a free-text edit (a reworded comment) has no derivable hash, so the measurement is one the file cannot hand a reader (Opportunity CONTROL A 460bb8d2485d withdrawn, re-run with quotable wording; 11:45Z); rule: re-measure with quotable text or drop the hash for that row (coordinator's numbering 128). (129) a test named for a headline design decision whose assertions a wrong implementation also satisfies (Coach's "the split is on time, not on message count" asserted only the ratio, truthiness and a date shape; a count-based split passed 12/0), invisible to a mutation table whose every row reproduces; only the seventh part, which tests never go red, finds it (Model audit 11:45Z; coordinator's numbering 129). (130) a count from a filesystem walk attributed to a commit: scripts/lint.mjs walks the tree incl. untracked files, so 877 was claimed where git ls-tree -r <commit> gives 876 (Trade Brain 11:47Z); counts named against a commit come from the commit (coordinator's numbering 130). Same message: the coordinator's fifth inherited cite ("callers pass isMine", a local const), rulings struck.

Continues: [[gridiron-failure-modes-16-2026-09-20]] (131 on).
