---
name: gridiron-failure-modes-16-2026-09-20
description: Page 16 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 11:51Z 2026-09-20 on (131 onward).
metadata:
  type: project
---

Continues [[gridiron-failure-modes-15-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-14-2026-09-20]]).

**Catalogue (cont.):** (131) a module-absent RED (the suite fails every test for one import reason) read as per-test RED evidence, so a mutation table whose every row reproduces covers under half the suite while reading as complete (Coach's tool layer: RED b590c2a "0 pass, 1 file erroring on import"; ten of seventeen tests turned red by nothing; grading 2 of 12); rule: for a new module the seventh part applies to every test, union of reds against the full list (Model audit 11:49Z; coordinator's numbering 131). (132) a DDL constant read as a creation site: AVAILABILITY_RATES_DDL in contingency.js is a definition nothing there executes; the creator is whoever executes it (fit-availability.mjs:54/:55), and a definition nothing executes is a finding, not a table (wiring map 11:50Z); (133) brace depth measured through the line that opens it (db.exec(`CREATE TABLE read as depth 1) sent five import-created tables to first_write; measure depth at line start (coordinator's numbering 132, 133). (119, addendum 11:52Z, second coordinator instance: rulings stamped 11:53Z and 11:55Z were estimates made before a 11:51:36Z reading, restamped 11:51Z; the coordinator has no clock except tool results, so a stamp is the last tool clock read, never an estimate; page 14 holds the entry and is full.) (134) a regex alternation assertion satisfied by the wrong alternative: WHY_UNSCHEDULED checked against /gateway|Jev/ still passed after "calls a paid gateway per manager" was deleted, a guarantee guarded by a word (chat sync 11:52Z); assert each fact separately; (135) mutation rows run against a suite that does not assert the rule, read as survivors (chat sync's C1-C3 pointed at archetype-as-of while the tests live in wiring-absent-states); each row names its suite and the runner fails a row whose suite touches nothing (coordinator's numbering 134, 135). (136) a hold cut from a stale local main (102 commits behind origin/main) passes every local check on the wrong base (fantasy plan 11:53Z; caught when docs/tdd was absent); rule: fetch, cut from origin/main, `git merge-base --is-ancestor origin/main HEAD` before the first push; (137) `git add -A` stages a gitignore difference between bases (the 203 MB Sleeper corpus, ignored on 791b131, not on the old main); explicit paths only; (138) a probe's bracket ("nothing answers past 110 s", eight lives 01:50Z-02:13Z) carried as a night-wide rule after three reads exceeded it (104/106/116 s; Release 11:53Z); a passive log cannot bound failures by age (coordinator's numbering 136-138). (139) `git add -A` on a merge-resolution commit safe only because the worktree held nothing else (Scheduler 0e0bb86, self-reported, "luck, not method"); (140) a guard that throws on the declined case and not the granted one: clusteredDiff refuses "not clustered" but returns a clustered interval on a misaligned grant because groups is sized to the shorter array (60/30/30 → −0.8 significant, truth +0.10; Model audit on 5674cf1); a length refusal is the fix; (141) a scan of two directories reported as a whole-tree count (Coach's eleven → twenty-nine on the full scan); (142) clock drift, third instance for one thread after two corrections (Coach self-stamped 12:16Z at queue 11:59:11Z); remedy: the first line of every message is the pasted `date -u` output, a stamp without it is not a stamp (coordinator's numbering 139-142).

Continues: [[gridiron-failure-modes-17-2026-09-20]] (143 on).
