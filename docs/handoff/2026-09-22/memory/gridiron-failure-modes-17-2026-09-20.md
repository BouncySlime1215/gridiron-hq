---
name: gridiron-failure-modes-17-2026-09-20
description: Page 17 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 12:00Z 2026-09-20 on (143 onward).
metadata:
  type: project
---

Continues [[gridiron-failure-modes-16-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-15-2026-09-20]]).

**Catalogue (cont.):** (143) quoting a run whose summary lines were cut by `tail -20` would have substituted the previous head's counts; caught by the author, re-run (Trade Brain 12:00Z, self-reported, positive instance). (144) a redirect ruled but never received, acted on as if delivered (the coordinator's 11:40Z Coach item, "pin only your own three"); remedy: anything ruled by send_message only is unruled until message_thread confirms delivery (channel rule addendum ≥12:02Z). (145) a "the only CREATE TABLE" claim that a grep of the whole tree refutes with six hits, five in fixtures: state the scope of the grep in the claim (Coach, caught by the Scheduler). (146) a migration with IF NOT EXISTS proves nothing about the live table's shape, only about the text (Scheduler, self-stated; PRAGMA table_info at the go). (147) creating a table empty by migration turns an honest throw into a silent zero (Scheduler on league_season_teams). (148) a source census whose extension list omits two extensions the repo uses for table-creating code (.py, .mts), self-found by the wiring map. (149) a count that double-counts a subgroup (Coach's 29 vs 26), caught by the wiring map's arithmetic (coordinator's numbering 143-149). (150) a second reader running one of two suites a spec names and reporting the lower kill count (Model audit M71 at 3, 4 on re-run; self-owned). (151) a table-name regex reading comments and inventing `default` and `statement` as tables (Coach, self-found; nfl-news-signal.js:285, data-lineage-inventory.mjs:110). (152) controls NOT APPLIED on the first run because anchors were written from reworded text, caught by the harness and disclosed (Coach, positive instance). (153) a docs-only check whose empty diff on an absent commit reads as a pass (`git diff --name-only A B | grep -v '^docs/'`, exit swallowed; Model audit, self-found on its first false pass; method unsound, answers happened to be right, separated as such). (154) a "pushed" claim for three heads where the third was not on the remote (wiring map 23ed6da), caught only by a second reader's fetch; rule: a pushed head is proven by an ls-remote line, not by the word "pushed" (coordinator's numbering 150-154). (155) a setup check keyed on last_status === 'never run' that passes a source which ran and wrote zero rows (UI on routes/model.js:685; pending the wiring map's verification on main). (156) a module-identity assertion satisfied by a type-only import: a test that cannot fail for its stated reason (UI, self-found). (157) a mutation runner verifying with `git diff` reports NOT APPLIED on untracked files that had applied (UI, self-found; `git add -N` first). (158) a cited check ("exactly two docs readers") that a re-run finds five, adopted as a sentence before anyone ran it: cite the command, not the count (UI, caught by Model audit ≥12:11Z). (159) the two runner faults as one family: a verification step that cannot distinguish its two outcomes (Model audit's absent-as-clean, UI's applied-as-not-applied); a runner must prove its verification can report both (coordinator's numbering 155-159).

Continues: [[gridiron-failure-modes-18-2026-09-20]] (160 on).
