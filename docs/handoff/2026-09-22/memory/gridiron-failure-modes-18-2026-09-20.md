---
name: gridiron-failure-modes-18-2026-09-20
description: Page 18 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 12:13Z 2026-09-20 on (160 onward).
metadata:
  type: project
---

Continues [[gridiron-failure-modes-17-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-16-2026-09-20]]).

**Catalogue (cont.):** (160) a relayed cause stated without its scope: "no migration creates league_season_teams" is true of main and false of PR #47, whose 064_league_history_tables.js:29 creates it (the coordinator's sixth inherited cite, caught by chat sync 12:13Z; the Scheduler's "no new migration" stands with the scope). (161) a bare catch that maps a missing table, a corrupt DB and a programming error to one empty value (routes/trades.js:500-503 `catch { archetypes = new Map(); }`, the CLAUDE.md-named shape that shipped two bugs); narrow to the one expected error, name the state beside the empty value, rethrow the rest. (162) a cached absence that would outlive the migration ending it (leagueHistoryState() left uncached on purpose: runMigrations() at startup and #47's merge end the absence; chat sync, avoided by design). (163) the regex-alternation assertion, third instance (chat sync F3, F9 after 123 and 134): each alternation branch gets its own assertion with its own message (clause adopted ≥12:13Z; coordinator's numbering 160-163). (164) a fixture that passes on a degenerate world (team 1 wins every title, every delta zero for the wrong reason) until a bounds guard on the baseline is added, 0 < title_before < 1 (feature audit 860e1a2, self-found). (165) a code comment asserting non-determinism that an inert injection disproves (season-sim.js:378-380 "would introduce noise"; buildProjections draws no random numbers; pending the fantasy plan's confirmation). (166) a green-together measurement taken at a head that then moved (feature audit's b104fab + 4389a7a while the wiring map advanced to 23ed6da; self-caught by re-fetch); rule: a pair measurement names both heads and is re-run when either moves. (167) a suite-wide shape found three times by mutation before anyone grepped for it (Model audit's alternation sweep: 28 in 20 files; positive instance); rule: after the second instance of a shape, grep the suite (coordinator's numbering 164-167). (168) a head described as docs-only that adds a test file (wiring map 23ed6da: twelve test lines, three new health-route tests; "three new tests pass" was the tell; caught by Model audit's fetch 12:17Z); rule: a diff that touches test/ is never docs-only, whatever the commit message says. (169) a comment giving a false reason for a right behaviour (season-sim.js:378-380 "rebuilding would introduce noise"; the shared build is right for performance and robustness; pending the fantasy plan's empirical test; coordinator's numbering 168, 169). (170) a scratch merge that would carry two other PRs' commits, correctly not pushed under the freeze (Trade Brain's a3715b7 and 1a07bf4, 12:21Z, positive instance); rule: a scratch merge is measured, patched and never pushed.

Continues: [[gridiron-failure-modes-19-2026-09-20]] (171 on).
