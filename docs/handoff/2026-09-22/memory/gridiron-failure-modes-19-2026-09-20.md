---
name: gridiron-failure-modes-19-2026-09-20
description: Page 19 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 12:22Z 2026-09-20 on (171 onward).
metadata:
  type: project
---

Continues [[gridiron-failure-modes-18-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-17-2026-09-20]]).

**Catalogue (cont.):** (171) two buckets printed as different sections but reasoned about as one map: the route-deletion fixpoint wrote ALREADY-UNREACHED symbols into the `falls` map insideFalling() reads, so a kept function (horizonValue inside sellHigh) printed as falling with a route; a tool-level instance of the healthy-looking shape (feature audit on the wiring map's script, 12:22Z). (172) an early-return walk that keeps only the last path to a symbol and prints it as the only path (freeAgents, four production sites, one printed). (173) a code path that ANDs two conditions and ORs two causes into one sentence, so the test's alternation can never distinguish them (consensus-weights.js:336-337; the defect is in the code, the alternation the symptom). (174) an alternation that silently drops a pin on the more damning branch (draft-abstention-audit.test.js:357, /Do not ship/). (175) a wording regex that matches a neighbouring problem's sentence when a code enum exists to assert (trade-proposals.test.js:194 vs RESPONSE_PROBLEMS; coordinator's numbering 171-175). (176) `pgrep -f "node --test"` matching its own bash command line reports a finished run as live (feature audit 12:23Z, self-found); rule: the log's completion marker is the record. (177) a null compared with > 0 folding "no rows" into "no variance": consensus-weights.js has measureSourceErrors return null for zero rows and `c.variance > 0` false on null, so three states wear one sentence and the reported n is Math.min across sources (feature audit, pending its RED; the fix a closed set of causes on the object; coordinator's numbering 176, 177). (178) a split that a neighbouring clause satisfies, caught only by re-running the mutation (chat sync league-chat-sync.test.js:58, /laptop|Mac/i vs "not the Mac" one sentence earlier; self-found 12:25Z); rule: re-run the motivating mutation after every split. (179) a "within one" tolerance that would have shipped 12 targets against a row holding 11 with a citation attached (Coach M102 survivor, self-found; a new test now pins the exact count). (180) an assertion without a message echoing the secret it guards into the log on failure (Google sign-in roster-snapshots.test.js:371, pending measurement; coordinator's numbering 178-180). (181) a literal control byte in a regex (a backspace where \b was meant, scripts/wiring-map.mjs:293), invisible in viewers, silencing a branch for the file's whole life (wiring map, self-found 12:25Z; `grep -P '\x08'`); a test greps the checker for control characters. (182) a static-import-only detector missing `await import(...)` and misattributing a whole file's handles (wiring map foreignOnlyFile, self-found). (183) a twelve-line test-file diff that is a block comment: a code head by diff, a no-op by count; the full check on the head settles it, not a reading (23ed6da). (184) an assertion branch that can never match because the producer's spelling differs ("injury report" vs injury-report; Opportunity); the dead-branch tell. (185) a page-explanation registration slot written by two mounted components, the later wins and the loser's cleanup clears it (UI, self-found; one registration per mounted tree). (186) a runner deciding APPLIED by `old in src` when the anchor matches twice (UI, self-found; fixed by count(old) == 1 with a refusal control; coordinator's numbering 181-186).

Continues: [[gridiron-failure-modes-20-2026-09-20]] (187 on).
