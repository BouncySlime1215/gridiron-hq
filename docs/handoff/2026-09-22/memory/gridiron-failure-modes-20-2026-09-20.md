---
name: gridiron-failure-modes-20-2026-09-20
description: Page 20 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 12:28Z 2026-09-20 on (187 onward).
metadata:
  type: project
---

Continues [[gridiron-failure-modes-19-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-18-2026-09-20]]).

**Catalogue (cont.):** (187) a narrowed catch made unreachable by the callee's own fix on another branch, leaving the page reporting 'present' on a database that cannot look anyone up: two individually correct changes (Trade Brain's narrowed catch d5fe7f6; chat sync's archetypesFor returning an empty map at 48324ff:1126) composing into the failure both were written to prevent (Trade Brain, caught on the scratch merge bfb3233, 12:28Z; the fix removes the catch and reads leagueHistoryState() directly, landing with the jev swap as one mailbox). (188) a served field null in every reachable state teaching the caller something untrue (read_failed once the owner stops throwing; replaced by read_reason). (189) the fifth inherited line reference, eight lines off (valuation-map :433/:483 vs the regex sites :441/:491); rule: grep the pattern, never the line. (190) a test file that reports one failure that is not a test result when a Node flag is missing (--experimental-test-module-mocks, the CLAUDE.md mock.module trap; Opportunity; coordinator's numbering 187-190). (191) an assertion inside an arm the fixture never enters, so a split there changes nothing (draft-abstention-audit.test.js:357, seasons_inverted empty; found by probe, not by editing; feature audit 12:33Z). (192) a null compared with > 0 inventing a second finding from the absence of the first (consensus-weights: a zero-row cell also reported a missing variance; confirmed by RED 6a9bb93). (193) the missing pin sits on the worse verdict (the anti-selective "Do not ship" branch had no reachable assertion). (194) `git checkout <file>` to revert a probe discards the uncommitted fix in the same file and says nothing (feature audit, self-found); rule: revert a probe by `git stash` or a patch, or commit the fix first. (195) a parallel enum nothing keys off, written and removed before shipping (VERDICTS; positive instance; coordinator's numbering 191-195). (196) `sed -i` on a running script: the file on disk changes, the process keeps the old one; three repoint claims false; caught by reading the process's own open descriptor (Model audit 12:33Z, self-found); rule: never edit a running script in place, a repoint is a new file under a new name. (197) a mutation row whose `old` value was inferred rather than measured (Opportunity row 3, 1 → 1 not 0 → 1; the unmeasured row was the wrong one). (198) a tool-level dead branch (the backspace byte) confirmed as the only control byte across 130 branch heads (Model audit pass A; coordinator's numbering 196-198). (199) a page-explanation registration answered from the URL alone on one tab (DraftHub under /draft, the LiveDraft registration firing only under /live-draft/:id; UI 12:35Z, self-found). (200) a runner kept in a scratchpad cannot be re-derived by the reader (UI's argument; positive instance: runner and mutation list now committed under docs/tdd/sweeps/; coordinator's numbering 199, 200). (201) an alternation with one branch in each half of a two-half sentence (reading, threshold), satisfied by deleting either half (Trade Brain M2/M3 on the acceptance sentence, 12:36Z; one assertion per half). (202) a reason naming the sample it has instead of the one it needs, reading as a measurement not a shortfall (M4, the min_n reason at four sites; all four old patterns survived). (203) a test named "malformed is refused" that cannot tell malformed from truncated, saved only by a neighbouring test (M9, caught by G7; stated as the honest exception; the code is now asserted first; coordinator's numbering 201-203).

Continues: [[gridiron-failure-modes-21-2026-09-20]] (204 on).
