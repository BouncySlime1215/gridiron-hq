---
name: gridiron-failure-modes-21-2026-09-20
description: Page 21 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 12:37Z 2026-09-20 on (204 onward).
metadata:
  type: project
---

Continues [[gridiron-failure-modes-20-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-19-2026-09-20]]).

**Catalogue (cont.):** (204) a module with no production importer that is a deliberately parked study with its verdict in the header: "no caller" is not "unwired" (consensus-weights.js, header :59-64 DECLINED, the reason inline at routes/aggregates.js:212-217; feature audit 12:37Z, near-miss, self-caught, the positive instance). (205) a writer with no caller and no job still reporting a job name to the sync ledger (syncFfcAdpHistory() at consensus-weights.js:139, the only writer of nfl_historical_ffc_adp, reports `ffc_adp_history`; the Finding 7 shape; live table unread). (206) a code comment whose whole purpose is to stop a redo pointing at a path that no longer resolves (routes/aggregates.js:217 "See docs/CONSENSUS_WEIGHTS.md", the file at docs/evidence/historical/); 80 such citation sites across server/**/*.js, nine at files gone entirely; the wiring map's report extension lists them, owners fix inside normal commits. (207) a pasted clock four minutes ahead of the queue time (feature audit "12:41" at queue 12:37:21Z; note only, both recorded; coordinator's numbering 204-207). (208) a pin test passing the function an offset production never passes, so it could not distinguish the branch orders it was written to pin (wiring map test 57, handleFor given the SQL text's offset instead of the opening quote; found by a mutation that killed nothing; now asserts f.text[at] === '`' first). (209) test fixtures using REAL table names read by a census as create sites, in the commit fixing that very row (wiring map tests 76/77 vs league_transactions_raw and messages; caught on the before/after diff); rule: fixture names carry a zz_fixture_ prefix or the census excludes test/. (210) a commit pushed without its own measurement, covered by the tip check (wiring map c238f1f, self-stated slip). (211) a stamp written from a sense of elapsed time behind a `date -u:` label (feature audit "12:41", self-withdrawn; every stamp pasted from here). (212) a shared fantasy model listed as betting because of two betting importers (preseason-model.js, nine fantasy-chain importers; routing corrected to the fantasy plan; coordinator's numbering 208-212). (213) a coordinator note conflating a call site with an import site, so "waiverUpgrades falls with the route" was false: the call at routes/trades.js:191 falls with GET /brain/waivers, the import at :35 survives and would fail routes/trades.js at load (the 11:44Z send_message line, the seventh inherited cite, the coordinator's own; caught by the feature audit against origin/main 12:16Z, reported 12:38Z). (214) send_message lag measured at up to ~4.8 hours: thirteen notes 07:47Z-11:56Z delivered to the feature audit in one batch at ~12:38Z, the largest instance tonight; message_thread is the only channel for anything that must land. (215) evidence files written before the standard reached the thread, upgraded to the eight parts before push rather than after, with the tree-identity proof (feature audit, positive instance; coordinator's numbering 213-215).

Continues (entries from 13:05Z on): [[gridiron-failure-modes-22-2026-09-20]].
