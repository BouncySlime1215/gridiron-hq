---
name: gridiron-failure-modes-6-2026-09-20
description: Page 6 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from after 07:08Z 2026-09-20 on that did not fit on page 5.
metadata:
  type: project
---

Continues [[gridiron-failure-modes-5-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]], [[gridiron-failure-modes-3-2026-09-20]], [[gridiron-failure-modes-4-2026-09-20]]).

**Catalogue (cont.):** (38) a test whose expected value is its own input (the fantasy plan's constant-pin test fed its own constant in as the prior and asserted it came back; survived the 0.85 mutation; third instance from that thread tonight, all found only by mutation; rule: the expected value comes from a source the code under test cannot see); (39) the coordinator dispatched producer lines (routes/leagues.js:125/:160, which request view=mMatchup and store the response) as parser sites, restating a finding without reading the consumer (caught by Google sign-in 07:07Z; rule: before dispatching a line number as a defect site, read what the line does).

**(40)-(41), Trade Brain 07:10Z:** (40) a delta measured on a product with a zero factor reads as "no effect" (ablation on unpriced players: their_value = our_value x multiplier, our_value 0, so sources moving the multiplier 6% and 3.25% showed delta 0; rule: when a quantity is a product, ablate each factor, not just the product); (41) a crash sentence read as an honest absence because the route's error path speaks the same channel as the panel (P10: removing the absent-player branch made the layer throw and the test accepted the error text; rule: assert that two absence states differ from each other, and that the panel, not the error handler, answers the known-absent case).

**(42)-(44), UI 07:11Z:** (42) two vocabularies for one quantity on two payloads, each correct alone (BasisChip's role/pooled/constants/unfitted_position from contingency.js:574 vs the accessor's fitted/durability_prior/default_durability/unrecognised), caught by the chip's author when handed the second (rule: a new served enum is checked against every enum already served for the same quantity before it ships; the wiring map's two-names rule applies to values, not only to function names); (43) an API error string rendered as the user interface on a draft-night button (SourcePill: every bookmarklet click returned 400 and the catch printed the route's parameter documentation; rule: a catch that prints the server's message is a missing UI state); (44) an assertion watching a symbol rather than a decision (a served-field check watched an identifier, so `{false && (` passed; second distinct shape in the UI stack; rule: assert the rendered decision, not the presence of the identifier).

**(45)-(47), after 07:12Z:** (45) the coordinator relayed a "duplicate parser" finding to a third thread without reading the consumer; the target (manager-signals.js:249-250, last_week_margin) was a one-scalar signal whose semantics differ, and the file was another thread's with four commits on it (caught by chat sync 07:11Z; rule: no cross-thread dispatch names a line as a duplicate until someone has read what the line returns); (46, REWRITTEN 07:14Z) a sweep diffing a PR branch against its own PR head reports it empty, and the coordinator passed that on as "carries nothing" and suggested deleting it (o3wt2p-mainthread-holds 3902ba7 IS PR #77, one commit 413/6 over its base timer-tier 64f3ef2; third time it came round; rule: compare a PR branch against its BASE; the coordinator proposes no branch deletion, ever, on a sweep's word; a relay is not Nick's authorisation for a destructive act); (47) a test that pins a line number fails on every edit above it and says nothing about why (wiring map's test/health-route-single.test.js: "expected one health registration, found 1"; rule: offsets in messages, never in comparisons).

Continues (entries from after 07:14Z on): [[gridiron-failure-modes-7-2026-09-20]].
