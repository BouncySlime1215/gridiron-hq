---
name: gridiron-failure-modes-7-2026-09-20
description: Page 7 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from after 07:14Z 2026-09-20 on that did not fit on page 6.
metadata:
  type: project
---

Continues [[gridiron-failure-modes-6-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-5-2026-09-20]]).

**Catalogue (cont.):** (48) two fields named as_of on one payload in two date formats (chat.as_of from datetime('now') at extract_league_chat.py:281, transactions.as_of and the archetype build ISO; incomparable on one payload; chat sync 07:14Z; rule: ISO 8601 UTC at every producer, pinned by a parse test on the served payload); (49) a MIN/MAX pair over a column with one value by construction, the T5 shape reappearing in new code (first_seen = MIN(computed_at) of a CREATE TABLE AS with one datetime('now'), identical to MAX; rule: a fixture that cannot make MIN and MAX differ cannot test the pair); (50) three answers to "how old is the chat data" across two threads (rollup MAX(computed_at); lastPull().finished_at; newest_message) and no ruling until they were read side by side (rule: an age on a payload is the data's own newest timestamp; everything else is provenance); (51) a process-level property stamped onto every row (lineup-brain.js:640 on main: a row reads role whenever the fit tables are loaded, whoever the player is; both prose-matching accessors sit downstream; Opportunity 07:16Z, coordinator's numbering 51, asked for page 6, which was full; rule: a per-row field is set from the row's own path, and a NO-OP control that rewords the prose proves the tests pin the field, not the sentence); (52) the coordinator's fold of unfitted_position into default_durability (after 07:11Z) would have told a reader a kicker had a durability prior and orphaned a live UI arm (BasisChip.tsx:53, Lineup.tsx:382, News.tsx:177); caught by the producer reading the consumer (coordinator's numbering 52; rule: a vocabulary merge is checked against every rendering arm before it is decided); (53) a memo key using ?? treats null and undefined alike when they are different calls (player-week-engine kOverride ?? 'active': null = no fit, hand-set constants; undefined = whatever is active), serving the fitted engine to a caller bypassing it (fantasy plan 07:23Z, coordinator's numbering 53; rule: a key distinguishes every call that means something different, including null from undefined); (54) an unkilled mutation with no explanation reads exactly like an untested line (M4: no-fit keyed 0 instead of null, equivalent because fit ids autoincrement from 1; rule: equivalent mutations are named and explained in the evidence file); (55) a fix that labels a case with one string can serve one caller's engine to another with different constants (the supplied-vector case; rule: what varies goes in the key); (56) a design document asserting a property of itself that does not exist (#73 section 2: every token gets a dark value; client/src/index.css has no prefers-color-scheme or [data-theme] block, the app has no dark mode; UI 07:24Z, coordinator's numbering 56; rule: a design-system claim is pinned by a test against the stylesheet like any served claim); (57) a guard that only bites in one direction leaves the "no consumer yet" state to be found later, as useNumberRoll was (rule: a guard on an absent thing also requires its own removal when the thing appears; UI's stat-table test goes red when TeamDetail imports the table without the document changing).

Continues: [[gridiron-failure-modes-8-2026-09-20]] (58-60, from 07:25Z).
