---
name: gridiron-failure-modes-8-2026-09-20
description: Page 8 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 07:25Z 2026-09-20 on that did not fit on page 7.
metadata:
  type: project
  modified: 2026-09-22T04:19:24.886Z
---

Continues [[gridiron-failure-modes-7-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-6-2026-09-20]]).

**Catalogue (cont.):** (58) a report that reads every "no caller" row as a deletion names live code as falling (the route-deletion impact report's first run: requirePlatformAdmin with POST /managers/rebuild, ruled kept, and the walk-forward chain the wiring map itself keeps; wiring map 07:25Z, coordinator's numbering 58; rule: a verdict store shared by every instrument, docs/wiring/route-verdicts.json, one reason per row); (59) four extractor bugs each producing a clean confident wrong answer (a comment counted as a caller; an import counted as a use, 36 → 0 falling; `name(` missing a function passed as a value, middleware read as unreached; an exported const counted as a function, 126 rows mostly `export const db = new DatabaseSync()`), found only by a case with a known answer, Trade Brain's positionLiquidity chain (rule: run every new checker against a chain somebody already traced by hand, and refuse a disagreeing answer); (60) figures typed from an earlier run instead of read from the regenerated file (17/22 sent, 18/21 in the file; corrected a minute later; rule: a number in a message is read from the file it describes at the head it names); (61) a rejection that tested the fitter was cited for a year as a rejection of the literals (shrinkage-fit.js:465-473 tested the fitter; projections.js:94-125's yards_per 34, catch_rate 26, td_rate 70 were never graded until Model audit 07:27Z, when a nine-point geometric grid with k=0 and k=Infinity bookends showed they hold; coordinator's numbering 61; rule: a "tested rejection" names what was tested; a literal is graded by sweeping it, not by fitting around it); (62) a deliberate silent fallback in a significance function manufactured a winning interval when the caller's lengths mismatched (pairedBootstrapDiff drops to an unclustered resample when groups.length !== n, backtest-significance.js:80; the first pass "beat shipped" and vanished on the fix; rule: a fallback that changes the statistic is a served flag the caller asserts, `clustered: boolean`, never a silent switch); (63) a signal wired to a route whose only caller is an unrouted page reads as wired in every report and is seen by nobody (usage_coverage on GET /api/model/status, whose one caller Model.tsx nothing imports and no Route declares; wiring map 07:27Z, on a branch written to fix an instance of the same shape; coordinator's numbering 63; rule: "has a caller" is not "reaches a reader"; a served field names the surface a person sees it on); (64) two holds from one thread collided because one deleted the route the other extended (route-gate caac88a vs usage-coverage 2626712), caught only when the author re-read its own commit message (rule: a thread's branch-pair sweep includes its own branches).

**Correction (2026-09-22):** (58)'s `docs/wiring/route-verdicts.json` checked against `main` at `654ff93` — not present. Design intent stands; not a live repo artifact until created.

Continues: [[gridiron-failure-modes-9-2026-09-20]] (65-67, from 07:30Z).
