---
name: gridiron-failure-modes-9-2026-09-20
description: Page 9 of the gridiron-hq "healthy-looking and not working" catalogue and rules; entries from 07:30Z 2026-09-20 on that did not fit on page 8.
metadata:
  type: project
---

Continues [[gridiron-failure-modes-8-2026-09-20]] (earlier pages: [[gridiron-failure-modes]], [[gridiron-failure-modes-2-2026-09-20]] to [[gridiron-failure-modes-7-2026-09-20]]).

**Catalogue (cont.):** (65) a dead import makes a service look consumed and seeds a phantom consumer into the next reachability report (byeOutlook from roster-risk.js imported by routes/trades.js and unused before tonight; Trade Brain 07:30Z, coordinator's numbering 65; rule: prune imports in the commit that removes their last use); (66) a hand-kept security probe list rots: red for the right reason once, green for the wrong reason later when a route is added and the list is not (test/cross-account-league-access.test.js named five deleted routes; now derived from every GET the router declares under /:leagueId with a floor so an empty derivation cannot pass, 403 or 410 accepted; rule: derive security probe lists from the router with a floor on the count); (67) a reachability report that ignores test imports calls a test-pinned function unreached (positionRequirements() via test/pick-reasoning.test.js:9, :114; rule: test-only reachability is its own class in the report); (68) a thread reported another's push missing from a stale fetch (fantasy plan 07:23Z "availability-basis.js not on a6d00bb"; it had been on the remote since 09860ba at 07:19Z; Opportunity 07:32Z, coordinator's numbering 68; rule: fetch before claiming a remote lacks a commit); (69) two constants with the same digits on opposite sides of a curve would merge into one name and then move together (DEFAULT_DURABILITY_PRIOR is the input to the report-status curve; DEFAULT_ACTIVE_PROBABILITY / FALLBACK_ACTIVE_PROBABILITY stand in for the output; rule: a constant's identity is the quantity and the side of the transform it sits on, never its digits); (70) an equality test against a served three-decimal number misreads a real measurement as a default (a real 0.920 reads as the constant; rule: read the provenance flag durability_prior_measured, never compare to the constant; pinned by test); (71) a brittle test tripped two threads and each fixed it in place, producing a collision on a file neither owns (test/health-route-single.test.js compared the literal "server/index.js:86 → /api/health"; the wiring map at caac88a and Coach at 73e0760 both rewrote it; the wiring map's version wins; Coach 07:33Z, coordinator's numbering 71; rule: report a test that breaks on unrelated edits to its owner once; the first fix wins; later threads leave it and note the failure); (72) a thread put two decisions to Nick in its own thread (Coach 07:34Z, "Two things need you": the $3/day budget and whether to read the Mac chat corpus), both already on his morning list as item 10; the 20:48Z rule broken; steered 07:36Z to edit the reply in place to results only (coordinator's numbering 72; rule restated: results only in thread replies; one item at a time to Nick, from the coordinator; an edit in place sends no notification and is the repair); (73) a stacked hold written before the base's contract existed carried a competing vocabulary and constant that read as agreeing (fantasy plan avail-basis a7ac178 on a6d00bb: its own list ['fitted', 'durability_prior', 'default_durability', 'unrecognised'] and DEFAULT_ACTIVE_PROBABILITY = 0.92 beside Opportunity's availability-basis.js at 9732f83; three conflicts with the ruling; found by the sweep's ancestor column, +1/-3; Opportunity 07:34Z, coordinator's numbering 73; rule: a stack member re-merges its base before each contract-bearing commit, and the sweep's ancestor column is read before any table is sent).

Continues: [[gridiron-failure-modes-10-2026-09-20]] (74-77, from 07:35Z).
