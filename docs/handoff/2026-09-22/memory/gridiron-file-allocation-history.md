---
name: gridiron-file-allocation-history
description: Verbatim grant history/corrections for gridiron-file-allocation.md, moved out to keep that file under cap
metadata:
  type: project
  modified: 2026-09-22T16:44:57.882Z
---
**Correction 2026-09-22 07:55Z:** the coordinator wrongly told Model evidence audit for ~1h that it owned `projections.js`. It does NOT — `projections` is Fantasy plan's, and Model evidence audit holds no server file. Division: Fantasy plan BUILDS in projections.js (incl. the §9 comment hand-off and the gated participation/shrinkage units); Model evidence audit AUTHORS the evidence documents and is the grading counterpart. `nfl-ai-replay.js` is betting-side: findings recorded, NOT edited (fantasy-only scope).

**Grant 2026-09-22 12:36Z (coordinator, settled):** `weekly-weight-store.js` granted to **Scheduler for the `-epoch-fallback-loud` branch only**, then it reverts. The grant existed implicitly from the 07:25Z unit assignment ("epoch-rollover-after-promotion gap, make the fallback loud"), which cannot be done without that file, but it was never written down — so for hours the map said Scheduler owned five files and Scheduler had three commits on a sixth. **The model-fitting chain stays Fantasy plan's, `weekly-ensemble.js` included** (it exports `WEEKLY_ENSEMBLE_WEIGHTS`, `weeklyEnsemblePrediction`, `weeklyEnsembleMode`, and is what `promote-weekly-ensemble.mjs` and `promote-early-week-weights.mjs` fit). Fantasy plan was told and may object.

**The lesson, not the entry:** assigning a unit grants the files that unit needs, and the grant is invisible until someone writes it here. A thread that finds itself editing a file the map does not give it should say so rather than assume the map is stale in its favour — and whoever assigns a unit should record the grant at assignment time, not when it is challenged.

**Grant 2026-09-22 16:28Z (coordinator):** `nfl-model-growth.js` + `nfl-advanced.js` → **Scheduler** (depth-sync/growth-job unit); `football-context.js` → **Feature audit** (feed-zero unit, narrowed to :93 only); `scripts/build-manager-archetypes.mjs` → **Coach** (paid-run guard unit). `player-week-engine.js` owner still UNASSIGNED (roster-source reporting unit queued) despite the current map listing it under Fantasy plan "in full" — flagged, not yet reconciled.

**Correction 2026-09-22 16:11Z/16:43Z:** `projections.js` moved OUT of Fantasy plan and IN to **Model evidence audit** — it now edits the code it grades directly (target-share prior, K.yards_per constants). Fantasy plan **keeps `weekly-ensemble.js`** unchanged; the two files are not the same model-fitting chain despite both feeding projections.
