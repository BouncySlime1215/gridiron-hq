---
name: gridiron-held-branches-13-2026-09-20
description: Page 13 of the no-PR "-hold" branch ledger for the 2026-09-20 GitHub freeze; heads reported from 07:51Z (UI outlook-hold af7f01a, stack of eleven, ONE PR from the tip) and later.
metadata:
  type: project
---

Continues [[gridiron-held-branches-12-2026-09-20]] (merge order for stacked holds: /mnt/project-files/hold-branch-sweep-2026-09-20T0736Z.md, fresh file at the go).

| Thread | Branch | Head | Lands how | Notes |
|---|---|---|---|---|
| UI (stack tip) | claude/project-thread-xiezr0-outlook-hold | af7f01a (07:51Z; c0376bc the durability amendment below it) on d4685f2 | ONE new PR from af7f01a (ruling 07:53Z); stack of eleven above main: 4536340 → deb492a → d1cc317 → 22b2cb9 → ce66060 → b502852 → 96ea84d → 01645a0 → d4685f2 → c0376bc → af7f01a; after #43 #46 #53 #57 #58 #60 #78 merge | League Hub outlook panel DONE: typecheck clean, 3,173 / 3,132 / 0 / 41, build 2.63 s, smoke passed; mutations APPLIED 19 runs (16 outlook, 3 amendment) all red; the amendment reads durability_prior_measured `=== true`; clamp in client/src/lib/percent.js + glossary `neverCertain` flag applied by formatValue on every surface. Next on the tip (07:53Z): Trade Lab tones for 'unpriced' / 'partly unpriced' and a distinct unknown-string fallback (TradeCard.tsx:229, :41; TradeLab.tsx:336), per-side unpriced row under "Market value" (TradeCard.tsx:116) rendered only when the fields are present; all evidence files on the stack folded up to six parts |
| Wiring map (NEW) | claude/wiring-map-8f96ur-usage-coverage-setup-hold | 52a55cc (07:53Z; stack 791b131 → 527b193 RED → e87a3ca GREEN (#66) → 84bae8a RED → 52a55cc GREEN) | new PR off main AFTER #36's route-gate merge; branch-pair sweep vs 8307d09: ONE conflict, one hunk, the routes/model.js import line; resolution `import { syncAll as syncNflverse, usageCoverage } from '../services/nflverse.js';` (usageSeasons, usageFor dropped); 6 tests pass and check exit 0 on the merged tree | usage_coverage on GET /api/model/setup-status: { job, seasons [2022..2026], per_season, missing, never_run, stamp, stamp_disagrees }; four exclusive states HEALTHY / NEVER RUN / STALE / LYING (stamp ok AND missing non-empty; an extra row pushed into `missing` so DataSetupBanner prints the sentence with no client change). RED at e87a3ca 6/0, GREEN 6/6; six injections caught (M2 usageCoverage() with no window = the real defect); docs/tdd/setup-status-usage-coverage.tdd.md (tests cited by ordinal: titles, SHA, control, sixth part in the next commit); full check 2,969 / 2,928 / 0 / 41, smoke 32 teams |
| ~~Wiring map~~ SUPERSEDED | ~~claude/wiring-map-8f96ur-usage-coverage-hold~~ | ~~2626712~~ (put the field on the deleted GET /api/model/status) | stays pushed as the record; NOT merged, NOT force-pushed, NOT deleted | superseded by 52a55cc, 07:53Z |
| Fantasy plan | claude/project-thread-f921do-memo-key-hold | 9fce773 → 761af34 (11:01Z, p15) | ONE PR from 9fce773 for the avail-basis/memo-key pair (ruling 07:57Z); avail-basis-hold a7ac178 stays a recorded ancestor, not a PR target (thread wrote "a7ac174"; ls-remote confirmation asked). #42 head d68a5f3 IS an ancestor of 803074d: #42 merges first, the O4 stack fast-forwards behind it | activeProbabilityFor imports AVAILABILITY_BASIS, DEFAULT_DURABILITY_PRIOR, isAvailabilityBasis from availability-basis.js, local copies deleted (test asserts the old exports are gone); six arms; the 0.920 collision test proves the basis is read from the row. Check 2,975 / 2,934 / 0 / 41; sweep B1-B7 APPLIED with SHA and named tests + CONTROL, base hash 51fe1716. Pending: DEFAULT_ACTIVE_PROBABILITY swap after Opportunity's export; memo-fit and weekly-scores evidence rows; then the offseason-model hold; then D31 |

Continues: [[gridiron-held-branches-14-2026-09-20]].
