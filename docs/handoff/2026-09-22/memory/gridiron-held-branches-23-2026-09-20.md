---
name: gridiron-held-branches-23-2026-09-20
description: Page 23 of the no-PR "-hold" branch ledger for the 2026-09-20 GitHub freeze; heads from 11:53Z on (fantasy plan arm-align 5674cf1, Release plan ab3e66e) and the hold-cutting rule.
metadata:
  type: project
---

Continues [[gridiron-held-branches-22-2026-09-20]]. Merge order: /mnt/project-files/hold-branch-sweep-2026-09-20T1115Z.md (the ONE source until the go sweep). RULE 11:56Z for every hold: cut from origin/main after a fetch; `git merge-base --is-ancestor origin/main HEAD` before the first push; explicit paths, never `git add -A` (failure modes 136, 137); `git check-ignore` needs the trailing slash (`data/derived/`) or a path inside, since .gitignore:75 is directory-only and the directory is absent in a fresh clone (Opportunity 11:56Z). Rule checks passed: Trade Brain 7156e8a (11:55Z), Opportunity f68059d (11:56Z), Scheduler 64cb90e/8709ec6 (11:56Z; its own add -A on 0e0bb86 flagged, failure mode 139), Google sign-in a2e7f97 (11:58Z; remote heads unchanged: #71 8b1a036, #48 b76963d, #51 2bea7ec; the outlook patch still applies), Model audit 2b7f8e2.

| Thread | Branch | Head | Lands how | Notes |
|---|---|---|---|---|
| Fantasy plan (NEW) | claude/project-thread-f921do-arm-align-hold | 5674cf1 (11:53Z; off main 791b131, stacks on nothing) | new PR off main | D34 FIXED: server/services/pooled-arms.js (arms pooled on season|team|player_id, compared on shared rows; comparison_basis on every interval; <10 shared rows refused with reason; arm_coverage; gbm_error recorded; fitter injectable; clusteredDiff throws; `clustered` on every return); 2,960 / 2,919 / 0 / 41 (off main); nine mutations + inert control, bases 308a8a406084 / 360bae562188; docs/tdd/pooled-arm-alignment.tdd.md; two survivors fixed (clusteredDiff throw; repeated key refused). Process catch: first cut from a local main 102 behind, `git add -A` staged the 203 MB corpus, branch deleted, sweep re-run hash for hash. Model audit measures it; fantasy plan runs the probe. Routed: nfl-offseason-change.js, nfl-team-strength.js (fantasy plan, clustered assertions); nfl-prop-player-heads.js out of scope |
| Release | claude/release-train-2yv3x6-hold | ab3e66e (11:53Z; was c117daa) | #35 a60858d | plan head; 262 starts as of 11:49:29Z (691 reads, 365 hangs, three files; third pass 48 rows); "110 s" is the probe's bracket, not a ceiling (answers at 104/106/116 s); two guards: passive logs cannot bound failures by age, first 60 s of a life is routing silence (grace_period 60 s) |
| Coach | claude/coach-grounded-4l8hno-hold | 0b8e77d (queued 11:59:11Z, self-stamped 12:16Z, drift; hold only, no PR; was 4e93e98) | new PR off main | 3,111 / 3,070 / 0 / 41 in 462 s; 34 commits, 51 files, +8,701/−4; body /mnt/project-files/pr-coach-body-0b8e77d.md (4e93e98 file superseded). Table origins: created_at_runtime_by on every catalog entry, catalogue 41 → 55, catalogCoverage() splits not_built_yet / missing_from_database; docs/tdd/coach-table-origins.tdd.md, seven injections killed, two controls; ONE EXISTING TEST CHANGED ("every catalogued table exists in the schema") rather than the code, awaiting Model audit's grade (chain measures 0b8e77d in place of 4e93e98). Its twenty-nine names → wiring map diff; Finding 7 claim → Scheduler (066); eleven struck. ≥12:06Z: the changed test graded JUSTIFIED by Model audit (two escape trials, nine rows reproduce); items 1, 2 closed at LOCAL 733b703 (not a head until pushed with numbers), 6 closed by amendment (created_at_runtime_by stays on all fourteen catalogued entries; the 11:40Z three-only redirect never arrived), 7 in progress, 3, 4, 5 open; pasted `date -u` first line |

Continues: [[gridiron-held-branches-24-2026-09-20]].
