---
name: gridiron-morning-message-inputs-11-2026-09-20
description: Eleventh page of overnight inputs for the 2026-09-20 morning message: wiring map correction (route-no-caller already exists, 462 rows) and committer-address note; espn-market resolver fix allocated to the caller commit.
metadata:
  type: project
---

- Continues [[gridiron-morning-message-inputs-10-2026-09-20]].
- Wiring map 02:31Z CORRECTION: no new rule needed; route-no-caller already exists and reports 462 routes no client file calls, including GET /api/decision-inbox, GET /api/trades/:leagueId/trends, POST /api/decision-inbox/:id/resolve, GET /api/trades/trends/watch and six more; its earlier "rule missing" claim came from a truncated pipeline read. The real finding: 462 undifferentiated rows is a rule that reports nothing; fix = rank uncalled routes by reach (modules and tables crossed), lead with the ten that do the most work for nobody. Credit to feature-audit, found by hand in the right order. COMMITTER NOTE for the morning: every commit the wiring map pushed tonight carries Nick's address as committer (matches branch history); a local check wants noreply@anthropic.com; fixed on the two local #36 commits; pushed ones would need a force-push, forbidden, LEFT AS IS, coordinator decision. Batch: #43 pre-fix control clean after the guard (same six rows, same two green); #63 running.
- Feature audit 02:32Z ALLOCATION: server/services/espn-market.js stays feature-audit's (#55 already edits it: espnMarketFreshness newest_season/oldest_season); the resolver fix (:19 stops selecting espn_s2/swid, :31 stops hand-building the header, throw rather than anonymous) lands in the commit that adds a caller, through platform/espn-credentials.js, which stays Google sign-in's and nobody else edits. Feature-audit has no #48-based branch. Window branch re-check at 1,148 tests no failures; #55 queued.
- Feature audit 02:33Z, F1 CONTRACT SETTLED (one agreed statement, both halves): on 7c27517 trade-engine.js:421 `const currentWeekBasePpg = coordinated?.ready ? coordinated.corrected_ppg : weeklyPpg;` is unchanged and deliberate. Two questions: "what do I correct" = base must be the structural head, null when absent, never a silent ensemble base (that was F1: ensemble + a structural-trained correction, double-counting ensemble_shift); "what do I show when there is nothing to correct" = the plain ensemble, openly (null would blank current_week_ppg on every surface until the first fantasy_coordinator_refit runs). The remaining fault is the NAME: one field carries two quantities depending on a flag the reader is not looking at (fantasy_coordinator: null is served beside it). Fix = a current_week_basis field 'coordinated' | 'ensemble' beside it, same shape as availability_source (#57), window.basis (#74), acceptance (#62). FOLLOW-UP AFTER THE GO on feature-audit's side (not on #57 tonight: release verified 7c27517; not on #64: scope). Fantasy plan told to build :571/:578 to the same contract (structural base, null when absent; labelled ensemble for display when not ready; basis field). Coordinator's earlier "no ensemble fallback" corrected to "no ensemble BASE". Window branch re-check 1,536 tests no failures. Chat sync 02:33Z: holding, nothing new, nothing unsaved.
[[gridiron-morning-message-inputs-12-2026-09-20]]
