---
name: gridiron-morning-message-inputs-18-2026-09-20
description: Page 18 of the chained inputs for Nick's 06:00Z 2026-09-20 morning message
metadata:
  type: project
---

- Continues [[gridiron-morning-message-inputs-17-2026-09-20]].
- Release 02:48Z: CORRECTION to item 1: two things start at exactly 90 s, nfl_model_growth and the live data tier (24 jobs, 23 of 24 on the request thread (agreed 03:01Z, release + scheduler; release withdrew 21); two-week-old source comment names the live tier as the earlier-found cause). Neither #56/#59/#61/#63 touches the live tier. Morning wording: a death ~3 min after the unset is the other candidate, not the fix failing, not a rollback reason; the brake stops both. Live-tier move routed to scheduler 02:50Z (hold branch only); answer pending. Refs (server/services/scheduler.js on 791b131): live start `liveTimer = tier('live', live, liveIntervalSeconds * 1000)` :1800; liveIntervalSeconds = 90 default at :1717, server/index.js:75 passes no override (the intervalMinutes 5 call is server/index.js:75, not :83 as page 16 says; signature default 30); tier() is a plain setInterval with no leading call, :1775; the 2026-09-07 comment is :1721-1731, sentence to quote :1723-1726: "the betting-side live tier's 90-second polling of a 6GB+ synchronous SQLite database (node:sqlite has no worker thread; a slow query blocks the whole HTTP server, not just the caller) was found to be the actual cause of the app going periodically unresponsive", ending "rather than chase which of a dozen 3-to-5-minute jobs is the one currently holding the lock"; offThread resolves false at :1605 for 23 of 24 live jobs (agreed 03:01Z, release + scheduler; release's 21 withdrawn, its text search over-ran a comment boundary: player_rosters :1114 and nfl_book_feeds_extra :1180 carry no offThread; seven declarations file-wide at :1141, :1144, :1147, :1187, :1314, :1335, :1432 = nflverse_crosswalk, nflverse_weekly_usage, nflverse_snap_counts, nfelo_sync, nfl_reports, ffopportunity, evidence_daemon; only evidence_daemon :1314 is live). Runs query in step 3 is now a discriminator: a running row ~90 s into a life = the job started; its absence across several lives points at the live tier. Brake does not depend on the attribution: SCHEDULER_DISABLED=1 returns before any tier starts, so one command stops both. Open figure: the comment says 6GB+, live memory says 445 MB + 445 MB WAL; not checkable without flyctl; step 3's ls -la /data answers it in the morning. Release's rec: the brake-first order is now backed by the app's own source, not our prose.
- Release 02:55Z, clean negative result: across both instruments (335 reads, 137 failures) not one 503. healthHandler runs SELECT 1 and returns 503 when it throws, so a locked-but-turning loop would show 503s; every failure was a client-side timeout (90 passive log, 47 probe). So the event loop is not turning; this is synchronous work on the main thread, not a writer holding a lock. Rules out busy_timeout, WAL tuning, retry-on-busy as morning fixes; confirms #59/#63 are the right kind of fix and extending the same off-thread treatment to the live tier is the right follow-up if the cycle survives the deploy. No unauthenticated endpoint can settle live-tier vs growth-job tonight; the runs query in step 3 is the discriminator. Release hold head now e6b6785 (03:05Z; clean fast-forward onto #35 a60858d, merge-base --is-ancestor confirmed; was 234e0e0).
Continues at [[gridiron-morning-message-inputs-19-2026-09-20]].
