---
name: gridiron-morning-message-inputs-19-2026-09-20
description: Page 19 of the chained inputs for Nick's 06:00Z 2026-09-20 morning message
metadata:
  type: project
---

- Continues [[gridiron-morning-message-inputs-18-2026-09-20]].
- Fantasy plan 02:55Z: outlook fit store done on a second hold branch e76357b (migration 065 claimed; 062 x3, 063 #48, 064 #47; if #47/#48 renumber they must not take 065). O4 root cause: history-corpus.js:48 returns null when the corpus dir is absent and the Dockerfile copies only client/dist, server, scripts, so every O4 number resolved to nothing on Fly silently. k = 7.2 from 26,836 team-seasons, weeks 2-8 sign-clean, 186,678 rows identical from storage. Two guarded traps: positional coefficient vectors (feature list stored with the fit) and a lost k making verdictFor(NaN) return 'fine'. Twelve mutations, twelve caught; two read-side checks needed row-corruption tests. OPEN: nothing reads activeOutlookFit() yet; consumer is a route change; wiring gate rule 4 (new module reaching no live surface) would fail check:wiring until it is wired; allocation sent to fantasy plan 02:57Z (the route that serves #42's panel). Fitter script goes in Nick's run sheet, never on Fly.
- Scheduler 02:55Z (read against 791b131): live jobs inline = 23 of 24 by name (only evidence_daemon declares offThread among live jobs; seven offThread declarations in the whole file: nflverse_crosswalk, nflverse_weekly_usage, nflverse_snap_counts, nfelo_sync, nfl_reports, ffopportunity, evidence_daemon; player_rosters and nfl_book_feeds_extra do NOT declare it); release said 21 of 24, then withdrew it: AGREED 23 of 24 (03:01Z, release + scheduler). NEW FINDING: #59 passes bootOffThread(j) for the boot pass but the live timer calls runIfStale(j) with no override, so 14 live jobs run in a worker once at boot and return to the request thread at 90 s and every 90 s after (the #63 defect on another tier); the first 90 s after the unset are NOT representative of the next 90. REC for morning item 1: live-tier off-thread move = FOLLOW-UP AFTER the unset, not in the morning merge (brake stops the live tier too so it cannot help the 600 s proof; merging it into the same deploy gives a death in the 900 s window two candidate causes; the four 3-minute betting jobs the comment blames are NOT moved, worker cost at 3-min cadence unmeasured). Ready if Nick prefers one deploy; his call. Hold branch claude/project-thread-o3wt2p-growth-offthread-hold, head pending; 13 jobs moved (six growth ingests; live: rss_news, espn_news, nfl_news_signals, player_rosters, nfl_injuries, nfl_transactions); every remaining inline job named with a reason in a suite-enforced register; league_rosters tried and reverted (two tests stub globalThis.fetch on the main thread; fix is #45's shape, export the decision).
- Release 02:56Z: memory said seven jobs at consecutive_failures 1 were a counter reset by a restart; backwards. record() runs only after job.run() returns, so a killed job increments nothing; those counters are a floor on abandoned attempts and sync_log cannot show a killed job. After the fix the counters start being written for the first time; they do not 'move'. Release corrected gridiron-502-vs-503-names-the-fault in place; routed to scheduler for #61's rationale 02:59Z.
Continues at [[gridiron-morning-message-inputs-20-2026-09-20]].
