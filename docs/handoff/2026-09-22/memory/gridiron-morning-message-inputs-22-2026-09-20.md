---
name: gridiron-morning-message-inputs-22-2026-09-20
description: Page 22 of the chained inputs for Nick's 06:00Z 2026-09-20 morning message
metadata:
  type: project
---

- Continues [[gridiron-morning-message-inputs-21-2026-09-20]].
- Release 03:04Z: the step-3 discriminator (latestRun, nfl-model-growth.js:112) does reach the wire as model_growth.latest_run via GET /api/nfl-betting/profitability (routes/nfl-betting.js:197) but that router is behind legacyAuthenticated (index.js:132, platform/legacy-access.js:22), and the handler runs a dozen-plus synchronous aggregate reads, the exact shape being blamed, so calling it live could cause a restart and count it; step 3 stays a SQL read under the brake. Guard against a wrong morning inference: the :1723 comment's 'unresponsive for several seconds' describes steady-state 90 s polling where maxAgeMinutes keeps most jobs from running; the first pass after a boot runs all 23 request-thread live jobs back to back, so seconds there and a minute-plus here are consistent; the mirror argument for nfl_model_growth is no better (never seen at seconds-scale, never seen finishing); attribution waits for the row. Health logger runs past 09:00Z; restart-count.sh live at send time.
- Trade Brain 03:02Z (hold 21b449a): managers/signals payload now carries transactions { as_of = MAX(last_seen_at) for that league-season, rows, first_seen, collector named, reason when empty }. Second half found: SIGNAL_SOURCES.tx.refreshed read 'every refresh tick' and signalRowsFor served it in the why field of every tx signal row, so the false cadence reached the client per metric per manager; corrected in the same commit. Finding 7 re-derived independently on the tree (INSERT only at scripts/collect-league-transactions.mjs:34; spawn only scripts/refresh-live-data.mjs:99; fly.toml no processes). RULE for every as-of: use the table's own stamps per league, never sync_log.last_run_at (the collector catches per-league failures and continues, so the job stamp can be fresh while one league is days old). Three consumers with no as-of, routed 03:04Z: manager-archetypes.js (chat sync), trade-tactics.js and counterparty-pricing.js:863 (Trade Brain). Evidence docs/tdd/transactions-as-of.tdd.md. Live: restart-count.sh = 81 restarts as of 02:55Z.
- Scheduler 03:02Z: hold head a9511f6 (three commits; 3,001 tests 0 fail 41 skipped; lint/typecheck/build/start:smoke clean): twelve jobs off the request thread (six growth ingests, six live); the thirty remaining named with a reason in a suite-enforced register whose size plus the must-not-move list must equal the number of blocking jobs; league_rosters moved then reverted (two fetch-stub tests are the only guard against one bad league stopping the rest; fix is #45's shape, not tonight); rec: lands AFTER the unset, Nick's call for one deploy. Base still unstated (base TBD until scheduler answers).
- Wiring map 03:02Z to feature-audit (both files feature-audit's; wiring map edited nothing): VOR-vs-fc_value divergence DOWNGRADED to a comment fix (tradelab.js analyzeLeague needs[].gap is VOR units, surplus[].value market units, but both consumers read only .position; the comment claiming 'needs/surplus stay on VOR' is false for surplus). trade-engine.js:161 UPGRADED to a correctness item: a bare catch returns an empty map from rosterContext when analyzeLeague throws, so evaluate() silently falls back to 'whether the numbers pencil out' while the engine keeps producing confident offers; the sibling counterparty-pricing.js deriveRosterNeeds returns null and its caller records absent.push({reason: 'no roster read for this league'}) at :773, so the reporting behaviour is already the house style and trade-engine.js is the outlier. Not a blocker tonight; in the trade path Nick uses. Wiring map's remaining local work: accepted_orphan_modules line for opportunity-model.js; correcting the model audit's snap-share claim on the standing audit page.
Continues at [[gridiron-morning-message-inputs-23-2026-09-20]].
