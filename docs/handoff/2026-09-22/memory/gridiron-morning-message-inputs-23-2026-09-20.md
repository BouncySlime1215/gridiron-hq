---
name: gridiron-morning-message-inputs-23-2026-09-20
description: Page 23 of the chained inputs for Nick's 06:00Z 2026-09-20 morning message
metadata:
  type: project
---

- Continues [[gridiron-morning-message-inputs-22-2026-09-20]].
- Wiring map 03:05Z: accepted_orphan_modules line for opportunity-model.js already exists (annotations.json, bf003e8, on #36's 252c896; gate honours it at wiring-map.mjs:2592). Snap-share unit finding, routed 03:07Z: player_week_snaps.offense_pct is written unscaled from nflverse (nflverse.js:294, REAL, no unit, no constraint); news-fantasy-impact.js:136 decides the unit PER ROW (x (offense_pct <= 1 ? 100 : 1)); News.tsx:471 prints it as '% snaps'. The ': 1' branch is dead today (source is 0-1), and if percent-unit values ever land, a 1%-snaps player reads as 100% while an 85% player still reads 85%, wrong exactly for the low-usage players a news signal is about. offense_pct exists in two tables (nfl_snaps via nfl-advanced.js:192, player_week_snaps), both unscaled, nothing asserts agreement; snap_change_points (model-signal-quality.js:104) is a points delta, not a share. Owners: news-fantasy-impact.js UI, nflverse.js fantasy plan; audit's page carries the snap-share claim. Wiring map's standing audit page is at v16: https://claude.ai/artifact/CUKM2d3hSkhfYAGLRbuMjY. Branches unchanged (aede8b3, 2626712).
- UI 03:08Z: teams page done on teams-basis-hold (head pending). Answer to Nick's 'teams page is fucked, no model stuff': there IS model content and it is good, the Measured identity block (percentiles vs the other 31 teams within season, thousands of team-weeks) was already above the prose; the defect was the prose card sitting at identical weight. Deliberately NOT done: no coordinator names invented for the 13 TBD slots (repo cannot answer it; 'TBD (camp)' now renders as an absence); no date shown (nfl_teams has no updated_at, core-and-fantasy.js:62-84, edit route writes none; note says undated; a test fails if a timestamp column is ever added so the real date gets shown). Morning message: the tendencies service falls back to the most recent season with play-by-play and labels it amber '(prior season)'; whether the live page serves 2026 or labelled 2025 is a live read, asked of UI 03:09Z; not to be claimed either way until read. UI running total: six commits, four pre-freeze PRs (#73 #75 #76 #78), two hold branches. Next: League Hub championship drill-down from the existing odds payload (UI depth, no new model), then the snap-share reader fix.
- Google sign-in 03:09Z (hold f288898): callback comment added; the route has no test that goes red if it vanishes, so the caller-graph sweep is the only guard. Category note for the map (routed 03:11Z): /api/auth/google/start is reached by href not fetch, and every provider callback or webhook reads the same way; the rule needs an 'external or non-fetch caller' category, not one annotation per route. Opportunity 03:09Z: lane done pending Nick's step-7 gate dry-run (runs only from his terminal, a morning event); hold ffe8363 unchanged; morning: push the two hold commits onto #72 before its diff is read, #15 and #72 marked ready by Nick; offered read-only second-reader capacity, given the ledger ancestor check 03:11Z.
Continues at [[gridiron-morning-message-inputs-24-2026-09-20]].
