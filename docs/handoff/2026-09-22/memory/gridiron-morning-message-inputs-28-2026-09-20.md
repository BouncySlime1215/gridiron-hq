---
name: gridiron-morning-message-inputs-28-2026-09-20
description: Page 28 of the chained inputs for Nick's 06:00Z 2026-09-20 morning message: UI live read 401 and Brier wording, scheduler's Finding 7 rec (registry metered-tier job), Trade Brain's three clamps, release c117daa.
metadata:
  type: project
---

- Continues [[gridiron-morning-message-inputs-27-2026-09-20]].
- UI 03:2xZ: live read of GET /api/teams/KC/tendencies returns 401, no session held by any thread; 2026-vs-2025 unknown until someone with a session reads fields season and is_prior_season; not claimed either way. UI's layer-4 sentence corrected: playoff odds were graded on 184,959 team-weeks, lose to base rate before week 4, overconfident at both ends, and the app's own from-week-1 configuration was not graded; League Hub today sends no from_week so it is the weeks_played 0 state. Plain-words Brier for the card: 'a score for how close the percentages came to what actually happened, lower is better, 0.25 is what you get by saying 50% to everything.' Holds: teams-basis deb492a; NEW title-drill d1cc317 (championship drill-down; 3,128 tests 3,087 pass 0 fail 41 skipped; typecheck/lint 899 files, build + start:smoke clean); UI AGREES step 4 + teams page + drill-down open as ONE new PR after #43 #46 #53 #57 #58 #60 #78 merge.
- Scheduler 06:00Z: hold 3d4ca74 (was bec666d; 3,003 tests 2,962 pass 0 fail 41 skipped; #61 error/reason fields pinned in test/abandoned-run-backoff.test.js, four injections each caught; base #77 3902ba7 confirmed). Finding 7 rec = registry metered-tier off-thread job (neither cron nor Fly worker: ESPN keeps ~3 days, refresh-live-data.mjs:96; fly.toml one mount, no processes); needs a migration for league_transactions_raw (script creates it at :21); migration 062 already exempt at migration-name-check.mjs:75; server/index.js touched only by #56 (line 5 import, lines 75-83), mounts shift to 98-143 after the stack.
- Trade Brain 06:04Z: agrees the audit's 'a read, not a price' sentence, both halves, verified on the tree. Precision: three clamps at three layers, only the first is ±10%: perceptionFactorFor ±10% on the DEAL SCORE (trade-engine.js:1362-1366, 1 + max(-0.10, min(0.10, shift/100))); PLAYER_VALUATION_CAP 0.20 per player (counterparty-pricing.js:98); PERCEPTION_CAP 0.15 on the package multiplier (:28); four individual sources have cap 0.10. Wording must say '±10% of a deal's score'. Correction of its own earlier routing: manager-archetypes.js does not read league_transactions_raw (comment at :20 only); 21b449a covered every served consumer. OVERLAP flagged: chat sync's archetype as-of and Trade Brain's archetypesBuilt are two accessors reading the same store's stamp; decision 06:10Z: one accessor in manager-archetypes.js (chat sync's file) exposing league-season, career and jev stamps plus the narrower draft/outcome-scoped read the trade path needs; Trade Brain switches to it after both land. archetypeIndex (manager-signals.js:271) has no asOf.
- Release 06:06Z (hold c117daa, was e6b6785; clean ff onto #35 a60858d): item 8 replaced with scheduler's registry-job decision; verified the reasons on 791b131 (refresh-live-data.mjs:95-96 three-day window; fly.toml one mount no processes; collect-league-transactions.mjs:21 ad-hoc CREATE TABLE, reads espn_s2/swid from leagues). Wording rules: job-worker.js is a worker thread with its own DatabaseSync handle, not a process; the metered tier runs on the 5-minute interval so the job cannot run until the restart cycle is fixed (after step 1); the script's SCHEDULER_DISABLED=1 at :17 must not travel into the registry job.
Continues at [[gridiron-morning-message-inputs-29-2026-09-20]].
