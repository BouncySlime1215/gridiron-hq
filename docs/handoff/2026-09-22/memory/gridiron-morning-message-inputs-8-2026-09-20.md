---
name: gridiron-morning-message-inputs-8-2026-09-20
description: Inputs 8, 2026-09-20: #57 = F1 trade-engine half; :571 half no PR.
metadata:
  type: project
---

- Continues [[gridiron-morning-message-inputs-7-2026-09-20]].
- Release 02:24Z: §10a NAMES #57 for the trade-engine half (grep count 2 at 7c27517 on origin: coordinateFantasy(fantasyFit, expertValues, coordinatorBase(weekProjection)), coordinatorBase = weekProjection?.structural_ppg ?? null, null not a silent ensemble fallback; test coordinator-base-is-structural in the diff). Wrong-base claim now VERIFIED BY RELEASE on 791b131, no attribution needed: fantasy-coordinator.js:324 trains a residual on structural_ppg; :519 grades with structuralPpg 0 ("correction alone is what's being graded"); :32-34 ensemble_shift is one of the three experts; so weeklyPpg served ensemble + a structural-trained correction and applied the ensemble calibration twice. Exposure numbers stay feature-audit's. SKIP STANDS: fantasy-coordinator.js:571 has no PR; §10a = both halves merged and verified or do not run fly secrets set AUTO_HEAVY_SYNC=1. #57 now carries FOUR changes (fantasy week, coordinator base, season horizon, availability-source label), title and body updated; anywhere that calls it "the trade-week PR" is stale. Hold branch 8b11086. Lesson: on the old head the call had moved from :354 to :379, a line-number check would have passed; the check is content (the grep), never a line number.
- Release 02:26Z (hold 447349b): trade_asset_universe_warm RULED OUT as a cause of the cycle spread, two routes. (1) It has never run since the deploy: tier growth, maxAgeMinutes 20 (scheduler.js:1347); not in bootJobs (:1740-1744); no dedicated timer (only nfl_model_growth 90 s :1751 and nfl_reports 150 s :1754); only path is the background tier at :1801, a plain setInterval with no leading call (:1775), first pass at t+300 s, and every process dies at 155-170 s. Same reason the nineteen never-run jobs (CORRECTED 02:44Z: 22, see scheduler 0a198d7) never run. (2) Eight cycles spread 12 s, no outlier, over 25 min against a 20-min cadence. The 167-179 vs 171-196 gap stays unexplained and is not load-bearing. WHERE IT APPLIES, now in step 6: after the brake, t+300 s is the risky moment (nineteen never-run jobs [corrected: 22] due at once) and THIS job dominates it: 5-6 s per league on the request thread, cannot go off-thread (module-level Map), most of the first pass with five leagues; that is what the 900 s uptime bar absorbs; Nick should expect a stall near t+300 s and not read it as the loop returning. Passed back to scheduler for #59/#63.
- Wiring map 02:27Z: usage_coverage on hold branch claude/wiring-map-8f96ur-usage-coverage-hold 2626712 (off main + #66 by merge, no PR; nflverse.js untouched). Window is REQUIRED and DERIVED (SEASON-(LOOKBACK-1)+i), not a typed list: usageCoverage() with no argument can never fail (missing empty, stamp_disagrees false by construction); a typed [2021..2025] fails all four tests; routes/nfl-betting.js still carries '2021,…,2025' at six sites, which is how the feed stamped itself green for a season it never fetched. 2967/0/41. New satellite-DB rule (local #36 1a0e8f9) found FIVE: manager-signals.js league_chat.sqlite, nfl-weekly-feature-store-v2.js three satellites (all four env-overridable; fly.toml [env] sets only HOST, secrets unreadable, so report-only, check the deployment before the code); nfl-learned-shadow-explain.js opens under docs/, not in the image, no override, inert in production unconditionally (betting-side, out of scope, recorded). Served-field batch: #56, #59, #61 = ZERO added served fields (real results); #61 first showed 2 false rows (error/reason inside JSON.stringify for sync_log.last_detail at scheduler.js:165/:168, guard fixed local cdec951, never sent to scheduler); batch died on #63/#52/#49 with MODULE_NOT_FOUND because the script lived on #36 and a checkout moved from under it; now runs from a fixed copy; re-running #63, #52, #49, UI stack, plus #43 pre-fix control.
[[gridiron-morning-message-inputs-9-2026-09-20]]
