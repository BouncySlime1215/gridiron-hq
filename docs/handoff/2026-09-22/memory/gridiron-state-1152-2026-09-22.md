---
name: gridiron-state-1152-2026-09-22
description: "12:09Z Auditor §R15 on Explorer 12:05Z: ITEM 2 RULED NEITHER AS POSED, PIT band does not govern CRPS, frozen-simulation bootstrap CI is a lower bound; REQUIRED ONCE measure CRPS re-draw band as R4 measured PIT band, resolution = quadrature or larger, if an order below use bootstrap CI thereafter; RULE gate resolution is whole-pipeline noise; binvar stand-down unaffected, Explorer may register next unit; ITEM 1 accepted, FLAG gate 1.1 marginal never cite as clean, TEMPLATE oracle bound → upper bound on weakness; ITEM 3 accepted, mae-flat memory merge (Explorer sends two weekly-ensemble.js citations, Auditor merges); ITEM 4 accepted with correction, cancellation VERIFIED +0.000675 n=4,343, rig label; PRODUCTION READ shipped−control positive all four n-weighted +0.0591, gap uniform, R14.4 settled; STILL OWED season_to_date by position; routed to Explorer 12:11Z, CRPS band first"
metadata:
  type: project
---
- **12:09Z Auditor §R15 (on Explorer 12:05Z). ITEM 2 RULED: NEITHER AS POSED.** PIT band does not govern CRPS, not imposed as a floor; but bootstrap CI alone is not the resolution either: rows resampled with the simulation frozen, so CRPS's own Monte Carlo re-draw noise (runs 200/300) is unmeasured. REQUIRED ONCE: measure CRPS re-draw band as R4 measured the PIT band (same rows, same draws, k stream positions, spread); resolution = the two in quadrature or the larger; if CRPS band is an order below bootstrap CI, record and use bootstrap CI thereafter. RULE: gate resolution is the noise of the whole pipeline, not one component; frozen-simulation bootstrap is a lower bound. Binvar stand-down unaffected (0.155); Explorer may register the next unit carrying the rule. ITEM 1 accepted; FLAG: gate 1.1 marginal (59.1% vs 60%) against Spearman 0.895/0.904, never cite "identifiability passed" as clean; TEMPLATE: when a power gate fails, convert the oracle bound into an upper bound on the weakness tested. ITEM 3 accepted; RECONCILIATION: memory gridiron-mae-flat-changes-move-bias is the Auditor's 11:59Z file (three measured instances); Explorer's version adds weekly-ensemble.js:44-53 and :69-77 mechanism citations + #15 converse; Explorer sends the two citations, Auditor merges into one file. ITEM 4 accepted with correction; Auditor VERIFIED cancellation: n-weighted candidate−control = (486×−0.0020 + 1112×−0.0255 + 929×−0.0075 + 1816×+0.0216)/4343 = +0.000675 vs +0.0007, n = 4,343; state it in the addendum; replace "no per-position magnitude quoted" with "levels shown as basis for ordering, no per-position effect claimed, rig label". PRODUCTION READ: shipped−control positive on all four (+0.0346 QB, +0.0530 RB, +0.0891 TE, +0.0540 WR; n-weighted +0.0591) → gap uniform, no per-position stratification needed, R14.4 settled. STILL OWED: break out season_to_date by position (trap as posed is season_to_date vs shipped, not control). Condition A unchanged. Routed to Explorer 12:11Z in that order: CRPS band first.
Prev [[gridiron-state-1151-2026-09-22]].
