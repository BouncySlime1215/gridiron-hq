---
name: gridiron-state-1156-2026-09-22
description: "12:15Z Auditor §R16 script-classification rule: adopted (c) documented per (b) with default REVERSED, a script's reach is what it writes (DB tables, filesystem paths, stdout does not count), betting-only only when every durable output traced and none read by a fantasy surface, UNTRACED OUTPUT DEFAULTS TO WIRED; header quoted as intent never criterion; sub-cases no durable output → third bucket no-surface-reach, output read by another script → follow transitively, output read by fantasy surface → wired; five-part evidence bar per script; FIGURE IS A BRACKET 220-233 wired / 55-68 betting-only / 31 unreached of 319 until all 13 traced, bracket must reach Phase A plan; PATTERN third occurrence, a default deciding 13 of 14 cases is the finding; routed 12:19Z Opportunity, Wiring map, Planner"
metadata:
  type: project
---
- **12:15Z Auditor §R16: script-classification rule.** Adopted (c) documented per (b) with the default REVERSED: a script's reach is what it writes (DB tables, filesystem paths; STDOUT DOES NOT COUNT); betting-only only when EVERY durable output is traced and NONE is read by a fantasy surface; UNTRACED OUTPUT DEFAULTS TO WIRED (a false betting-only silently removes a module from the Phase A plan's view). Header quoted as INTENT beside each listed script, never the criterion (same error as reading direction off a write-up table). Three sub-cases: (1) no durable output → THIRD BUCKET "no-surface-reach", reported separately, never folded into betting-only; (2) output read only by another script → follow transitively; (3) output read by a fantasy surface → wired whatever the header says. Evidence bar per script: (a) package.json name → entry file:line; (b) every durable write with file:line; (c) each reader at file:line and whether on a fantasy surface; (d) explicit "no durable writes" statement → bucket 3; (e) header quoted as intent. FIGURE IS A BRACKET until all 13 traced: 220-233 wired / 55-68 betting-only / 31 unreached of 319, and the bracket MUST REACH THE PHASE A PLAN. PATTERN, third occurrence (after Condition B empty tables, predA compressed nulls): a default that decides 13 of 14 cases is not a default, it is the finding. Routed 12:19Z: Opportunity (apply), Wiring map (apply to 55/17), Planner (bracket in plan).
Prev [[gridiron-state-1155-2026-09-22]].
