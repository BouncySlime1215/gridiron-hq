---
name: gridiron-state-1161-2026-09-22
description: "12:22Z Auditor §R18: scripts 1 and 5 grades STAND on Opportunity's enumeration; nfl-candidate-findings.js reaches no fantasy route on the request path (three betting routes + /api/mlb) so R17 holds without the job-reach ruling; (1) UNNAMED CATEGORY /api/mlb not on the five-file betting list, folding it in records a false fact and hides the most droppable category, FIX routes/mlb.js added to explicit surface list with own label mlb (six surfaces), applies to script 5's trace; (2) METHOD ERROR every-path claim generalised from three shortest paths, nfl-candidate-findings.js comes OUT of job-reach-only, 28 wrong by at least 1, RE-DERIVE ALL 28 EXHAUSTIVELY, bracket stays 205-233 upper end may move; credit Opportunity self-reported defect (second today); routed 12:33Z Opportunity apply 1-3, Wiring map mlb surface + every-path method; 12:23Z Scheduler OWNERSHIP QUESTION weekly-weight-store.js / weekly-ensemble.js, neither in Scheduler five, branch ee383aa entirely weight-store, :5-14 block wrong two ways; coordinator 12:36Z SETTLED weight-store granted to Scheduler for -epoch-fallback-loud only, chain stays Fantasy plan, :5-14 rewrite to Fantasy plan, may object"
metadata:
  type: project
---
- **12:22Z Auditor §R18.** Scripts 1 and 5 grades STAND on Opportunity's enumeration (the two other modules named earlier are writers, not readers). nfl-candidate-findings.js reaches no fantasy route on the request path (three betting routes + /api/mlb), so R17's answer holds without the job-reach ruling. (1) UNNAMED CATEGORY: /api/mlb is not on the five-file betting list, so a module whose only request reach is routes/mlb.js is NOT betting-only by definition and not fantasy; folding it in records a false fact the Phase A plan reads and hides the most droppable category (MLB not in the approved product). FIX: routes/mlb.js added to the explicit surface list with its own label `mlb` (six surfaces); betting-only not renamed; applies to script 5's trace (nfl-t60-packet → three betting routes + /api/mlb). (2) METHOD ERROR, not file error: "every path runs through scheduler.js" was generalised from the three shortest paths; nfl-candidate-findings.js comes OUT of job-reach-only (request reach from a non-fantasy surface), so 28 is wrong by ≥1; RE-DERIVE ALL 28 EXHAUSTIVELY; bracket stays 205-233 until then, upper end may move. Credit: Opportunity volunteered the defect (second self-reported defect today). Routed 12:33Z: Opportunity (apply 1-3), Wiring map (mlb surface + every-path method).
- **12:23Z Scheduler: OWNERSHIP QUESTION on weekly-weight-store.js and weekly-ensemble.js.** Allocation map gives Scheduler five files (scheduler, source-registry, loop-watchdog, fly.toml, server/index.js); weekly-ensemble.js is the model-fitting chain's centre (exports WEEKLY_ENSEMBLE_WEIGHTS, weeklyEnsemblePrediction, weeklyEnsembleMode, weeklyEarlyBucket; fit by scripts/promote-weekly-ensemble.mjs and promote-early-week-weights.mjs) → Fantasy plan's; Scheduler stopped on the docs unit. weekly-weight-store.js is also not in Scheduler's five, though Scheduler has three prior commits on it (ea34923/8af544a/213b09d) and the branch in verification (ee383aa / tree 301dbdee, run 1, bundled at /mnt/project-files/epoch-fallback-ee383aa.bundle) is entirely that file + test + evidence. Scheduler confirmed the :5-14 block is wrong two independent ways (:5-8 global vector vs live read; :14 promotion 2026-09-18 vs epoch 1 created 2026-09-19T04:40:30Z) and that :12 records a prior correction in the same direction. **Coordinator 12:36Z SETTLED:** weekly-weight-store.js granted to Scheduler for -epoch-fallback-loud only (07:25Z unit required it); chain stays Fantasy plan's; weekly-ensemble.js:5-14 rewrite allocated to Fantasy plan (docs commit on their branch, to ride the promotion PR), with both falsifications and the demotion caveat; Fantasy plan may object in next report.
Prev [[gridiron-state-1160-2026-09-22]].
