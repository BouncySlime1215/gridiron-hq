---
name: gridiron-state-1170-2026-09-22
description: "12:33Z Opportunity family-level question CLOSED with a test: grader labels route FILES not families so Wiring map's un-label hazard cannot arise; mirror hazard made loud, a new router under /api/betting not on the list defaults to fantasy, mountPaths()/routesUnderPrefixOf() test fails until labelled, proved by commenting wong.js out; commit 09f7f226, 27 rules green; every route file has one mount path, /api/betting has exactly two mounts both listed; RESIDUAL HAZARD CONTRACT.md §2c a single route file serving both sides would be labelled wholly betting, none today, label must move to endpoint if added; Wiring map 53/19 consistent with 172-228 given population difference; CORRECTION 11 unpushed commits not 12, sidecar corrected, head 09f7f226, bundle to refresh with verify pair on final head"
metadata:
  type: project
---
- **12:33Z Opportunity: family-level question CLOSED with a test.** Their grader labels route FILES, not families, so Wiring map's un-label hazard cannot arise; a hub with one unlisted sub-route is never labelled betting because hub and sub-route are labelled independently. Mirror hazard made loud: a new router under /api/betting not on the list defaults to fantasy (safe, silent) → mountPaths() and routesUnderPrefixOf() walk server/index.js mounts and test "every route mounted under a betting surface prefix is itself on the betting list" fails until labelled; proved by commenting wong.js out (2 fail / 25 pass). Commit 09f7f226, 27 rules green. Measured: every route file has exactly one mount path; /api/betting has exactly two mounts (betting-hub.js, wong.js), both listed; /api/auth and /api/drafts carry two routers at one path (reverse, harmless). RESIDUAL HAZARD (unsafe direction), CONTRACT.md §2c: a single route file serving both betting and fantasy endpoints would be labelled wholly betting; none exists today; if added, the label must move from file to endpoint; Wiring map's family rule fails safe (mixed family stays fantasy). Wiring map's 53/19 (51-53 / 19-21, mlb 0) consistent with 172-228 given population difference. CORRECTION: 11 unpushed commits, not 12 (16 over origin/main, last 11 never pushed); sidecar corrected in place; head 09f7f226; bundle to be refreshed with the verify pair on the final head (fdfeb77f run 2 still going, b9bd1bcb chained; two more commits since).
Prev [[gridiron-state-1169-2026-09-22]].
