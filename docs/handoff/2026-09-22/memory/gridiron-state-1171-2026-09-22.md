---
name: gridiron-state-1171-2026-09-22
description: "12:36Z Wiring map two script-default rows TRACED EMPIRICALLY on a migrated DB copy: audit:nfl → pick-reasoning writes at IMPORT time 32 model_feature_contracts + 11 model_registry rows via model-governance.js top-level seeds, readers only betting/mlb surfaces (nfl-research.js:268, mlb-research.js:69, page-explain-tools.js:17), no fantasy reader → GRADE wired-betting-only; diagnose:nfl-passing run did NO WORK (n 0), not a traced output, stays WIRED under untraced-defaults-to-wired; GRADES 52 wired / 20 betting-only, residual ±1; grader not edited yet; OWN MAP DEFECT job-body await import at scheduler.js:1002 counted as ordinary edge, inflates script reach, fix after the pair as separate edge class; coordinator 13:11Z HARD RULE never run run-news-event-impact.mjs or any Anthropic-client script empirically, model-governance seed tables passed to Opportunity; verify2x on 1998f50 run 1 in flight; 14 commits held"
metadata:
  type: project
---
- **12:36Z Wiring map: two script-default rows TRACED EMPIRICALLY** (DB copy migrated, script run with GRIDIRON_DB_PATH on the copy, sqlite_master + row counts diffed; primary untouched at migration 000). `audit:nfl` → nfl-blind-audit.mjs → pick-reasoning: writes at IMPORT time 32 rows model_feature_contracts (model-governance.js:68) + 11 rows model_registry (:90), seedContracts()/seedRegistry() at module top level (:88-89), reached statically via nfl-blind-audit.js → nfl-ensemble.js → nfl-player-value.js → nfl-pregame.js → model-governance.js; INSERT … ON CONFLICT DO NOTHING seeds the server also writes at boot. Readers: model-governance.js:100-101/:107-108 (only read sites); nfl-research.js:268 (/api/nfl-market); mlb-research.js:69 (/api/mlb); nfl-pick-watch.js:54 registry('NFL') → only static importer page-explain-tools.js:17 (/api/betting @2), others dynamic (scheduler.js:1002 job body; betting-hub.js:598/:606/:614 handlers); nfl-betting.js:665 is a different featureContracts (nfl-ensemble.js:2796). No fantasy reader → GRADE wired-betting-only. `diagnose:nfl-passing` → diagnose-passing-components.mjs → nfl-passing-diagnostic: run left schema and all row counts identical (default 5 seasons and single season) but the run did NO WORK (no prop-replay rows: seasons [], n 0, 0.117s) → not a traced output; statically only write-bearing module on its path is gamescript.js (INSERT/UPDATE game_lines at :55/:134/:155/:166 in historical-line sync, not in gameScriptFor :389 which propReplayRows uses); stays WIRED under untraced-defaults-to-wired. GRADES 52 wired / 20 betting-only, residual ±1 → 51-52 / 20-21; grader not edited yet (working-tree edits break the verify2x write-tree guard). OWN MAP DEFECT: map counts a job-body `await import('./nfl-pick-watch.js')` at scheduler.js:1002 as an ordinary edge (literal specifier), inflating script reach by every dynamic import that fires only when a job runs (R17 two-kinds-of-reach inside the generator); to fix after the pair as a separate edge class (same as Opportunity's). Coordinator 13:11Z: HARD RULE never run run-news-event-impact.mjs or any Anthropic-client script empirically (R&D nothing paid); model-governance seed tables passed to Opportunity for script 1's (b). verify2x on 1998f50/cf4484d9 run 1 ~7 min in; 14 commits held.
Prev [[gridiron-state-1170-2026-09-22]].
