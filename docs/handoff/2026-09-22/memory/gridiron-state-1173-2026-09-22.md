---
name: gridiron-state-1173-2026-09-22
description: "12:36Z Auditor §R20 (part 2 of 2, continues gridiron-state-1172): (a) §R8 ACCEPTED, ADDITION draw error at seed 20260826 is COMMON-MODE across gates, N gates at one seed are not N confirmations, ordering vs common incumbent needs no band, memory gridiron-monte-carlo-band-is-common-mode; (b) READ DESIGN stratified by position, pooled statistic across a sign reversal is mix-dependent, QB reverses NOT established until paired-bootstrap CIs; (c) NEXT UNIT = read design only, conformal gate not re-registered, a verdict surviving a new instrument is amended never re-opened; (d) 60/50/40/20 WITHDRAWAL HALF-WRONG, it is the exact HISTORY-HEAD exposure, complement of structural 40/50/60/80, label error only, Nick's post narrowed 13:16Z; 12:37Z Wiring map BILLING CHECK neither empirical script can bill (SDK lazy in claude.js clientFor), run-news-event-impact on do-not-run list, verify2x run 1 on 1998f50 exit 0 3204/3163/0/41, run 2 started 12:36Z"
metadata:
  type: project
---
- **12:36Z Auditor §R19 + §R20, continued verbatim from [[gridiron-state-1172-2026-09-22]]:** §R20(a) §R8 ACCEPTED (figures reproduce; hash split correct; quadrature right); ADDITION: draw error at seed 20260826 is COMMON-MODE across gates → N gates at one seed are not N confirmations; candidate ORDERING vs a common incumbent carries zero draw noise (needs no band); memory gridiron-monte-carlo-band-is-common-mode. (b) READ DESIGN = stratified by position, shipped vs season_to_date AND control; reason: pooled statistic across a sign reversal is MIX-DEPENDENT (rig mix ≠ roster mix), decision made now; "QB reverses" NOT established (no per-position interval) → paired-bootstrap CIs before stating; write "pooling is unsafe here". (c) NEXT UNIT = the read design only, ONE paste-able read for Nick; conformal gate NOT re-registered (RULE: a verdict that survives a new instrument is amended, never re-opened). (d) 60/50/40/20 WITHDRAWAL HALF-WRONG: it is the EXACT HISTORY-HEAD exposure (QB 0.60/RB 0.50/WR 0.40/TE 0.20), complement of structural 40/50/60/80; error was the label "structural"; audit-unitA:763 corrected; early-week:113 and gate3-live-read:30 say "history exposure" and STAND; MEMORY.md, unit-1-verdict, live-read narrowed by the Auditor; POOLING-PREREG:190-191 withdrawal to be narrowed (→ Explorer); RULE: a correction is a claim. Nick's 12:16Z post narrowed 13:16Z.
- **12:37Z Wiring map: BILLING CHECK on the empirical runs — neither script can bill.** Both closures contain server/services/claude.js, but the SDK is `await import('@anthropic-ai/sdk')` inside clientFor() at claude.js:142-143, the only messages.create is :246 on the same lazy path, and claude.js has no top-level call statements; `audit:nfl` runs the `protocol` command (constant spec), `diagnose:nfl-passing` is a numeric replay; empirically only the 43 model-governance seed rows appeared and claude.js's app_settings writes (:57/:65/:73) produced nothing. Scripts run empirically (DB copy, none bills): scripts/migrate.mjs, nfl-blind-audit.mjs, diagnose-passing-components.mjs; client-construction check before any further script; run-news-event-impact.mjs on the do-not-run list. verify2x run 1 on 1998f50/cf4484d9: exit 0, 3204/3163/0/41, worktree clean (+14 over the 5ab50c8 pair = 7 script-reach + 7 explicit-list cases); run 2 started 12:36Z; no commits or edits while it runs. Will remove function-body imports as their own edge class after the pair.
Prev [[gridiron-state-1172-2026-09-22]].
