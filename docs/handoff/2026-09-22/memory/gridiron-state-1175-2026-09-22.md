---
name: gridiron-state-1175-2026-09-22
description: "12:40Z Planner (R&D-cleanup) PAID-RUN GUARD PAIR IN local: RED aa964bf GREEN 85598b6 (tree 72c1ef78, 6/6), verify 2x exit 0 3,043/3,002/0/41, typecheck and lint clean, build/smoke not run (accepted 13:24Z); run-news-event-impact.mjs exits 1 unless GRIDIRON_ALLOW_PAID_RUN set (presence only, value never in the verdict); LESSON first version claimed refusal before DB open but static imports open the DB at module scope, mutation survived, fix = await import after the guard, ENOTDIR test path, 9 mutations killed; OTHER BILLED PATHS build-negotiation-profiles.mjs default bills (→ Coach 13:24Z, reuse paid-run-optin.mjs), build-manager-archetypes key-presence gate, run-nfl-ai-replay user action, nine routes behind getApiKey(); for Nick env opt-in is the weakest gate that works, alternatives interactive confirm / --yes-i-will-pay / spend cap; bundle requested 13:24Z; 13:26Z coordinator posted Nick three decisions (projection target, paid-run gate kind, brake + veto + stale_findings trigger), sizing note 172-228 of 319; MEMORY.md single-writer rule 13:27Z after the Auditor clobbered line 39 twice"
metadata:
  type: project
---
- **12:40Z Planner (R&D-cleanup): PAID-RUN GUARD PAIR IN, local.** RED aa964bf (tree b06e7c43; 5 tests, 0 pass, scripts/paid-run-optin.mjs absent); GREEN 85598b6 (tree 72c1ef78; 6/6); branch 2 ahead of origin. Verify 2x identical, exit 0: 3,043/3,002/0/41 (394.5s/395.3s); typecheck clean; lint clean 894 files; build and start:smoke not run (no client or boot-path change; coordinator accepted 13:24Z). Behaviour: scripts/run-news-event-impact.mjs exits 1 with a one-line reason unless GRIDIRON_ALLOW_PAID_RUN is set non-empty (presence only; reason names the variable never the value; a test asserts the verdict never carries the value). LESSON: first version placed the guard after two static imports and claimed it refused "before the database is opened" — FALSE: server/db/index.js:19 runs mkdirSync + opens the DB at module scope, and static imports run before any statement; the mutation moving the guard below runMigrations SURVIVED; fix = load runMigrations/rows via `await import` after the guard; the test's missing-directory DB path discriminated nothing (mkdirSync creates it) → now a path whose parent is a file (ENOTDIR); 9 mutations killed after, no-op control survived. OTHER BILLED PATHS (docs/tdd/paid-run-opt-in.tdd.md, unchanged): scripts/build-negotiation-profiles.mjs:244/:402 (callClaude; --dry-run :45 is an opt-out, DEFAULT BILLS → allocated to Coach 13:24Z, reuse paid-run-optin.mjs); scripts/build-manager-archetypes.mjs:147 (exits without AI_GATEWAY_API_KEY, caps JEV_MAX_USD; gateway not Anthropic; key-presence gate not spend opt-in); server/scripts/run-nfl-ai-replay.js:2 (server-spawned worker, user action, no opt-in); nine route files behind getApiKey(), request-driven. For Nick: env opt-in is the weakest gate that works; alternatives interactive confirmation, required --yes-i-will-pay flag, hard spend cap (JEV_MAX_USD style); default is no longer "runs and bills". Bundle requested 13:24Z.
- **13:26Z coordinator posted to Nick (message cmsg_01YAsw8AnFv4ioRMQw8dfPmTGVeZnY1LLaMqaBiX2jo6MZ): three decisions** — (1) projection target MAE vs mean-unbiased (recommend mean-unbiased for summed surfaces, MAE for ranking, graded first); (2) paid-run gate kind (recommend keep env opt-in; flag / spend cap alternatives; console check for past runs); (3) brake + inert veto + stale_findings trigger, decided with item 3 (deploy). Sizing note: Phase A inventory 172-228 of 319, not 233. Standing items unchanged (push authority, rotations, #95, usage). **MEMORY.md single-writer rule 13:27Z:** the Auditor clobbered line 39 twice (12:17:29Z, 12:37:32Z) from stale copies; Auditor told to send replacement text for its line to the coordinator instead of writing MEMORY.md directly.
Prev [[gridiron-state-1174-2026-09-22]].
