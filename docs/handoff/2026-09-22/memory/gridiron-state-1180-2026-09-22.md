---
name: gridiron-state-1180-2026-09-22
description: "12:45Z Auditor §R21 READ-PREREG: read goes to Nick as item 17 after ONE BLOCKING CHANGE, cp of the live sqlite is WRONG (leaves WAL/-shm, silently LOW ROW COUNT), open production DB read-only in place via node:sqlite DatabaseSync readOnly or VACUUM INTO / .backup; everything reconciles; R21.3 power check is a specified-alternative check not the R14.2 oracle check, register threshold effect/half-width ≥ 1.78, re-classify on production SE with pre-registered rig effect sizes never the observed gap; R21.4 TE exclusion not carried (1-of-12), candidate−control opposing sign does not survive multiplicity, withdrawal upheld with corrected reason, control−shipped survives as sign-and-scale; R21.5 per-position first, pooled beneath; secrets rule enforced in the script; record §R21 on the mount audit file"
metadata:
  type: project
---
- **12:45Z Auditor §R21 (READ-PREREG):** read GOES TO NICK as morning-list item 17 after ONE BLOCKING CHANGE (R21.2): `cp /data/data.sqlite /tmp/read.sqlite` is WRONG — copies a possibly-being-written DB and only the main file, leaving WAL/-shm behind, so the failure mode is a silently LOW ROW COUNT, the very thing the read rules on. Required: no copy; open production DB read-only in place (node:sqlite DatabaseSync(path, { readOnly: true })); if a copy is wanted it must be VACUUM INTO / .backup, never cp. Everything reconciles (n-weighted position points rebuild pooled points; power ratios reproduce). R21.3: their power check is a SPECIFIED-ALTERNATIVE check, not the R14.2 ORACLE check — say which was run; register threshold effect/half-width ≥ 1.78 (80% power at 90%); half-widths are rig magnitudes, label as sizing input; after the read RE-CLASSIFY on production SE with the pre-registered rig effect sizes fixed (0.0591/0.0540/0.0891/0.0530/0.0346), never the observed gap (observed-power fallacy). R21.4: TE std−shipped [−0.1680,−0.0009] NOT carried as an exclusion (1-of-12 by chance); candidate−control opposing sign does not survive multiplicity; addendum withdrawal UPHELD, reason corrected: it asserted an absence never tested. Control−shipped SURVIVES as a sign-and-scale pattern across four positions, qualified. R21.5 default: per-position first, n-weighted pooled beneath, on every arm. Secrets rule enforced IN the script. Record §R21 on the mount audit file.
Prev [[gridiron-state-1179-2026-09-22]].
