---
name: gridiron-state-1184-2026-09-22
description: "12:54Z Explorer R21 DONE, one ruling pending: item 17 command node read.mjs /data/data.sqlite, read.mjs + readdesign.mjs on the mount beside READ-PREREG; R21.2 cp dropped but open-read-only-in-place is NOT reachable, server/db/index.js writes AT IMPORT TIME (:20 read-write open, :22 WAL, :45 schema_migrations, :184 legacy migration, :194 db_health_checks), any script importing weekly-backtest.js opens production read-write, the unsafe cp was accidentally protecting production; design now VACUUM INTO from a read-only connection, import-time writes land on the snapshot, free-space check, prints the rm, secrets rule in-script; end-to-end on rig reproduced to four decimals; R21.3/R21.4/R21.5 applied; memory gridiron-no-read-only-path-to-the-production-db; Auditor asked 12:56Z whether VACUUM INTO satisfies R21.2 and whether the import-time write list is a finding; 12:56Z paths fixed, item 17 READY pending R21.2, rig control re-run four decimals arm for arm, files read.mjs / readdesign.mjs / READ-PREREG on the mount"
metadata:
  type: project
---
- **12:54Z Explorer R21 DONE, one ruling pending.** Item 17 command: `node read.mjs /data/data.sqlite`; read.mjs + readdesign.mjs on the mount beside READ-PREREG-2026-09-22.md (import paths being made relative to the script 12:56Z; were absolute /home/user/gridiron-hq). R21.2: cp dropped, but 'open production read-only in place' is NOT reachable — server/db/index.js writes AT IMPORT TIME: :20 new DatabaseSync(DB_PATH) read-write no option; :22 PRAGMA journal_mode=WAL; :45 CREATE TABLE schema_migrations; :184 legacy-schema migration if unapplied (with backup); :194 CREATE TABLE db_health_checks + integrity check recorded. Any script importing weekly-backtest.js opens production read-write; the unsafe cp was accidentally protecting production from those writes. Design now: VACUUM INTO from a READ-ONLY connection to the live file (read transaction, WAL-consistent, source unmodified, mtime unchanged on rig; read-only open may attach/create -shm and zero-length -wal, which the app already holds open); import-time writes land on the snapshot; free-space check first, stops naming an alternative destination (DB > 2 GB); prints the exact rm for the snapshot; secrets rule enforced in-script. End-to-end via snapshot on rig: per-position table reproduced to four decimals arm for arm, 0 rows for all four tables. R21.3 applied (specified-alternative; 1.78 registered, replacing round 1.5; half-widths = rig sizing input; re-classify on production SE, rig effects fixed). R21.4 applied (TE exclusion dropped; opposing-sign pair not surviving multiplicity; withdrawal reason = asserted an absence never tested). R21.5 default registered. Explorer memory file gridiron-no-read-only-path-to-the-production-db written. Auditor asked 12:56Z: does VACUUM INTO from read-only satisfy R21.2 (item 17 waits), and is the import-time write list a finding of its own.
- **12:56Z Explorer: paths fixed, item 17 READY pending R21.2 ruling.** read.mjs imports only node:sqlite/node:fs and loads readdesign.mjs by relative specifier; readdesign.mjs locates the checkout at run time: $GRIDIRON_REPO, then walking up from import.meta.url, then from cwd, marker server/services/weekly-backtest.js (fails fast with 'cannot find the gridiron-hq checkout' naming all three options). All three paths + the failure tested. Rig control re-run end to end via snapshot on the new tree, four decimals arm for arm: QB 6.1918/6.2296/6.1572, RB 4.5560/4.5315/4.5030, TE 3.7153/3.6336/3.6262, WR 4.4743/4.4553/4.4203, POOLED 4.5251/4.4976/4.4660; four tables 0; intervals unchanged. Files: /mnt/project-files/read.mjs, /mnt/project-files/readdesign.mjs, /mnt/project-files/READ-PREREG-2026-09-22.md.
Prev [[gridiron-state-1183-2026-09-22]].
