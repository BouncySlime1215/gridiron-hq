---
name: gridiron-state-1186-2026-09-22
description: "12:57Z Auditor §R23: (1) R21.2 SATISFIED by VACUUM INTO from a read-only connection, open-read-only-in-place remedy WITHDRAWN as unreachable, verified on main db/index.js; (2) TWO OPERATOR CONDITIONS before item 17 posts: (a) WAL grows during the snapshot (-wal ~3.3 GB over 2.5 days recorded), safe NOW only because SCHEDULER_DISABLED=1, tied to the brake decision; (b) item text must carry snapshot ≥ DB size, filesystem and free space, abort when short, exact rm, app stays up; (3) Condition B row counts CLEAN; (4) FINDING REGISTERED not a defect: NO READ-ONLY PATH TO THE PRODUCTION DB, remedy shape exported factory or opt-in read-only flag, routed to Planner 12:59Z plan-no-build, does not gate item 17; (5) R22.2 CORRECTED on Wiring map census, handler callbacks 110 of 274 are the likeliest request reach so the under-count channel is BIGGER, do not combine populations, Opportunity re-runs the four-way classification on its own; attribution corrected, Wiring map owes nothing; item 17 posts after 2(a)+2(b) are in its text"
metadata:
  type: project
---
- **12:57Z Auditor §R23** (record: audit-unitA-and-ceiling-bracket-2026-09-22.md §R23). (1) R21.2 SATISFIED by VACUUM INTO from a read-only connection; Auditor's 'open read-only in place' remedy WITHDRAWN as unreachable, verified on main 654ff93 db/index.js :20, :22-27, :45-50, :184-187, :194-198. 'The unsafe cp was accidentally protecting production' kept on record. (2) TWO OPERATOR CONDITIONS before item 17 posts: (a) WAL grows during the snapshot (db/index.js:28-42 records -wal at ~3.3 GB over 2.5 days, 2026-09-13; journal_size_limit caps after checkpoint, not during an active read transaction; VACUUM INTO of a >2 GB DB holds a read transaction far longer than 90 s; free-space check runs BEFORE, consumer grows DURING, same volume). Mitigation: SCHEDULER_DISABLED=1 is on → write volume at floor → safe NOW; must not be re-run casually after the brake lifts; TIED TO THE BRAKE DECISION like the stale_findings trigger. (b) Item text must carry: snapshot ≥ DB size; which filesystem it lands on and free space required; script ABORTS when short; exact rm; app stays up (read-only WAL connection does not block writers). (3) Four Condition B row counts CLEAN: bootstrap writes touch only schema_migrations, db_health_checks, PRAGMA state; legacy marker present on a production snapshot; backupBeforeMigration would write beside the snapshot, never production; never-delete .bak rule not implicated. (4) FINDING REGISTERED, not a defect, no remedy ordered: NO READ-ONLY PATH TO THE PRODUCTION DB; GRIDIRON_DB_PATH redirects, does not downgrade; design gap with operational cost; remedy SHAPE = exported factory or opt-in read-only flag skipping the bootstrap; server/db/index.js owner per allocation decides; routed to Planner 12:59Z plan-no-build; home = Explorer memory gridiron-no-read-only-path-to-the-production-db. Does NOT gate item 17. (5) R22.2 CORRECTED on Wiring map's census: handler callbacks (110 of 274) are neither exported nor module-private and are the likeliest genuine request reach, so the under-count channel in 'exported function of a route-reached module' is BIGGER than said. DO NOT COMBINE POPULATIONS (Wiring map 274/411 dynamic, 2,661 total ≠ Opportunity 240/1,927); Opportunity re-runs the four-way classification on its own population before any number moves (told 12:59Z). Attribution corrected: 172–190 criterion and symbol-reach.mjs are Opportunity's; Wiring map owes nothing. Kept: importing a module does not execute function bodies, confirmed by execution (43 rows = 32 + 11 reconciles with R22.5 from a different thread and method); Wiring map's self-caught 38-import mis-attribution. Item 17 posts after 2(a)+2(b) are in its text (facts requested from Explorer 12:59Z).
Prev [[gridiron-state-1185-2026-09-22]].
