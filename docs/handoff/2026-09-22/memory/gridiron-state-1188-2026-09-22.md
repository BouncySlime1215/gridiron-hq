---
name: gridiron-state-1188-2026-09-22
description: "12:59Z Explorer R23(a)/(b) applied, ITEM 17 POSTED to Nick 13:03Z: run while brake on, default snapshot /data/read-snapshot.sqlite, needs 1.5 x (main + WAL) free with statfs before start, STOP message and exit 1 when short with nothing written, app stays up, prints exact rm, brake line prints presence only never a value, output to be pasted in the Explorer thread; 12:59Z Planner §R23.4 registered as PLAN-08-read-only-db-path-2026-09-22.md plan-no-build, owner UNRESOLVED lookup routed to coordinator, placed behind Phase A items as tooling, moves only on a Phase A item needing a production read; finding verified on tree with seven import-time writes ending in the health-check upsert :203-219, line corrections DB_PATH :10 and schema_migrations :44-50; remedy shape only, module-scope bootstrap makes an in-function flag too late, db is a module-level constant export so a factory is a unit not a patch, factory-vs-flag undecided"
metadata:
  type: project
---
- **12:59Z Explorer R23(a)/(b) applied; ITEM 17 POSTED to Nick 13:03Z (post cmsg_01YAsw8AnFv4ioRMQw8dfPmTNbRA3MqsFQ1txkivhzR1bU).** Script + prereg now carry: run while brake on (WAL cannot checkpoint during the read transaction; db/index.js:28-42 war story); default snapshot /data/read-snapshot.sqlite (only volume sized for it on Fly; second argument relocates); needs 1.5 × (main + WAL) free, statfs on destination dir before start (was 1.2×, raised per R23(a), reason beside the constant); if short: 'STOP: not enough free space… Nothing has been written.' + alternative command, exit 1, verified no file created; app stays up (read-only WAL connection does not block writers); prints exact rm at end. Brake line prints PRESENCE only, honest when absent ('not set in this shell; does not mean off on the app' + WAL warning), printed before the space check so visible on abort; never prints any value. Output to be pasted in the Explorer thread.
- **12:59Z Planner: §R23.4 registered as /mnt/project-files/PLAN-08-read-only-db-path-2026-09-22.md, plan-no-build.** Owner UNRESOLVED (Planner has no memory tools; allocation file not on the mount) — lookup routed to coordinator; file says no thread starts it on the file's strength. Placement: behind current Phase A items, unnumbered, as tooling; the one thing that moves it forward is a Phase A item needing a production read (today: weekly_ensemble_fits promotion state). Finding verified on tree: seven import-time writes ending in INSERT … ON CONFLICT DO UPDATE health-check row at :203-219; GRIDIRON_DB_PATH redirects without downgrading. Line corrections: DB_PATH is :10 (not :9); schema_migrations statement opens :44, CREATE text :45-50; documentation at :8-9, :18, :180-182, :190-193. Remedy shape only: bootstrap runs at module scope so an in-function flag is too late (same ordering the paid-run guard hit); db is a module-level constant export at :20 so a factory changes shape for every consumer → a unit, not a patch; factory-vs-flag undecided.
Prev [[gridiron-state-1187-2026-09-22]].
