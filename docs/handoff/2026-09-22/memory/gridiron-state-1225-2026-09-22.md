---
name: gridiron-state-1225-2026-09-22
description: "16:44Z: Fantasy plan reads gradedAvailabilityMultiplier as dead code off main (zero call sites); Auditor R39-R40 clears #68 to merge on one text fix and folds R39's support mismatch into the coupled-grade unit with a pre-registered default"
metadata:
  type: project
  modified: 2026-09-22T16:46:53.639Z
---
- **16:44Z Fantasy plan**, read from code: **`gradedAvailabilityMultiplier` does NOT exist on `main` ac31922d** — only on branch `shrinkage-efficiency-weighting` @ 1a70355, defined `nfl-player-context.js:525`, **ZERO call sites in `server/`** (no applying line; built-ungraded dead code on that branch). Serve side is as-of via `valueAsKnown(entity,'injury_report',decisionAt)` (`:528`, on `nfl_feature_revisions`). Fit side `fitGradedAvailability` (`:424`) refit under R19.6 to earliest revision (CTE `:441-449`, commit cfab134); population = players with a real usage row in a prior week, bucketed by earliest `injury_report` revision for the following week (`:452-469`), minN 30, seasons minus in-progress (`:425`). Relayed to Model evidence audit.
- **16:44Z Auditor R39-R40.** **R40:** #68 may **MERGE on CI green** — the "unmerged" condition in R35 bound the *code*, and that's satisfied by the default-off flag with a pinned default; **one blocking condition:** fix the PR text before merge (it says no server file touched; the prior is in `projections.js`). Distinction registered: default-off + pinned default is not a behaviour change, default-on is. Same relief **offered, not ordered**, to Plan 01 / #106. R19.6 as-of refit still required before any flag flips on. `projections.js:599/605` (shrink then × team pass attempts, no availability condition) is code-side corroboration of the R35.3 hedge diagnosis. **R39:** three-support mismatch (observation `:492` includes zero-snap weeks; prior `:395` and k-fit `shrinkage-fit.js:330` gate on `> 0`) **FOLDED into the coupled-grade unit** (Model evidence audit). Sharpest statement: shrink interpolates between two different quantities, so `n/(n+k)` doubles as an unintended availability weighting — contamination is n-dependent (check: correlate per-player understatement with n). Observation-support choice deferred to the coupled grade. **PRE-REGISTERED DEFAULT: exclude zeros, price availability in the multiplier** (three reasons: explicit/gradeable; one site changes vs two + k refit; asymmetric cost since k is non-identifiable from mu). Endorsed Planner's refusal to touch `:492`. Planner to clarify the 34.1% denominator.
- **Coordinator relays 16:46Z**: Model evidence audit, Fantasy plan (relief offer), Planner (denominator + n-correlation check). Merge authority answered at 16:44:57Z — see [[gridiron-state-1226-2026-09-22]].
Prev [[gridiron-state-1224-2026-09-22]]. Next [[gridiron-state-1226-2026-09-22]].
