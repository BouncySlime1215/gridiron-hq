---
name: gridiron-state-1266-2026-09-22
description: "18:03Z-18:10Z thirty-second batch: Fantasy plan RUN 1 clean on d4c6e49, R51.1 conditions commit, backtest-significance.js allocated for one observed_diff unit; #116 FINAL head 6dfc123d, #85 PUSHED 69909edd; Explorer CORRECTION — nfl_depth has no second scheduled writer, four tables full force, role-detection claim withdrawn, snap_counts CSV ingested twice into near-homonym tables; proposed fleet rule NAME THE TABLE, NEVER THE CONCEPT"
metadata:
  type: project
  modified: 2026-09-22T18:12:00.000Z
---
- **Fantasy plan:** RUN 1 on d4c6e49 exit 0, 3135/3094/0/41; RUN 2 in flight. Pushes d4c6e49, then a commit with R51.1 conditions 1-3 (source-scan guard test; opt-in named inline; docstring/TDD file state default-on only at the coupled grade's call site). Working-tree RED shape → Auditor question. **`backtest-significance.js` was NOT Fantasy plan's** (48 importers, 45 call `pairedBootstrapDiff`) → **ALLOCATED to Fantasy plan 18:10Z for ONE additive `observed_diff` unit** ([[gridiron-file-allocation]]); `:119` `toFixed(4)` rounds at the 0.0003 resolution.
- **Opportunity:** **#116 FINAL head 6dfc123d** (docs only over 93870a92; code identical since 28b801b8). **#85 PUSHED 69909edd** (check 0, 3546/3505/0/41; wiring 1 = main's three; guard ebc0d9dd; comment posted). Stale-reason sweep of all open PR bodies assigned to the Evidence Auditor (read-only).
- **Explorer CORRECTION 18:08Z** (appended to the package; md5 unchanged 0b633a73…): **`nfl_depth` has NO second scheduled writer** — `scheduler.js:1164-1167` → `routes/nfldata.js:217` writes `roster_players.depth_slot/depth_order`; the timer is deliberately withheld `:1156-1162` (51 MB CSV, OOM on 2 GB). So **four tables are full-force** (`nfl_snaps`, `nfl_ngs`, `nfl_pfr_adv`, `nfl_depth`); only `nfl_injuries` has a second writer (`:826-831`; `:820-825` header records the same never-scheduled shape once fixed). `role-changepoint.js:49` reads `player_week_snaps` (scheduled `:1150-1153` via `nflverse.js:270` `syncSnapCounts`) → **role-detection claim WITHDRAWN.** Exposed `nfl_snaps` readers: `nfl-availability.js`, `nfl-matchup-specialists.js`, `nfl-evidence.js`, `nfl-advanced.js`. **NEW: the same nflverse snap_counts CSV is ingested twice** — `nfl-advanced.js:174` `syncSnaps` → `nfl_snaps` (name-keyed, keeps defense/ST, unscheduled) vs `syncSnapCounts` → `player_week_snaps` (player_id-keyed, offense only, scheduled). Pre-reg amended: injuries predicted no gap, depth predicted gap, `player_week_snaps` as control. Relayed to Scheduler with **"do NOT put nflverse depth on a timer."**
- **PROPOSED FLEET RULE** (pending Auditor; adopt unless it objects): **NAME THE TABLE, NEVER THE CONCEPT** — near-homonym pairs `nfl_snaps`/`player_week_snaps`, `syncSnaps`/`syncSnapCounts`, `syncDepthCharts`/`syncDepthChart` [[gridiron-name-the-table-rule]].
Prev [[gridiron-state-1265-2026-09-22]]. Next [[gridiron-state-1267-2026-09-22]].
