---
name: gridiron-state-1271-2026-09-22
description: "18:28Z-18:31Z fortieth batch: Planner's 070 pre-reg — LEAD FINDING the participation columns cannot serve 2026 (pbp_participation CSV 404 for 2026), so build the 059 FTN charting unit with 070 as held-out history; count corrections to the charting-gap finding (writeTeamWeeks :563, 91 suffixes, six single-consumer columns); Opportunity's R54.2 falsification PASSED exactly, partition script building; Release draft #127 (deploy button + .dockerignore) and the two Nick items for the token secret"
metadata:
  type: project
  modified: 2026-09-22T18:33:00.000Z
---
(Coordinator asked for this as "state 1269"; 1269 holds R54, so this is 1271.)
- **Planner pre-reg** `/mnt/project-files/PREREG-070-CHARTING-TEAM-WEEK-2026-09-22.md`. **LEAD FINDING: the 070 participation columns cannot serve 2026** — `ingestFormations` (`nfl-formations.js:89`) fetches `pbp_participation_${season}.csv`; 2022-2025 → 206, **2026 → 404**; history-only. **Recommendation: build the 059 FTN charting unit with 070 as held-out history** (the fifth fit/serve mismatch of the day, avoided). §1-§4 written for 059: 91 pbp features incumbent; MAE + mean signed error; level + information; 2025 held out; clustered bootstrap; oracle power gate first; thresholds fixed now. Production `nfl_play_charting` rows unverified → if empty it is a backfill item (PLAN-09 §4 with the QBR crosswalk). Sharp not opened. **Routed to the Auditor for the 059-vs-070 ruling.**
- **COUNT CORRECTIONS on 4a8085c** to [[gridiron-charting-never-reaches-team-week]] (Explorer asked to append a dated correction): `writeTeamWeeks` is `nfl-pbp.js:563` (INSERT `:564`), not `:539`; **91 suffixes `:447-559`**, not 88; **six** single-consumer 059 columns, not five (`catchable` has three); `drop_` is uncountable by grep.
- **Opportunity:** R55 record fixed in both places (the bug was in untracked `/tmp/importers.mjs`, not the grader). **R54.2 falsification PASSED on b0c1616d** (write-tree 500bab36…, node v22.22.2, ts 5.9.3): request-only 172/65/6/18/11/47, request+job 228/55/3/2/10/21, **both exact.** Building the partition script on branch `claude/project-thread-w45mur-reach-ladder` off f620a120; own draft PR, held.
- **Release draft PR #127** (coordinator asked for this as an append to 1269; recorded here): `deploy.yml` workflow_dispatch-only deploy button — stages the brake, fails unless the "Scheduler disabled" line is logged, never releases the brake; `.dockerignore` cuts the build context **491 MB → 12 MB**, keeps local DBs out. Head **43116ab**; check 0, 3541/3500/0/41; wiring 1 = main's three; mutation 22/22; R52.2 in. Queued at the Evidence Auditor after #100, held for the gate sha.
- **NICK ITEMS for the 17:53Z-cadence post** (Waiting-on-you, copy-paste format), **after #127 merges:** (1) `fly tokens create deploy --app gridiron-hq --name github-actions-deploy --expiry 8760h`; (2) paste it as `FLY_API_TOKEN` at `https://github.com/BouncySlime1215/gridiron-hq/settings/secrets/actions/new`. Optional read-only check for earlier images: `fly ssh console --app gridiron-hq -C "find /app -name '*.sqlite*' -o -name '*.bak'"`. **The secret value never appears in chat.**
Prev [[gridiron-state-1270-2026-09-22]]. Next [[gridiron-state-1272-2026-09-22]].
