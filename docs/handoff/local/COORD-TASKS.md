# COORD-TASKS: the coordinator's own hands-on list (always non-empty; the 5-min COORDINATOR TICK pulls from the top)
Add tasks the moment you discover them; strike them with the result + time from `date`.
1. Run ~/gridiron-local/bin/verify-warroom.sh after every merge to main that touches the War Room/campaign; file any FAIL as a task.
2. Browser-audit localhost:5177 War Room + Coach at 1440x900 and 375x812 after each UI merge (WR-L4 #328, WR-E2E #330, #332, WR-POLISH, NUMBER-DOT, UI-BATCH): file new defects into build units or fix small ones yourself.
3. Review each finished PR's diff yourself for the 10 AM path (#328, #330, #332, int4, WR-POLISH, NUMBER-DOT, VOICE-02, REFRESH-L4, REPRO-01): look for the freeze pattern (heavy work on the request thread), second producers, names, invented numbers.
4. [int4 MERGED 3b72b78d 05:28; after-int4.sh running] After int4 merges: remove #327 from auto-intake.seen and put it in merge-order; restart refresh with GRIDIRON_WARROOM_LEAGUES=4 once REFRESH-L4 merges; re-run the league-4 plan; re-run verify-warroom (the untouchables must PASS).
5. Keep the morning brief draft current in MORNING-BRIEF.md as results land (don't leave it all for 9:37).
6. Check cloud sessions' pushes (FIXB batches) and the budget; relaunch stalled ones only within budget.
7. Tighten tests where reviewers found gaps (e.g. the #325 served-preview pricing 1/10; the #326 retro 0/8 label).
8. [DONE in #345 CARD-CLARITY] After int4 merges: one-token fix produce-plans.mjs 'of: entries.length' -> 'of: r.of' (WR-POLISH follow-up); check the batch-4 untouchable field name matches #333's hideUntouchableTargets mapping (untouchables_by_roster / target.untouchable).
9. 04:04 int4 ETA: steps 1-6a done by 04:03; the rest (#325 #306 #321 #300 #320 #303 #324 #322 #294) + suite + smoke + CI -> watch; on merge: FF the app, verify-warroom, browser audit, release UI-BATCH / MSG-WIRE / COUNTERPART-02b / FEAS-140-ESPN-WIRE / TWIN-01.
10. The morning brief lives at ~/gridiron-local/evidence/MORNING-BRIEF.md (PRIVATE: has names; never in the handoff repo). Update it as results land.
11. [DONE 05:51 basis 9/9] 04:26 After auto-intake fast-forwards the app past #332 (bd482809): re-run produce-plans --leagues 4 on the LIVE DB into ~/gridiron-local/warroom/plans.json (only one produce-plans at a time) so cards carry the p_yes_band basis; then 'I sent it' works in the app. Verify with verify-warroom.sh + one 'I sent it' via the API on a DB copy (not live).
12. [DONE 05:07 in #339 d3a4cab2] After #333 merges: flip E2E B10b from todo to a real test (health dot amber on a warn audit).
13. ~04:45 [DELEGATED to CARD-CLARITY wf_8ac2b8fb-5cc, with the card ladder + send_when time] After #333 merges (smoke FIX-11 G3): attention.js#leverage says "best move worth 0.0 pts, next move changed" when the re-run has NO move. Pass hasMove; with no move say "no move clears your sliders" (+ "changed since last refresh"); the top strip hides "rank 1 of 1" when only one league is ranked. Bundle with task 12 (B10b) in one coordinator PR; the produce-plans.mjs 'of: r.of' token (task 8) waits for int4.
14. ~04:45 auto-intake v4 holds PRs with pending FIXPR/FIXB jobs; watch that ordered PRs still flow (a stuck FIXPR job would block the order behind it: park or finish it).
15. ~04:49 [#331+#341 now ride INT5] After #331 (+#341 MSG-CHECK-FIX) reach main: the live chat DB has NO nick_voice_profile table, so the served Coach text is unstyled. Back up league_chat.sqlite, then run `node scripts/build-nick-voice.mjs --db <live chat DB>` (writes the profile table; texts stay local), restart the app, and check one served draft is styled (GRIDIRON_NICK_VOICE=1).
16. ~04:49 #307 COACH-BRIEF added GET /api/coach/brief/:leagueId but no UI renders it: after #307 merges, make the Coach dock show the morning brief (small build unit or hands-on).
17. 05:08 Stacked PRs: after #307 merges -> `gh pr edit 343 --base main` AND `gh pr edit 346 --base main` (COACH-ANSWERS, 12/12 starter questions answered, p95 87 ms), update-pr.sh 343, put 343 in merge-order right after 339 (Coach morning brief in the dock; preview mode already turns GRIDIRON_COACH_BRIEF_ENABLED on). After int4 (#340, which contains #306) merges -> retarget #331 (voice) to main, update-pr 331, then #341 (MSG-CHECK-FIX, base claude/local-voice-01) merges into #331 first or is retargeted after #331; then task 15 (build the live nick_voice_profile).
18. 05:08 COACH-ANSWERS (wf_480bb1b0-658) and CARD-CLARITY (wf_8ac2b8fb-5cc) and AVAIL-HORIZON (wf_61aec2d5-683) are the 10 AM-critical builds: when each lands, put its PR right behind the UI PRs in merge-order, and re-run the L4 plan + verify-warroom after AVAIL-HORIZON.
19. 05:20 [DONE #353, INT6] After #340 merges: R&D r46 KILLED IDEA-103 (unlucky-record teams do NOT sell cheap: <=1.1 pp value moved for a ~2-win luck gap vs up to 5% priced). server/services/counterparty-pricing.js VALUATION_SOURCES.luck_self_view (cap 0.05, min_n 4, fitted:false, ~lines 800-821) prices exactly that and switches on at 2026 week 5. Coordinator PR: set its cap to 0 and mark it tested-dead citing r46 IDEA-103, with a test that the term moves nothing; merge before week 5.
20. 05:21 PLAN CHANGE: the one-by-one merge train (~20 min CI each, serialized) can't land ~12 PRs before the 08:31 freeze. INT5 agent builds claude/local-integration-5 on int4's head with 337 307 271 342 339 345 344 343 346 327 (one PR, one CI). Those 10 are held in auto-intake.seen (evidence/int5-held.txt): if INT5 drops one, un-hold it. Tasks 17 (retarget 343/346) and the #327 un-hold are superseded by INT5. Gate INT5 myself (merge-queue-v3.sh <PR>) right after #340 merges. Later arrivals (AVAIL-HORIZON, PLAN-BASELINE, TEAM-NAMES) either join INT5 if it's still open or ride an INT6 at ~07:30.
21. 05:32 int4 left 3 non-blocking follow-ups: FIX-320-2 (planner doesn't pass the ESPN lineup context: FEAS-140-ESPN-WIRE unit covers it), FIX-294-2 (E4-live has no source table yet: E4-REDO r49 / later unit), FIX-311-1 (produce-plans --tick has no caller: fold into REFRESH-L4 follow-up or a local job). Local Node-25-only failures on main: route-deletion-impact, page-explain (4), nfl-news-events (7) - CI (Node 22) green; don't chase before 10 AM.
22. 06:06 [INT6 LAUNCHED 06:54 on INT5's local head] DRILL: when INT5 pushes its PR, launch an INT6 integrator on INT5's branch with the INT6 candidates (evidence/int6-candidates.txt) + any 10 AM PR that lands by 07:30; hold them in auto-intake.seen; gate INT6 right after INT5 merges; target merged by ~08:10.
23. 06:06 DRILL: if AVAIL-HORIZON-2 isn't ready by 07:30, the brief + Coach say it plainly (ESPN-check range ~12-20% playoffs / ~1% title, sim fix in progress); nothing invented in the UI.
25. 07:04 after INT6 merges: rm ~/gridiron-local/evidence/intake.pause (resume the one-by-one order: 258, 273, 268, ...), un-hold any PR INT6 dropped, re-run the L4 plan + verify-warroom (title_now > 0 with the preview flag, names 10/10).
26. 07:16 [MOOT: #362 made INT6] If FLIP-LEGS-2 misses INT6: export GRIDIRON_FLIP_LEGS=0 in ~/gridiron-local/run.sh and refresh.sh before the 08:31 check (a 2-player leg would display as one player).
27. 07:56 FREEZE until 10:00: at ~10:05 rm ~/gridiron-local/evidence/intake.pause to resume the one-by-one order; queue a conflict fix for #258 (SKIP: conflict merging main).
28. 09:30 BEFORE MERGING PLAYER-SCORE: Nick's ruling 9/23 (TRADE-INSANE-RND.md:169): FantasyPros consensus is INTERNAL input/referee only, NEVER displayed or committed (#165 repo guard); pages show OUR numbers. Strip fp_ros_rank from served plan fields and the Blue Chip board UI; keep gap flags computed internally (labels like 'rising' / 'owner may undervalue' without FP numbers); no FP data in fixtures/tests. Same rule for TRADE-LENSES / ROADMAP-TIERS.
29. 09:30 LOVE check correction: R&D r8 / RL-8-1 (td-regression.js header): points-over-expected (luck) is already priced by consensus and discounted by real trade partners -> zero weight; LOVE = usage (expected points) + draft capital + health; luck shown as an explanation line only, until r52 DRAFT-VS-LUCK says otherwise.

30. (09:40) Refresh FANTASY-ENGINE-MASTER-PLAN.md section 00 parts B + F: last reorganised 09-18; War Room, Coach brief, People Board, title odds, NO-OVERPAY, protect-mine, O1-RADAR/O2-ML/O3-JEV since. Doc-only; one pass after the 10:00 freeze lifts.
