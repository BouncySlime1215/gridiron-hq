# Red-team drill (06:06, overdue from 05:41 because I was mid-turn; facts from commands, not memory)

## State (measured)
- main 3b72b78d (batch 4) is Nick's app since 05:34; verify-warroom-0551 all PASS except title_now = 0 (AVAIL-HORIZON-2 building).
- INT5 (10 AM UI batch): 12 of 15 merged in its branch by 06:02 (337 307 271 342 339 345 344 343 346 327 347 + main); left 331 341 348, then tests + smoke + push + one CI + my gate.
- Builds: AVAIL-HORIZON-2, FLIP-LEGS, PARTNER-KERNEL, TEAM-NAMES-2, REPRO-01. INT6 candidates: 350 353 351 352.
- Disk 14 GB (gzip running), load 14-19, usage gate OK.

## Attacks (what would disappoint Nick at 10:00) -> response
1. The strip still says "Title odds 0.0%" if AVAIL-HORIZON-2 misses again (#349 fell just short). -> Cutoff 07:30: if it isn't green by then, the brief says it plainly (honest range ~12-20% playoffs / ~1% title from the ESPN check) and Coach's answer cites the same; no invented number in the UI.
2. INT5 CI fails after 15 merges -> the agent drops the offending PR rather than looping; I gate the rest.
3. INT6 can't wait for INT5's merge: serial = too late for the 08:31 freeze. -> Launch the INT6 agent the moment INT5 pushes, based on INT5's branch (same trick as INT5 on int4). Hard cutoff for INT6 contents 07:30; gate by ~08:10.
4. New builds that finish after 07:30 miss 10 AM. -> After ~07:00 launch only 10 AM fixes found by the checks; R&D continues (it doesn't ship).
5. Disk: 5 builds x 930 MB DB copies + smokes. -> finished worktrees removed (plain git worktree remove), old backups gzipped; guard at 12 GB.
6. North star check: go-get-X planner is live (2-step plan); flip radar shows 0 usable flips (FLIP-LEGS); Title Plan blocked by title_now 0 (AVAIL-2); people drive partner choice (PEOPLE-BOARD #352, PARTNER-KERNEL). Coach answers (#346) + brief (#343) + voice (#331/#341) in INT5. Aligned; biggest gap = title odds.
7. Hallucination: every brief number cites a run (verify-warroom-0551, title-zero.md, PR Measured sections). Keep it that way at 09:37.

## Plan changes
- INT6 agent at INT5 push time, cutoff 07:30 (task 22). Title-odds fallback wording ready by 07:30 (task 23).

## Mini-drill 07:46 (the 07:41 cron was preempted by notifications)
- State: main b7c79d94 (INT5) live and verified 07:40 (all PASS); INT6 #364 in CI (11 PRs, smoke OK on 898406ee); load 3.5, disk 21 GB.
- Attack: INT6 CI fails or merges after 08:25 -> fallback: the 08:31 pass tests INT5 (already verified) and the brief says plainly which batch-6 items aren't live; no rushed fix-merges after 08:31 except a crash/freeze in the War Room or Coach.
- Freeze: no new build launches until the 08:31 pass is done (R&D paused; builds only for defects the pass finds). REPRO-01 keeps running (not merged before 10).
- Honesty: brief lines about batch 6 are marked "checked on a copy at 07:37" until after-int6's live verify replaces them.
