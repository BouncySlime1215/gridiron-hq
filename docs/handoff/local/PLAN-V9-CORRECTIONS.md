# PLAN v9: what the verified skill study changes (2026-09-23 ~1:50 AM ET; Nick: "change entire module plans if you need; rules still apply")
Verified facts (8 skeptic passes, SKILL-REPORT.md): (a) the repeatable skill is mostly ENGAGEMENT/DILIGENCE (adds per week takes ~56% of the predictable part once included; start/sit ~20%); (b) dead-starter blunders cost 2.8 pts/week and repeat; clever calls among healthy players +0.68/week and barely repeat; (c) in trades, LINEUP GAIN beats paper value (0.62 vs 0.115 lineup points per point; they disagree in 80% of trades) and hot starts regress toward pedigree; "best player in the deal" is NOT an edge (-2.5 to -5.3 with value held equal); IR cost ~4-6 pts; (d) the measured levers explain only 12-15% of the year-to-year carry-over: most of what makes a manager good is unmeasured, so population priors are weak and Nick's OWN graded record matters most; (e) "more moves -> more wins" is an association, not a proven effect.
## What changes
1. NEW PILLAR 1: DILIGENCE ENGINE ("never miss anything, in all 5 leagues"). The biggest repeatable skill is attention; the app can supply it. Promoted from side units to a top program, built in phase 1b (cheap, lean): SK-01 command center, SS-01 dead-starter guard, ST-03 inactives/late-swap windows, WV-02 injury replacement within the first waiver run, WV-01 streaming, dormant-league autopilot (leagues 1 and 5: weekly moves drafted for one tap), ST-04 bounded autopilot (after N12), a weekly "attention score" per league (moves made, dead starters, stale lineups) on My team. Test: zero dead starters, zero missed injury replacements, every league touched weekly; graded by GR-01.
2. PILLAR 2: TRADE MACHINE, re-centred on the one verified edge: the gap between PAPER value (what his screen shows) and LINEUP value (what it does for each roster). TM-01's objective = Nick's lineup gain + title odds subject to his-screen fairness; TM-28 (their screen) moves up; the pedigree regression tag stays (re-fit); REMOVED: the best-player flag; SHRUNK: injured-player discount (4-6 pts), NX-02 IR-slot arbitrage (deprioritized to phase 6); "trade less, better" stays as a guard. Fewer, bigger-gap deals; each must clear a minimum lineup-gain threshold with its interval.
3. PILLAR 3: CHAMPIONSHIP ENGINE + GRADING, with grading re-weighted toward NICK'S OWN RECORD (fact d): GR-01 ledger and GR-02 report card come first in phase 1; population models (AI-04/05, CE-04) carry wide priors and must show they add to Nick's own history before steering; every "this works" claim in the app cites Nick's graded results once n allows, the population only as a prior.
4. START/SIT narrowed: availability and late information only (ST-01 oracle, SS-01, ST-03). ST-02 matchup-aware floor/ceiling drops to phase 5 behind its history test (it is "clever calls among healthy players", which barely repeat). The recency-bias guard (ST-07) stays: it is a blunder guard.
5. WAIVERS reframed: make moves one tap (drafted claims, streams, drop suggestions); never claim "more moves wins"; show the association honestly in the report card.
6. Wording rule: pitches, tags, coach answers and the board use only verified numbers; the three corrected claims are banned phrases (best-player edge, IR 30 pts, start/sit half of skill).
## New build order
Phase 0 (running) -> Phase 1a Foundations (CE-05, TR-01, TR-03, AI-01, TM-09, TM-06 + FantasyCalc test, GR-05, GR-01) -> Phase 1b DILIGENCE ENGINE (SK-01, SS-01, WV-01, WV-02, ST-03, dormant-league drafts, attention score; lean builds, ~6 h, before Thursday's week-3 kickoff) -> Phase 2 Engine (CE-01/02/03/09/06/10) -> Phase 3 Trade Machine v1 (TM-28 their screen, TM-01 lineup-gap finder, TM-02 tags incl. pedigree regression, TM-03 target board + LS-01, AI-05, TM-04, TM-05, DD-01, GT-01, GR-02, GR-06, LL-01) -> Phase 5 AI tier -> Phase 6 insane tier (NX-02 and ST-02 moved here).

## Plan adjustment after R&D rounds 3-4 (06:45Z)
- FantasyCalc is a strong market (right 60.6% vs season-to-date on disagreements): the FantasyCalc disagreement test becomes the FIRST gate of the Trade Machine (the finder does not claim an edge until it beats FantasyCalc on forward weeks). Daily snapshots start now (FC-SNAP) so the test can run from week 4; the Wayback snapshots give a 2023-24 prior.
- The paper-value side of TM-01 uses FantasyCalc + ESPN as the "his screen" pair (NX-08 promoted into TM-28).
- New bug class found internally (fake win rates, swaps after kickoff, injured players valued as healthy): the UI lens and the structure lens add a check "no number on a page without a calibration source". RL-4-1 (injury-return valuation) is scheduled right after BLEND-01 lands on the producer lines; it overlaps TR-01 and merges into it.
- Live inactives (RL-3-2: ~1 in 3 surprise inactives missed without a live Sunday source) joins the Diligence Engine phase 1b.

## Plan adjustment after R&D round 5 (08:10Z)
- TM-14 (public hype) CLOSED for attention/mention volume: Wikipedia page views (the only terms-clean hype source) add nothing over FantasyPros rank, over ESPN's weekly number, or in 1,627 real 1-for-1 trades (kill test, reproduced; oracle bound +0.0001). Paying for X/Reddit volume is not justified (extrapolation, not measured). ECR rank moves stay open, folded into B-15. AI-05's "star power" input drops attention buzz.
- Bye weeks are mispriced in two places (both reproduced): trade-engine prices a bye-week player at exactly 75% of his value (hand-set 0.25 x this week + 0.75 x ROS; changes the top trade card in 4 of 5 leagues and disagrees with the waiver page), and My Team's weekly range counts a bye-week starter's full points. RL-5-2 folds into TR-03 / S-07 real horizons (the bye costs 1 of the remaining weeks); RL-5-3 is a lean fix that goes in the next lean batch. Rule added to the structure lens: no hand-set blend weight behind a served number without its measurement.

## Plan adjustment after R&D round 6 (11:10Z)
- KILLED (evidence, reproduced; 2023-24 graded, fit 2021-22): future-opponent schedule strength adds nothing over consensus + byes at 5 weeks, rest of season or weeks 15-17, and every leak-free version is null or worse. Dropped: TM-02's schedule-swing tag (TM-02 keeps regression, role trend, injury), TM-12 forward schedule strength as a trade signal, TM-19 playoff-week specialist, NX-10 playoff-week rentals, AI-08's schedule-cliff feature. Kept: byes (a real, large control: +0.043 at 2 weeks); next week's lookahead line stays a candidate only via a forward check with the schedule-blind arm (validator's correction). CE-01 may still use ESPN's lookahead lines (already in game_lines, unread) to simulate future games.
- URGENT BUG: the Start/Sit waiver card tells Nick to drop the wrong player, priced 0.0/0.0, in all 5 leagues (RL-6-4) -> next build loop, first.
- CRITICAL BUG: Trade Lab's title-odds deltas are mostly noise because before/after seasons are not paired (common random numbers broken in tradeImpact) (RL-6-3) -> next build loop, critical (Fable claims check); CE-09 depends on it.
- AI: an 8-cent web-search read of injured players caught injury news ESPN's forward projection missed; frozen as a third arm of the W3-W8 availability test (RL-6-2) -> joins AI-15/ST-01.

## Plan adjustment after AI-01 declined and TM-09 landed (11:50Z)
- AI-01 DECLINED (pre-registered, Fable-checked): expected-points "true value" is no better than season-to-date PPG at predicting the next 5 weeks (2021-24), clearly worse than the ros_ppg the trade card already serves, and FantasyCalc beats all of them (pair accuracy FC 0.729 vs season-to-date 0.690 vs xFP 0.677 on the 2023-24 snapshots). Luck carries forward (slopes 0.37-0.58), so "sell luck" is weaker than assumed.
- CONSEQUENCE: the Trade Machine's value base becomes the MARKET (FantasyCalc, now snapshotted daily by FC-SNAP) blended with ros_ppg only where a test shows it adds; our edge is not a better player value but (1) lineup fit (the paper-vs-lineup gap, the one verified trade edge), (2) availability and timing, (3) the human side (acceptance, tilt, reaction lag). TM-01 is re-specified on that basis; AI-02 usage forecaster is deprioritized behind the market-based value test.
- TM-09 market prices (#183): price model beats the ESPN-rank baseline on 2024 held out (MAE 1.727 -> 1.654, gain 0.073 [0.064, 0.083]); hype decays but modestly (c 0.252 vs placebo 0.154); decision win rate 0.553-0.560. Three hype producers now exist (players.js heuristicVerdict, waiver-brain sellHigh, TM-09): unify (new unit S-19) before any page shows hype.

## Plan adjustment after R&D round 7 (12:20Z)
- TRADE EDGE FOUND (valid-fixable, reproduced): when a starter goes out, ESPN's number for his fill-in jumps about +3, but only ~35-40% of the jump lasts the next 4 weeks; the vacancy-specific overstatement is ~1.2-1.5 pts/game (validator's corrected size). 39 rostered slots across the 5 leagues hold a week-3 fill-in. A real "looks good, fades" signal: the their-eyes value (ESPN base) carries the borrowed number, so SELL fill-ins at their screen price and do not BUY them at it. Lands on TR-03's ESPN base and TM-01 (not trade-engine.js:410, which serves our own engine; validator correction). Related code to unify: role-scenario-engine.js:130-183 teammate_return, player-week-engine applyRedistribution (off), player-case.js:163-176 teammate_out factor. Unit RL-7-1, re-specified per the validator.
- BLEND-02 pre-screen: on ESPN's number only the Vegas layer helps, a little; weather and vacated-volume layers dropped from BLEND-02.
- BUG: the snap-count loader joins players by name (one player has no snaps; another's snaps are saved on a retired namesake). It feeds WV-02's snap-share ranking and the usage models: S-20, first in the next loop.
- BUG: the glossary PR #75 describes 6 of 15 numbers wrongly and its tests still pass: UX-14; #75 held until fixed.

## Plan adjustment after R&D round 8 (13:15Z)
- KILLED (reproduced under a stricter as-of rule): the regression / TD-luck tag. Consensus rank already prices points-above-expected (adding it changes 2023-24 pair accuracy by about 0), humans already discount most of it in real trades, and consensus prices do not fall later. Dropped: TM-02's regression tag, TR-05's regression-risk tag, AI-08's regression classifier. RL-8-1 retires the in-season td_luck weights (player-case.js verdict, lineup-brain flag, registry entry) and rewrites td-regression.js's false header; it keeps the fitted preseason feature (preseason-model.js td_luck_pg_1), which is a different, tested input.
- Cumulative lesson (rounds 5-8 + AI-01): public-data "edges" (hype, schedule, regression, xFP) are priced by consensus. The Trade Machine's remaining edges are lineup fit (verified), the fill-in borrowed role (round 7), availability/timing, and the human side (acceptance, tilt, lineup tells). TM-02 now keeps only role trend and injury/teammate-return tags, each behind its own test.
- TRADE MACHINE GATE RE-AIMED (round 8, fixable): FantasyCalc and consensus ECR tie on ROS pair accuracy (+0.005 [-0.005,+0.016]), so "beat FantasyCalc forward" is nearly unpassable and the wrong target. The real edge is that humans ACCEPT trades that consensus says are lopsided: median consensus gap 1.63 ROS ppg in 1,359 real 1-for-1 trades; on 2023-24 disagreement trades the consensus-favoured side won 57.6% [51.5, 63.3] and gained +0.89 [+0.19, +1.52] ROS ppg, with no detectable adverse selection. New gate for TM-01: offers built on consensus/market value (ESPN + FantasyCalc + ECR where licensed) that the counterparty is likely to accept, graded in LINEUP points (the next test: re-run the arm in lineup points, RL-8-2). The FantasyCalc disagreement test stays as a diagnostic, not the gate.
- BUG: every strength-of-schedule rank the app prints is made up (computeSOS has no empty-data check on an unsynced fc_value table; Washington is saved as WSH and listed as its own opponent); it sits in the Buy/Sell fallback for all 663 rostered players and produced a stored SELL citing "schedule ranks 32/32". Fix: remove the fake rank now (schedule strength was killed as a signal in round 6 anyway) and fix the WSH code: RL-8-3, next loop, urgent.

## Plan adjustment after R&D round 9 (13:40Z)
- TRADE EDGE (valid, replicated out of sample 2016-20): starters on teams with a .700+ record through W10 score ~0.58-0.64 of normal in the NFL's final regular week (rest), vs ~0.79 for the rest of the league; league 1's championship includes W18 (final scoring period 18). ESPN's own forward projections price W18 at 1.000, so the counterparty's screen overvalues top-team starters for that league's final. Size it as the extra effect over the league (~0.78 ratio; validator's correction), as a team-level rest event in weeklyAvailability (one producer, contingency.js:933) read by season-sim and playoff_ppg. RL-9-1, sequenced after RL-6-3 (same season-sim region). Revives a narrow, tested slice of TM-19 (only final-week rest, not schedule strength).
- ENGINE FIX (fixable): the season sim treats each future week's injury as a fresh coin flip, but absences come in multi-week spells: CE-03 must sample spells (duration curves), not independent weeks (RL-9-2 folds into CE-03).
- TRADE ENGINE BUG (fixable): 2-for-1 deals are called fair by adding player values as if roster spots were free; the lineup-value (replacement) view must price the freed/filled roster spot (RL-9-3 folds into TM-01's lineup-gap objective; TM-42 replacement-level pricing moves up).

## Plan adjustment after R&D round 10 (14:45Z)
- The dead-starter guard's inactive feed already exists in data we sync: ESPN's final pregame projection was 0 for 92% of surprise gameday scratches (2021-24, reproduced independently), and a relevant player ESPN had at 0 did not play ~90% of the time. RL-10-1 feeds SS-01's inactive hook from projected_points (no new source, no migration) using "last week's projection >= 5, now 0" as the rule (validator correction; snapshots update in place, so flip times cannot be recovered).
- ONE PRODUCER DECISION: RL-10-1 (ESPN zero) becomes the inactive producer; RL-3-2 (#184, Bluesky feed, needs a migration) is re-scoped to a timing supplement ONLY if the W4-W5 timing test shows ESPN flips too late (after inactives, ~90 min before kickoff). Until then #184 stays held; the app never has two producers for the same signal.
- W4-W5 timing test: an append-only poller (every 10 min on Sundays) writes JSONL under rnd/loop/ to measure when ESPN's projection flips vs the official inactives time. RL-10-2.
- The pre-registered "boost the scratched player's teammates" idea was killed (ESPN already moves them).

## Plan adjustment after RL-8-2 declined (15:40Z) - Trade Machine gate
- RL-8-2 (critical, pre-registered, Fable claims): in LINEUP points, the consensus-favoured side of real 2023-24 disagreement trades won only 51.5% (bar 55%, CI above 50%; MDE 6.8 pts). DECLINED as the gate.
- Post-hoc split (must be re-tested, not built on yet): 1-for-1 swaps keep the edge (58%, same as in player points); 2-for-1 trades reverse it (43.5%) because summed player value overprices the side consolidating into one player. This matches R&D r9's roster-spot finding (RL-9-3) and the skill study's 'lineup value beats paper value'.
- New gate (pre-register now, test on data not yet used for this question: 2021-22 Sleeper trades, then 2026 forward): (a) 1-for-1: consensus-favoured side wins >55% in lineup points; (b) 2-for-1/2-for-2: a LINEUP-value model with roster-spot (replacement) cost predicts the winner >55% where summed consensus fails. The Trade Machine ships 1-for-1 ideas first if (a) passes; multi-player ideas only after (b). New unit RL-8-2b (critical).

## Plan adjustment after R&D round 11 (16:50Z) - who says yes
- VALID (replicated independently; fit 2021-23, confirmed 2024): the managers most likely to trade are the ACTIVE ones (pickups per week, already traded this season): top vs bottom fifth trade ~3x as often; activity alone ranks who trades at AUC 0.65 (pairs 0.68). "Just lost" adds only ~6-7% and loss size doesn't matter; teams that left a dead starter in trade 25% LESS (checked out). Bubble position and roster fit add nothing beyond activity.
- CHANGES: TM-17 pain calendar is demoted (tilt/loss is weak; keep only as a minor tag); the target board (TM-03, #190) and receptiveness lead with ACTIVITY; checked-out managers are flagged as poor trade partners (waivers beat trades with them). RL-11-1: read tx_waiver_moves (already computed at manager-signals.js:221, unread) as a per-week activity term in counterparty-pricing's receptiveness, gated like tx_accept_rate, labelled "chance he completes a trade", with its own factor on ManagerRead/ManagerBoard. Same-region edit risk with B-10/B-11 (sequence).

## Plan adjustment after R&D round 12 (17:50Z)
- The injury replacement ("who inherits the snaps") should come from ESPN's weekly projection, not the depth chart or last week's snaps: ESPN's top teammate led the position's snaps in ~90% of RB starter-out weeks vs ~67-71% for the depth chart (TE 83% vs 58%); last-week snaps tie the chart. Valid-fixable (QB edge only from the chart naming a QB who didn't play; tie rule needed). CHANGES: WV-02's replacement ranking (#178) and news-lag-trader's beneficiaryOf switch to ESPN's weekly projection (RL-12-1: persist the weekly rows stats.js:55 throws away into one producer table espn_week_projection, append-only; depth chart only as labelled fallback). Migration needed -> Nick's blanket migration answer.
- BUG (valid): the Sleeper injury flag is never switched off, so healed players stay marked down (RL-12-2, urgent: it biases availability everywhere).
- BUG (valid): the player card's news misses a third of stories the News page already attributes to him (RL-12-3; one news-attribution producer).

## Plan adjustment after R&D round 13 (18:55Z)
- TRADE GATE DATA WITHOUT THE 2025 HOLDOUT (fixable): a 2026 forward panel of public Sleeper leagues (about half the corpus leagues renewed; they trade at the corpus rate; matchup rows are complete) can supply fresh 1-for-1 disagreement trades. Corrected sizes (validator): n=200 needs ~1,100 leagues through week 11 (~40-80k in-season API calls over the season). Offers Nick an alternative to NICK-2025: build RL-13-1 (the puller, local-only, aggregates only) as RL-8-2b step 0, pre-register the rate check and the test before any 2026 outcome is computed. Needs Nick's explicit OK for the in-season call volume under his Sleeper approval (NICK-SLEEPER-2026).
- BUG (fixable): receptiveness mixes two scales (raw accept rate ~0.3 blended into a 0.5-centred percentile score), so managers who ANSWER offers look harder to trade with even when they accept more than the league; it multiplies every Trade Lab idea. Fix: convert the accept rate to the league percentile before blending (RL-13-2, high; touches the same lines as #203 RL-11-1: build on #203).
- WAIVERS (valid): all 5 leagues reset waiver priority every week (inverse standings), so priority cannot be saved; claim ORDER within the week is the real lever. Changes the waiver advice: never "save your priority"; rank claims by value and file them all (RL-13-3, lean).
