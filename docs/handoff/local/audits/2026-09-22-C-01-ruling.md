# C-01 ruling: which rule the C12 start/sit gate gates on

**Independent Auditor, 2026-09-22 19:25 EDT.** Unit C-01 (plan item C12). Branch
`claude/local-c-01-startsit-baseline-gate`, head `b97d5ea2` (tree `1a388719`). Requested in
WORK-QUEUE.md §12 (:602). Every number is from a **local copy, not production**.

Labels: **T** = Tested, reproduced by me in this audit. **C** = Cited from another producer, not
reproduced here.

**Status: not mergeable as it stands.** It can merge once acceptance items A1-A9 (§3c) are met,
one guard run is done (rule 4), and an Auditor check of the fix head, in a fresh session,
confirms them.

## 0. Ruling in brief

- **(a) Which rule gates.** C12's gate must gate on the plan's named rule, "start highest
  projection". In Nick's ESPN leagues that means ESPN's weekly projection. The season average is
  a fair floor but it is not the plan's dumb version. It *was* pre-registered as the gating rule
  (prereg §1, §8). The same prereg also named ESPN as the follow-up rule, and addendum 1 left
  that swap to the Auditor. This ruling makes the swap, toward the rule that is harder for the
  model.
- **(b) The page.** No green "Beats" headline while the plan's own rule points against us in
  every measured variant. Today's headline is an amber "Not shown to beat ESPN's projection",
  plus the week-2 direction and its caveat. The average result stays below it, as a weaker
  check, with no green.
- **(c) Before merge.** Keep the pre-registered average verdict exactly as computed and rename
  it `average_check`. Top-level `verdict`, the stored governance audit and the sync_log detail
  the Coach reads must carry the plan-rule verdict. File prereg addendum 2 for the plan-rule
  verdict before week 3's first kickoff (2026-09-25 00:15Z). Items A1-A9.
- **(d) Scope.** The §12 grant covers the refresh-loop files. One file is outside both the unit
  row and the grant: `server/index.js` (+4 lines). It needs a one-line coordinator grant.
  Nothing else is out of scope.

## 1. The question

- Plan item 12 (PLAN-ITEMS-1-25.md:49, Nick verbatim): "Nothing ships unless it beats the dumb
  version: start/sit must beat "start highest projection," ... If it can't beat dumb, it's
  decoration."
- Unit row (WORK-QUEUE.md:231): C-01 "Standing start/sit gate vs "start highest projection""
  and "decision win rate + points vs the dumb rule, CI; failing weeks shown".
- The dispute (WORK-QUEUE.md:602). The verdict `beats_dumb` and the green chip
  (StartSitGate.tsx:43, :125) are graded against "start the higher average". The literal rule,
  ESPN's projection (`served.vs_espn`), is `dumb_ahead` for 2026 W2.

## 2. Evidence

### 2.1 Timing record: prereg before the run, addendum 1 before arm B

| event | time (-0400) | source |
|---|---|---|
| `c01-config-probe.mjs`: reads weight-set ids and the stored k-vector only, no outcome | 16:18 | scratchpad mtime; file read |
| prereg `a2ea8714` | 16:20:51 | `git log --date=iso-strict` |
| RED `d7c37773` (fixture tests) | 16:26:58 | git |
| first real gate run, `c01/gate-run-1.out` | 16:34:04 | mtime |
| addendum 1 `f55d2eb6` (arm B design; "Arm B below is blind at commit time") | 17:18:46 | git |
| first arm-B computation, `c01-fix/gate-run-2.out` | 17:30-17:31 | mtime, `gate-run-2-head.txt` |

I searched scratchpad analysis files older than 17:18:46 for `league_roster_snapshots`,
`projected_points`, `vs_espn` and `espn_proj`. No C-01 computation turned up; the hits were
other units' files (S-18, audits, wiring map). That fits arm B being blind, though a negative
search cannot prove it.

### 2.2 Reproduction of the builder's run (T)

```
A=/private/tmp/claude-501/-Users-nick-matta-Claude/132cb810-8a5e-4b69-9adb-90c88dbae198/scratchpad/c01-audit
git -C /Users/nick_matta/Documents/GitHub/gridiron-hq worktree add --detach /Users/nick_matta/gridiron-local/wt/C-01-audit b97d5ea2
ln -s /Users/nick_matta/Documents/GitHub/gridiron-hq/node_modules /Users/nick_matta/gridiron-local/wt/C-01-audit/node_modules
cp /Users/nick_matta/gridiron-local/wt/C-01/.local-db/data-b.sqlite $A/data-b.sqlite
shasum -a 256 $A/data-b.sqlite
#   b9086412006153571a2bb3e3acfb41aba136bd9688cecd33e708c1a59fb32bed  (= evidence §4a, byte-identical copy)
cd /Users/nick_matta/gridiron-local/wt/C-01-audit && GRIDIRON_DB_PATH=$A/data-b.sqlite GRIDIRON_DB_INTEGRITY_CHECK=off \
  SCHEDULER_DISABLED=1 /usr/bin/time -l node scripts/run-start-sit-gate.mjs --iterations 2000 > $A/gate-run-audit.out
#   exit 0; 12.14 s wall; 465,649,664 B max RSS; "verdict: beats_dumb"; HEAD b97d5ea2, tree 1a388719
diff <result JSON of gate-run-audit.out> <result JSON of c01-fix/gate-run-2.out>   ->  "JSON IDENTICAL"
```

| window / arm | role | rows | disagreements | our pick won | points per disagreement | player-clustered 90% CI (points) | win-rate player CI | direction |
|---|---|---|---|---|---|---|---|---|
| past 2024-25, W5-18 | G1-G3 (verdict) | — | 6,633 (264 players) | 0.5497 | +1.3703 | [+0.7958, +1.9428]; week CI [+0.9566, +1.7702] | [0.5256, 0.5733] | ours ahead |
| 2026 W2, replayed on today's settings | G4 (verdict) | 360 | 571 | 0.5806 | +2.7923 | [+0.3103, +5.5129] | [0.4908, 0.6770] | ours ahead |
| A. served vs average | descriptive | 360 | 63 | 0.5556 | +3.4905 | [−2.1295, +9.2611] | [0.3462, 0.7692] | ours ahead, CI straddles 0 |
| **B. served vs ESPN (the plan's rule)** | descriptive | 153 (207 had no ESPN value; 0 conflicting) | 286 (69 players) | **0.3776** | **−3.4207** | **[−6.5246, −0.3637]** | [0.2537, 0.5057] | **ESPN ahead** |

- **Gates.** G1 0.7958, G2 0.9566, G3 0.5256 and G4 2.7923 all pass, giving `beats_dumb`.
- **The week-2 served snapshot.**
  - Captured 2026-09-17T18:56:10Z with `weight_fit` frozen-2023.
  - That is before the k-fit (`fitted_at` 2026-09-18T02:52Z).
  - The replay's champion for that week is fit-2.
- **Correction to the ruling request.** "Forward win rate 0.5556" is arm A: descriptive, n 63,
  with a CI that straddles 0. The verdict's G4 reads the replay row, +2.79 on n 571.
- **Degenerate interval.** A one-week window returns a zero-width "week-clustered CI":
  [−3.4207, −3.4207] for arm B and [+2.7923, +2.7923] for the replay. That is one cluster, not an
  interval (item A7).

### 2.3 Independent recomputation, my own code with no gate import (T)

`python3 $A/indep_arm_b.py $A/data-b.sqlite` produced `$A/indep_arm_b.out`. The script
rebuilds the population, actuals and pairs from raw tables. It takes only the rule definitions
from prereg §3 and addendum 1 §3.

- **PPR check.** My PPR from `player_week_usage` (INT −2) matches 351/351 settled
  `weekly_prediction_snapshots.actual` values.
- **Population.** Snapshot skill rows active in 2026 W1 come to 360, the same as the gate's 360.
- **Week-2 actuals are complete.** `player_week_usage` 2026 W2 covers 32 of 32 teams (525 rows).

| arm | rows | pairs | disagreements | our pick won | points per disagreement | player-clustered 90% CI (my RNG, 2,000 draws) |
|---|---|---|---|---|---|---|
| B, ESPN at lock (pre-registered arm B) | 153 | 784 | 286 | 0.3776 | −3.4207 | [−6.4039, −0.3200] |
| B′, ESPN's Thursday capture (22:08Z, 3 h after our snapshot), same 153 rows | 153 | 847 | 308 | 0.4172 | −2.3490 | [−5.7580, +1.1337] |
| B″, Thursday capture, every row it covers | 342 | 1,102 | 381 | 0.4186 | −2.2757 | [−5.4234, +0.6708] |
| A, served vs season-to-date | 360 | 1,233 | 63 | 0.5556 | +3.4905 | [−2.0665, +9.7198] |

- **Match with the gate.** Point estimates are identical to the gate and to the builder's
  stress script. The CIs agree within bootstrap noise.
- **ESPN's number moves between Thursday and lock** (`$A/espn_moved.py`). 127 of 153 values
  moved by more than 0.01 points, with a mean |Δ| of 0.726. That matches the builder. At any
  precision, 149 of 153 moved.

**Reading.** Every variant of the plan's rule points ESPN's way. Only the at-lock variant
excludes 0, and that variant carries ESPN's later news, a bias addendum 1 §3 (limit 1) declared
before the run. B″ is the variant closest to a same-cutoff comparison, and it does not exclude
0.

### 2.4 Where the stand-in verdict reaches a reader today (T)

1. **Panel.** I rendered my gate output with the builder's `render-real.mjs`, pointed at the
   audit worktree (`$A/render-audit.out`).
   - It has **1 emerald chip**.
   - The heading reads "Does our projection beat the dumb rule?" (StartSitGate.tsx:125).
   - The chip reads 'Beats "start the higher average"', followed by "Our projection picked the
     better player more often than "start the higher average", in past seasons and this one."
     (:43-44).
   - ESPN's result only appears four lines lower, in plain text.
   - The text is byte-identical to the claims skeptic's `c01-stats-r3/render-b97d.out`.
2. **Coach.** The job's sync_log detail carries `verdict: result.verdict`
   (start-sit-gate.js:463). The Coach reads sync_log's "last detail line" (coach/catalog.js:337),
   so the Coach can report `beats_dumb` as the gate's verdict.
3. **Governance store.** `recordGateAudit` stores `promotion_eligible` when every gate passes
   (model-governance.js:154-157). Only G1-G4 are passed in (start-sit-gate.js:459). The route
   serves the result as `audit_verdict`.
4. **Payload text.** `BASELINE_TEXT` opens "The dumb rule:" (start-sit-gate.js:84). ESPN's
   text opens "The literal dumb rule:" (:82).

### 2.5 Outside evidence (C)

- **RL-1-1** (LOOP-LOG.md:15-19; assessed :241-338, "the numbers hold up"). On 2026 W2, free
  weekly consensus orders start/sit pairs better than our structural head:
  - Sleeper: +0.042 [+0.011, +0.074] pair accuracy.
  - FantasyPros ECR: +0.034 [+0.003, +0.065].
  - ESPN (rostered players): +0.020 [−0.021, +0.063].
  - Over 2023-24, ECR beats season-to-date by +0.028 [+0.023, +0.034] on games played.
  - W3 captures from four sources were frozen and hashed before W3's first kickoff. The
    assessor calls the W3 same-cutoff grade "still the decider" and says to add the consensus
    to C-01 through its owner, not to redefine C-01 (:315).
- **Indirect scale only; different seasons and populations, not a test.**
  - Our replay's pair-accuracy edge over the season average (2024-25) is 0.6056 − 0.5929 =
    +0.013 (T).
  - ECR's edge over season-to-date (2023-24) is +0.028 (C).
  - So the public consensus clears the season-average bar by more than we do. That is why
    clearing it cannot stand in for beating the projection.

## 3. Rulings

### (a) C12's gate gates on the plan's rule: our served projection vs ESPN's weekly projection

The following all carry this comparison:

- the headline
- the top-level `verdict`
- the governance record
- the sync_log detail the Coach reads

The season average stays as a pre-registered **floor check**, reported beside the verdict and
never as the verdict.

1. **The plan names the rule.**
   - PLAN-ITEMS-1-25.md:49 and WORK-QUEUE.md:231 both name "start highest projection".
   - In Nick's five ESPN leagues, the projection a manager starts by is ESPN's. Addendum 1 §3
     says so itself: "The projection a manager actually sees is ESPN's."
2. **The season average is not that rule.**
   - Prereg §1 re-read the plan as "the dumbest projection a manager has". In the plan,
     "dumb" describes the rule (start whoever is projected higher, without thinking), not the
     quality of the projection.
   - A season-to-date average is a naive forecast. It is the minimum bar of Hyndman & Koehler
     (2006) and a legitimate floor.
   - It is also the weaker bar: our projection clears it (G1-G3), while ESPN's projection beats
     ours in every measured variant of W2 (§2.2-2.3).
3. **Passing the weaker bar cannot establish the stronger one.** The average verdict can never
   say C12 is met.

**Was the average pre-registered as the gating rule? Yes.**

- Prereg §1 (:26-28): "**The dumb rule (β).** Start the player with the higher
  **season-to-date PPR average** ...".
- Prereg §8 (:138-152): "Ship rule (verdict the page shows)"; `beats_dumb` = G1 ∧ G2 ∧ G3 ∧ G4.
- Addendum 1 (:6-7): "The ship rule (G1-G4 and the verdict table, prereg §8) is
  **unchanged**." Of arm B (:71): "**Not part of the verdict.**"

**The same files anticipated the swap and gave it to the Auditor.**

- Prereg §1 (:30-33): "Why not ESPN's weekly projection as β: `espn_player_market_weekly`
  holds 2026 week 2 only ... so no past season can be graded on it. The gate interface is
  baseline-agnostic; ESPN's projection can be plugged in as β once weeks accumulate (named
  follow-up)."
- Addendum 1 §3 (:71-73): "Whether arm B should replace the season average as the gate's dumb
  rule once it holds enough weeks is a ruling for the Independent Auditor."
- The line "the prereg forbids swapping the gating rule after the fact" (tdd.md:32;
  WORK-QUEUE.md:602) is **not in either prereg file**. `grep -n -i -E 'swap|after the fact'`
  over both finds only addendum :44, which is about arm A's policy value. The line states the
  general principle; (c) deals with that.
- Rule 2 asks for pre-registration "with the Auditor". This prereg was committed but not filed
  (addendum 1 §5). Filing is the step at which the β substitution would have been caught before
  the run.

### (b) What the page must show

**A green "beats" headline cannot stand while the plan's own rule has not been shown beaten.**
The headline answers the plan's question from the plan-rule verdict. Every other result sits
below it, labelled with the rule it used.

**The current page overstates.**

- Its heading, "Does our projection beat the dumb rule?", is singular, so a reader takes it to
  mean the plan's rule.
- It answers with a green "Beats" and the words "in past seasons and this one", about a rule
  the plan did not name.
- Naming the average on the chip (`95952b44`) does not fix this. The question, the colour and
  the position still say "C12 passed".
- The Coach and the governance record carry the same `beats_dumb` / `promotion_eligible` with no
  label at all (§2.4).

**What the page must show on today's data** (direction only, rule 3):

1. **Heading.** It names the plan's rule, e.g. "Does our projection beat ESPN's projection?"
   with a line "The plan's dumb rule: start whoever ESPN projects higher."
2. **Chip.** Amber, "Not shown to beat ESPN's projection".
3. **Line under the chip.** "In the one week measured (2026 week 2), ESPN's pick scored more.
   ESPN's number was read at lineup lock, after news our projection did not have, which
   favours ESPN. Until this passes, treat our start/sit calls as no better than ESPN's
   projection."
4. **The average check, below.** Plain text, no chip, no green: 'Weaker check, set before the
   numbers: against "start the higher season average", our pick scored more in 2024-2025 and
   in 2026 week 2 replayed.' If this floor ever fails, it becomes a rose warning line.
5. **Lost weeks.** All four groups stay, with ESPN's first.
6. **Green (emerald).** It appears only when the plan-rule verdict is `beats_dumb`.

### (c) What must change before merge

**Handling "no swapping after the fact":**

1. **What the principle is for.** It stops a builder from picking, after seeing the data, the
   comparison that flatters the model. This change goes the other way: to the harder rule, one
   fixed by Nick's plan text (2026-09-21) before the prereg existed (2026-09-22 16:20). No
   threshold, window or pair rule of H1 changes.
2. **H1 keeps running as registered.** It is reported under its true name, `average_check`
   (value `beats_dumb` today). This ruling changes what H1 answers (a floor check, not C12). It
   changes no number.
3. **The plan-rule verdict is new, and it gets its own filed prereg (addendum 2).** The filing
   must come before any outcome it gates on that is not already disclosed here, i.e. before week
   3's first kickoff, 2026-09-25 00:15Z.
4. **Week 2 stays in.** Arm B's design was registered blind (17:18:46) before it was computed
   (about 17:30). Dropping a known-unfavourable week after seeing it is the flattering move the
   principle forbids. Keeping it is the conservative choice.

**Acceptance items** (the fixer's work is about half a day):

| # | Change | Where | Proof |
|---|---|---|---|
| A1 | Top-level `verdict` is the **plan-rule verdict** | `start-sit-gate.js` `runStartSitGate` (:442) | RED: a fixture with G1-G4 all true and `served.vs_espn` `dumb_ahead` gives `verdict !== 'beats_dumb'` |
| A2 | H1 kept unchanged as `average_check {verdict, gates: G1-G4, rule, prereg}`. It never fills the top-level `verdict` | same | Existing G1-G4 fixture tests re-pointed to `average_check`. On this ruling's copy it still reads `beats_dumb`, with G1 0.7958, G2 0.9566, G3 0.5256, G4 2.7923 |
| A3 | `plan_rule {verdict, reason, source ('espn_at_lock' \| 'espn_same_cutoff'), weeks_graded, direction, gates, mde80, prereg}`, with the states from addendum 2 | same | One fixture per state: 1 at-lock week, ESPN ahead → `not_shown` (never `loses_to_dumb`); 4 same-cutoff weeks with CI upper < 0 → `loses_to_dumb`; 4 passing weeks → `beats_dumb`; 3 passing weeks → `not_shown` (too few weeks) |
| A4 | The plan-rule gate is added to the gates passed to `recordGateAudit` | `refreshStartSitGate` (:459) | RED: the A1 fixture stores `model_gate_audits.verdict === 'blocked'` |
| A5 | The Coach-readable detail carries `verdict` = plan rule and `average_verdict` = H1 | `refreshStartSitGate` (:463) | RED: the A1 fixture gives `detail.verdict !== 'beats_dumb'` and `detail.average_verdict === 'beats_dumb'` |
| A6 | Panel per §3(b). Texts renamed: `BASELINE_TEXT` "The dumb rule:" becomes the weaker check; `ESPN_BASELINE_TEXT` becomes "The plan's dumb rule". The `JOBS.start_sit_gate` label and the Lineup.tsx:259 comment name ESPN's projection first | `StartSitGate.tsx`; `start-sit-gate.js:82-84`; `scheduler.js`; `Lineup.tsx` | Render test on the real result shape (this ruling's `gate-run-audit.out`) finds no `bg-emerald` and no "Beats", and ESPN is named in the heading and chip. The test fails on `b97d5ea2`, which renders 1 emerald chip. The rule-3 digit grep still finds nothing |
| A7 | Week-clustered CI is `null`, with a reason, below 2 week clusters | `baseline-gate.js` `gradeDecisions` | RED: a one-week fixture gives `ci90.week === null` (it is [x, x] today) |
| A8 | Prereg addendum 2 for the plan-rule verdict (contents below) | `docs/evidence/2026-09-22/start-sit-baseline-gate-prereg-addendum-2.md` | Committed before 2026-09-25 00:15Z. The coordinator logs it in WORKLOG.jsonl (unit C-01, sha, commit time) |
| A9 | Evidence revision 4 | `docs/tdd/2026-09-22-start-sit-baseline-gate.tdd.md` | See the A9 list below |

A9 in full:

- §0c cites this ruling.
- The tdd.md:32 sentence is corrected. The accurate statement: prereg §1 names ESPN as the
  follow-up rule, addendum 1 §3 gives the swap to the Auditor, and this ruling makes it.
- RED/GREEN shas are given with the failing assertion inline (rule 7).
- A sweep kills the call-site mutant "top-level `verdict` ← `average_check.verdict`", and
  includes a not-applied control.
- One gate run on a sha-recorded copy, plus the real-panel render on the fix head.

**What addendum 2 must contain (A8):**

1. **What was seen before filing.**
   - Arm B for W2 and its variants (§2.2-2.3 here).
   - RL-1-1's W2 numbers.
   - W3 outcomes: unseen.
2. **β.** ESPN's weekly projection, with the design exactly as in addendum 1 §3:
   - population and one value per player-week
   - both players at or above 8.0
   - pairs, grading and sign convention
   - player-clustered pigeonhole 90% CI, 2,000 draws, seed 1
3. **Window.**
   - Every served week from 2026 W2 on, pooled, with W2 included.
   - The served model's identity is recorded per week.
   - A change of served model (S-03 at W5) is reported as its own sub-window beside the pooled
     one.
4. **Sources.**
   - "At lock" (`league_roster_snapshots` final rows) favours ESPN, so it can support
     `beats_dumb` (conservative) but never `loses_to_dumb`.
   - "Same cutoff" means ESPN captured alongside our snapshot (RL-1-1), or our served number
     captured at lock (S-12).
   - Once 4 or more same-cutoff weeks exist, they alone decide the verdict. Until then the
     at-lock window decides.
5. **Pass.**
   - Pooled points player-CI lower bound > 0 **and** win-rate player-CI lower bound > 0.5,
     over at least 4 graded weeks.
   - Any week-clustered criterion applies only at a cluster count its own null calibration
     supports, calibrated the way evidence §4 did.
6. **Loss.** Same-cutoff pooled points player-CI upper bound < 0, over at least 4 weeks.
7. **Otherwise `not_shown`.**
   - It carries a `reason`: too few weeks, ESPN ahead at lock only, or not distinguishable.
   - It also carries the direction, weeks graded and MDE.
8. **Chip wording for every state,** as in §3(b).

### (d) File scope

- **Granted** (§12, :601): `scripts/refresh-live-data.mjs` (+6) and
  `test/refresh-loop-steps.test.js` (+16).
- **Covered by the row** (:231, "new gate file, scheduler job, Lineup panel"):
  - the new gate, route, panel and script files, 5 new test files, and 3 docs
  - `server/services/scheduler.js` (+21/−1). F-04, A-23 and B-17 also edit this file, so expect
    a merge there (disclosed in §7.12).
  - `client/src/pages/Lineup.tsx` (+5)
- **Not covered: `server/index.js` (+4: import at :55, mount at :144).**
  - The file is allocated to Scheduler (gridiron-file-allocation.md:14).
  - It is not named in the row or in the grant.
  - Precedent: Coach holds an explicit "one /api/coach mount line in server/index.js" grant
    (same file, :18).
  - Fix: a one-line coordinator grant. No code change.
- **The acceptance items stay inside these files,** plus addendum 2 (the unit's own
  docs/evidence). RL-1-1's capture files (migration 071 and the rest) are not C-01's: C-01 reads
  that capture once it lands.

## 4. The other rulings the skeptics listed (c01-held.json)

- **Rule 2, a prereg committed but not filed.**
  - The timing requirement is met (§2.1). The filing requirement is not.
  - Accepted for H1's scope by this ruling. The git timestamps provide the tamper-evident
    record that filing exists to provide.
  - The missing design review is what let a stand-in become the headline.
  - Addendum 2 must be filed (A8).
- **The two-factor (pigeonhole) player CI: accepted as the primary interval.**
  - My independent implementation reproduces it within bootstrap noise (§2.3).
  - It is conservative by construction: 1.652x on a fixture with no recurring players, against
    Owen's √3 ≈ 1.73 (evidence §7.4, C).
  - The null calibration gave 1/40 false G1 passes against a nominal 2/40 (evidence §4, C).
  - Below 2 week clusters the week CI must be null (A7).
- **Rule 3 against the row's "win rate + points, CI".**
  - Rule 3 governs what Nick sees: direction only.
  - The row is satisfied by the stored evidence and `GET /api/gates/start-sit`.
  - Today no Nick-facing reader of that route shows magnitudes. The panel reads directions.
    `grep -rn model_gate_audits server/services/coach` finds 0 lines; the control, `sync_log`
    in the same grep, finds catalog.js:337.
  - Any future Nick-facing reader of the route must use directions only.

## 5. What this means for other units

1. **RL-1-1.**
   - Its same-cutoff ESPN capture (migration 071, which needs Nick's N9 word) is the data C-01
     needs to ever return a clean loss. Without it the gate can only pass or stay `not_shown`.
   - Its `consensus-gate.js` and the /lineup `consensus_gate` would be a second producer of
     "ours vs ESPN at start/sit". They must feed C-01's gate instead: one number, one producer.
     This matches the assessor's "do not redefine C-01" (LOOP-LOG.md:315).
   - Recommendation: the coordinator puts N9 to Nick now, for C12's sake, rather than after
     W3-W8.
2. **S-02 and S-03 (due before W5, 2026-10-08).**
   - S-02 chooses among our own constructions on 2025. It may claim "beats our other arms",
     never "beats ESPN or consensus". The only evidence on that question points the other way
     (§2.3, §2.5).
   - S-02 gets no consensus arm, because that would open 2025 (LOOP-LOG.md:316).
   - S-03's served W5 model becomes a new sub-window in the plan-rule gate (A8.3).
3. **C-10** (its row says it reads this gate). It must read `plan_rule`, never `average_check`.
4. **C-02 and C-03** (waivers vs "add highest projected FA", trades vs "offer fair value"; both
   share `baseline-gate.js`).
   - Same principle: the verdict gates on the plan's named rule.
   - A stand-in may be pre-registered only as a floor.
   - The prereg is filed with the Auditor before any number is run, not only committed.
5. **HX-01** (WORK-QUEUE.md:609; Nick at 7:20 PM, "we should've been testing on historical
   data").
   - It can give the plan-rule gate a past window: ESPN past-season projections for Nick's
     leagues, through the app's own client.
   - ECR stays benchmark-only for licence reasons (LOOP-LOG.md:317-321).
   - That past window needs its own filed prereg, and 2025 stays out.
6. **Coordinator.**
   - After merge, C12's start/sit line (WORK-QUEUE.md:76) stays **partial: "instrument live;
     plan bar not shown met"** until the plan-rule verdict reads `beats_dumb`.
   - Nothing in the app switches on this verdict (prereg §8).

## 6. Housekeeping

- **Outputs kept in `$A`:** `gate-run-audit.out` and `.err`, `gate-run-audit.json`,
  `indep_arm_b.py` and `.out`, `espn_moved.py` and `.out`, `render-audit.mjs` and `.out`.
- **Audit worktree:** removed after this ruling (`git worktree remove`). My DB copy was deleted.
- **The C-01 worktree was not touched.** The Auditor is done with `.local-db/data-b.sqlite` and
  `.local-db/data.sqlite`, so the owner may delete them (the builder's open question 4).
