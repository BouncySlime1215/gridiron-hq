# RL-6-3 (PR #192) independent audit ruling: CLEARED

Auditor: critical unit, 2026-09-23. Tree audited: `origin/claude/local-rl-6-3-trade-impact-paired-seeds` at `1601d97e` (merge of origin/main `4773401a`), in a detached worktree (removed after). Control tree for "old" numbers: `origin/main:server/services/season-sim.js` copied beside the new one as `season-sim-main.js` (same deps; new `correlatedSampler`/`correlatedNormals` are backward compatible with no keys).

Data: APFS clone (`cp -c`) of `~/gridiron-local/data.sqlite` + `-wal` (the WAL held 18 MB of committed pages past the main file; copied and checkpointed). Leagues read with `SELECT *` only inside the app's own `row()`; the sqlite3 listing selected id/name/counts only, never `espn_s2`/`swid`. One clone per league so the module-load migration did not race (it did on the first attempt: `UNIQUE constraint failed: schema_migrations.name`).

Env for every run: `GRIDIRON_DB_PATH=<clone> SCHEDULER_DISABLED=1 GRIDIRON_DB_INTEGRITY_CHECK=off NODE_OPTIONS='--import ./test/offline-guard.mjs'`. Script: session scratchpad `audit.mjs <league> 400 12` (5 leagues in parallel, 20-22 min each).

## Verdicts by item

| Item | Result |
|---|---|
| (a) no-op reorder gives exactly 0 | PASS, 5/5 leagues (old code: 1.75-5.75 pp) |
| (b) paired SE vs unpaired spread; unbiased | PASS: reported SE matches or over-covers the seed-to-seed sd (ratio 1.00-1.76, never under); paired sd is 0.45-0.81x the unpaired sd (variance cut 1.5-5x); paired mean within 0.83 SE of the unpaired mean on all 5 |
| (c) greying threshold stated, matches SE | PASS: `TRADE_DELTA_NOISE_SE = 2`; flag = `abs(delta) > 2*SE`; UI prints `±2*SE` and greys when the flag is false |
| (d) league-rules is the bracket source | PASS: `league-rules.js` not in the diff; `season-sim.js:291 rules = leagueRules(lg)`, `:441 playBracket(field, rules.schedule, ...)` |
| (e) no unpaired producer of title-odds deltas | PASS: 3 callers, all through `tradeImpact` with no own seed/scoring |

## (a) Null reorder: every roster listed in reverse, seed 7, 400 runs, max abs title-odds change over all teams

| League | teams | NEW max abs delta | OLD (origin/main) max abs delta |
|---|---|---|---|
| L1 | 8 | 0.00 pp | 1.75 pp |
| L2 | 10 | 0.00 pp | 4.25 pp |
| L3 | 8 | 0.00 pp | 2.00 pp |
| L4 | 10 | 0.00 pp | 3.50 pp |
| L5 | 10 | 0.00 pp | 5.75 pp |

Zero is exact (`Math.max(...abs(perm - base)) === 0`), not "within 1e-9". The OLD column is the known-nonzero control.

## (b) One real 1-for-1 per league (my best RB/WR for the partner's best RB/WR), 12 seeds x 400 runs, `me.title_delta`

Columns: reported = mean of the PR's published `title_delta_se`; paired sd = sd of the NEW `tradeImpact` delta across the 12 seeds; old sd = same for the origin/main `tradeImpact`; unpaired sd = sd of (independent-seed after) - (independent-seed before).

| League | deal | reported SE | paired sd | old-pairing sd | unpaired sd | mean paired | mean old | mean unpaired | (paired - unpaired) / SE of gap |
|---|---|---|---|---|---|---|---|---|---|
| L1 | Achane -> Smith-Njigba | 2.66 | 2.62 | 2.81 | 3.22 | +8.90 | +5.46 | +8.87 | 0.02 |
| L2 | B. Robinson -> Swift | 1.65 | 1.60 | 1.29 | 2.22 | -4.73 | -5.85 | -4.98 | 0.32 |
| L3 | J. Taylor -> McCaffrey | 0.69 | 0.69 | 0.48 | 1.13 | -0.12 | +0.15 | -0.19 | 0.16 |
| L4 | N. Collins -> Smith-Njigba | 0.97 | 0.55 | 1.01 | 1.09 | +2.90 | +2.54 | +3.19 | -0.83 |
| L5 | J. Taylor -> N. Collins | 1.99 | 1.27 | 3.29 | 2.83 | -6.27 | -6.46 | -6.02 | -0.28 |

All in pp. Reading:

- SE calibration: reported/paired-sd = 1.02, 1.03, 1.00, 1.76, 1.57. The published SE never under-states the seed-to-seed spread; on L4/L5 it is conservative (the paired-difference SE counts run-to-run noise that partly cancels across seeds). Matches the PR's own finding (median ratio 1.18, IQR 0.81-1.73).
- Paired vs truly unpaired: variance ratio (unpaired/paired)^2 = 1.5, 1.9, 2.7, 3.9, 5.0. Real on 5/5 and large on 3/5. "Far smaller" holds against an unpaired design; against the OLD code's seed-to-seed sd it is a wash (old 2.81/1.29/0.48/1.01/3.29 vs new 2.62/1.60/0.69/0.55/1.27), because the old pairing was partial, not absent. The PR's evidence file says this in so many words (section 6b, "cannot claim a 4x variance cut"); it does not over-claim.
- Unbiased: the paired mean sits within 0.83 SE of the independent-arms mean on all five leagues (a paired design changes variance, not expectation, and that is what shows).
- Extra finding in the PR's favour: on L1 the OLD paired estimate was +5.46 pp against +8.87 pp for independent arms (gap 3.4 pp, about 2.8 SE of the gap). The position-keyed draws were not only noisier; on that league they shifted the point estimate. The new estimator does not (+8.90).

## (c) Threshold

`server/services/season-sim.js`: `export const TRADE_DELTA_NOISE_SE = 2;` and `title_delta_clears_noise: title_delta_se != null && Math.abs(title_delta) > TRADE_DELTA_NOISE_SE * title_delta_se` (same for playoff). `TradeLab.tsx` greys (`text-slate-400`, "within noise") when the flag is not `true` and prints `±(2 * title_delta_se * 100)`; `TradeCard.tsx` mutes on the same flag and prints the same band; `title-odds-trades.js` note text says "±2 standard errors". `trade-verify.js#contradictionBar` uses `SE_FACTOR * pairedSe` when the SE is published. One rule, one constant.

## (d) Bracket source

`git diff origin/main...HEAD --stat -- server/services/league-rules.js` is empty. `season-sim.js` line 291 `const rules = leagueRules(lg)`, line 441 `playBracket(field, rules.schedule, ...)`, playoff teams/median game/seeding all off `rules`. `test/league-rules-bracket-sim.test.js` 4/4 and `test/league-rules.test.js` 15/15 on this tree.

## (e) Producers

`grep -rn "tradeImpact\|simulateSeason" server client/src` (excluding season-sim.js itself): `title-odds-trades.js:68`, `routes/model.js:471`, `routes/trades.js:1156` call `tradeImpact` and none passes `seed:` or `scoring:` (the PR's test #12 pins this at source level). `routes/model.js:458` (`/simulate`) and `trade-engine.js:1380` (horizon playoff odds) are single-arm `simulateSeason` calls, not deltas. No second producer of `title_delta`.

## Other checks on this tree

- `test/rl-6-3-trade-impact-paired.test.js` 12/12. Neighbours: trade-verify 24/24, b-01-real-record-odds 8/8, trade-engine-correctness 15/15, wiring-map 90/90.
- `npm run typecheck` and `npm run lint` exit 0 (the PR's evidence file said the client was not type-checked there).
- No import cycle introduced: `season-sim.js` now imports `SENSE_CHECK_SIM_RUNS` from `trade-verify.js`, which has no imports.
- `simulateSeason` draws exactly one number from the shared stream (`world`); every other draw is keyed `(world, player, week[, run])`. Both arms are inside the same `withRandomSeed`, so `world` is equal.

## Named residuals (none blocking)

1. Variance cut vs the OLD code is small on this tree (old pairing was partial). The user-visible change is the exact-zero reorder, a calibrated SE, one seed per league sync across tab/card/sense-check, and the L1-style bias going away.
2. Published SE is conservative on 2/5 leagues (ratio 1.6-1.8): some real deltas will be greyed that a seed-level test would call real. Safe direction.
3. Cost: the tab now runs 1,200 runs per deal (was 800); a 3-deal shortlist took 18-44 s first load in the PR's own measurement.

## Ruling

CLEARED. Commands to reproduce are above (script in the audit session's scratchpad; not committed).
