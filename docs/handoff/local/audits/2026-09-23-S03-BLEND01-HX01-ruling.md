# Ruling: S-03 (#166), BLEND-01 (#164), HX-01 (#165)

**Independent Auditor, 2026-09-23 ~03:00 EDT.** Charter: `gridiron-merge-gate-v2/SKILL.md` §1-6 and
`VERIFICATION-RULES.md`. Every number is from a **local copy, not production**. Worktrees:
`wt/audit-166` (d4739e83), `wt/audit-164` (4ad2573e; later merged with d4739e83 locally as
fc8610b5, tree 60138712, for the served-number check), `wt/audit-165` (6555e85e). All three
were removed after the audit.

Labels: **T** = reproduced by me in this audit. **C** = cited, not reproduced.

## Summary

| PR | Ruling | Blocking reason (the only ones) |
|---|---|---|
| #166 S-03 | **HELD** | Migration 072 has no recorded Nick word (hard rule §5 / WQ rule 12), and the body says "no migrations". Everything else passes. |
| #164 BLEND-01 | **HELD** | It carries S-03 at 8ddebcd8 plus migration 072, so it cannot merge before #166. One holdout look (the claims skeptic's re-run of 2026 W2) has no ledger row. The held-off served number checks out: 0 diffs. |
| #165 HX-01 | **HELD** | FantasyPros licence: Nick has not yet said yes or no (the PR's own open item 1) on committing FantasyPros-derived aggregates to a PUBLIC repo. The pre-registration itself quotes the FantasyPros ToS "no republish" clause. Plus one label fix (2022, below). |

Merge order once cleared: #166, then #164 (after it merges main that contains #166), then #165 (independent).

---

## #166 S-03: HELD

### (a) Pre-registration predates every result. PASS (T)
```
4dec70de 2026-09-22 19:40:02 docs: S-03 audit and pre-registration of the walk-forward grade, before any number
153669da 2026-09-22 20:02:08 feat: S-03 walk-forward library and runner (GREEN)   <- the run that produced the numbers
ab246d93 2026-09-22 20:07:50 ...  first commit of weekly-construction-walk-forward-output.json
293dca58 2026-09-22 20:49:12 docs: S-03 result and evidence
$ git diff 4dec70de HEAD -- docs/evidence/2026-09-22/weekly-construction-walk-forward-preregistration.md | wc -l
0
```

### (b) Holdout looks recorded. PASS (T)
`HOLDOUT-LEDGER.md` rows F001-F004 (8ddebcd8). Non-blocking: F001's "shipped" column says
"yes (coordinator on in both windows)". That contradicts addendum 1 item 5, where production
has no promoted fit. Relabel it.

### (c) One headline number reproduced. PASS (T)
Claim: fit 7, week 3, correction S1 minus structural: n 1196, mean -0.636, 1195 exactly -0.636.
```
sqlite3 -readonly ~/gridiron-local/data.sqlite ".backup 'wt/audit-166/.local-db/data.sqlite'"
npm run db:migrate                      # copy only -> "Applied: 072_fantasy_coordinator_fit_promotion"
node scripts/promote-fantasy-coordinator-fit.mjs --id 7 --evidence docs/evidence/2026-09-22/weekly-construction-walk-forward-output.json
  [promote] windows {"2-4":"on","5-17":"on"}
  [promote] reproduction: 23334 examples, max coefficient difference 0
node scripts/weekly-construction-walk-forward.mjs --served-identity --week 3 --out <scratch>/audit166-w3.json
  correction_s1_minus_structural: {"n":1196,"mean":-0.636,"sd":0,"min":-0.636,"max":-0.635,"most_common":{"value":-0.636,"count":1195}}
```
Field-by-field against the committed `-w3.json`, only two fields differ: `promoted_at`, and
`no_game` 673 vs 674. The copy is newer, so one asset now has a game.

### (d) Served number after merge. PASS (T + C)
Production has no promoted fit, and neither does the live local DB (`PRAGMA table_info(fantasy_coordinator_fits)`
has no `promoted` column today). So after merge, and after migration 072 runs at boot, the
coordinator is **off**. The served number is ensemble A × game factor × p, with **the lift off**
(`waiver-brain.js:183` `BETTING_LINE_LIFT.on: false`; `vegasLift` returns multiplier 1). The PR
states this construction. Tests that pin the unpromoted path passed in my run: "with only an
unpromoted candidate, the player page serves the ensemble", and "an unpromoted candidate is
never served, however new".

### (e) Tests. PASS (T)
```
node --test test/served-weekly-construction.test.js test/fantasy-coordinator.test.js \
  test/weekly-construction-walk-forward.test.js test/route-deletion-impact.test.js
ℹ tests 55  ℹ pass 54  ℹ fail 1
✖ a call site inside an already-unreached function is a survivor, not a casualty
/tmp/gate-S-03.log: tests 4268 / pass 4226 / fail 1 / skip 41; only failure test/route-deletion-impact.test.js:155
CI (Node 22) on d4739e83: pass
```

### Blocking: migration 072 without Nick's word
`server/migrations/072_fantasy_coordinator_fit_promotion.js` adds two columns with
`ALTER TABLE fantasy_coordinator_fits ADD COLUMN promoted ...` and `ADD COLUMN promotion_json`.
The server runs it at boot. The skill's §5 and WQ rule 12 both say: no migrations without
Nick's own word. `grep -rn 072` over WORK-QUEUE.md, the handoff logs and WORKLOG.jsonl finds
no approval. The precedent is #94/067, which was cleared only as "confirmed under Nick's F4 GO".
The PR body's §5 says "No secrets, no migrations", which is false. #164's body gets it right:
"S-03's additive 072 ... needs S-03's own approval path".
**To clear:** record Nick's yes for 072 (additive, 2 columns, `down` drops only those two), and
correct the body's §5. No code change is needed.

### Ruling on the departure from S-02 amendment 1 (requested by the PR)
1. **Lift off: upheld.** Nick's rule (b) outranks a unit amendment. The lift never passed a
   pre-registered rule: it failed 2025 in both windows and 2023-2024 in all four
   season-windows, with MDE80 0.015-0.040. So "on" was never earned. The only "pass" is amendment
   1 §4's served-chain diagnostic, which is one season and offsets the pooled-p bias (0.708 for
   94.5% players). That is not a lift verdict. **Cost, disclosed and accepted:** the lift's
   start/sit calls win 0.52-0.565 of disagreements in 6 of 6 season-windows, 0.07-0.43 pp of
   pair accuracy. The follow-up is a pre-registered mean-preserving (rank-only) lift.
2. **Coordinator weeks 2-4: may be promoted**, after production's own forward check (addendum
   1 item 5). MAE and squared error agree in both walk-forward seasons (ΔMSE -2.86, -2.17,
   intervals exclude 0), and ΔSpearman is positive (+0.0235, +0.0105). That is more than a
   level shift, so amendment 1 §4's level objection does not reach this window. Guess: the
   pooled-p bias does not reverse a gain this size.
3. **Coordinator weeks 5-17: stays off.** The pass is median-only: ΔMSE intervals include 0, the
   correction is a flat -0.64, and S1 loses to B/D/S2 with a start/sit win rate of 0.490-0.491
   against A. Label it "unconfirmed: median-only pass" until A-11 or the joint S-02/S-04 grade
   decides.
4. **Condition on any promotion (not on merge):** as written, `scripts/promote-fantasy-coordinator-fit.mjs`
   takes its windows from the evidence's decisions. On my copy it printed
   `windows {"2-4":"on","5-17":"on"}`. So TDD §7's operational step would switch weeks 5-17 on,
   which contradicts item 3. Before anyone runs §7, the script needs a windows override (a RED
   test first: `--windows 2-4=on,5-17=off` stores exactly that). `promoteFantasyCoordinatorFit`
   already accepts per-window on/off. Until then, do not run §7.

---

## #164 BLEND-01: HELD

### (a) Pre-registration first. PASS (T)
```
7c443485 2026-09-22 20:21:29 docs: BLEND-01 ... pre-registration ..., before any number
91fdf3f4 2026-09-22 21:08:05 first commit of weekly-blend-tournament-output.json
$ git diff 7c443485 4ad2573e -- docs/evidence/2026-09-22/weekly-blend-tournament-preregistration.md | wc -l
0
```

### (b) Holdout looks. PARTIAL (T)
F005-F009 cover BLEND-01's own 2026 W2 runs. The PR's open item 2 says the claims skeptic
re-spent 2026 W2 and that look has no row. **To clear:** add the F-row.

### (c) Headline reproduced with independent code. PASS (T)
Claim: 2023-2024, 10,338 rows, 108,905 pairs; pair accuracy ESPN 0.683, ours 0.6359, diff +0.0471.
I wrote my own Python pair enumeration over the rows that the committed `--assemble` wrote
(`wt/BLEND-01/.local-db/blend-rows/rows-2023,2024.ndjson`). It uses the same tie rule as
`pairScore`, lib:171, and does not touch 2026:
```
rows 10442 with_espn 10338
pairs 108905 {'ours': 0.6359, 'espn': 0.683} diff 0.0471
```
Scope: this reproduces the grade from the assembled rows. It does not re-run the assembly.

### (d) Served number after merge: ours, held off. PASS (T)
I merged S-03's final head into #164 locally (fc8610b5, tree 60138712, equal to
`git merge-tree d4739e83 4ad2573e`). Then I ran `round2/dump.mjs` on the same DB copy
(unpromoted, which is production's state) for d4739e83 and for the merged tree:
```
league 0..5: n 8640, cw_nonzero 526, current_week_ppg diffs vs #166 = 0 (all 6 leagues)
known-nonzero control: ESPN present 194, ESPN != served 157 per league
ctx {"on": false, "candidate": "espn", "verdict": "held (waiver_ungraded, espn_zero_reads_as_missing, labels_describe_ours, s03_identity)"}
```
So ESPN wins the tournament, stays off, and our number is served, byte-identical to #166.

### (e) Tests. PASS (C)
`/tmp/gate-BLEND-01.log`: 4284 / 4242 / fail 1 / skip 41. The only failure is route-deletion-impact.test.js:155. CI green on 4ad2573e.

### Blocking
1. **Stacked on a held PR.** The branch contains 8ddebcd8, which brings migration 072 and
   S-03's served change, but not S-03's fix round. Merging it first would ship 072 and S-03 by
   the back door. **To clear:** #166 merges, #164 merges main, and the dump check is re-run on
   that head (0 diffs expected; my merged-tree run says 0).
2. Holdout F-row for the skeptic's 2026 W2 re-spend.

Non-blocking: the output JSON `decision` says `"on": true, "verdict": "shipped"`, but serving
is held by `SERVING_HOLDS`. Add a one-line note to the evidence file so nobody quotes
"shipped".

---

## #165 HX-01: HELD

### (a) Pre-registration first. PASS (T)
```
b635345b 2026-09-22 19:35:36 docs: pre-register HX-01 ..., before any number
5cfc289f 2026-09-22 19:40:42 test: HX-01 ... (RED)
c100bf91 2026-09-22 20:06:01 docs: HX-01 result  (first commit of the output JSON)
$ git diff b635345b HEAD -- docs/evidence/2026-09-22/historical-consensus-head-to-head-preregistration.md | wc -l
0
```

### (b) Holdout looks. PASS (T)
F001-F015 are in the ledger, and 2025 is never opened (the runner refuses it). My reproduction
below re-graded 2026 W2 on the builder's copy. **That look needs a ledger row** (it is mine,
so the coordinator adds it; one line).

### (c) Headline reproduced. PASS (T), on the pushed head
The committed output was measured on d0aa396b. The builder said a re-run on the head had not
been shown and called that a guess. I re-ran `--full` on **6555e85e (tree 4d82aea8)**, using
an APFS clone (`cp -c`) of the builder's `.local-db` (DB, FantasyPros CSV, id crosswalk).
It exited 0 after about 25 minutes at load 30-50.
```
results.primary.ours_vs_consensus.pooled.decisions:
mine      n 22659, win_rate 0.4217, points_per_decision -1.8558, player ci90 [-2.2242, -1.4686], win [0.4065, 0.4376]
committed n 22659, win_rate 0.4217, points_per_decision -1.8558, player ci90 [-2.2242, -1.4686], win [0.4065, 0.4376]
field-by-field diff of the whole output (tree, timestamps, database excluded): 0 differing fields
```
This closes the PR's open item 3. The builder can relabel the output's tree to 6555e85e, citing this run.

### (d) Served number. N/A
This is a study: no route, job or page. The diff against main is HX-01's 8 files. The C-01
commits in `git log` are the #160 squash already on main.

### (e) Tests. PASS (C)
`/tmp/gate-HX-01.log`: 4268 / 4226 / fail 1 / skip 41. The only failure is route-deletion-impact.test.js:155. CI green on 6555e85e.

### Blocking: licence
The repo is **PUBLIC** (`gh repo view --json visibility` returns PUBLIC). Pre-registration
§12 quotes the FantasyPros ToS, which bars republishing without written permission. It also
says the fleet licence rule refuses a source with a personal-use clause, and that whether
aggregates may be committed publicly "is Nick's call". The PR's open item 1 asks Nick for a
yes/no. The skill's §5 says to check the licence before measuring external data. Merging to
main adds these aggregates to the public default branch.
**To clear:** Nick's yes. If he says no, move the output JSON and ledger rows out of the public
repo, keep the scripts and tests, and have the evidence cite a local path.

### Ruling on the PR's question 2: does standing rule 3's k control cover the coordinator's training replay? **Yes.**
S-03's pre-registration (4dec70de §3) already treats it that way: "2021 examples would break
rule 3." HX-01 pre-registered examples from 2021, so its 2022 OURS arm uses a coordinator fit
**100% on k-uncontrolled rows** (`coordinator.rows_from_k_excluded_seasons`: 2022
5280/5280; 2023 47.9%; 2024 31.4%). The pre-registered result stands as computed, and the
direction holds. The post-hoc served recipe (from 2022), pooled 2023-2024, gives -1.6385
[-2.1543, -1.1561] against the pre-registered -1.6828, so consensus is ahead either way.
**Required label (to clear):** the title and summary must say 2022 is graded with a
coordinator trained on hand-set-k replays. They should quote 2023-2024 on the served recipe
next to the pooled -1.86. Future units start coordinator examples at 2022.
