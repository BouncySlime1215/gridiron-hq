# TM-09 (PR #183) Independent Auditor ruling — 2026-09-23

**RULING: CLEARED** (one owner-risk note on (d); two non-blocking notes)

Head audited: `5a8cb0ad` (merge of origin/main into claude/local-tm-09-market-prices); unit commits `9e405725..607a1c29`.
Detached worktree `~/gridiron-local/wt/audit-183` (removed after). Clone read only.

## (a) Prereg predates results — PASS
```
git log --reverse --format='%h %ad %s' --date=iso 3ac59fea..refs/audit/pr183 --no-merges
git log --format='%h %ad' 3ac59fea..refs/audit/pr183 -- <file>   # per file
```
- prereg `9e405725` 05:07:25, touched once, never edited after.
- first script + JSON `0e0553f2` 05:10:35; evidence `c7bf740e` 05:14:54; fix `fb0a4406` 05:24:27.
- Prereg contains sample counts only (15,977 / 12,814 / 4,702), no prices or errors.

## (b) No 2025 outcome in any value; re-run reproduces — PASS
Code read of `scripts/rnd/tm09_market_prices.py`:
- trades: SQL `where s.season <= ?` with `MAX_SEASON=2024`.
- EV curves + k: `rebuild_ev(C, A, FIT_SEASONS=(2021..2024))`; `win`, `raw`, isotonic fit and k-SSE loop only over `seasons`. `Values` uses the rebuilt `ev`/`kby`, never `A['EV']`/`A['K_BY_SEASON']`.
- ADP pools, points, kickoffs: indexed by the trade's own season only.
- replacement ppg: `team_seasons.repl_*` from `build_03_leagues.py:104` `repl_ppg(season, ...)`, same-season window (in-season hindsight, not 2025).

Re-run:
```
cd ~/gridiron-local/wt/audit-183
nice -n 10 /Library/Frameworks/Python.framework/Versions/3.12/bin/python3 scripts/rnd/tm09_market_prices.py --out-json $SCRATCH/tm09-rerun.json
cmp server/data/trade-market/tm09-market-prices.json $SCRATCH/tm09-rerun.json   # -> identical
```
- 31 s, exit 0. Output **byte-identical** to the committed JSON.
- counts: sides 12,814 -> observations 13,135; k=3 every season.
- H1: baseline MAE **1.7270** -> model MAE **1.6540**, gain 0.0730 [0.064, 0.083], win rate 0.560, `passed: true`. Matches the claim.
- Hype identity: 2,054 player_week rows, **0** with |hype-(price-value)| > 0.0006.

## (c) Aggregates only — PASS
- Top keys: meta, results, position_price_to_value, cells, player_weeks.
- cells: pos, week_bin, size_bin, n, price_to_value, median_hype.
- player_weeks: season, week, sleeper_id, pos, n, price, value, hype. min n = 3.
- Every key path scanned for user/owner/league/roster/name/display/avatar/trade_id/chain: only `n_chains`/`n_test_chains` (counts).
- No integer with 12+ digits in the file (no league/user ids).
- Sample: `{season:2021, week:1, sleeper_id:'1234', pos:'QB', n:3, price:0.334, value:0.84, hype:-0.506}`.

## (d) Sleeper terms — PASS ON OWNER DECISION, NOT ON THE TERMS
- There is no written "note" in `rnd/data/public-league-terms/`; only raw HTML. `sleeper-terms.html` is a 404 redirect stub; the real ToS is `sleeper-terms2.html` (Last Updated Aug 27, 2026).
- `sleeper-docs.html`: API "free to use for non-commercial purposes".
- `sleeper-terms2.html` forbids automated extraction or scraping "without the express written consent of Sleeper" and grants a personal, non-commercial licence. **The terms do not permit this.** Nothing in them allows publishing derived aggregates.
- What covers it: Nick's decision in WORK-QUEUE.md:316 (2026-09-22 ~20:10Z, "ignore - do it"; polite rate, aggregates only in the public repo). The PR follows that rule: aggregates only, n >= 3, no ids or names.
- Risk Nick owns: a public repo holding Sleeper-derived data. It is non-commercial today; if the app ever goes commercial, this table needs a Sleeper licence or removal.

## (e) No page shows hype — PASS
```
git diff --stat 3ac59fea HEAD -- client/        # empty
grep -rnE "/market/|tm09|marketForPlayer|trade-market" client/src   # only unrelated Leagues.tsx copy
grep -rniE "\bhype\b" client/src               # none
```
- The only change is `server/routes/trades.js`, which adds `GET /:leagueId/market/:playerId`. Its payload includes hype and `other_hype_producers`, but no client code calls it. S-19/AI-04 unification is still owed.

## Non-blocking notes
1. The brief says "14,882 real Sleeper trade sides", but the corpus and re-run give 12,814 eligible sides (15,977 total 2021-2024) and 13,135 observations. Fix the headline number wherever it is quoted.
2. The leave-target-season-out curves for 2021-2023 contain 2024 outcomes, and H1 trains on 2021-2023 and tests on 2024. The target is market price, not outcome, so the effect is likely small. The PR discloses it. The PR also reports (the audit did not re-check this) that a single global ratio (0.9086) matches the cell table (MAE 1.6513 vs 1.6540), so the H1 gain comes from one level, not from cell structure.

Cleanup: `git worktree remove ~/gridiron-local/wt/audit-183`; `git update-ref -d refs/audit/pr183`.
