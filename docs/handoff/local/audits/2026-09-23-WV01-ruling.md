# WV-01 (PR #176) Independent Auditor ruling — 2026-09-23

**Ruling: HELD on (e) only.** (a)-(d) pass. Head audited: `c009a4b4`. Fix is about 15 minutes (below), or a one-line decision by Nick.

## (a) Pre-registration predates results — PASS
- `git log --format='%h A:%ad C:%cd' --date=iso origin/main..c009a4b4`: prereg `c5f38e7f` 04:47:54 → RED `dac6afd1` 04:50:21 → GREEN 04:53 → results `3e8fc030` 04:58:28. Author = committer dates.
- `git diff c5f38e7f c009a4b4 -- docs/tdd/2026-09-23-wv-01-streaming-board.tdd.md | grep '^@@'` → one hunk `@@ -102,3 +102,152 @@`: pure append after section 2; the pre-registration text (rule, K=10, split, ship rule) is byte-unchanged.

## (b) Headline reproduced — PASS (exact)
```
git worktree add --detach /Users/nick_matta/gridiron-local/wt/audit-176 c009a4b4
cp -c /Users/nick_matta/gridiron-local/data.sqlite <wt>/.local-db/data.sqlite
GRIDIRON_DB_PATH=<wt>/.local-db/data.sqlite GRIDIRON_DB_INTEGRITY_CHECK=off SCHEDULER_DISABLED=1 \
  node docs/evidence/streaming-def-history.mjs \
  --nflverse /Users/nick_matta/Documents/GitHub/gridiron-hq/data/line-history/nflverse.sqlite
```
- Pooled 2022-2025 K=10: 57 swaps, **+2.86 [+0.93, +4.75]**, SE 0.97, MDE80 2.72. Dev +2.77 [+0.56, +4.88]; 2025 +3.14 [-0.50, +7.00].
- Chase baseline -0.48 [-2.49, +1.61]; win rate 67.4% of 72 weeks; K=0 +2.56, K=16 +2.64; vs drafted +2.55; mechanism +0.45 [+0.38, +0.52]; forward 2026 scored weeks [1], 0 swap-weeks. Ship rule PASS. Every number in the PR body and TDD doc matches.
- Script imports production `rankDefenses` and `linesFor` (no re-implementation).

## (c) No leakage — PASS
- 2022-2025 `game_lines` rows: all `source = nflverse`, no `closing_*` stored, so the ranker uses `implied_points` = total/2 - spread/2.
- Stored spread/total vs nflverse `games.spread_line/total_line` (the pre-kickoff consensus close), 2,038 joined rows: **0** total mismatches, **0** spread mismatches, **0** implied_points formula mismatches (attach query on the clone).
- Free-agent pool (`topK`) uses points from weeks < w only (week < 4: previous season). Held/added defense fixed before week w's outcome. Outcome-existence filter (`P(...) != null`) drops only teams with missing stats, not by value.
- Live path: a started game is ranked on its frozen pre-kickoff close and not offered (locked).

## (d) Holdout ledger — PASS
- L154 (+3.14 [-0.50, +7.00], 14 swaps, direction check), L155 (chase / win rate / slope, not gating), F001 (0 swap-weeks computable) match the rerun.
- Numbering: origin/main ends at L153, so L154/L155 are next; F001 is the first F row, columns match the L table.
- Forward-uncomputable confirmed independently: 2026 defensive counts exist for week 1 only in `stats_team_week`, `play_by_play`, `stats_player_week` (`player_stats_def` has none). Even with week 2 available, forward n would be 1 swap-week.

## (e) Card ships ON before a 2026 forward check — HELD
- `docs/evidence/STATS-METHOD.md` rule 5 (line 406) applies to "every unit's ship decision" (line 13): with no forward weeks the result "ships default-off, behind a named flag, labelled **unconfirmed forward**". It has no exemption for a zero-parameter ranking.
- The builder's argument has merit on the substance. The ranking is the market's own number, has no fitted parameters, is pre-registered, and matches an independent study (+1.94 on real managers' swaps; slope +0.45 vs +0.446). The overfitting risk rule 5 guards against is small here. But the card does more than show a line: it gives a swap suggestion and quotes "+2.9 (0.9 to 4.8)", and both are the tested result. Shipping it ON under a new label is an exemption the builder granted itself. Only Nick can grant one.
- Fix, pick one:
  1. **Builder (about 15 minutes):** gate the `/:leagueId/streams` card behind a named flag, default off (e.g. `WV01_STREAMING_BOARD`), and change the copy to "unconfirmed forward" (keep "Not yet checked on 2026 games" as the plain-English line under it). Rerun `test/streaming-board.test.js` plus one test that the card is hidden when the flag is off. Record F002 and turn the flag on once nflverse posts 2026 weeks 2+ and the forward direction holds.
  2. **Nick (1 line):** add a written exception to STATS-METHOD.md rule 5 for "rankings that display a market number with no fitted parameters", then this PR clears as-is.

## Nonblocking (carried, agree with PR)
- TDD doc section 1 names `/api/waivers/:leagueId/streams`; shipped route is `/api/trades/:leagueId/streams`.
- The card hardcodes "+2.9 [0.9, 4.8]" in `StreamingBoard.tsx`. It uses the study's scoring, not ESPN league scoring (no yards-allowed tiers).
- Surviving mutants: `team_id` passthrough (C1), `closing_total ?? total` one-sided branch (U3), `>=` vs `>` MIN_EDGE (M7).

No secrets or names were read or written. `leagues.espn_s2/swid` were never selected. Worktree and DB clone removed after the audit.
