# Integration card: #116 fix: symbol-reach misses namespace imports, so a reached symbol reads as dead

State: MERGED · range `d6d7bd5a38b58201910d778af8344f29c55c9f0d^..d6d7bd5a38b58201910d778af8344f29c55c9f0d` · generated 2026-09-22T20:17Z by integration-card.sh

## Files (3)
- docs/tdd/symbol-reach-namespace.tdd.md
- scripts/symbol-reach.mjs
- test/symbol-reach-two-counts.test.js

## Symbols touched and every caller on origin/main
- `importersOfSymbol` (defined: scripts/symbol-reach.mjs ) · 3 refs · callers: scripts/symbol-reach.mjs:251 scripts/symbol-reach.mjs:328 

## Routes touched → client pages
- no route definitions changed

## Tables
- written: 
- read: 

## Scheduler jobs and flags
- jobs: 
- env/flags: 
- migrations: 

## Intake verdict (filled by the integration intake agent)

1. **Upstream:** N/A — this is the producer. `importersOfSymbol` in `scripts/symbol-reach.mjs` now resolves `import * as ns`, `const ns = await import(...)`, and the `.catch()`-wrapped dynamic form, in addition to the named/destructured imports it already read (`git show d6d7bd5a -- scripts/symbol-reach.mjs`).
2. **Downstream:** clean. Every caller of `importersOfSymbol` is internal to `scripts/symbol-reach.mjs` itself (card: 3 refs, all same file) or a test (`docs/wiring/WIRING-MAP.md:1517-1518` lists it "imported only by a test"). `scripts/reach-grade.mjs`'s file-level graph is unaffected — its `classifyImportEdges` already counted namespace imports as file edges before this fix (commit message; confirmed no `reach-grade.mjs` changes in this diff), so the file-level `wired`/`unreached` counts in `docs/inventory/CONTRACT.md` do not move. The defect and the fix are both strictly symbol-level.
3. **Reach:** N/A, dev tooling only — no route/page.
4. **Links / stale docs:** `docs/tdd/symbol-reach.tdd.md:137-141` ("What this does NOT settle") still says *"A symbol reached through a namespace import, a re-export chain, or a computed property is invisible to [`importersOfSymbol`]"* — that first clause is now false for the plain `import * as ns` / dynamic-import case this PR fixed (`git grep -n "namespace import" docs/tdd/symbol-reach.tdd.md` → line 139). Re-export chains and computed properties remain real, unfixed blind spots per the diff. Separately, `scripts/wiring-map.mjs`'s own namespace-import blind spot (`docs/tdd/wiring-map-namespace-imports.tdd.md`) is a different tool/file and this PR does not touch it — do not conflate the two when following up.
5. **Plan:** no plan item closes or unblocks.
6. **Follow-ups:** INT-116-1 added below (doc correction only, no behavior risk).
