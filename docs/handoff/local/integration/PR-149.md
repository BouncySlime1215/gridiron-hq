# Integration card: #149 A read-only check that the live disk has room for the migration snapshot before we deploy

State: MERGED · range `18650f2359b6337fae34ebb487eafdf6dd508fcc^..18650f2359b6337fae34ebb487eafdf6dd508fcc` · generated 2026-09-22T21:26Z by integration-card.sh

## Files (1)
- .github/workflows/fly-preflight.yml

## Symbols touched and every caller on origin/main

## Routes touched → client pages
- no route definitions changed

## Tables
- written: 
- read: 

## Scheduler jobs and flags
- jobs: 
- env/flags: 
- migrations: 

## Intake verdict (filled by the integration intake agent)

1. **Upstream:** n/a — CI workflow only, no app-code producer.
2. **Downstream:** n/a.
3. **Reach:** n/a — a GitHub Actions workflow, not a page.
4. **Links:** n/a.
5. **Plan:** supports F-16's deploy-delta checklist / N2 (next deploy + brake).
6. **Follow-ups:** confirmed the reported root cause and that it's still unfixed. `gh run list --workflow=fly-preflight.yml`: run `35786657095` (2026-09-22T21:26:28Z) failed `Error: app gridiron-hq has no started VMs`; run `35788776774` (21:48:08Z) passed only because the coordinator woke the site with a request first. Reading `.github/workflows/fly-preflight.yml` on origin/main confirms there is still no wake step — it goes straight from "Set up flyctl" to `flyctl volumes list` / `flyctl ssh console`, both of which need a running VM. Queued as **INT-149-1**: add an HTTP GET retry loop against `https://gridiron-hq.fly.dev/api/health` before the ssh step, so the workflow wakes the machine itself instead of depending on someone else having hit the site first.
