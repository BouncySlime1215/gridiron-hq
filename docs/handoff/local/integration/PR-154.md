# Integration card: #154 Docs: 2025 holdout ledger and one statistics contract for every unit (S-00)

State: MERGED · range `dd7cec2013eddfde54b1b75e3db986211d763885^..dd7cec2013eddfde54b1b75e3db986211d763885` · generated 2026-09-22T23:26Z by integration-card.sh

## Files (3)
- docs/evidence/HOLDOUT-LEDGER.md
- docs/evidence/STATS-METHOD.md
- docs/tdd/2026-09-22-stats-method-contract.tdd.md

## Symbols touched and every caller on origin/main
- `settleWeeklyPredictions` (defined: server/services/weekly-learning.js ) · 4 refs · callers: server/services/nfl-model-growth.js:29 server/services/nfl-model-growth.js:345 server/services/weekly-learning.js:406 

## Routes touched → client pages
- no route definitions changed

## Tables
- written: at table 
- read: asymmetric audit both collections docs either implementation interval intervals mass_test_harness model model_backtests model_dataset_versions model_experiments nfl_model_growth_runs player_week_usage round sync_log week weekly_ensemble_fits weekly_prediction_snapshots what zero 

## Scheduler jobs and flags
- jobs: 
- env/flags: process.env.SCHEDULER_DISABLED 
- migrations: 

## Intake verdict (filled by the integration intake agent)

1. **Upstream.** Docs-only (3 files, all under `docs/`); no production code path. The card's one
   "symbol touched" (`settleWeeklyPredictions`) is a prose citation inside the diff, not a code
   change: `git show dd7cec20 | grep -n settleWeeklyPredictions` hits only a table row and an
   embedded `git grep` example citing `weekly-learning.js:155/:406` and
   `nfl-model-growth.js:307` as existing code. `git show --stat dd7cec20` confirms 3 files
   changed, 0 under `server/` or `client/`.
2. **Downstream.** N/A — no code changed, so no caller was left on old behaviour.
3. **Reach.** No route changed; by design this is a methods contract, not a served number, so it
   does not reach a page.
4. **Links.** Nothing on origin/main points at either new file yet: `git grep -rn
   "HOLDOUT-LEDGER" -- .` and `git grep -rn "STATS-METHOD" -- .` each return only the file's own
   self-reference (0 external citations). Known-nonzero control: the same grep style for
   `mass_test_harness` (a script both docs say they reuse) returns 3 real hits
   (`scripts/model-lab/mass_test_harness.py`, `audit_corpus.mts`, `audit_corpus.ts`), so the null
   result is real, not a grep miss. Notably #155 (S-02, merged just before this on the stack)
   says in its own evidence file: "No `docs/evidence/HOLDOUT-LEDGER.md` on origin/main, so the
   looks are recorded here" (`weekly-construction-grade.md:277`) — written before the ledger
   existed, and nothing has since gone back to file those looks there.
5. **Plan.** Addresses the "no sealed fantasy holdout" gap STRUCTURE-MAP logs at M13 (known: WQ
   C-10, C18) with an explicit contract and ledger. WORK-QUEUE.md's HX-01 row already leans on
   the concept informally ("2025 NOT used (used-up holdout, S-00 ledger)") — the follow-up below
   makes that reliance literal instead of just a name.
6. **Follow-ups.** 1 added: **INT-154-1** — backfill S-02's already-computed 2025 holdout looks
   into the new ledger, and point new statistical units (S-03, A-11, HX-01, RL-1-*, BLEND-01) at
   `STATS-METHOD.md`/`HOLDOUT-LEDGER.md` going forward. See WORK-QUEUE.md §7.
