# Integration card: #157 Add an always-visible data credit line; fix ffopportunity's licence (F-08)

State: MERGED · range `7a9d75f691418cba262f4257ad7f059701a1f7a9^..7a9d75f691418cba262f4257ad7f059701a1f7a9` · generated 2026-09-22T23:36Z by integration-card.sh

## Files (8)
- client/src/App.tsx
- client/src/components/DataFreshnessBanner.tsx
- docs/tdd/2026-09-22-data-credit-line.tdd.md
- docs/tdd/sweeps/data-credit-line-mutations.mjs
- server/routes/data-freshness.js
- server/services/ffopportunity.js
- server/services/ftn-charting-source.js
- test/data-credit-line.test.js

## Symbols touched and every caller on origin/main
- `DATA_CREDITS` (defined: client/src/components/DataFreshnessBanner.tsx ) · 5 refs · callers: client/src/components/DataFreshnessBanner.tsx:238 client/src/components/DataFreshnessBanner.tsx:246 client/src/components/DataFreshnessBanner.tsx:248 server/routes/data-freshness.js:29 
- `DataCredit` (defined: client/src/components/DataFreshnessBanner.tsx ) · 3 refs · callers: client/src/App.tsx:12 client/src/App.tsx:160 
- `DataFreshnessBanner` (defined: client/src/components/DataFreshnessBanner.tsx ) · 5 refs · callers: client/src/App.tsx:11 client/src/App.tsx:12 client/src/App.tsx:119 server/routes/data-freshness.js:29 
- `FFOPPORTUNITY_SOURCE` (defined: server/services/ffopportunity.js ) · 6 refs · callers: server/routes/data-freshness.js:5 server/routes/data-freshness.js:44 server/services/ffopportunity.js:86 server/services/ffopportunity.js:93 server/services/ffopportunity.js:131 
- `FTN_CHARTING_SOURCE` (defined: server/services/ftn-charting-source.js ) · 3 refs · callers: server/routes/data-freshness.js:6 server/routes/data-freshness.js:44 

## Routes touched → client pages
- no route definitions changed

## Tables
- written: 
- read: outside that 

## Scheduler jobs and flags
- jobs: 
- env/flags: 
- migrations: 

## Intake verdict (filled by the integration intake agent)

1. **Upstream.** One canonical list: `DATA_CREDITS` (DataFreshnessBanner.tsx) is held by
   `test/data-credit-line.test.js` to mirror `sources` on `GET /api/data-freshness`
   (server/routes/data-freshness.js:40: `[NFLVERSE_SOURCE, FFOPPORTUNITY_SOURCE,
   FTN_CHARTING_SOURCE]`) — one producer, not a second hand-typed copy.
2. **Downstream.** `FFOPPORTUNITY_SOURCE`'s only real consumers outside its own file are the
   freshness route and `test/nflverse-attribution.test.js`, which deep-equals the whole object
   rather than hardcoding the licence string (`git grep -n "FFOPPORTUNITY_SOURCE"` — 2 hits, both
   self-referential) — so the CC BY 4.0 → CC BY-SA 4.0 correction breaks nothing downstream.
3. **Reach.** Confirmed, strongly: `DataCredit` is a stateless, hook-free, prop-less footer
   sibling of `<main>` in App.tsx (pinned by the PR's own AST-level test), so it renders on every
   page regardless of banner/dismissal state.
4. **Links — the specific question asked.** The ffopportunity fix was a **mislabelled licence,
   not new code**: its README's "Terms of Use" says CC BY-SA 4.0, not the CC BY 4.0 the code
   comment previously claimed; this PR corrects the claim and adds `license_url`.
   **FTN's CC BY-SA credit is already added by this same PR** —
   `server/services/ftn-charting-source.js` (new) is in both `DATA_CREDITS` and `sources`,
   attributed "FTN Data via nflverse" per nflreadr's loader docs, and it already has real data
   behind it (`nfl_play_charting` via `nfl-formations.js#ingestCharting`, 187k+ local rows per
   STRUCTURE-MAP) — no gap remains there. **Wikipedia is the one still missing.**
   `server/routes/accolades.js` already fetches Wikipedia infobox highlights into
   `player_accolades` (source values `espn` / `wikipedia+espn`) and renders on a live page
   (`client/src/pages/TeamDetail.tsx:195`, mounted at `/teams/:abbr`, App.tsx:132), but
   Wikipedia's CC BY-SA licence is in neither `DATA_CREDITS` nor `sources`, and `git grep -n
   "license\|CC BY" -- server/routes/accolades.js` is empty. `sqlite3 -readonly
   ~/gridiron-local/data.sqlite "SELECT COUNT(*) FROM player_accolades"` → 0 on the local copy
   today, so this is latent, not actively serving uncredited content in Nick's own data yet — but
   the route is live and would start doing so the moment it's run. No "RD-02" label exists
   anywhere in the repo, WORK-QUEUE.md or STRUCTURE-MAP.md (`grep -rn "RD-02"` — 0 hits in all
   three); flagging this since the check named it and it could not be located.
5. **Plan.** Closes WORK-QUEUE.md §3 F-08's row and unblocks the credit-line half of INT-150-1's
   production gate ("Production: run only after... F-08 lands the CC BY-SA... credit line" —
   WORK-QUEUE.md §7, already queued).
6. **Follow-ups.** 1 added: **INT-157-1** — add the Wikipedia CC BY-SA 3.0 credit before/when
   `player_accolades` starts populating. See WORK-QUEUE.md §7.
