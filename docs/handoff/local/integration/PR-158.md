# Integration card: #158 Fix ESPN RSS news stamped an hour into the future (EST/EDT parsing) (R-07)

State: MERGED · range `a3e2bf354da4c59891414ea93e1de802b20fce54^..a3e2bf354da4c59891414ea93e1de802b20fce54` · generated 2026-09-22T23:57Z by integration-card.sh

## Files (4)
- docs/tdd/2026-09-22-news-published-at-timezone.tdd.md
- server/news/ingest.js
- server/news/normalize.js
- test/news-published-at-timezone.test.js

## Symbols touched and every caller on origin/main
- `extractEntities` (defined: server/news/normalize.js ) · 6 refs · callers: server/news/normalize.js:91 server/routes/espn.js:6 server/routes/espn.js:169 server/routes/espn.js:190 server/routes/espn.js:217 
- `ingestAllSources` (defined: server/news/ingest.js ) · 5 refs · callers: server/routes/news.js:4 server/routes/news.js:35 server/services/scheduler.js:770 server/services/scheduler.js:773 
- `ingestRssSource` (defined: server/news/ingest.js ) · 2 refs · callers: server/news/ingest.js:104 
- `loadIdentity` (defined: server/news/ingest.js ) · 6 refs · callers: server/news/ingest.js:71 server/news/twitter-ingest.js:25 server/news/twitter-ingest.js:183 server/services/nfl-transactions.js:24 server/services/nfl-transactions.js:43 
- `normalizeNewsItem` (defined: server/news/normalize.js ) · 8 refs · callers: server/news/ingest.js:2 server/news/ingest.js:78 server/news/twitter-ingest.js:11 server/news/twitter-ingest.js:23 server/news/twitter-ingest.js:203 server/services/nfl-transactions.js:22 server/services/nfl-transactions.js:62 

## Routes touched → client pages
- no route definitions changed

## Tables
- written: 
- read: march 

## Scheduler jobs and flags
- jobs: 
- env/flags: 
- migrations: 

## Intake verdict (filled by the integration intake agent)

1. **Upstream.** One choke point: `parsePublishedAt` (server/news/normalize.js) uses the existing
   shared `zonedDateTime` (services/date-util.js), and `normalizeNewsItem` is the single function
   every ingester already calls (`server/news/ingest.js:2,78`, `twitter-ingest.js:11,23,203`,
   `nfl-transactions.js:22,62`) — so RSS, Twitter and transaction ingest are fixed together, not
   three separate patches.
2. **Downstream / "are other feeds parsing the same way."** Grepped for the same Eastern-time
   pattern outside this PR's files. Found exactly one other Eastern-time converter in the repo,
   `server/services/book-feeds-extra.js:84` `easternToIso` — a **different, already-correct**
   implementation (real `Intl.DateTimeFormat` offset lookup for betting-odds feeds, not a
   zone-letter misread) that the PR's own TDD doc already names as "a second copy of the same
   idea... not touched here" (tdd:30,403) — a duplication-to-consolidate note, not a live bug,
   and out of fantasy-news/injury scope. No other news/injury ingester parses dates outside
   `normalizeNewsItem`.
3. **Reach.** Confirmed — `routes/news.js` orders and ages every desk story by
   `COALESCE(published_at, date)`; the TDD doc's own live check (§6.1) ran the real `GET
   /api/news/desk` handler against a scratch copy.
4. **Links / downstream re-check — the specific question asked, traced past the PR's own text.**
   `nfl-news-signal.js:227-230` reads `news_items.published_at` and copies it verbatim into
   `nfl_news_signals.published_at` (:215), which `signal-latency.js` (STRUCTURE-MAP's C16 basis,
   currently betting-only) and `nfl-news-market-latency.js` both read; fantasy's
   `news-fantasy-impact.js:75` also keys `nextTeamGame` off `signal.published_at` directly. Per
   the TDD doc's own "Known defect 1," **historical rows are not rewritten** — all 196 local RSS
   rows still carry their pre-fix stamp — so any of those three consumers computed over
   daylight-time-period history is still off by up to an hour until a backfill runs, and a
   backfill is a data UPDATE gated on Nick's word (standing rule 12). The doc separately
   already names two more unfixed items, neither yet queued: **F-R07-1** (two producers —
   `espn.js:174` `insertArticles` and the RSS writer — write `published_at` for the same ESPN
   story and disagree; 7 duplicate pairs on the local copy, one story listed twice on the desk),
   and a `fresh_24h` text-vs-time comparison bug (`routes/news.js:89`, `nfl-diagnostic.js:21`:
   268 stories read "fresh" by text compare vs 146 by `julianday` on the same local copy).
5. **Plan.** R-07 is not yet in any WQ phase table; it's a data-quality prerequisite for C15/C16
   (both "not started" for fantasy per STRUCTURE-MAP) — those units should inherit the
   "historical rows uncorrected" caveat once someone picks them up.
6. **Follow-ups.** 3 added: **INT-158-1** (unify the two published_at producers, F-R07-1),
   **INT-158-2** (fix the `fresh_24h` text-comparison bug), **INT-158-3** (Nick's word + backfill
   historical daylight-time rows, then re-check signal-latency.js / nfl-news-market-latency.js /
   news-fantasy-impact.js numbers spanning the contaminated window). See WORK-QUEUE.md §7.
