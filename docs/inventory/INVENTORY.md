# The honest inventory

Generated by `scripts/inventory.mjs` from `docs/wiring/wiring-map.json` and
`docs/inventory/evidence-overlay.json`. Never hand-edited. Regenerate with
`node scripts/inventory.mjs`; validate with `node scripts/inventory.mjs --check`.

Commit `90a1fba`. Map generated 2026-09-22T01:29:56.059Z. 872 rows.

## The one thing to read first

Row counts are LOCAL: read from `server/data.sqlite`, which is a migrated dev database, not production. 4 of 218 tables hold any rows. A LOCAL count of 0 means "cannot be confirmed here", not "empty in the app" — the rows that matter live in the Fly database this container cannot read. Every status that turns on "current rows" and cannot be proven is `unclassified` with that reason, never guessed.

## Counts by status

| status | rows | meaning |
|---|---|---|
| wired | 77 | reachable from a live surface and reads a table with current rows (LOCAL) |
| half_done | 197 | code exists, reaches no live surface (or none with confirmable data) |
| silently_broken | 4 | reachable, returns success, reads or writes nothing real |
| dead | 15 | unreachable and imported by nothing but tests |
| (model, ungraded) | 60 | a model, blank status, handed to the model-evidence audit thread |
| unclassified | 519 | could not be defended statically; each row carries a reason |

## By kind

| kind | rows |
|---|---|
| model | 61 |
| pipeline | 250 |
| job | 62 |
| route | 32 |
| page | 23 |
| table | 327 |
| script | 117 |

## silently_broken — 4

- **player_rosters** (job, scheduler) — refreshPlayerRosters throws "Provided value cannot be bound to SQLite parameter 3." at scheduler.js:1196 on every run (a bind-type code fault, not an empty-DB artefact); observed 2026-09-22 against a migrated empty DB, 1 fetch made, 1 row written before the throw; statusFromDetail records the throw as error, so nextDueMinutes backs it off to re-run every 5 min instead of every 3h; visible in sync_log, invisible on every surface. Measured by the scheduler thread, 654ff93.
- **trade-tactics.js** (model, trade-brain) — bare try/catch at :230-235 against the missing league_transactions_raw table falls through to blank() at :251-257, returning a manager-history finding (n:0, fitted:false) with no signal that the underlying data does not exist. Graded by the model-evidence audit thread, 654ff93.
- **GET /model/setup-status** (route, ui) — client/src/components/DataSetupBanner.tsx reads /model/setup-status; the endpoint's health check is `last_status === 'never run'`, so a source that ran once and wrote zero current rows reads as fine. player_week_usage holds zero 2026 rows while the banner shows OK. Measured by the UI thread, 654ff93.
- **league_transactions_raw** (table, scheduler) — created by no migration and no server/db/schema file — its only CREATE TABLE is in test fixtures (test/manager-data-pipeline.test.js:131 and peers). Read by six services (manager-archetypes, trade-engine, counterparty-pricing, manager-signals, bluff-detector, trade-tactics); only manager-signals.js:167 guards with tableExists, so the other five read a table that does not exist outside the test run and return results as if it did. Measured by the model-evidence audit thread, 654ff93.

## dead — 15

- **consensus-weights.js** (pipeline) — wiring-map: server/services/consensus-weights.js reaches no live surface and is imported only by tests
- **draft-abstention-audit.js** (pipeline) — wiring-map: server/services/draft-abstention-audit.js reaches no live surface and is imported only by tests
- **espn-market.js** (pipeline) — wiring-map: server/services/espn-market.js reaches no live surface and is imported by nothing
- **historical-adp-scrapes.js** (pipeline) — wiring-map: server/services/historical-adp-scrapes.js reaches no live surface and is imported only by tests
- **line-move-study.js** (pipeline) — wiring-map: server/services/line-move-study.js reaches no live surface and is imported only by tests
- **nfl-execution-staking-policy.js** (pipeline) — wiring-map: server/services/nfl-execution-staking-policy.js reaches no live surface and is imported only by tests
- **nfl-execution-stress.js** (pipeline) — wiring-map: server/services/nfl-execution-stress.js reaches no live surface and is imported only by tests
- **nfl-preseason-blend.js** (pipeline) — wiring-map: server/services/nfl-preseason-blend.js reaches no live surface and is imported only by tests
- **nfl-prop-player-heads.js** (pipeline) — wiring-map: server/services/nfl-prop-player-heads.js reaches no live surface and is imported only by tests
- **nfl-prop-player-weekly-heads.js** (pipeline) — wiring-map: server/services/nfl-prop-player-weekly-heads.js reaches no live surface and is imported only by tests
- **nfl-team-strength.js** (pipeline) — wiring-map: server/services/nfl-team-strength.js reaches no live surface and is imported only by tests
- **purged-walk-forward.js** (pipeline) — wiring-map: server/services/purged-walk-forward.js reaches no live surface and is imported only by tests
- **td-features.js** (pipeline) — wiring-map: server/services/td-features.js reaches no live surface and is imported by nothing
- **vegas-fantasy.js** (pipeline) — wiring-map: server/services/vegas-fantasy.js reaches no live surface and is imported by nothing
- **_recheck-variance-only.mjs** (script) — wiring-map: scripts/_recheck-variance-only.mjs is imported by nothing and is not a runnable entry point

The full row set, with evidence per row, is in `inventory.json`.
