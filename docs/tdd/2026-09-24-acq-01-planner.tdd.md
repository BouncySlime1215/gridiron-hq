# ACQ-01: the "go get player X" planner as a producer

RED `304326b` "test: RED ACQ-01 planner…" · GREEN follows ·
`test/acq-planner.test.js`, 17 cases, fixture `test/fixtures/acq-league.mjs` (made up, no real data).

Base: PR #238 head `a08c49e` (WARROOM-CONTRACT). The entry this writes is validated by
`validateLeague` from that PR's `server/services/campaign/plans-schema.js`.

## What it adds

`server/services/acq/`:

| file | job |
|---|---|
| `value-band.js` | screen-fair window (-12%..+18% on his side), `pairsInBand`: sorted sweep + binary search for 2-for-1 pairs whose summed value lands in the band (IDEA-038) |
| `planner.js` | `planAcquisition(adapter)`: single-player title values, targets, direct 1-for-1 / 2-for-1 / 1-for-2, chip-first paths (third-team trade or claim, then the target), linear shortlist, exact rescore, full expectation with stranded legs, per-step backups, IDEA-038 summary, time and rescore budget |
| `contract.js` | planner result -> #238 `alternatives`, `next_move`, `targets` (+ `finder_best_expected` when measured); every other section `unknown` with the reason it is not ACQ-01's |
| `world-adapter.js` | the real league: one `tradeImpactWorld` with the top free agents in its universe, composed rescore (sanity-checked against `tradeImpact`), P(yes) = `acceptanceBand` on `readDeal` |
| `produce.js` | default-off via `previewUnconfirmed()`; off, it builds nothing |

`scripts/acq/produce-acq-plans.mjs`: CLI, league 4 by default, writes the plans file outside
the repo, prints a names-free summary.

Contract change (additive): step gains optional `p_yes_band {low, high}` (INTEGRATION-AUDIT-0923
row P25). #238's producer fixture carries it so "writes every declared path" stays green.

## RED

At `304326b`:

```
Error [ERR_MODULE_NOT_FOUND]: Cannot find module '.../server/services/acq/value-band.js' imported from .../test/acq-planner.test.js
# pass 0
# fail 1
```

## A defect the tests found before GREEN

The first draft capped the chip pool (150) across shapes, sorted by market gain. On the fixture,
target 22's in-run 1-for-1 arm scored 0.0596 while a 1-for-1-only search scored 0.0826: pair chips
had crowded 1-for-1 chips out of the pool, which would have overstated IDEA-038's gain. The cap is
now per shape, and a pair chip never feeds the 1-for-1 arm. The test "the in-run 1-for-1 arm scores
exactly what a 1-for-1-only search does" pins it at chip limits 150, 5, 2 and 1.

## Liveness: mutation sweep (GREEN tree)

| mutant | tests failing |
|---|---|
| M1 `pairsInBand` returns nothing | 3 |
| M2 a declined later step resets Nick to today | 1 |
| M3 P(yes) priced from Nick's side, not his | 1 |
| M4 backup ignores the shared prefix | 1 |
| M5 preview switch ignored | 1 |
| M6 the 1-for-1 finish after a chip is not kept | 1 |
| M7 rescore cap ignored | 1 |
| M8 chip cap pooled across shapes | 1 |

## Not proved here

The real-league run (runtime under 300 s, the 2-for-1 gain on league 4, the composed-rescore
sanity check) needs the local DB; it is a `LOCAL:` line in the PR.
