# REACH-01: say why every path died, and search the targets Nick can actually reach

RED `<this branch's test commit>` · GREEN follows · `test/campaign-reach-01.test.js`.

Unit source: `docs/handoff/local/ONE-PLAN.md` on `claude/handoff-package-2026-09-22`,
sections 4 (cause 1 and 2, fix A) and 5 (night 1, items 1-4 and 6).

## Scope

In this unit, in the plan's order:

1. `dropped_by_reason`: per risk mode, how many scored paths each gate removed
   (tolerance sliders by kind, the all-in `p_complete` floor, the fresh-dice
   confirm), plus the search-level count the no-overpay cap turned away and the
   targets the reach filter skipped. Numbers the planner already computes,
   aggregated; nothing new is priced.
2. Reach filter before the top-N target slice: a target is skipped only when no
   path shape the search can build could read fair on its owner's screen
   (`fairBand(value).lo` above the best package a direct give, a one-chip chain
   or a two-chip chain could hand over). A necessary-condition bound, so it can
   only skip targets the search could never have found.
3. `--targets 8` in the refresh loop's producer launch.
4. The chained finish and `flipReach` package up to the risk mode's
   `max_give_per_step` (today both are fixed at 2).
6. The closest miss prints its whole chain (first leg, then the overpaying
   finish), not just the last step.

Out of this unit: item 5 (GETS-REAL floor) is unit GETS-FLOOR; trade memory is
unit TRADE-MEMORY. Both run in their own threads and branches.

## Flag

`GRIDIRON_REACH`: `1` on; unset or anything else off. Amended after the
first push: it no longer follows the preview switch, so it stays off on
Nick's machine until the league-4 measurement passes. Items 2, 3 and 4 run
only when it is on; off, the planner and the loop launch are today's. Items 1
and 6 are diagnostics and text: always on, and they move no number.

## Pre-registration (written before any code)

Fixtures only (made-up players, `test/fixtures/campaign-league.mjs` and small
linear worlds in the test file). No real league data.

| # | Metric | Pass bar | What fails it |
|---|---|---|---|
| M1 | `dropped_by_reason` accounts for every scored path | per mode, `kept + sum(tolerance) + p_complete_floor == candidates_scored`; `search.no_overpay == no_overpay.rejected`; objective mode's `confirm_failed` equals the deck cards the confirm pass dropped | any count off by one |
| M2 | Reach filter never drops a findable target | on every fixture target the filter skips, the unfiltered search returns 0 plans for it | a skipped target has a plan unfiltered |
| M3 | Reach filter frees slots | a reachable target ranked 4th by upgrade is searched and gets a plan with the flag on and `targets 8`; with the flag off and `targets 3` it is not searched | the 4th target is still absent with the flag on |
| M4 | Chained finish widens with the mode | a chain whose finish needs 3 gives is found in all_in with the flag on and not with it off; balanced (max give 2) is unchanged | the 3-give finish is missing on, or present off |
| M5 | Flag off is today | `planLeague` output with the flag off deep-equals the incumbent's on the fixture league, except the new `reach` diagnostics | any other field differs |
| M6 | Closest miss names the chain | a McBride-shaped fixture (chip step fair and in cap, finish overpays) produces a no-move reason naming both legs in order | only the finish is named |

Real-league effects (how many league 4 targets the filter skips, whether an
Olave-class card now reaches the deck, runtime at 8 targets) need the live DB
and are listed in the PR under "Needs local measurement".

## Results (GREEN, after the pre-registration above)

RED `57f0f16`: `test/campaign-reach-01.test.js` failed (no `reach.js`). GREEN: the commit after it.

| # | Result |
|---|---|
| M1 | PASS: in all 3 objective modes x flag on/off, every mode's `kept + tolerance + p_complete_floor == candidates_scored`; `search.no_overpay == no_overpay.rejected`; confirm counted for the objective mode only |
| M2 | PASS: the three fixture targets priced out of reach produce 0 paths when searched unfiltered |
| M3 | PASS: flag on skips exactly those three and searches the next three, which produce paths |
| M4 | PASS: the chip-then-3-for-1 finish is found at chainGive 3 and not at 2 |
| M5 | PASS: regenerating `test/fixtures/warroom-contract/producer-plans.json` with the flag off changes only `_run.inputs.reach` (4 leagues; league 2's world fails by design) |
| M6 | PASS: the sink keeps the chip leg in front of the overpaying finish; the no-move reason prints both legs in order |

One correction to a pre-computed number while writing the test: the plan's closest-miss chain
(two of Nick's for the 6,166 piece, then that piece + one more) peaks at 8,086 (3,583 + 1,910 in,
6,166 + 1,920 out), not 8,076. It does not change any verdict.
