# STEP-LOG: E5 live grading

`test/campaign-step-log.test.js` (8, new). RED: fails at import (no
`server/services/campaign/step-log.js`). GREEN: 8 of 8; `fix-07-warroom-inputs`
15 of 15 and `eval-seams` 14 of 14 unchanged.

## Gates

| gate | what |
|---|---|
| S1 | "I sent it" -> one `campaign_steps` row (card's predicted gain + se) once the 10-minute undo window closes; none before, none if retracted |
| S2 | an ESPN proposal from Nick's team that is exactly a plan step (partner + players by ESPN id, within 48 h of the plans file) -> step row + `trade_outcomes.move_id`; near-misses write nothing |
| S2b | a chained move's second step matches by its own index; a move repeated in next_move and the deck counts once |
| S3 | a proposal an "I sent it" offer already matched is not logged twice |
| S4 | accepted -> realized = p_title(first snapshot at/after settle) - p_title(last snapshot at/before send); no later snapshot -> stays open with the reason |
| S5 | declined: `snapshot` (default) same difference, `zero` writes 0 |
| S6 | idempotent; E5 grades the rows (n realized, awaiting counted) |
| S7 | FIX-07's consume path writes through the same insert: one row per (league, move, step) |
