# LADDER-01: ladder cards (shadow)

ONE-PLAN section 5, night 5: "chained finish may spend the acquired piece with the mode's max give;
backup branch at each 'no'; Fuck-it ladders capped at 2 steps while p is the 0.30 guess (floor
0.03)". Deliverable: "ladder cards depth -> level-below -> blue chip with P(yes) per step and the
backup". Acceptance there: "0 final-leg gets below the floor".

Flag: `GRIDIRON_LADDER=1`. Default off, and preview does NOT turn it on. Off, the planner never
calls the module and the `ladders` section is written as `unknown` ("Ladder cards are off"), like
`blue_chips` with its flag off: the contract tests require every section in every entry. No
other section changes.

## Pre-registration (written before the GREEN code)

- Metric 1: final gets under Blue chip 83 on ladder cards. Pass bar: 0.
- Metric 2: rungs that give 160 / 80 / 277 or an `adapter.untouchable` id, or get 290. Pass bar: 0.
- Metric 3: rungs that give more market value than they get past the cap. Pass bar: 0.
- Metric 4: Fuck-it cards longer than 2 rungs while any rung's p is the guess. Pass bar: 0.
- Metric 5: every rung carries p labelled `guess: true` and an `on_no` (backup or stop). Pass bar: 100%.
- Metric 6 (shadow): with the flag on, every section of the entry other than `ladders` equals
  the flag-off entry (deep-equal), in all three modes. Pass bar: identical.
- Fails it: any single violation above, or the flag-on entry failing `validateLeague`.

## What changed

- `server/services/campaign/ladder.js` (new, pure): `ladderFlag`, `tierOf`, `scoreReader`
  (`adapter.scoreOf`, else the PLAYER-SCORE board rows, else none: fail closed), `ladderCards`
  (filters above, each counted in `dropped_by_reason`; backup at each "no" is the best other
  kept ladder sharing the rungs before it, else "stop" with what the earlier rungs landed), and
  `ladderSection` (the typed plans-file section; every number `guess: true`).
- `planner.js`: with the flag on, `ladderCards(ranked, ...)` after the mode ranking; result gains
  `ladders`. Nothing else reads it.
- `view.js`: `ladders` section always written; `unknown` with the reason when the flag is off.
- `plans-schema.js`: optional section `ladders`, `LADDER_TIERS`.
- Fixtures: `producer-plans.json` regenerated (`make-producer-plans.mjs`; the diff is the
  `ladders` unknown section on the 4 planned leagues and nothing else); `ui-contract-plans.json`
  gains the same unknown section on its 2 planned leagues; `warroom-plans-contract.test.js`
  PENDING lists the `ladders.value` paths (flag-off fixture, as for `blue_chips`).
- The search is NOT changed. `stepsFrom` already reads the chained state's roster, so a chained
  finish can spend the acquired piece today (test 3 proves it). Widening that finish to the
  mode's max give is REACH-01's `chainGive` (#378); this unit reads whatever the search returns,
  so it composes with #378 without touching the same lines.

## RED

Commit `7cf6609`: `test/campaign-ladder-01.test.js` fails with `ERR_MODULE_NOT_FOUND` for
`ladder.js`.

Test fixes after RED (the tests were wrong, not the code): the hand-built "unscored" plan's
get had no market value, so it dropped as an overpay before the floor was read (gave `99` a
value); the overpay case spent a piece it never acquired, so it was not a ladder (rung 1 now
brings it in); the 3-rung Fuck-it case had a rung giving 100 for 90, an overpay (9 now 100);
`validateLeague` returns `{ errors }`, not an array.

## GREEN

`test/campaign-ladder-01.test.js`: 13 of 13 pass. On the fixture league (four made-up teams)
with the flag on: balanced 4 cards (54 considered: 24 not a ladder, 26 final under 83),
all-in 1 card (2 rungs), safe 0 (4 considered, all under the floor). Every other section equals
the flag-off entry in all three modes.

First full `npm test` after GREEN: 5 contract failures (every section must be in every entry;
the fixtures lacked `ladders`; the contract's paths had no producer). Fixed as above; see the
PR for the re-run.

## Not measured here

League 4. See the PR's "Needs local measurement".

## Review fixes (coordinator batch-B on #394)

RED `b857f46` (tests 14-18 fail), GREEN in the next commit; `test/campaign-ladder-01.test.js` 18 of 18.

1. Held floor: every player Nick holds at the end (gets minus later gives, `heldAtEnd`) must score
   83+, unscored fails closed (`held_below_floor`, `held_unscored`). Test 14 (a 1-for-2 rung). On the
   fixture this dropped 2 balanced cards that kept a sub-83 piece (4 -> 2 cards).
2. Backups: with confirm dice, a backup is offered only when it beats doing nothing on them
   (`confirmVerdict` not failed) and shows the confirm-dice expected (`dice: 'confirm'`); without,
   it is labelled `dice: 'planning'`. Test 15.
3. Sold players: `sold` (Set or `id -> truthy`, e.g. trade memory's `excluded`) drops any rung that
   gets one (`sold`); 290 stays pinned. Test 16. main has no trade memory yet, so the planner passes
   `adapter.sold` (absent today); on #398 it is `TM.excluded`.
4. Destination untouchables join `adapter.untouchable` and NEVER_GIVE. Tests 17-18.
5. The section says `dice: 'planning'` with a `basis` sentence. Test 18.

Full `npm test`: 5,868 tests, 5,822 pass, 0 fail, 46 skipped. Wiring, typecheck, lint exit 0.

## Batch B (main `decf7ebf`, integration-7)

Merge `daac3b0`, RED `a8adf64` (tests 15, 16,
19, 20, 21 fail), GREEN `300ed02`. `test/campaign-ladder-01.test.js`
21 of 21. The ladder now keeps no copy of any rule; each is read from main's one home:

1. Floor on everything held: `gets-floor.js` `floorRead` over `heldAtEnd` (moved into
   `gets-floor.js`; the planner's `failsHeld` uses the same function). The destination's
   `min_get_score` may raise the floor, never lower it. The ladder applies it even when
   `GRIDIRON_GETS_FLOOR=0` (fixture: 22 balanced paths dropped `final_below_floor` in that run).
2. Backups: only the planner's `confirmedActive` (main's confirm-dice beats-doing-nothing gate).
   No confirm dice, no backup. Fixture, confirm world failed: backups 5 -> 0 (balanced), 5 -> 0
   (all_in); with confirm dice, 5 backups each, unchanged.
3. Buy-backs and reversals: `trade-memory.js` `applyTradeMemory` with main's `TM` (any player Nick
   sold, from any team). Drop reasons `sold`, `reversal`, `memory_floor`. 290 via `PINNED_NEVER_GET`.
4. Never give: `never-give.js` `PINNED_NEVER_GIVE` + `adapter.untouchable` + the destination's
   untouchables. The local `NEVER_GIVE`, `NEVER_GET`, `FINAL_FLOOR` and `scoreReader` are gone
   (test 19 reads the source for id literals).

Test 10 changed: it now passes `confirmed` explicitly, because a planning-dice backup is no longer
allowed under main's rule. That is the rule changing, not the test being bent.
