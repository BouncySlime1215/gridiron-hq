# TM-34 DEADLINE MODE: TDD record (2026-09-25)

Unit: ONE-PLAN section 5 night 15 (TM-34), batch D item 10. Flag `GRIDIRON_DEADLINE_MODE` (`1` or `shadow`
computes into `_run.inputs.deadline_mode`; default off). Nothing served moves; preview mode never turns it on.

## Pre-registration

Metric and pass bar, per check (tests in `test/campaign-tm-34-deadline.test.js`, `test/trade-tactics.test.js` G5c2):

| id | What | Pass bar | What fails it |
|---|---|---|---|
| D1 | Flag | `1`/`shadow` -> shadow; unset, `0`, preview -> off | preview or any other value turning it on |
| D2 | Clock | league 4 deadlineDate 1796230800000 -> deadline 2026-12-02T17:00Z, last real send 2026-11-30T17:00Z (24 h review + 24 h his window); phases normal / deadline (<= 14 d) / last_call (<= 7 d to last send) / closed / past | any other last-send time; a clock invented when the league has no deadline |
| D3 | send_by | every step's send_by <= last send; a chained path's step k leaves (n-1-k) x 2 days for later legs; too_late when send_by < now | a step whose send_by is after the last send |
| D4 | Last call | one row per reachable partner; excluded managers absent; 0 rows give a never-give id or get a never-get id; 0 rows with a too-late step | any rule-breaking or too-late row |
| D5 | Hold | rule-blocked players always held (reason `rule`); a player sold by a positive last-call offer is not held | a blocked player off the list |
| D6 | Quiet | checked out or >= 10 days since his last own move -> quiet; no record -> unknown (never active) | unknown read as active |
| D7 | Wait | option value of waiting on news = 0, labelled with the IDEA-151 kill; wait cost = speed-curve landing value (all days to the last send) minus (one round left), >= 0; under one round left -> unknown | negative cost; waiting reported as free when later rounds carry value |
| D8 | Planner | off: entry byte-identical; shadow: contract passes, served sections byte-identical to off, `deck_steps_after_last_send` = 0; untouchables never in a last-call give | any served-number change; a contract error |

## Before serving (Needs local measurement, league 4)

1. Clock: `_run.inputs.deadline_mode.clock.last_send_at` = `2026-11-30T17:00:00.000Z` on the real payload.
2. `deck_steps_after_last_send` = 0 and `deck_steps_too_late` reported, every run from 11/8.
3. Quiet read graded weekly: of rosters marked quiet, share with 0 own moves in the next 7 days >= 0.7 with n >= 5.
4. Planner runtime with the flag on within 2% of off.

## RED

Tests committed first; against main they fail on the missing module `server/services/campaign/deadline-mode.js`
and on `last_action_at` being absent from `timingRead`.

## GREEN

`deadline-mode.js` (pure) + planner / view / league-adapter / trade-tactics wiring. Results in the PR body.
