{
 "catalog": {
  "suites": [
   "test/coach-catalog.test.js",
   "test/coach-select.test.js"
  ],
  "titles": [
   "every catalogued table exists, unless only a script creates it",
   "every entry states grain, meaning, freshness and collection mode",
   "collection mode distinguishes hand-collected data, so an answer can show its age",
   "columns are read from the live database, not hand-copied",
   "a table that is not catalogued is not readable and has no entry",
   "the tables behind Nick's three example questions are covered",
   "coverage names what is present and not yet catalogued, and never contradicts itself",
   "the full catalog is serialisable and carries no Map or Set",
   "COACH_TABLES is frozen, so no caller can widen what Coach may read at runtime",
   "a table holding credentials declares them, so they can be withheld",
   "every redacted column named by the catalog actually exists on its table",
   "every stat the lexicon names sits in a table Coach is allowed to read",
   "a table the declared schema does not create says who does, and one it creates says nothing",
   "every file a runtime creator names exists, and one of them holds the CREATE TABLE",
   "the audit of Coach\u2019s own answers is readable, but the answers themselves are withheld",
   "an absent table is sorted by whether anything is known to build it",
   "a SELECT over catalogued tables returns rows, their columns and where they came from",
   "an aliased table is still resolved to the real table it reads",
   "values are bound, so a value that looks like SQL stays a value",
   "a write is refused by policy, and the row count is untouched",
   "the connection itself is read-only, so the policy check is not the only thing standing there",
   "a table the catalog does not describe is refused, and the refusal names it",
   "one catalogued table does not license an uncatalogued one beside it",
   "a WITH query is allowed when every table it reads is catalogued",
   "a second statement, an ATTACH and a PRAGMA are all refused",
   "a trailing semicolon on a single statement is fine",
   "more rows than the cap are truncated and say so, rather than being quietly cut",
   "a broken query fails as a SQL error the model can act on, not as a refusal",
   "an empty or non-string query is refused rather than reaching SQLite",
   "a redacted column cannot be selected, and the refusal names it",
   "a redacted column cannot be filtered on either, so it cannot be read a character at a time",
   "the rest of a table with a redacted column is still readable",
   "a forbidden keyword in the body of a WITH query is refused as policy, not left to SQLite",
   "a forbidden word inside a string literal is a value, not a keyword",
   "a second statement is refused even when it is itself only a SELECT",
   "a catalogued table that has not been built yet fails with who builds it, not SQLite\u2019s wording",
   "the friendlier wording is only for catalogued tables, never a name Coach does not know"
  ],
  "failed": []
 },
 "ledger": {
  "suites": [
   "test/coach-ledger.test.js",
   "test/coach-verify.test.js"
  ],
  "titles": [
   "a recorded query gets an id, and each of its cells is addressable",
   "ids are handed out in order, so a cite cannot point at the wrong query",
   "a cite that points at nothing resolves to null rather than to something nearby",
   "a derived value records its operation, its inputs and its formula",
   "every whitelisted operation computes what it says",
   "an operation outside the whitelist is refused, not improvised",
   "a derivation whose input does not resolve fails loudly instead of skipping it",
   "a derivation over a non-numeric cell fails, rather than producing NaN",
   "dividing by zero is refused rather than returned as Infinity",
   "a derived value can be the input to another, and the chain is readable",
   "the ledger reports which cited data is hand-collected, so an answer can show its age",
   "the ledger serialises to JSON with no Map or Set, so it can be shown to the user",
   "a claim whose numbers are all cited cells passes",
   "a number that is in no cited cell is a violation, and the violation names it",
   "a cell cited by another claim does not ground this one",
   "a value stated to fewer decimals than the row holds is grounded",
   "a share written as a percentage is grounded against the fraction it came from",
   "a percentage that is not the cited value is still caught",
   "a claim with no cites is a violation even when it contains no number",
   "a cite that resolves to nothing is a violation naming the cite",
   "a number worked out by the ledger grounds the claim that states it",
   "arithmetic the model did in its head is not grounded, however right it is",
   "a thousands separator, a leading plus and a minus sign are all read as the number",
   "hand-collected data obliges the answer to state its age",
   "an answer with no claims is a violation, not a pass by vacuum",
   "a refusal is a legitimate answer and needs no cites",
   "a number spelled out in words is a warning, not a block, and the warning says so",
   "the check reports how many numbers it examined, so an empty check is visible"
  ],
  "failed": []
 },
 "tools": {
  "suites": [
   "test/coach-tools.test.js"
  ],
  "titles": [
   "toRows: an array of objects is already rows",
   "toRows: a nested object becomes one row of dotted scalar columns",
   "toRows: an array of scalars becomes a row each, under `value`",
   "toRows: a wide result is capped and says it was capped",
   "toRows: nothing at all is no rows, not a row of nothing",
   "every tool declares a name, a description and an input schema Claude can read",
   "the definitions handed to Claude carry no functions",
   "sql_select runs through the guarded layer and lands in the ledger",
   "sql_select honours the row ceiling it was asked for, and says it truncated",
   "sql_select passes a refusal straight through, so the model sees the boundary",
   "a service tool records rows the same way a query does",
   "every service tool names a function that really exists, so no second implementation can hide",
   "compute goes through the ledger, so a worked-out number is traceable",
   "an unknown tool is refused by name rather than ignored",
   "catalog_lookup tells Coach what it may read without touching the ledger",
   "toRows: a long array inside a nested object is capped and says so",
   "a tool that throws leaves the ledger exactly as it was"
  ],
  "failed": []
 },
 "ask": {
  "suites": [
   "test/coach-ask.test.js",
   "test/coach-route.test.js"
  ],
  "titles": [
   "a question answered from one query comes back verified, with the ledger behind it",
   "the events describe what actually happened, in order",
   "an ungrounded number is rejected, named back to the model, and fixed on the retry",
   "the retry prompt names the exact numbers that failed, so it is actionable",
   "a sentence Coach cannot stand up twice does not ship",
   "a refused tool call is fed back to the model rather than failing the question",
   "a fault that is not a refusal ends the question instead of becoming a tool result",
   "a refusal with no claims is a valid answer and is not retried",
   "the loop is capped, and the last round is asked for an answer rather than another lookup",
   "spend is booked against the coach budget, and the model is one the price table knows",
   "every answer is written to the audit, verified or not",
   "the system prompt tells the model what it may read and how to cite",
   "what is on screen is context, never evidence",
   "a question comes back with the answer, the ledger it stands on and the trace",
   "every cite in the answer resolves into the ledger that travels with it",
   "an unverifiable claim never reaches the client",
   "a question that is missing, empty or not a string is refused with 400",
   "an over-long question or context is refused with 413 rather than sent to Claude",
   "an unauthenticated request is refused before anything is spent",
   "the catalog is readable, so the UI can say what Coach can and cannot see",
   "the audit is readable, and the grounding rate with it",
   "asking for the event stream streams the trace and ends with the result",
   "the ask route is rate limited, and says so before it spends anything"
  ],
  "failed": []
 },
 "lexicon": {
  "suites": [
   "test/coach-stat-names.test.js"
  ],
  "titles": [
   "every field the lexicon names is a real column of a real table",
   "every field points at a concept that exists",
   "every concept has a display name, a plain sentence and a direction",
   "a display name belongs to exactly one concept, so the same words never mean two things",
   "the same quantity in two tables resolves to the same name",
   "describeField gives a screen everything it needs to label a number",
   "an unknown field is null, not a guessed label",
   "a stat we do not store says so, and says what it would take",
   "the lexicon serialises for a client with no Maps and no functions",
   "the stats Nick named by hand are all covered one way or the other",
   "chance to play cannot be shown without its basis",
   "the emitted lexicon is the module, not a second copy that has drifted"
  ],
  "failed": []
 },
 "person": {
  "suites": [
   "test/coach-person-context.test.js",
   "test/coach-person-profile-script.test.js",
   "test/coach-person-variables.test.js"
  ],
  "titles": [
   "the Josh Smith rule is there, as a row, with who said it and when",
   "seeding twice does not duplicate it",
   "\"we\" fires on the sentence Nick described",
   "\"we\" does not fire on week, weather or were",
   "one person's rule never touches another person's messages",
   "a rule can be added for anyone, and it needs a person, a scope, a rule and an author",
   "a multi-word phrase matches as a phrase, not as its words",
   "a rule with no trigger applies to the person always, not to no message",
   "a retired rule stops applying but stays on the record",
   "the brief reads as a sentence a model or a person can act on",
   "matching is case-insensitive and survives punctuation",
   "the dry run reports every person and writes nothing",
   "--write loads the derived table, with the sample size on every row",
   "nothing the script stored carries a message, so the table can leave the machine",
   "running it twice replaces rather than duplicates",
   "a machine without the corpus is told so plainly, and exits non-zero",
   "a person with real volume gets variables, each with its own sample size",
   "reply latency is the real median of the real gaps",
   "talking to himself is not replying to anyone",
   "a thin variable is withheld, not reported as zero",
   "style variables count what they say they count",
   "the extractor\u2019s own aggregates are read, not recomputed",
   "confidence volatility is computed, because the extractor only keeps the mean",
   "the non-fantasy share comes from the signal the extractor already writes",
   "a person's context rules are counted, so a profile can say how much is being reinterpreted",
   "a variable we cannot compute is named, with what is missing",
   "an unknown person is empty, not invented",
   "nothing in a profile carries a message, so a derived profile can leave the machine",
   "a null says which kind of null it is"
  ],
  "failed": []
 },
 "grading": {
  "suites": [
   "test/coach-person-grading.test.js",
   "test/coach-person-profile-script.test.js"
  ],
  "titles": [
   "grading reports every computed variable, with the people behind each grade",
   "a variable that is the same in both halves passes",
   "a variable that changes between the halves fails, and is named",
   "too few people is \"not enough data\", never a pass and never a fail",
   "the extractor's own aggregates are reported as ungradeable, not as failures",
   "the split is on time, not on message count, and the report says where it fell",
   "only a pass may make a variable priceable, and it is a deliberate second step",
   "a person too thin in either half is left out of the grade entirely",
   "a variable nobody differs on is \"not enough data\", not a failure",
   "beating the average on size is not enough: people have to keep their order",
   "applying a grade takes priceable away as readily as it gives it",
   "the runner prints a grade for every variable and applies nothing by default",
   "the dry run reports every person and writes nothing",
   "--write loads the derived table, with the sample size on every row",
   "nothing the script stored carries a message, so the table can leave the machine",
   "running it twice replaces rather than duplicates",
   "a machine without the corpus is told so plainly, and exits non-zero"
  ],
  "failed": []
 },
 "creators": {
  "suites": [
   "test/coach-catalog.test.js",
   "test/coach-select.test.js"
  ],
  "titles": [
   "every catalogued table exists, unless only a script creates it",
   "every entry states grain, meaning, freshness and collection mode",
   "collection mode distinguishes hand-collected data, so an answer can show its age",
   "columns are read from the live database, not hand-copied",
   "a table that is not catalogued is not readable and has no entry",
   "the tables behind Nick's three example questions are covered",
   "coverage names what is present and not yet catalogued, and never contradicts itself",
   "the full catalog is serialisable and carries no Map or Set",
   "COACH_TABLES is frozen, so no caller can widen what Coach may read at runtime",
   "a table holding credentials declares them, so they can be withheld",
   "every redacted column named by the catalog actually exists on its table",
   "every stat the lexicon names sits in a table Coach is allowed to read",
   "a table the declared schema does not create says who does, and one it creates says nothing",
   "every file a runtime creator names exists, and one of them holds the CREATE TABLE",
   "the audit of Coach\u2019s own answers is readable, but the answers themselves are withheld",
   "an absent table is sorted by whether anything is known to build it",
   "a SELECT over catalogued tables returns rows, their columns and where they came from",
   "an aliased table is still resolved to the real table it reads",
   "values are bound, so a value that looks like SQL stays a value",
   "a write is refused by policy, and the row count is untouched",
   "the connection itself is read-only, so the policy check is not the only thing standing there",
   "a table the catalog does not describe is refused, and the refusal names it",
   "one catalogued table does not license an uncatalogued one beside it",
   "a WITH query is allowed when every table it reads is catalogued",
   "a second statement, an ATTACH and a PRAGMA are all refused",
   "a trailing semicolon on a single statement is fine",
   "more rows than the cap are truncated and say so, rather than being quietly cut",
   "a broken query fails as a SQL error the model can act on, not as a refusal",
   "an empty or non-string query is refused rather than reaching SQLite",
   "a redacted column cannot be selected, and the refusal names it",
   "a redacted column cannot be filtered on either, so it cannot be read a character at a time",
   "the rest of a table with a redacted column is still readable",
   "a forbidden keyword in the body of a WITH query is refused as policy, not left to SQLite",
   "a forbidden word inside a string literal is a value, not a keyword",
   "a second statement is refused even when it is itself only a SELECT",
   "a catalogued table that has not been built yet fails with who builds it, not SQLite\u2019s wording",
   "the friendlier wording is only for catalogued tables, never a name Coach does not know"
  ],
  "failed": []
 }
}