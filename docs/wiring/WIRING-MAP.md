# The wiring map

Generated by `node scripts/wiring-map.mjs`. Do not edit by hand — re-run it.
Walked 957 files, 308 tables, 779 surfaces (548 routes, 62 scheduler jobs).

Everything below is derived from the source. Edges a walker cannot see are
in `annotations.json` and marked **ASSERTED** where they appear.

## What this map cannot see

First, because both of these have already produced a wrong answer here.

- **ROWS, NOT WRITERS.** It can tell you whether code writes a table. It cannot tell you whether the rows are any good. league_member_identity has a writer reachable from a route, so this map calls it fed — and it is not, because a row only counts once its confidence is "confirmed", which only a person can set. A clean bill of health here is not evidence a surface has data.
- **WHOLE-FILE REPLACEMENT IS INVISIBLE.** A table filled by replacing an entire database file has no writer this map can find. Rows marked table-in-another-database are exactly that case and are NOT missing feeds. This map reported four of them as broken once; they were fine.
- **REACHABILITY IS NOT A CALL GRAPH.** Hop distance says a route imports something that imports the module. It does not prove the route calls it. Edges behind an off-by-default flag are listed separately (edge-behind-an-off-flag) because they are real as code and false as behaviour.
- **DYNAMIC NAMES ARE INVISIBLE.** A table or module reached only through an interpolated identifier does not appear at all.
- **ROUTES MATCH BY SHAPE.** /a/:id and /a/:other are the same path here.
- **COLUMNS WRITTEN THROUGH A BUILT COLUMN LIST ARE INVISIBLE.** An INSERT whose columns come from a JavaScript array, or an INSERT ... SELECT *, names no column this scan can read, so every column of that table is left alone rather than reported on evidence that does not exist. It found five such tables and said nothing about any of their columns.
- **A PARAMETER BUILT AT RUNTIME LOOKS UNPASSED.** The rule reads query strings out of source text. A caller that assembles one from variables would be missed, and the parameter would be reported as never passed when it is.
- **AN EDGE CAN CHANGE WITHOUT A DEPLOY.** scripts/fit-availability.mjs writes two tables and three live consumers price differently from that moment on, with no code change at all. The script, the tables and the consumers are all on this graph, but nothing here says that running it alters a process already serving requests. Same family as ROWS, NOT WRITERS: the shape of the wiring is not the state of it.
- **A CONSTANT IS NOT A FINDING UNTIL SOMEBODY HAS READ BOTH DIRECTIONS FROM IT.** constant-standing-in-for-a-model finds a typed number defaulting a fitted column. It cannot decide whether that default reaches anything, and there are two separate ways it does not. UPWARD: the filter that decides reachability is usually twenty lines earlier and sometimes in another file. On 2026-09-19 season-sim.js:226 was reported and repeated as the strongest case of the category; the roster is filtered to the covered positions at :201 and the fallback cannot fire for the positions that mattered. DOWNWARD: the constant is reached and then annihilated. Same day, trade-engine.js:346 on main at 791b131 does hand every K/DEF a 0.92, and all four consumers discard it - :359 multiplies it by a currentWeekPpg forced to 0 because :336 gives those positions a schedule with games: [], :385 weights that 0 at 0.25 against a rosPpg with no availability term, :398 and :406 need a weekProjection that projections.js:292 excludes them from, and :2827 sits behind a week_points > 0 filter they fail. Only the served field at :464 survives, which is a real defect of a different kind: a number displayed as if a model produced it. Three wrong claims came out of this one constant in one evening, from two threads. The line is always real; the sentence about what it means is the part that gets invented. Treat every hit as a lead to read, never a fact to relay.
- **SERVED-BUT-NOT-RENDERED ONLY READS res.**json. A field a service attaches to an object the route spreads is on the wire and not in this rule. Walking into the services made it 1,435 findings of which almost none were payload fields, so the rule takes the narrow, certain source and says so rather than being comprehensive and ignored.
- **A LINE NUMBER IS WORTHLESS WITHOUT ITS TREE.** On 2026-09-19 three threads cited contingency.js at :117, :835 and :836 for the same statement, each correct for the branch it had read. This map names the tree it read at the top of every artifact; quoting a line from it without that name is how the same hour gets spent twice.
- **THIS IS A SOURCE TREE, NOT THE RUNNING APP.** Every count and every edge here describes the checkout it was run in, named at the top of the file. On 2026-09-19 the deployed binary was ahead of main on contingency.js, serving three fields main does not have. Never read this map as a statement about what production is doing.

## How to read it

- **MISSING FEED** — a surface depends on something nothing produces. This is the one that hurts users: the page still renders, using a default or a constant, and looks fine.
- **ORPHAN** — something produced that reaches no surface. Wasted work, not a user-facing bug.

Betting rows are mapped and tagged `betting`. They are out of scope for work, in scope for knowing.

## Findings

**10 missing feed** — a surface depends on something nothing produces.
**2069 orphan** — something produced that reaches no surface.
230 context rows, listed because they are worth knowing and are usually fine.

The missing-feed list in full, because it is short and it is the one that matters:

- **espn_player_market** `[fantasy]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer server/services/espn-market.js:45, reader server/routes/aggregates.js:219, reader server/services/consensus-weights.js:526, reader server/services/espn-market.js:69
  - reached from /api/aggregates /api/drafts /api/leagues /api/players /api/trades /api/accolades /api/betting /api/espn /api/mlb /api/model /api/nfl /api/nfl-betting /api/stats /api/analysis /api/dev /api/nfl-market /api/props /api/rankings /api/teams
- **league_roster_snapshots** `[fantasy]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer server/migrations/058_league_roster_snapshots.js:59, writer scripts/collect-roster-snapshots.mjs:113, writer scripts/collect-roster-snapshots.mjs:116, writer scripts/collect-roster-snapshots.mjs:118
  - reached from /api/trades /api/model /api/players
- **league_season_teams** `[fantasy]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer scripts/backfill-league-history.mjs:112, reader server/services/manager-archetypes.js:241, reader server/services/manager-archetypes.js:819, reader server/services/manager-archetypes.js:831
  - reached from /api/trades
- **league_transactions_raw** `[fantasy]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer scripts/collect-league-transactions.mjs:34, reader server/services/counterparty-pricing.js:815, reader server/services/manager-signals.js:168, reader server/services/trade-tactics.js:232
  - reached from /api/league-chat /api/trades /api/model /api/players /api/accolades /api/aggregates /api/betting /api/dev /api/drafts /api/espn /api/leagues /api/mlb /api/nfl /api/nfl-betting /api/stats
- **nfl_availability_rates** `[fantasy]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer scripts/fit-availability.mjs:412, writer scripts/fit-availability.mjs:414, reader server/services/contingency.js:568, reader scripts/availability-decision-calibration.mjs:222
  - reached from /api/model /api/dev /api/leagues /api/news /api/players /api/trades /api/accolades /api/aggregates /api/betting /api/drafts /api/espn /api/mlb /api/nfl /api/nfl-betting /api/nfl-market /api/stats
- **nfl_availability_role_rates** `[fantasy]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer scripts/fit-availability.mjs:418, writer scripts/fit-availability.mjs:421, reader server/services/contingency.js:569, reader scripts/availability-decision-calibration.mjs:103
  - reached from /api/model /api/dev /api/leagues /api/news /api/players /api/trades /api/accolades /api/aggregates /api/betting /api/drafts /api/espn /api/mlb /api/nfl /api/nfl-betting /api/nfl-market /api/stats
- **nfl_live_possession_predictions** `[shared]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer server/services/nfl-live-ledger.js:98, reader server/services/nfl-expert-council.js:326, reader server/services/nfl-live-ledger.js:122, reader server/services/nfl-live-ledger.js:179
  - reached from /api/nfl-betting /api/accolades /api/aggregates /api/betting /api/espn /api/mlb /api/nfl /api/nfl-market /api/stats /api/analysis /api/dev /api/drafts /api/leagues /api/model /api/players /api/props /api/rankings /api/teams
- **nfl_live_possession_settlements** `[shared]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer server/services/nfl-live-ledger.js:131, reader server/services/nfl-expert-council.js:326, reader server/services/nfl-live-ledger.js:122, reader server/services/nfl-live-ledger.js:179
  - reached from /api/nfl-betting /api/accolades /api/aggregates /api/betting /api/espn /api/mlb /api/nfl /api/nfl-market /api/stats /api/analysis /api/dev /api/drafts /api/leagues /api/model /api/players /api/props /api/rankings /api/teams
- **nfl_rebuild_checkpoints** `[shared]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer scripts/nfl-2022-2025-rebuild.mjs:72, writer scripts/nfl-2022-2025-rebuild.mjs:79, writer scripts/nfl-2022-2025-rebuild.mjs:92, reader server/services/nfl-rebuild-progress.js:15
  - reached from /api/nfl-betting
- **nfl_rebuild_progress** `[shared]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer scripts/nfl-2022-2025-rebuild.mjs:49, reader server/services/nfl-rebuild-progress.js:8, reader scripts/nfl-2022-2025-rebuild.mjs:85, reader scripts/nfl-2022-2025-rebuild.mjs:95
  - reached from /api/nfl-betting

**154 should be wired** — nothing is broken enough to fail a test, and the code is answering from the wrong place, with a default, or from a column that is null on every row. This family is the one to read when the question is "what did we forget to connect", rather than "what is connected".

- **draft_pick_corrections.applied_at** `[column-read-never-written]` — declared on draft_pick_corrections and read on a live surface, and no INSERT or UPDATE in the repository ever sets it — so it is null on every row and the reader's fallback is what runs
  - server/services/draft-reconcile.js:173
- **draft_pick_quarantine.first_seen_at** `[column-read-never-written]` — declared on draft_pick_quarantine and read on a live surface, and no INSERT or UPDATE in the repository ever sets it — so it is null on every row and the reader's fallback is what runs
  - server/services/draft-reconcile.js:168
- **players.bye_week** `[column-read-never-written]` — declared on players and read on a live surface, and no INSERT or UPDATE in the repository ever sets it — so it is null on every row and the reader's fallback is what runs
  - server/services/draft-assist.js:202, server/services/draft-assist.js:885
- **backfillGameVariance()** `[producer-with-no-caller]` — fills nfl_game_variance, which a live surface reads, and nothing in the repository calls it — no route, no job, no script
  - server/services/nfl-postgame-truth.js:571, route:GET /api/nfl-betting/ai-replay/:id (2 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (2 hops)
- **backfillHistoricalQuoteTape()** `[producer-with-no-caller]` — fills nfl_quote_batches, nfl_quote_tape, which a live surface reads, and nothing in the repository calls it — no route, no job, no script
  - server/services/nfl-quote-tape.js:152, script:npm/node scripts/build-evidence-dataset.mjs (1 hop), route:DELETE /api/nfl-market/bets/:id (2 hops)
- **backfillNewsEntities()** `[producer-with-no-caller]` — fills news_items, which a live surface reads, and nothing in the repository calls it — no route, no job, no script
  - server/routes/espn.js:210, route:DELETE /api/news/:id (2 hops), route:GET /api/news (2 hops)
- **buildTotalCalibration()** `[producer-with-no-caller]` — fills nfl_total_calibrations, which a live surface reads, and nothing in the repository calls it — no route, no job, no script
  - server/services/nfl-total-calibration.js:308, route:GET /api/betting/abstentions (2 hops), route:GET /api/betting/audits (2 hops)
- **syncDraftRookieEvidence()** `[producer-with-no-caller]` — fills nfl_rookie_evidence, which a live surface reads, and nothing in the repository calls it — no route, no job, no script
  - server/services/nfl-rookies.js:237, boot:server/index.js (2 hops), job:espn_line_watch (2 hops)
- **syncEspnMarket()** `[producer-with-no-caller]` — fills espn_player_market, which a live surface reads, and nothing in the repository calls it — no route, no job, no script
  - server/services/espn-market.js:18, route:GET /api/aggregates (0 hops), route:POST /api/aggregates/create-board (0 hops)
- **activeKVector() vs activeKVectorFor()** `[two-names-different-sources]` — both are exported from this module and the names read as the same thing, but activeKVectorFor() reads player_week_usage, players and activeKVector() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/shrinkage-fit.js:449, server/services/shrinkage-fit.js:515
- **availability() vs availabilityBasis()** `[two-names-different-sources]` — both are exported from this module and the names read as the same thing, but availabilityBasis() reads nfl_availability_rates, nfl_availability_role_rates and availability() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/contingency.js:40, server/services/contingency.js:593
- **availability() vs weeklyAvailability()** `[two-names-different-sources]` — both are exported from this module and the names read as the same thing, but weeklyAvailability() reads nfl_teams, nfl_injuries, leagues, nfl_availability_rates, nfl_availability_role_rates, player_week_snaps and availability() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/contingency.js:40, server/services/contingency.js:885
- **nflResidualAudit() vs latestNflResidualAudit()** `[two-names-different-sources]` — both are exported from this module and the names read as the same thing, but latestNflResidualAudit() reads nfl_residual_audits and nflResidualAudit() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/nfl-research.js:57, server/services/nfl-research.js:109
- **registry() vs registryHistory()** `[two-names-different-sources]` — both are exported from this module and the names read as the same thing, but registryHistory() reads model_registry_history and registry() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/model-governance.js:105, server/services/model-governance.js:199
- **schemeChange() vs measureSchemeChangeEffect()** `[two-names-different-sources]` — both are exported from this module and the names read as the same thing, but measureSchemeChangeEffect() reads player_week_usage and schemeChange() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/nfl-scheme.js:87, server/services/nfl-scheme.js:170
- **walkForward() vs multiplierWalkForward()** `[two-names-different-sources]` — both are exported from this module and the names read as the same thing, but multiplierWalkForward() reads off_player_season_features and walkForward() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/offseason-model.js:1048, server/services/offseason-model.js:1599
- **walkForward() vs walkForwardV2()** `[two-names-different-sources]` — both are exported from this module and the names read as the same thing, but walkForwardV2() reads off_player_season_features and walkForward() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/offseason-model.js:1048, server/services/offseason-model.js:1432
- **active_probability ?? 0.92** `[constant-standing-in-for-a-model]` — 0.92 is used wherever active_probability is absent, and active_probability is a column the app fits and stores in model_predictions. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/news-fantasy-impact.js:87
- **active_probability ?? 0.92** `[constant-standing-in-for-a-model]` — 0.92 is used wherever active_probability is absent, and active_probability is a column the app fits and stores in model_predictions. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/role-scenario-engine.js:124
- **active_probability ?? 0.92** `[constant-standing-in-for-a-model]` — 0.92 is used wherever active_probability is absent, and active_probability is a column the app fits and stores in model_predictions. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/roster-risk.js:257
- **active_probability ?? 0.92** `[constant-standing-in-for-a-model]` — 0.92 is used wherever active_probability is absent, and active_probability is a column the app fits and stores in model_predictions. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/season-sim.js:226
- **active_probability ?? 0.92** `[constant-standing-in-for-a-model]` — 0.92 is used wherever active_probability is absent, and active_probability is a column the app fits and stores in model_predictions. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/trade-engine.js:346
- **clock_seconds ?? 900** `[constant-standing-in-for-a-model]` — 900 is used wherever clock_seconds is absent, and clock_seconds is a column the app fits and stores in nfl_play_by_play. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/nfl-espn-pbp.js:294
- **experience ?? 99** `[constant-standing-in-for-a-model]` — 99 is used wherever experience is absent, and experience is a column the app fits and stores in roster_players. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/routes/nfldata.js:334
- **home_rest ?? 7** `[constant-standing-in-for-a-model]` — 7 is used wherever home_rest is absent, and home_rest is a column the app fits and stores in off_schedule_games. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/nfl-specialists.js:220
- **importance ?? 2** `[constant-standing-in-for-a-model]` — 2 is used wherever importance is absent, and importance is a column the app fits and stores in news_items. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/news/store.js:31
- **importance ?? 2** `[constant-standing-in-for-a-model]` — 2 is used wherever importance is absent, and importance is a column the app fits and stores in news_items. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/routes/news.js:76
- **pick_seconds ?? 90** `[constant-standing-in-for-a-model]` — 90 is used wherever pick_seconds is absent, and pick_seconds is a column the app fits and stores in drafts. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/draft/store.js:86
- **unavailable_probability ?? 0.42** `[constant-standing-in-for-a-model]` — 0.42 is used wherever unavailable_probability is absent, and unavailable_probability is a column the app fits and stores in nfl_news_signals, nfl_news_signals_new, nfl_news_signals_old. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/nfl-postgame-truth.js:515
- **yes_price ?? 0.5** `[constant-standing-in-for-a-model]` — 0.5 is used wherever yes_price is absent, and yes_price is a column the app fits and stores in prediction_market_quotes, prediction_market_flow. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/prediction-markets.js:466
- **days** `[parameter-never-passed]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/dev.js:58
- **days** `[parameter-never-passed]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/news.js:224
- **from_week** `[parameter-never-passed]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/model.js:528
- **lookback** `[parameter-never-passed]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/trades.js:259
- **min_opportunities** `[parameter-never-passed]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/trades.js:340
- **min_projected** `[parameter-never-passed]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/trades.js:714
- **sims** `[parameter-never-passed]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/drafts.js:962
- **test_season** `[parameter-never-passed]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/model.js:705
- **chat_corpus** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/trades.js:609, route:GET /api/trades/:leagueId/brain/bye-risk (0 hops), route:GET /api/trades/:leagueId/brain/fragility (0 hops)
- **cooldown_ms** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/news.js:95, route:DELETE /api/news/:id (0 hops), route:GET /api/news (0 hops)
- **draft_complete** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/drafts.js:577, route:DELETE /api/drafts/:id (0 hops), route:DELETE /api/drafts/:id/picks/last (0 hops)
- **expires_in_days** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/google-auth.js:239, route:DELETE /api/auth/invites/:id (0 hops), route:GET /api/auth/accounts (0 hops)
- **expires_in_days** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/local-auth.js:110, route:GET /api/auth/pairing-info (0 hops), route:GET /api/auth/tunnel-url (0 hops)
- **game_script** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/model.js:451, route:GET /api/model/:leagueId/simulate (0 hops), route:GET /api/model/accuracy (0 hops)
- **gamescript_fitted** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/model.js:597, route:GET /api/model/:leagueId/simulate (0 hops), route:GET /api/model/accuracy (0 hops)
- **href_bytes** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/draft-capture.js:73, route:GET /api/drafts/:id/capture-bookmarklet (0 hops)
- **key_source** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/draft-capture.js:73, route:GET /api/drafts/:id/capture-bookmarklet (0 hops)
- **last_result** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/news.js:95, route:DELETE /api/news/:id (0 hops), route:GET /api/news (0 hops)
- **player_views** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/trades.js:609, route:GET /api/trades/:leagueId/brain/bye-risk (0 hops), route:GET /api/trades/:leagueId/brain/fragility (0 hops)
- **rosters_with_chat** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/trades.js:609, route:GET /api/trades/:leagueId/brain/bye-risk (0 hops), route:GET /api/trades/:leagueId/brain/fragility (0 hops)
- **rosters_with_signals** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/trades.js:609, route:GET /api/trades/:leagueId/brain/bye-risk (0 hops), route:GET /api/trades/:leagueId/brain/fragility (0 hops)
- **teams_with_stale_names** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/analysis.js:200, route:GET /api/analysis/validate (0 hops), route:POST /api/analysis/refresh (0 hops)
- **total_flags** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/analysis.js:200, route:GET /api/analysis/validate (0 hops), route:POST /api/analysis/refresh (0 hops)
- **usage_history** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/model.js:451, route:GET /api/model/:leagueId/simulate (0 hops), route:GET /api/model/accuracy (0 hops)
- **weekly_availability** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/model.js:451, route:GET /api/model/:leagueId/simulate (0 hops), route:GET /api/model/accuracy (0 hops)
- **weekly_projection** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/model.js:451, route:GET /api/model/:leagueId/simulate (0 hops), route:GET /api/model/accuracy (0 hops)
- **weekly_projection** `[served-but-not-rendered]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/players.js:76, route:GET /api/players (0 hops), route:GET /api/players/:id (0 hops)
- ...and 97 more in betting code, tagged and out of scope for work.

| rule | family | fantasy | betting | shared |
| --- | --- | --: | --: | --: |
| `column-read-never-written` | should-wire | 3 | 0 | 0 |
| `producer-with-no-caller` | should-wire | 0 | 0 | 6 |
| `two-names-different-sources` | should-wire | 2 | 1 | 6 |
| `constant-standing-in-for-a-model` | should-wire | 5 | 0 | 8 |
| `parameter-never-passed` | should-wire | 4 | 35 | 4 |
| `served-but-not-rendered` | should-wire | 7 | 61 | 12 |
| `table-hand-fed` | missing-feed | 6 | 0 | 4 |
| `cache-blind-to-its-inputs` | staleness | 0 | 0 | 3 |
| `table-in-another-database` | context | 2 | 0 | 8 |
| `table-never-scheduled` | context | 12 | 9 | 40 |
| `edge-behind-an-off-flag` | context | 1 | 0 | 1 |
| `module-only-tested` | orphan | 2 | 8 | 6 |
| `module-imported-by-nothing` | orphan | 1 | 0 | 14 |
| `module-reaches-no-surface` | orphan | 0 | 4 | 13 |
| `field-attached-never-read` | orphan | 4 | 18 | 39 |
| `value-computed-never-used` | orphan | 15 | 15 | 64 |
| `table-never-read` | orphan | 0 | 4 | 13 |
| `export-only-tested` | orphan | 71 | 90 | 200 |
| `export-imported-by-nothing` | orphan | 132 | 271 | 623 |
| `route-no-caller` | orphan | 52 | 331 | 79 |

### `column-read-never-written` — SHOULD WIRE (3)

- **draft_pick_corrections.applied_at** `[fantasy]` — declared on draft_pick_corrections and read on a live surface, and no INSERT or UPDATE in the repository ever sets it — so it is null on every row and the reader's fallback is what runs
  - server/services/draft-reconcile.js:173
- **draft_pick_quarantine.first_seen_at** `[fantasy]` — declared on draft_pick_quarantine and read on a live surface, and no INSERT or UPDATE in the repository ever sets it — so it is null on every row and the reader's fallback is what runs
  - server/services/draft-reconcile.js:168
- **players.bye_week** `[fantasy]` — declared on players and read on a live surface, and no INSERT or UPDATE in the repository ever sets it — so it is null on every row and the reader's fallback is what runs
  - server/services/draft-assist.js:202, server/services/draft-assist.js:885

### `producer-with-no-caller` — SHOULD WIRE (6)

- **backfillGameVariance()** `[shared]` — fills nfl_game_variance, which a live surface reads, and nothing in the repository calls it — no route, no job, no script
  - server/services/nfl-postgame-truth.js:571, route:GET /api/nfl-betting/ai-replay/:id (2 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (2 hops)
- **backfillHistoricalQuoteTape()** `[shared]` — fills nfl_quote_batches, nfl_quote_tape, which a live surface reads, and nothing in the repository calls it — no route, no job, no script
  - server/services/nfl-quote-tape.js:152, script:npm/node scripts/build-evidence-dataset.mjs (1 hop), route:DELETE /api/nfl-market/bets/:id (2 hops)
- **backfillNewsEntities()** `[shared]` — fills news_items, which a live surface reads, and nothing in the repository calls it — no route, no job, no script
  - server/routes/espn.js:210, route:DELETE /api/news/:id (2 hops), route:GET /api/news (2 hops)
- **buildTotalCalibration()** `[shared]` — fills nfl_total_calibrations, which a live surface reads, and nothing in the repository calls it — no route, no job, no script
  - server/services/nfl-total-calibration.js:308, route:GET /api/betting/abstentions (2 hops), route:GET /api/betting/audits (2 hops)
- **syncDraftRookieEvidence()** `[shared]` — fills nfl_rookie_evidence, which a live surface reads, and nothing in the repository calls it — no route, no job, no script
  - server/services/nfl-rookies.js:237, boot:server/index.js (2 hops), job:espn_line_watch (2 hops)
- **syncEspnMarket()** `[shared]` — fills espn_player_market, which a live surface reads, and nothing in the repository calls it — no route, no job, no script
  - server/services/espn-market.js:18, route:GET /api/aggregates (0 hops), route:POST /api/aggregates/create-board (0 hops)

### `two-names-different-sources` — SHOULD WIRE (9)

- **activeKVector() vs activeKVectorFor()** `[shared]` — both are exported from this module and the names read as the same thing, but activeKVectorFor() reads player_week_usage, players and activeKVector() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/shrinkage-fit.js:449, server/services/shrinkage-fit.js:515
- **availability() vs availabilityBasis()** `[fantasy]` — both are exported from this module and the names read as the same thing, but availabilityBasis() reads nfl_availability_rates, nfl_availability_role_rates and availability() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/contingency.js:40, server/services/contingency.js:593
- **availability() vs weeklyAvailability()** `[fantasy]` — both are exported from this module and the names read as the same thing, but weeklyAvailability() reads nfl_teams, nfl_injuries, leagues, nfl_availability_rates, nfl_availability_role_rates, player_week_snaps and availability() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/contingency.js:40, server/services/contingency.js:885
- **liveEdge() vs liveEdgeStatus()** `[betting]` — both are exported from this module and the names read as the same thing, but liveEdgeStatus() reads polymarket_quotes, nfl_play_by_play and liveEdge() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/live-edge.js:108, server/services/live-edge.js:237
- **nflResidualAudit() vs latestNflResidualAudit()** `[shared]` — both are exported from this module and the names read as the same thing, but latestNflResidualAudit() reads nfl_residual_audits and nflResidualAudit() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/nfl-research.js:57, server/services/nfl-research.js:109
- **registry() vs registryHistory()** `[shared]` — both are exported from this module and the names read as the same thing, but registryHistory() reads model_registry_history and registry() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/model-governance.js:105, server/services/model-governance.js:199
- **schemeChange() vs measureSchemeChangeEffect()** `[shared]` — both are exported from this module and the names read as the same thing, but measureSchemeChangeEffect() reads player_week_usage and schemeChange() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/nfl-scheme.js:87, server/services/nfl-scheme.js:170
- **walkForward() vs multiplierWalkForward()** `[shared]` — both are exported from this module and the names read as the same thing, but multiplierWalkForward() reads off_player_season_features and walkForward() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/offseason-model.js:1048, server/services/offseason-model.js:1599
- **walkForward() vs walkForwardV2()** `[shared]` — both are exported from this module and the names read as the same thing, but walkForwardV2() reads off_player_season_features and walkForward() does not. Whoever reaches for the shorter name gets an answer from a different source, with no error
  - server/services/offseason-model.js:1048, server/services/offseason-model.js:1432

### `constant-standing-in-for-a-model` — SHOULD WIRE (13)

- **active_probability ?? 0.92** `[fantasy]` — 0.92 is used wherever active_probability is absent, and active_probability is a column the app fits and stores in model_predictions. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/news-fantasy-impact.js:87
- **active_probability ?? 0.92** `[shared]` — 0.92 is used wherever active_probability is absent, and active_probability is a column the app fits and stores in model_predictions. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/role-scenario-engine.js:124
- **active_probability ?? 0.92** `[fantasy]` — 0.92 is used wherever active_probability is absent, and active_probability is a column the app fits and stores in model_predictions. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/roster-risk.js:257
- **active_probability ?? 0.92** `[fantasy]` — 0.92 is used wherever active_probability is absent, and active_probability is a column the app fits and stores in model_predictions. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/season-sim.js:226
- **active_probability ?? 0.92** `[fantasy]` — 0.92 is used wherever active_probability is absent, and active_probability is a column the app fits and stores in model_predictions. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/trade-engine.js:346
- **clock_seconds ?? 900** `[shared]` — 900 is used wherever clock_seconds is absent, and clock_seconds is a column the app fits and stores in nfl_play_by_play. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/nfl-espn-pbp.js:294
- **experience ?? 99** `[shared]` — 99 is used wherever experience is absent, and experience is a column the app fits and stores in roster_players. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/routes/nfldata.js:334
- **home_rest ?? 7** `[shared]` — 7 is used wherever home_rest is absent, and home_rest is a column the app fits and stores in off_schedule_games. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/nfl-specialists.js:220
- **importance ?? 2** `[shared]` — 2 is used wherever importance is absent, and importance is a column the app fits and stores in news_items. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/news/store.js:31
- **importance ?? 2** `[shared]` — 2 is used wherever importance is absent, and importance is a column the app fits and stores in news_items. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/routes/news.js:76
- **pick_seconds ?? 90** `[fantasy]` — 90 is used wherever pick_seconds is absent, and pick_seconds is a column the app fits and stores in drafts. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/draft/store.js:86
- **unavailable_probability ?? 0.42** `[shared]` — 0.42 is used wherever unavailable_probability is absent, and unavailable_probability is a column the app fits and stores in nfl_news_signals, nfl_news_signals_new, nfl_news_signals_old. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/nfl-postgame-truth.js:515
- **yes_price ?? 0.5** `[shared]` — 0.5 is used wherever yes_price is absent, and yes_price is a column the app fits and stores in prediction_market_quotes, prediction_market_flow. THIS DOES NOT PROVE THE FALLBACK CHANGES ANYTHING. Read in both directions before relaying it. UPWARD: if what populates the collection above this line was already filtered to the population the fit covers, the number is unreachable (season-sim.js:226 looked like the worst case of this rule and is filtered at :201). DOWNWARD: follow every consumer of the value, because a constant can be reached and still reach nothing (trade-engine.js:346 hands every K/DEF a 0.92 and all four consumers discard it, leaving only a served field with no calculation behind it). Three wrong claims came out of one constant on 2026-09-19. This is a lead to read, not a fact
  - server/services/prediction-markets.js:466

### `parameter-never-passed` — SHOULD WIRE (43)

- **alpha** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/betting-hub.js:687
- **away** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/betting-hub.js:830
- **bankroll** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:952
- **cadence_days** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/mlb.js:77
- **days** `[shared]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/dev.js:58
- **days** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/mlb.js:192
- **days** `[shared]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/news.js:224
- **days** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:326
- **depth** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:1598
- **end** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:246
- **from_week** `[shared]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/model.js:528
- **games** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:1199
- **home** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/betting-hub.js:830
- **kelly** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:953
- **lookback** `[fantasy]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/trades.js:259
- **lookback_days** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/mlb.js:76
- **lr** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:1599
- **max_events** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:158
- **max_picks** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:695
- **min** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/betting-hub.js:851
- **min_bets** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:713
- **min_books** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:905
- **min_edge** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:692
- **min_games** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:1582
- **min_gap** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/betting-hub.js:760
- **min_lean** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:1887
- **min_notional** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/betting-hub.js:769
- **min_opportunities** `[fantasy]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/trades.js:340
- **min_points** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:566
- **min_projected** `[fantasy]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/trades.js:714
- **min_volume** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/betting-hub.js:788
- **n** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/mlb.js:128
- **n** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:420
- **open_exposure** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:996
- **rounds** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/edge.js:64
- **seconds** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/betting-hub.js:831
- **sims** `[fantasy]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/drafts.js:962
- **since_days** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/betting-hub.js:526
- **start** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:246
- **test_season** `[shared]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/model.js:705
- **trees** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/nfl-betting.js:1597
- **units** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/betting-hub.js:553
- **window_hours** `[betting]` — read here with a default, and no page, script or extension in the repository ever puts it in a query string — so the default is not a fallback, it is the only value this endpoint has ever been given
  - server/routes/betting-hub.js:525

### `served-but-not-rendered` — SHOULD WIRE (80)

- **actual_count** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/props.js:193, route:GET /api/props/auto-picks (0 hops), route:GET /api/props/board (0 hops)
- **actual_nrfi** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/props.js:193, route:GET /api/props/auto-picks (0 hops), route:GET /api/props/board (0 hops)
- **american_price** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-market.js:252, route:DELETE /api/nfl-market/bets/:id (0 hops), route:GET /api/nfl-market/accuracy (0 hops)
- **bias_audit** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-betting.js:245, route:GET /api/nfl-betting/ai-replay/:id (0 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (0 hops)
- **blocked_count** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/execution-slate.js:106, route:GET /api/execution-slate/opportunities (0 hops), route:POST /api/execution-slate/recommend (0 hops)
- **blocked_reasons** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/wong.js:359, route:GET /api/betting/wong/board (0 hops), route:GET /api/betting/wong/combos (0 hops)
- **board_captured_at** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/wong.js:359, route:GET /api/betting/wong/board (0 hops), route:GET /api/betting/wong/combos (0 hops)
- **book_latency** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/betting-hub.js:396, route:GET /api/betting/abstentions (0 hops), route:GET /api/betting/audits (0 hops)
- **book_push_rule** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/wong.js:305, route:GET /api/betting/wong/board (0 hops), route:GET /api/betting/wong/combos (0 hops)
- **brier_score** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/props.js:209, route:GET /api/props/auto-picks (0 hops), route:GET /api/props/board (0 hops)
- **capture_triggers** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/betting-hub.js:396, route:GET /api/betting/abstentions (0 hops), route:GET /api/betting/audits (0 hops)
- **chat_corpus** `[fantasy]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/trades.js:609, route:GET /api/trades/:leagueId/brain/bye-risk (0 hops), route:GET /api/trades/:leagueId/brain/fragility (0 hops)
- **cooldown_ms** `[shared]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/news.js:95, route:DELETE /api/news/:id (0 hops), route:GET /api/news (0 hops)
- **decision_audit** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-market.js:469, route:DELETE /api/nfl-market/bets/:id (0 hops), route:GET /api/nfl-market/accuracy (0 hops)
- **draft_complete** `[fantasy]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/drafts.js:577, route:DELETE /api/drafts/:id (0 hops), route:DELETE /api/drafts/:id/picks/last (0 hops)
- **drives_with_plays** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-betting.js:1530, route:GET /api/nfl-betting/ai-replay/:id (0 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (0 hops)
- **duration_ms** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/wong.js:267, route:GET /api/betting/wong/board (0 hops), route:GET /api/betting/wong/combos (0 hops)
- **edge_points** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-market.js:252, route:DELETE /api/nfl-market/bets/:id (0 hops), route:GET /api/nfl-market/accuracy (0 hops)
- **eligible_candidates** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-market.js:469, route:DELETE /api/nfl-market/bets/:id (0 hops), route:GET /api/nfl-market/accuracy (0 hops)
- **engine_version** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-betting.js:670, route:GET /api/nfl-betting/ai-replay/:id (0 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (0 hops)
- **evidence_note** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-market.js:252, route:DELETE /api/nfl-market/bets/:id (0 hops), route:GET /api/nfl-market/accuracy (0 hops)
- **evidence_status** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-market.js:252, route:DELETE /api/nfl-market/bets/:id (0 hops), route:GET /api/nfl-market/accuracy (0 hops)
- **expert_council** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-betting.js:280, route:GET /api/nfl-betting/ai-replay/:id (0 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (0 hops)
- **expires_in_days** `[shared]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/google-auth.js:239, route:DELETE /api/auth/invites/:id (0 hops), route:GET /api/auth/accounts (0 hops)
- **expires_in_days** `[shared]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/local-auth.js:110, route:GET /api/auth/pairing-info (0 hops), route:GET /api/auth/tunnel-url (0 hops)
- **game_script** `[shared]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/model.js:451, route:GET /api/model/:leagueId/simulate (0 hops), route:GET /api/model/accuracy (0 hops)
- **games_on_slate** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/betting-hub.js:419, route:GET /api/betting/abstentions (0 hops), route:GET /api/betting/audits (0 hops)
- **gamescript_fitted** `[shared]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/model.js:597, route:GET /api/model/:leagueId/simulate (0 hops), route:GET /api/model/accuracy (0 hops)
- **href_bytes** `[fantasy]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/draft-capture.js:73, route:GET /api/drafts/:id/capture-bookmarklet (0 hops)
- **implied_probability** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-market.js:252, route:DELETE /api/nfl-market/bets/:id (0 hops), route:GET /api/nfl-market/accuracy (0 hops)
- **key_source** `[fantasy]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/draft-capture.js:73, route:GET /api/drafts/:id/capture-bookmarklet (0 hops)
- **last_result** `[shared]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/news.js:95, route:DELETE /api/news/:id (0 hops), route:GET /api/news (0 hops)
- **market_margin** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-betting.js:1820, route:GET /api/nfl-betting/ai-replay/:id (0 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (0 hops)
- **mlb_cheapest** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/betting-hub.js:377, route:GET /api/betting/abstentions (0 hops), route:GET /api/betting/audits (0 hops)
- **model_margin** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-betting.js:1820, route:GET /api/nfl-betting/ai-replay/:id (0 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (0 hops)
- **model_probability** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-market.js:252, route:DELETE /api/nfl-market/bets/:id (0 hops), route:GET /api/nfl-market/accuracy (0 hops)
- **nfl_cheapest** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/betting-hub.js:377, route:GET /api/betting/abstentions (0 hops), route:GET /api/betting/audits (0 hops)
- **odds_cache** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-betting.js:1082, route:GET /api/nfl-betting/ai-replay/:id (0 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (0 hops)
- **open_tickets** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/wong.js:663, route:GET /api/betting/wong/board (0 hops), route:GET /api/betting/wong/combos (0 hops)
- **paper_tickets** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/wong.js:663, route:GET /api/betting/wong/board (0 hops), route:GET /api/betting/wong/combos (0 hops)
- **parlay_api** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/mlb.js:159, route:GET /api/mlb/auto-picks (0 hops), route:GET /api/mlb/board (0 hops)
- **placed_profit_units** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/wong.js:663, route:GET /api/betting/wong/board (0 hops), route:GET /api/betting/wong/combos (0 hops)
- **placed_tickets** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/wong.js:663, route:GET /api/betting/wong/board (0 hops), route:GET /api/betting/wong/combos (0 hops)
- **play_influence** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-betting.js:1251, route:GET /api/nfl-betting/ai-replay/:id (0 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (0 hops)
- **play_model** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-betting.js:1530, route:GET /api/nfl-betting/ai-replay/:id (0 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (0 hops)
- **player_views** `[fantasy]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/trades.js:609, route:GET /api/trades/:leagueId/brain/bye-risk (0 hops), route:GET /api/trades/:leagueId/brain/fragility (0 hops)
- **price_note** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/betting-hub.js:419, route:GET /api/betting/abstentions (0 hops), route:GET /api/betting/audits (0 hops)
- **profile_fell_back** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-betting.js:1530, route:GET /api/nfl-betting/ai-replay/:id (0 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (0 hops)
- **prop_calibration** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-betting.js:348, route:GET /api/nfl-betting/ai-replay/:id (0 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (0 hops)
- **prop_premium** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/betting-hub.js:377, route:GET /api/betting/abstentions (0 hops), route:GET /api/betting/audits (0 hops)
- **push_rule_note** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/wong.js:305, route:GET /api/betting/wong/board (0 hops), route:GET /api/betting/wong/combos (0 hops)
- **qualifying_legs** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/wong.js:359, route:GET /api/betting/wong/board (0 hops), route:GET /api/betting/wong/combos (0 hops)
- **quoted_price** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/betting-hub.js:419, route:GET /api/betting/abstentions (0 hops), route:GET /api/betting/audits (0 hops)
- **refreshed_at** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/wong.js:267, route:GET /api/betting/wong/board (0 hops), route:GET /api/betting/wong/combos (0 hops)
- **requested_date** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/mlb.js:136, route:GET /api/mlb/auto-picks (0 hops), route:GET /api/mlb/board (0 hops)
- **rosters_with_chat** `[fantasy]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/trades.js:609, route:GET /api/trades/:leagueId/brain/bye-risk (0 hops), route:GET /api/trades/:leagueId/brain/fragility (0 hops)
- **rosters_with_signals** `[fantasy]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/trades.js:609, route:GET /api/trades/:leagueId/brain/bye-risk (0 hops), route:GET /api/trades/:leagueId/brain/fragility (0 hops)
- **safe_endpoint** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/nfl-betting.js:955, route:GET /api/nfl-betting/ai-replay/:id (0 hops), route:GET /api/nfl-betting/ai-replay/:id/logs (0 hops)
- **scope_note** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/execution-slate.js:106, route:GET /api/execution-slate/opportunities (0 hops), route:POST /api/execution-slate/recommend (0 hops)
- **season_summary** `[betting]` — sent on a payload a page does fetch, and no file under client/src or the extension mentions this name anywhere — so it is computed on every request and nothing shows it
  - server/routes/wong.js:663, route:GET /api/betting/wong/board (0 hops), route:GET /api/betting/wong/combos (0 hops)
- _… 20 more in wiring-map.json_

### `table-hand-fed` — MISSING FEED (10)

- **espn_player_market** `[fantasy]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer server/services/espn-market.js:45, reader server/routes/aggregates.js:219, reader server/services/consensus-weights.js:526, reader server/services/espn-market.js:69, reader server/services/espn-market.js:76
  - wired into (≤3 hops): routes /api/aggregates@0 /api/drafts@1 /api/leagues@1 /api/players@1 /api/trades@1 /api/accolades@2 /api/betting@2 /api/espn@2; jobs espn_line_watch@2 polymarket_line_watch@2; scripts scripts/build-negotiation-profiles.mjs@2 scripts/eval-lineup-objectives.mjs@2 scripts/fit-ros-projection.mjs@2
  - read in: server/routes/aggregates.js, server/services/consensus-weights.js, server/services/espn-market.js, server/services/manager-archetypes.js, server/services/preseason-model.js
  - **ASSERTED**: syncEspnMarket() exists and nothing calls it. Not a deliberate manual step — an unfinished one.
- **league_roster_snapshots** `[fantasy]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer server/migrations/058_league_roster_snapshots.js:59, writer scripts/collect-roster-snapshots.mjs:113, writer scripts/collect-roster-snapshots.mjs:116, writer scripts/collect-roster-snapshots.mjs:118, reader server/services/bluff-detector.js:83
  - wired into (≤3 hops): routes /api/trades@2 /api/model@3 /api/players@3; scripts scripts/collect-roster-snapshots.mjs@0 scripts/build-negotiation-profiles.mjs@1
  - read in: server/services/bluff-detector.js, scripts/collect-roster-snapshots.mjs
  - **ASSERTED**: `node scripts/collect-roster-snapshots.mjs`, run by hand per league.
- **league_season_teams** `[fantasy]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer scripts/backfill-league-history.mjs:112, reader server/services/manager-archetypes.js:241, reader server/services/manager-archetypes.js:819, reader server/services/manager-archetypes.js:831, reader scripts/build-manager-archetypes.mjs:65
  - wired into (≤3 hops): routes /api/trades@1; scripts scripts/build-manager-archetypes.mjs@0 scripts/luck-panel.mjs@0
  - read in: server/services/manager-archetypes.js, scripts/build-manager-archetypes.mjs, scripts/luck-panel.mjs
- **league_transactions_raw** `[fantasy]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer scripts/collect-league-transactions.mjs:34, reader server/services/counterparty-pricing.js:815, reader server/services/manager-signals.js:168, reader server/services/trade-tactics.js:232, reader server/services/trade-tactics.js:385
  - wired into (≤3 hops): routes /api/league-chat@1 /api/trades@1 /api/model@2 /api/players@2 /api/accolades@3 /api/aggregates@3 /api/betting@3 /api/dev@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/build-negotiation-profiles.mjs@0 scripts/collect-league-transactions.mjs@0 scripts/refresh-live-data.mjs@0
  - read in: server/services/counterparty-pricing.js, server/services/manager-signals.js, server/services/trade-tactics.js, scripts/build-negotiation-profiles.mjs, scripts/collect-league-transactions.mjs, scripts/refresh-live-data.mjs
  - **ASSERTED**: `node scripts/collect-league-transactions.mjs`, run by hand per league.
- **nfl_availability_rates** `[fantasy]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer scripts/fit-availability.mjs:412, writer scripts/fit-availability.mjs:414, reader server/services/contingency.js:568, reader scripts/availability-decision-calibration.mjs:222, reader scripts/fit-availability.mjs:60
  - wired into (≤3 hops): routes /api/model@1 /api/dev@2 /api/leagues@2 /api/news@2 /api/players@2 /api/trades@2 /api/accolades@3 /api/aggregates@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/availability-decision-calibration.mjs@0 scripts/fit-availability.mjs@0 scripts/fit-posture-calibration.mjs@1
  - read in: server/services/contingency.js, scripts/availability-decision-calibration.mjs, scripts/fit-availability.mjs
  - **ASSERTED**: Produced by `node scripts/fit-availability.mjs` over a shell on the machine. There is no route and no job that runs it, by design: it is a 5-season fit with a 2,000-draw bootstrap.
- **nfl_availability_role_rates** `[fantasy]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer scripts/fit-availability.mjs:418, writer scripts/fit-availability.mjs:421, reader server/services/contingency.js:569, reader scripts/availability-decision-calibration.mjs:103
  - wired into (≤3 hops): routes /api/model@1 /api/dev@2 /api/leagues@2 /api/news@2 /api/players@2 /api/trades@2 /api/accolades@3 /api/aggregates@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/availability-decision-calibration.mjs@0 scripts/fit-availability.mjs@1 scripts/fit-posture-calibration.mjs@1
  - read in: server/services/contingency.js, scripts/availability-decision-calibration.mjs
  - **ASSERTED**: Same script, and only if its pre-registered gate passes on held-out 2025. A failed gate writes zero rows on purpose; an empty table is a legitimate outcome, not a bug.
- **nfl_live_possession_predictions** `[shared]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer server/services/nfl-live-ledger.js:98, reader server/services/nfl-expert-council.js:326, reader server/services/nfl-live-ledger.js:122, reader server/services/nfl-live-ledger.js:179, reader server/services/nfl-live-ledger.js:203
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/accolades@2 /api/aggregates@2 /api/betting@2 /api/espn@2 /api/mlb@2 /api/nfl@2 /api/nfl-market@2; jobs espn_line_watch@2 polymarket_line_watch@2; scripts scripts/nfl-2022-2025-rebuild.mjs@1 scripts/nfl-blind-audit.mjs@2 scripts/refresh-live-data.mjs@2
  - read in: server/services/nfl-expert-council.js, server/services/nfl-live-ledger.js
- **nfl_live_possession_settlements** `[shared]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer server/services/nfl-live-ledger.js:131, reader server/services/nfl-expert-council.js:326, reader server/services/nfl-live-ledger.js:122, reader server/services/nfl-live-ledger.js:179
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/accolades@2 /api/aggregates@2 /api/betting@2 /api/espn@2 /api/mlb@2 /api/nfl@2 /api/nfl-market@2; jobs espn_line_watch@2 polymarket_line_watch@2; scripts scripts/nfl-2022-2025-rebuild.mjs@1 scripts/nfl-blind-audit.mjs@2 scripts/refresh-live-data.mjs@2
  - read in: server/services/nfl-expert-council.js, server/services/nfl-live-ledger.js
- **nfl_rebuild_checkpoints** `[shared]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer scripts/nfl-2022-2025-rebuild.mjs:72, writer scripts/nfl-2022-2025-rebuild.mjs:79, writer scripts/nfl-2022-2025-rebuild.mjs:92, reader server/services/nfl-rebuild-progress.js:15, reader scripts/nfl-2022-2025-rebuild.mjs:58
  - wired into (≤3 hops): routes /api/nfl-betting@1; scripts scripts/nfl-2022-2025-rebuild.mjs@0
  - read in: server/services/nfl-rebuild-progress.js, scripts/nfl-2022-2025-rebuild.mjs
  - **ASSERTED**: Written by the 2022-2025 rebuild script while it runs. Correctly manual.
- **nfl_rebuild_progress** `[shared]` — read on a served surface, but every writer is a script someone has to remember to run
  - writer scripts/nfl-2022-2025-rebuild.mjs:49, reader server/services/nfl-rebuild-progress.js:8, reader scripts/nfl-2022-2025-rebuild.mjs:85, reader scripts/nfl-2022-2025-rebuild.mjs:95
  - wired into (≤3 hops): routes /api/nfl-betting@1; scripts scripts/nfl-2022-2025-rebuild.mjs@0
  - read in: server/services/nfl-rebuild-progress.js, scripts/nfl-2022-2025-rebuild.mjs
  - **ASSERTED**: Written by the 2022-2025 rebuild script while it runs. Correctly manual.

### `cache-blind-to-its-inputs` — STALENESS (3)

- **server/routes/model.js** `[shared]` — memoises 3 value(s) under a CONSTANT key, in a module that reads 10 table(s). There is no input in the key, so the first answer of the process is served until the process ends, whatever is written underneath it
  - 'handcuffs', 'cascades', 'avail'
- **server/services/nfl-weekly-feature-store-v2.js** `[shared]` — memoises 1 value(s) under a CONSTANT key, in a module that reads 13 table(s). There is no input in the key, so the first answer of the process is served until the process ends, whatever is written underneath it
  - 'nflverse:participation'
- **server/services/preseason-model.js** `[shared]` — memoises 2 value(s) under a CONSTANT key, in a module that reads 10 table(s). There is no input in the key, so the first answer of the process is served until the process ends, whatever is written underneath it
  - 'names', 'bio'

### `table-in-another-database` — CONTEXT (10)

- **jev_chat_signals** `[shared]` — not in the app's database — every query against it runs on a separate handle opened on another file, CHAT_DB. Whatever fills it does so by replacing that file, which this map cannot see and must not report as a missing writer
  - server/services/bluff-detector.js:101, server/services/bluff-detector.js:101, server/services/bluff-detector.js:115, server/services/manager-signals.js:112
- **manager_chat_profile** `[fantasy]` — not in the app's database — every query against it runs on a separate handle opened on another file, CHAT_DB. Whatever fills it does so by replacing that file, which this map cannot see and must not report as a missing writer
  - server/services/manager-signals.js:133, server/services/manager-signals.js:467, scripts/build-negotiation-profiles.mjs:342
- **manager_player_sentiment** `[fantasy]` — not in the app's database — every query against it runs on a separate handle opened on another file. Whatever fills it does so by replacing that file, which this map cannot see and must not report as a missing writer
  - server/services/manager-signals.js:378
- **messages** `[shared]` — not in the app's database — every query against it runs on a separate handle opened on tmp, another file, file, CHAT_DB. Whatever fills it does so by replacing that file, which this map cannot see and must not report as a missing writer
  - server/routes/league-chat.js:143, server/services/bluff-detector.js:101, server/services/bluff-detector.js:115, server/services/league-chat-sync.js:104
- **negotiation_profiles** `[shared]` — not in the app's database — every query against it runs on a separate handle opened on another file, CHAT_DB. Whatever fills it does so by replacing that file, which this map cannot see and must not report as a missing writer
  - server/services/counterparty-pricing.js:932, server/services/counterparty-pricing.js:1052, scripts/build-negotiation-profiles.mjs:363
- **sh_crawl** `[shared]` — not in the app's database — every query against it runs on a separate handle opened on dbPath. Whatever fills it does so by replacing that file, which this map cannot see and must not report as a missing writer
  - scripts/collect-sleeper-history.mjs:78, scripts/collect-sleeper-history.mjs:79, scripts/collect-sleeper-history.mjs:80
- **sh_leagues** `[shared]` — not in the app's database — every query against it runs on a separate handle opened on dbPath. Whatever fills it does so by replacing that file, which this map cannot see and must not report as a missing writer
  - scripts/collect-sleeper-history.mjs:81, scripts/collect-sleeper-history.mjs:82
- **sh_team_seasons** `[shared]` — not in the app's database — every query against it runs on a separate handle opened on dbPath. Whatever fills it does so by replacing that file, which this map cannot see and must not report as a missing writer
- **sh_team_weeks** `[shared]` — not in the app's database — every query against it runs on a separate handle opened on dbPath. Whatever fills it does so by replacing that file, which this map cannot see and must not report as a missing writer
- **sh_transactions** `[shared]` — not in the app's database — every query against it runs on a separate handle opened on dbPath. Whatever fills it does so by replacing that file, which this map cannot see and must not report as a missing writer

### `table-never-scheduled` — CONTEXT (61)

Grouped by file, heaviest first. Full list in `wiring-map.json`.

| file | count |
| --- | --: |
| `writer server/services/nfl-blind-audit.js` | 4 |
| `writer server/migrations/062_google_identity_and_invites.js` | 3 |
| `writer server/migrations/006_identity_and_draft_authorization.js` | 3 |
| `writer server/services/nfl-engine-backfill.js` | 3 |
| `writer server/services/draft-ingest.js` | 2 |
| `writer server/draft/store.js` | 2 |
| `writer server/migrations/007_model_permissions_and_upgrade_guard.js` | 2 |
| `writer server/services/manager-archetypes.js` | 2 |
| `writer server/services/nfl-ai-replay.js` | 2 |
| `writer server/migrations/023_execution_lifecycle_ledger.js` | 2 |
| `writer server/services/nfl-research.js` | 2 |
| `writer server/migrations/019_nfl_news_events.js` | 2 |
| `writer server/services/nfl-evidence.js` | 2 |
| `writer server/migrations/012_teaser_execution_ledger.js` | 2 |
| `writer server/migrations/002_platform_audit_log.js` | 1 |
| `writer server/services/audit-registry.js` | 1 |
| `writer server/migrations/017_auth_pairing_codes.js` | 1 |
| `writer server/services/decision-basis.js` | 1 |
| `writer server/routes/drafts.js` | 1 |
| `writer server/migrations/015_manager_profiles.js` | 1 |
| _… 22 more files_ | 22 |

### `edge-behind-an-off-flag` — CONTEXT (2)

- **applyRedistribution() in server/services/player-week-engine.js** `[fantasy]` — runs only when `redistributeVolume` is true, and the only thing that sets it true is scripts/eval-redistribution.mjs — so this is code the running app never reaches, however real the import edge looks
  - server/services/player-week-engine.js:189
- **report() in scripts/capture-availability-baseline.mjs** `[shared]` — runs only when `baseline` is true, and the only thing that sets it true is nothing in the repository — so this is code the running app never reaches, however real the import edge looks
  - scripts/capture-availability-baseline.mjs:323

### `module-only-tested` — ORPHAN (16)

- **server/betting/nfl/contracts/total-probabilities.js** `[betting]` — imported only by its test (test/total-probabilities.test.js) — built, verified, never wired in
  - test/total-probabilities.test.js
- **server/betting/nfl/forecast/spread-family-adapters.js** `[betting]` — imported only by its test (test/python-artifact.test.js, test/spread-family-adapters.test.js) — built, verified, never wired in
  - test/python-artifact.test.js, test/spread-family-adapters.test.js
- **server/betting/nfl/strategy/teaser-season.js** `[betting]` — imported only by its test (test/teaser-season.test.js) — built, verified, never wired in
  - test/teaser-season.test.js
- **server/betting/nfl/strategy/teaser-staking.js** `[betting]` — imported only by its test (test/teaser-staking.test.js) — built, verified, never wired in
  - test/teaser-staking.test.js
- **server/services/consensus-weights.js** `[shared]` — imported only by its test (test/consensus-weights.test.js) — built, verified, never wired in
  - test/consensus-weights.test.js
- **server/services/draft-abstention-audit.js** `[fantasy]` — imported only by its test (test/draft-abstention-audit.test.js) — built, verified, never wired in
  - test/draft-abstention-audit.test.js
- **server/services/historical-adp-scrapes.js** `[shared]` — imported only by its test (test/historical-adp-scrapes.test.js) — built, verified, never wired in
  - test/historical-adp-scrapes.test.js
- **server/services/joint-score-backtest.js** `[shared]` — imported only by its test (test/nfl-joint-score.test.js) — built, verified, never wired in
  - test/nfl-joint-score.test.js
- **server/services/line-move-study.js** `[betting]` — imported only by its test (test/line-move-study.test.js) — built, verified, never wired in
  - test/line-move-study.test.js
- **server/services/nfl-execution-staking-policy.js** `[betting]` — imported only by its test (test/nfl-execution-staking-policy.test.js) — built, verified, never wired in
  - test/nfl-execution-staking-policy.test.js
- **server/services/nfl-execution-stress.js** `[betting]` — imported only by its test (test/nfl-execution-stress.test.js) — built, verified, never wired in
  - test/nfl-execution-stress.test.js
- **server/services/nfl-preseason-blend.js** `[shared]` — imported only by its test (test/nfl-preseason-blend.test.js, test/preseason-blend-cutoff.test.js) — built, verified, never wired in
  - test/nfl-preseason-blend.test.js, test/preseason-blend-cutoff.test.js
- **server/services/nfl-prop-player-heads.js** `[betting]` — imported only by its test (test/nfl-prop-player-heads.test.js) — built, verified, never wired in
  - test/nfl-prop-player-heads.test.js
- **server/services/nfl-prop-player-weekly-heads.js** `[fantasy]` — imported only by its test (test/nfl-prop-player-weekly-heads.test.js) — built, verified, never wired in
  - test/nfl-prop-player-weekly-heads.test.js
- **server/services/nfl-team-strength.js** `[shared]` — imported only by its test (test/nfl-team-strength.test.js) — built, verified, never wired in
  - test/nfl-team-strength.test.js
- **server/services/purged-walk-forward.js** `[shared]` — imported only by its test (test/purged-walk-forward.test.js) — built, verified, never wired in
  - test/purged-walk-forward.test.js

### `module-imported-by-nothing` — ORPHAN (15)

- **client/src/components/StaleBanner.tsx** `[shared]` — no file in the repository imports it
- **client/src/features/model-lab/ModelRegistryPanel.tsx** `[shared]` — no file in the repository imports it
- **client/src/pages/Edge.tsx** `[shared]` — no file in the repository imports it
- **client/src/pages/Model.tsx** `[shared]` — no file in the repository imports it
- **client/src/pages/Projections.tsx** `[fantasy]` — no file in the repository imports it
- **client/src/pages/Rankings.tsx** `[shared]` — no file in the repository imports it
- **scripts/_bottom-up-team-total-worker.mjs** `[shared]` — no file in the repository imports it
- **scripts/_prepare-validation-db.mjs** `[shared]` — no file in the repository imports it
- **scripts/_recheck-variance-only.mjs** `[shared]` — no file in the repository imports it
- **server/platform/providers.js** `[shared]` — no file in the repository imports it
- **server/platform/provision-auth.js** `[shared]` — no file in the repository imports it
- **server/scripts/run-nfl-ai-replay.js** `[shared]` — no file in the repository imports it
- **server/scripts/sync-history.js** `[shared]` — no file in the repository imports it
- **server/services/espn-market.js** `[shared]` — no file in the repository imports it
- **server/services/td-features.js** `[shared]` — no file in the repository imports it

### `module-reaches-no-surface` — ORPHAN (17)

- **client/src/components/StatTable.tsx** `[shared]` — imported by 1 file(s), none of which any route, job or page can reach
  - client/src/pages/Rankings.tsx
- **server/services/alt-spread-import.js** `[shared]` — imported by 1 file(s), none of which any route, job or page can reach
  - scripts/import-alt-spreads.mjs
- **server/services/bottom-up-team-total.js** `[shared]` — imported by 2 file(s), none of which any route, job or page can reach
  - scripts/_bottom-up-team-total-worker.mjs, scripts/_recheck-variance-only.mjs
- **server/services/forecast-combination.js** `[shared]` — imported by 2 file(s), none of which any route, job or page can reach
  - server/services/joint-score-backtest.js, server/services/opportunity-model.js
- **server/services/gate-verdicts.js** `[shared]` — imported by 2 file(s), none of which any route, job or page can reach
  - scripts/fit-posture-calibration.mjs, scripts/fit-weekly-coverage.mjs
- **server/services/nfl-ensemble-rank.js** `[shared]` — imported by 1 file(s), none of which any route, job or page can reach
  - server/services/forecast-combination.js
- **server/services/nfl-execution-clv-downsize.js** `[betting]` — imported by 1 file(s), none of which any route, job or page can reach
  - server/services/nfl-execution-staking-policy.js
- **server/services/nfl-joint-score.js** `[shared]` — imported by 1 file(s), none of which any route, job or page can reach
  - server/services/joint-score-backtest.js
- **server/services/nfl-live-ledger.js** `[shared]` — imported by 1 file(s), none of which any route, job or page can reach
  - scripts/nfl-2022-2025-rebuild.mjs
- **server/services/nfl-news-event-impact.js** `[shared]` — imported by 1 file(s), none of which any route, job or page can reach
  - scripts/run-news-event-impact.mjs
- **server/services/nfl-props-player-features-weekly.js** `[betting]` — imported by 1 file(s), none of which any route, job or page can reach
  - server/services/nfl-prop-player-weekly-heads.js
- **server/services/nfl-props-player-features.js** `[betting]` — imported by 1 file(s), none of which any route, job or page can reach
  - server/services/nfl-prop-player-heads.js
- **server/services/nfl-qb-quality.js** `[shared]` — imported by 1 file(s), none of which any route, job or page can reach
  - scripts/export-qb-quality-features.mjs
- **server/services/nfl-weekly-feature-store-v2.js** `[shared]` — imported by 2 file(s), none of which any route, job or page can reach
  - scripts/backfill-feature-store.mjs, scripts/grade-feature-vector.mjs
- **server/services/opportunity-model.js** `[shared]` — imported by 1 file(s), none of which any route, job or page can reach
  - scripts/study-opportunity-volume.mjs
- **server/services/props-total-consistency.js** `[betting]` — imported by 1 file(s), none of which any route, job or page can reach
  - scripts/audit-props-total-consistency.mjs
- **server/services/sleeper-history.js** `[shared]` — imported by 1 file(s), none of which any route, job or page can reach
  - scripts/collect-sleeper-history.mjs

### `field-attached-never-read` — ORPHAN (61)

- **age_band (server/services/nfl-player-context.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-player-context.js:184
  - wired into (≤3 hops): routes /api/model@2 /api/betting@3 /api/dev@3 /api/leagues@3 /api/news@3 /api/nfl-betting@3 /api/nfl-market@3 /api/players@3; scripts scripts/build-negotiation-profiles.mjs@3 scripts/build-role-scenario-lab.mjs@3 scripts/fit-prop-calibration.mjs@3
- **cache_invalidated (server/routes/nfl-betting.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/routes/nfl-betting.js:1138
  - wired into (≤3 hops): routes /api/nfl-betting@0
- **candidate_findings (server/services/nfl-model-growth.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-model-growth.js:256
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/accolades@2 /api/aggregates@2 /api/betting@2 /api/espn@2 /api/mlb@2 /api/nfl@2 /api/stats@2; jobs espn_line_watch@2 polymarket_line_watch@2; scripts scripts/refresh-live-data.mjs@2 scripts/nfl-2022-2025-rebuild.mjs@3 scripts/sync-ffopportunity.mjs@3
- **capital_hhi (server/services/manager-archetypes.js)** `[fantasy]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/manager-archetypes.js:314
  - wired into (≤3 hops): routes /api/trades@1
- **cold_c (server/services/line-move-study.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/line-move-study.js:201
- **competitiveness (server/routes/tradelab.js)** `[fantasy]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/routes/tradelab.js:178
  - wired into (≤3 hops): routes /api/tradelab@0 /api/model@2 /api/players@2 /api/trades@2 /api/accolades@3 /api/aggregates@3 /api/betting@3 /api/dev@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/build-negotiation-profiles.mjs@2 scripts/eval-lineup-objectives.mjs@3 scripts/fit-posture-calibration.mjs@3
- **current_lines (server/routes/model.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/routes/model.js:636
  - wired into (≤3 hops): routes /api/model@0 /api/dev@1 /api/leagues@1 /api/accolades@3 /api/aggregates@3 /api/betting@3 /api/espn@3 /api/mlb@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/refresh-live-data.mjs@3
- **estimate_usd (server/services/llm-budget.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/llm-budget.js:308
  - wired into (≤3 hops): routes /api/accolades@2 /api/analysis@2 /api/betting@2 /api/dev@2 /api/drafts@2 /api/edge@2 /api/execution-slate@2 /api/news@2; scripts scripts/build-negotiation-profiles.mjs@2 scripts/run-news-event-impact.mjs@3
- **fitted_alpha (server/services/nfl-market.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-market.js:593
  - wired into (≤3 hops): routes /api/betting@1 /api/nfl-betting@1 /api/nfl-market@1 /api/mlb@2 /api/accolades@3 /api/aggregates@3 /api/espn@3 /api/nfl@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/verify-m4-coverage.mjs@1 scripts/fit-prop-calibration.mjs@2 scripts/audit-passing-specialists.mjs@3
- **fitted_carryover (server/services/nfl-market.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-market.js:593
  - wired into (≤3 hops): routes /api/betting@1 /api/nfl-betting@1 /api/nfl-market@1 /api/mlb@2 /api/accolades@3 /api/aggregates@3 /api/espn@3 /api/nfl@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/verify-m4-coverage.mjs@1 scripts/fit-prop-calibration.mjs@2 scripts/audit-passing-specialists.mjs@3
- **historical_lines (server/routes/model.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/routes/model.js:635
  - wired into (≤3 hops): routes /api/model@0 /api/dev@1 /api/leagues@1 /api/accolades@3 /api/aggregates@3 /api/betting@3 /api/espn@3 /api/mlb@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/refresh-live-data.mjs@3
- **holm_threshold (server/services/nfl-passing-specialists.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-passing-specialists.js:192
  - wired into (≤3 hops): routes /api/nfl-betting@1; scripts scripts/audit-passing-specialists.mjs@1
- **injury_game_status_severity (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:734
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **injury_game_status_severity (server/services/nfl-weekly-feature-store.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store.js:254
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/trades@2 /api/accolades@3 /api/aggregates@3 /api/betting@3 /api/espn@3 /api/mlb@3 /api/nfl@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/nfl-2022-2025-rebuild.mjs@1 scripts/refresh-live-data.mjs@3
- **injury_practice_status_severity (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:735
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **injury_practice_status_severity (server/services/nfl-weekly-feature-store.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store.js:255
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/trades@2 /api/accolades@3 /api/aggregates@3 /api/betting@3 /api/espn@3 /api/mlb@3 /api/nfl@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/nfl-2022-2025-rebuild.mjs@1 scripts/refresh-live-data.mjs@3
- **lastIndex (server/services/nfl-postgame-truth.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-postgame-truth.js:242
  - wired into (≤3 hops): routes /api/nfl-betting@2 /api/accolades@3 /api/aggregates@3 /api/betting@3 /api/espn@3 /api/mlb@3 /api/nfl@3 /api/nfl-market@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/opener-clv-pass2.mjs@1 scripts/nfl-2022-2025-rebuild.mjs@2 scripts/nfl-blind-audit.mjs@2
- **lastRunAt (server/platform/jobs.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/platform/jobs.js:32
  - wired into (≤3 hops): routes /api/drafts@1
- **lifecycle_scored (server/services/nfl-expert-council.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-expert-council.js:822
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/accolades@2 /api/aggregates@2 /api/betting@2 /api/espn@2 /api/mlb@2 /api/nfl@2 /api/nfl-market@2; jobs espn_line_watch@2 polymarket_line_watch@2; scripts scripts/nfl-2022-2025-rebuild.mjs@2 scripts/nfl-blind-audit.mjs@2 scripts/refresh-live-data.mjs@2
- **margin_80_interval_coverage (server/services/nfl-expert-council.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-expert-council.js:821
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/accolades@2 /api/aggregates@2 /api/betting@2 /api/espn@2 /api/mlb@2 /api/nfl@2 /api/nfl-market@2; jobs espn_line_watch@2 polymarket_line_watch@2; scripts scripts/nfl-2022-2025-rebuild.mjs@2 scripts/nfl-blind-audit.mjs@2 scripts/refresh-live-data.mjs@2
- **market_quote_reason (server/services/nfl-auto-picks.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-auto-picks.js:336
  - wired into (≤3 hops): routes /api/betting@1 /api/nfl-betting@1 /api/nfl-market@1 /api/accolades@2 /api/aggregates@2 /api/espn@2 /api/mlb@2 /api/nfl@2; jobs nfl_t60_runner@1 espn_line_watch@2 nfl_learned_shadow@2 polymarket_line_watch@2; scripts scripts/refresh-live-data.mjs@2 scripts/nfl-2022-2025-rebuild.mjs@3 scripts/sync-ffopportunity.mjs@3
- **metrics_observed (server/services/nfl-rookie-ingest.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-rookie-ingest.js:107
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/accolades@2 /api/aggregates@2 /api/betting@2 /api/espn@2 /api/mlb@2 /api/nfl@2 /api/stats@2; jobs espn_line_watch@2 polymarket_line_watch@2; scripts scripts/refresh-live-data.mjs@2 scripts/nfl-2022-2025-rebuild.mjs@3 scripts/sync-ffopportunity.mjs@3
- **mz_man_catch_rate (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:379
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **mz_man_target_rate (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:375
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **mz_man_yards_per_route (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:371
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **mz_target_rate_gap (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:377
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **mz_yards_per_route_gap (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:373
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **mz_zone_catch_rate (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:380
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **mz_zone_target_rate (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:376
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **mz_zone_yards_per_route (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:372
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **newest_message (server/services/league-chat-sync.js)** `[fantasy]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/league-chat-sync.js:104
  - wired into (≤3 hops): routes /api/league-chat@1
- **nfelo_hfa_pts (server/services/line-move-study.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/line-move-study.js:219
- **nfelo_qb_adj_diff (server/services/line-move-study.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/line-move-study.js:216
- **nfelo_qbelo_diff (server/services/line-move-study.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/line-move-study.js:217
- **ol_changes (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:422
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **ol_same_five (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:423
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **opportunity_context (server/services/player-week-engine.js)** `[fantasy]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/player-week-engine.js:351
  - wired into (≤3 hops): routes /api/model@1 /api/betting@2 /api/dev@2 /api/leagues@2 /api/news@2 /api/nfl-betting@2 /api/nfl-market@2 /api/players@2; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/build-negotiation-profiles.mjs@2 scripts/build-role-scenario-lab.mjs@2 scripts/fit-prop-calibration.mjs@2
- **outdoor_weather_known (server/services/line-move-study.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/line-move-study.js:202
- **part_def_zone (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:354
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **part_off_zone_faced (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:353
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **pff_overall_grade (server/services/nfl-weekly-feature-store-v2.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store-v2.js:744
  - wired into (≤3 hops): scripts scripts/backfill-feature-store.mjs@1 scripts/grade-feature-vector.mjs@1
- **pff_overall_grade (server/services/nfl-weekly-feature-store.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-weekly-feature-store.js:264
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/trades@2 /api/accolades@3 /api/aggregates@3 /api/betting@3 /api/espn@3 /api/mlb@3 /api/nfl@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/nfl-2022-2025-rebuild.mjs@1 scripts/refresh-live-data.mjs@3
- **player_learning (server/services/nfl-model-growth.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-model-growth.js:265
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/accolades@2 /api/aggregates@2 /api/betting@2 /api/espn@2 /api/mlb@2 /api/nfl@2 /api/stats@2; jobs espn_line_watch@2 polymarket_line_watch@2; scripts scripts/refresh-live-data.mjs@2 scripts/nfl-2022-2025-rebuild.mjs@3 scripts/sync-ffopportunity.mjs@3
- **public_home_tickets (server/services/line-move-study.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/line-move-study.js:221
- **public_money_minus_tickets (server/services/line-move-study.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/line-move-study.js:222
- **qb_change_diff (server/services/line-move-study.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/line-move-study.js:195
- **qb_qbr_diff (server/services/line-move-study.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/line-move-study.js:194
- **rateLimited (server/routes/accolades.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/routes/accolades.js:55
  - wired into (≤3 hops): routes /api/accolades@0 /api/dev@1
- **raw_target (server/services/nfl-expert-coordinator.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-expert-coordinator.js:271
  - wired into (≤3 hops): routes /api/nfl-betting@2 /api/accolades@3 /api/aggregates@3 /api/betting@3 /api/espn@3 /api/mlb@3 /api/nfl@3 /api/nfl-market@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/nfl-2022-2025-rebuild.mjs@3 scripts/nfl-blind-audit.mjs@3 scripts/refresh-live-data.mjs@3
- **shared_weekly_state (server/services/nfl-model-growth.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-model-growth.js:242
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/accolades@2 /api/aggregates@2 /api/betting@2 /api/espn@2 /api/mlb@2 /api/nfl@2 /api/stats@2; jobs espn_line_watch@2 polymarket_line_watch@2; scripts scripts/refresh-live-data.mjs@2 scripts/nfl-2022-2025-rebuild.mjs@3 scripts/sync-ffopportunity.mjs@3
- **signal_reliability (server/services/nfl-model-growth.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-model-growth.js:212
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/accolades@2 /api/aggregates@2 /api/betting@2 /api/espn@2 /api/mlb@2 /api/nfl@2 /api/stats@2; jobs espn_line_watch@2 polymarket_line_watch@2; scripts scripts/refresh-live-data.mjs@2 scripts/nfl-2022-2025-rebuild.mjs@3 scripts/sync-ffopportunity.mjs@3
- **simulation_calibration (server/services/nfl-model-growth.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-model-growth.js:244
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/accolades@2 /api/aggregates@2 /api/betting@2 /api/espn@2 /api/mlb@2 /api/nfl@2 /api/stats@2; jobs espn_line_watch@2 polymarket_line_watch@2; scripts scripts/refresh-live-data.mjs@2 scripts/nfl-2022-2025-rebuild.mjs@3 scripts/sync-ffopportunity.mjs@3
- **specialist_learning (server/services/nfl-model-growth.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-model-growth.js:245
  - wired into (≤3 hops): routes /api/nfl-betting@1 /api/accolades@2 /api/aggregates@2 /api/betting@2 /api/espn@2 /api/mlb@2 /api/nfl@2 /api/stats@2; jobs espn_line_watch@2 polymarket_line_watch@2; scripts scripts/refresh-live-data.mjs@2 scripts/nfl-2022-2025-rebuild.mjs@3 scripts/sync-ffopportunity.mjs@3
- **total_forecasts (server/services/nfl-ensemble-rank.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/nfl-ensemble-rank.js:563
  - wired into (≤3 hops): scripts scripts/study-opportunity-volume.mjs@3
- **trigger_error (server/services/polymarket-lines.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/polymarket-lines.js:272
  - wired into (≤3 hops): routes /api/betting@1 /api/nfl-market@1 /api/accolades@2 /api/aggregates@2 /api/espn@2 /api/mlb@2 /api/nfl@2 /api/nfl-betting@2; jobs espn_line_watch@2 polymarket_line_watch@2; scripts scripts/refresh-live-data.mjs@2 scripts/nfl-2022-2025-rebuild.mjs@3 scripts/sync-ffopportunity.mjs@3
- **tweet_line_check (server/services/scheduler.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/scheduler.js:374
  - wired into (≤3 hops): routes /api/accolades@1 /api/aggregates@1 /api/betting@1 /api/espn@1 /api/mlb@1 /api/nfl@1 /api/nfl-betting@1 /api/stats@1; jobs espn_line_watch@1 polymarket_line_watch@1; scripts scripts/refresh-live-data.mjs@1 scripts/nfl-2022-2025-rebuild.mjs@2 scripts/sync-ffopportunity.mjs@2
- **v2_gbm (server/services/offseason-model.js)** `[shared]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/offseason-model.js:1469
  - wired into (≤3 hops): routes /api/model@2 /api/players@2 /api/trades@2 /api/accolades@3 /api/aggregates@3 /api/betting@3 /api/dev@3 /api/drafts@3; jobs espn_line_watch@3 polymarket_line_watch@3; scripts scripts/build-negotiation-profiles.mjs@2 scripts/eval-lineup-objectives.mjs@2 scripts/fit-posture-calibration.mjs@3
- **wind_forecast_known_lead2 (server/services/line-move-study.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/line-move-study.js:210
- **wind_forecast_known_lead3 (server/services/line-move-study.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/line-move-study.js:208
- **wind_kmh_forecast_lead2 (server/services/line-move-study.js)** `[betting]` — attached to a payload and read by nothing — not by the server, not by the client, not by a test
  - server/services/line-move-study.js:209
- _… 1 more in wiring-map.json_

### `value-computed-never-used` — ORPHAN (94)

- **__internal (server/services/props-total-consistency.js)** `[betting]` — assigned once and never referenced again anywhere in the repository
  - server/services/props-total-consistency.js:261
- **absentRank (server/services/opportunity-redistribution.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/opportunity-redistribution.js:133
- **ACTION_NETWORK_BOOK_IDS (server/services/prop-feeds.js)** `[betting]` — assigned once and never referenced again anywhere in the repository
  - server/services/prop-feeds.js:53
- **allowLine (server/routes/drafts.js)** `[fantasy]` — assigned once and never referenced again anywhere in the repository
  - server/routes/drafts.js:1085
- **ARCHIVE_MATCH (server/migrations/052_line_snapshot_receipt_clock.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/migrations/052_line_snapshot_receipt_clock.js:64
- **backedName (server/services/pick-reasoning.js)** `[betting]` — assigned once and never referenced again anywhere in the repository
  - server/services/pick-reasoning.js:149
- **boardLine (server/routes/drafts.js)** `[fantasy]` — assigned once and never referenced again anywhere in the repository
  - server/routes/drafts.js:1026
- **CALIBRATION_VERSION (server/services/nfl-forecast-identity.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/nfl-forecast-identity.js:106
- **CAREER_POSITIONS (server/services/player-career.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/player-career.js:322
- **CARRYOVER_FROM_WEEK (server/services/football-first.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/football-first.js:142
- **controller (server/services/shadow-ledger.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/shadow-ledger.js:25
- **dataPath (server/platform/paths.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/platform/paths.js:67
- **docsPath (server/platform/paths.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/platform/paths.js:70
- **DRAFT_WINDOW_AFTER_HOURS (server/services/scheduler.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/scheduler.js:41
- **DRAFT_WINDOW_BEFORE_MIN (server/services/scheduler.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/scheduler.js:40
- **DRIVER_LABEL (server/services/preseason-model.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/preseason-model.js:490
- **ESPN_TRANSACTIONS (server/services/nfl-transactions.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/nfl-transactions.js:28
- **excludedKey (server/services/nfl-ensemble.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/nfl-ensemble.js:2389
- **excludeKey (server/services/trade-engine.js)** `[fantasy]` — assigned once and never referenced again anywhere in the repository
  - server/services/trade-engine.js:1415
- **familyKey (server/services/nfl-ensemble.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/nfl-ensemble.js:2391
- **FINALIZE_AFTER_HOURS (server/services/draft-ingest.js)** `[fantasy]` — assigned once and never referenced again anywhere in the repository
  - server/services/draft-ingest.js:271
- **FLOW_TTL_MINUTES (server/routes/google-auth.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/routes/google-auth.js:14
- **fmtFactor (server/routes/nfl-betting.js)** `[betting]` — assigned once and never referenced again anywhere in the repository
  - server/routes/nfl-betting.js:485
- **fmtLine (server/services/nfl-replay.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/nfl-replay.js:414
- **fmtPlayer (server/routes/trades.js)** `[fantasy]` — assigned once and never referenced again anywhere in the repository
  - server/routes/trades.js:1031
- **fmtRisk (server/routes/trades.js)** `[fantasy]` — assigned once and never referenced again anywhere in the repository
  - server/routes/trades.js:1046
- **fmtUnit (server/routes/analysis.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/routes/analysis.js:27
- **FPI_URL (server/services/nfl-external-ratings.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/nfl-external-ratings.js:39
- **gameContextKey (server/services/nfl-ensemble.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/nfl-ensemble.js:2404
- **GAMMA (server/services/polymarket.js)** `[betting]` — assigned once and never referenced again anywhere in the repository
  - server/services/polymarket.js:34
- **goneClause (server/routes/nfldata.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/routes/nfldata.js:80
- **gonePlaceholders (server/routes/nfldata.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/routes/nfldata.js:79
- **governanceProtocol (server/services/model-governance.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/model-governance.js:204
- **gRank (server/services/opportunity-redistribution.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/opportunity-redistribution.js:215
- **HANDOFF_TTL_SECONDS (server/routes/google-auth.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/routes/google-auth.js:15
- **HISTORICAL_FORECAST_API (server/services/nfl-weather-history.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/nfl-weather-history.js:26
- **INSIDER_HANDLES (server/news/twitter-ingest.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/news/twitter-ingest.js:117
- **JOINT_SCORE_VERDICT (server/services/nfl-joint-score.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/nfl-joint-score.js:920
- **KALSHI (server/services/prediction-markets.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/prediction-markets.js:36
- **legacyActor (server/migrations/009_authoritative_actor_and_ownership_guards.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/migrations/009_authoritative_actor_and_ownership_guards.js:29
- **MARGIN_MODEL_VERDICT (server/betting/nfl/strategy/margin-distribution.js)** `[betting]` — assigned once and never referenced again anywhere in the repository
  - server/betting/nfl/strategy/margin-distribution.js:1642
- **MATCHUP_EVIDENCE (server/services/matchups.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/matchups.js:76
- **MLB_BASE (server/services/mlb-pregame.js)** `[betting]` — assigned once and never referenced again anywhere in the repository
  - server/services/mlb-pregame.js:9
- **mlbPlayerProps (server/services/parlay-api.js)** `[betting]` — assigned once and never referenced again anywhere in the repository
  - server/services/parlay-api.js:187
- **modeText (server/services/player-week-engine.js)** `[fantasy]` — assigned once and never referenced again anywhere in the repository
  - server/services/player-week-engine.js:736
- **MOVE_UNION (server/services/signal-latency.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/signal-latency.js:57
- **MOVER_RETENTION (server/services/draft-assist.js)** `[fantasy]` — assigned once and never referenced again anywhere in the repository
  - server/services/draft-assist.js:51
- **needLine (server/routes/drafts.js)** `[fantasy]` — assigned once and never referenced again anywhere in the repository
  - server/routes/drafts.js:1014
- **newsText (server/routes/analysis.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/routes/analysis.js:38
- **normalizedHeadline (server/news/normalize.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/news/normalize.js:16
- **ODDSTRADER_PAIDS (server/services/book-feeds.js)** `[betting]` — assigned once and never referenced again anywhere in the repository
  - server/services/book-feeds.js:96
- **OFFSEASON_CYCLE_SOURCE (server/services/nfl-offseason-cycle.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/nfl-offseason-cycle.js:37
- **OL_SLOTS (server/routes/nfldata.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/routes/nfldata.js:434
- **OPPORTUNITY_STATS (server/services/opportunity-model.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/opportunity-model.js:30
- **ORDER_INDEX (server/services/nfl-execution-lifecycle.js)** `[betting]` — assigned once and never referenced again anywhere in the repository
  - server/services/nfl-execution-lifecycle.js:72
- **ORIGINAL_LIST (server/migrations/027_decision_tape.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/migrations/027_decision_tape.js:69
- **otherName (server/services/pick-reasoning.js)** `[betting]` — assigned once and never referenced again anywhere in the repository
  - server/services/pick-reasoning.js:150
- **overrideKey (server/services/nfl-ensemble.js)** `[shared]` — assigned once and never referenced again anywhere in the repository
  - server/services/nfl-ensemble.js:2396
- **OWNER (server/routes/props.js)** `[betting]` — assigned once and never referenced again anywhere in the repository
  - server/routes/props.js:25
- **paids (server/services/odds-archive.js)** `[betting]` — assigned once and never referenced again anywhere in the repository
  - server/services/odds-archive.js:40
- _… 34 more in wiring-map.json_

### `table-never-read` — ORPHAN (17)

- **legacy_draft_quarantine** `[shared]` — written by 2 file(s), read by nothing
  - server/migrations/007_model_permissions_and_upgrade_guard.js:4, server/migrations/007_model_permissions_and_upgrade_guard.js:33, server/platform/provision-auth.js:52
- **model_actor_provenance_quarantine** `[shared]` — written by 1 file(s), read by nothing
  - server/migrations/009_authoritative_actor_and_ownership_guards.js:31, server/migrations/009_authoritative_actor_and_ownership_guards.js:47, server/migrations/009_authoritative_actor_and_ownership_guards.js:111
- **nfl_candidate_findings_new** `[shared]` — written by 1 file(s), read by nothing
  - server/migrations/039_candidate_finding_rule_version.js:64
- **nfl_candidate_findings_old** `[shared]` — written by 1 file(s), read by nothing
  - server/migrations/039_candidate_finding_rule_version.js:118
- **nfl_engine_backfill_checkpoints** `[shared]` — written by 1 file(s), read by nothing
  - server/services/nfl-engine-backfill.js:59
- **nfl_execution_lifecycle_events_new** `[betting]` — written by 2 file(s), read by nothing
  - server/migrations/027_decision_tape.js:194, server/migrations/028_settlement_corrections.js:27
- **nfl_execution_lifecycle_events_old** `[betting]` — written by 2 file(s), read by nothing
  - server/migrations/027_decision_tape.js:288, server/migrations/028_settlement_corrections.js:94
- **nfl_execution_opportunities_new** `[betting]` — written by 1 file(s), read by nothing
  - server/migrations/027_decision_tape.js:155
- **nfl_execution_opportunities_old** `[betting]` — written by 1 file(s), read by nothing
  - server/migrations/027_decision_tape.js:332
- **nfl_news_signals_new** `[shared]` — written by 1 file(s), read by nothing
  - server/migrations/053_nfl_news_signals_versioning.js:59
- **nfl_news_signals_old** `[shared]` — written by 1 file(s), read by nothing
  - server/migrations/053_nfl_news_signals_versioning.js:147
- **nfl_replay_bets** `[shared]` — written by 1 file(s), read by nothing
  - server/services/nfl-replay.js:434
- **nfl_replay_runs** `[shared]` — written by 1 file(s), read by nothing
  - server/services/nfl-replay.js:428
- **nfl_scottfree_game_features** `[shared]` — written by 2 file(s), read by nothing
  - server/migrations/022_scottfree_game_features.js:65, scripts/import-scottfree.mjs:83, scripts/import-scottfree.mjs:83
- **nfl_stadiums** `[shared]` — written by 1 file(s), read by nothing
  - server/services/nfelo.js:199, server/services/nfelo.js:199
- **nfl_team_stadiums** `[shared]` — written by 1 file(s), read by nothing
  - server/services/nfelo.js:220, server/services/nfelo.js:220
- **schema_preflight** `[shared]` — written by 1 file(s), read by nothing
  - server/db/preflight.js:268

### `export-only-tested` — ORPHAN (361)

Grouped by file, heaviest first. Full list in `wiring-map.json`.

| file | count |
| --- | --: |
| `server/services/lineup-brain.js` | 9 |
| `server/services/execution-slate-reasoning.js` | 8 |
| `server/services/nfl-sim-shape-calibration.js` | 8 |
| `server/betting/nfl/contracts/total-probabilities.js` | 7 |
| `server/services/kalshi-adverse-selection.js` | 7 |
| `server/services/nfl-execution-clv-downsize.js` | 7 |
| `server/services/nfl-family-contribution.js` | 7 |
| `server/services/trade-proposals.js` | 7 |
| `server/betting/nfl/contracts/forecast-packet.js` | 6 |
| `server/services/execution-fill.js` | 6 |
| `server/services/nfl-candidate-findings.js` | 6 |
| `server/services/nfl-execution-attribution.js` | 6 |
| `server/services/nfl-execution-staking-policy.js` | 6 |
| `server/services/nfl-team-strength.js` | 6 |
| `server/services/trade-engine.js` | 6 |
| `scripts/flag-qbr-2026-placeholders.mjs` | 5 |
| `server/news/twitter-ingest.js` | 5 |
| `server/services/lineup-posture.js` | 5 |
| `server/services/nfl-decision-tape.js` | 5 |
| `server/services/nfl-execution-clv.js` | 5 |
| _… 118 more files_ | 234 |

### `export-imported-by-nothing` — ORPHAN (1026)

Grouped by file, heaviest first. Full list in `wiring-map.json`.

| file | count |
| --- | --: |
| `server/services/offseason-model.js` | 44 |
| `scripts/wiring-map.mjs` | 36 |
| `server/betting/nfl/strategy/margin-distribution.js` | 25 |
| `server/betting/nfl/strategy/teaser-staking.js` | 24 |
| `server/services/offseason-data.js` | 24 |
| `server/betting/nfl/strategy/teaser-season.js` | 19 |
| `server/services/nfl-joint-score.js` | 18 |
| `server/services/nfl-sim-policy.js` | 18 |
| `server/services/contingency.js` | 17 |
| `server/services/parlay-api.js` | 17 |
| `scripts/promote-early-week-weights.mjs` | 16 |
| `server/services/alt-spread-import.js` | 16 |
| `server/services/forecast-combination.js` | 16 |
| `server/betting/nfl/strategy/t60-runner.js` | 15 |
| `server/services/nfl-ensemble-rank.js` | 15 |
| `server/services/consensus-weights.js` | 14 |
| `server/services/draft-abstention-audit.js` | 14 |
| `server/services/nfl-weekly-feature-store-v2.js` | 14 |
| `server/services/joint-score-backtest.js` | 13 |
| `server/services/ros-projection.js` | 12 |
| _… 227 more files_ | 639 |

### `route-no-caller` — ORPHAN (462)

Grouped by file, heaviest first. Full list in `wiring-map.json`.

| file | count |
| --- | --: |
| `server/routes/nfl-betting.js` | 170 |
| `server/routes/nfl-market.js` | 56 |
| `server/routes/betting-hub.js` | 49 |
| `server/routes/trades.js` | 37 |
| `server/routes/mlb.js` | 28 |
| `server/routes/model.js` | 27 |
| `server/routes/google-auth.js` | 10 |
| `server/routes/edge.js` | 10 |
| `server/routes/nfldata.js` | 10 |
| `server/routes/wong.js` | 9 |
| `server/routes/drafts.js` | 7 |
| `server/routes/accolades.js` | 5 |
| `server/routes/dev.js` | 5 |
| `server/routes/tradelab.js` | 5 |
| `server/routes/decision-inbox.js` | 4 |
| `server/routes/props.js` | 4 |
| `server/routes/teams.js` | 4 |
| `server/routes/props-tickets.js` | 3 |
| `server/routes/stats.js` | 3 |
| `server/routes/aggregates.js` | 2 |
| _… 9 more files_ | 14 |

## Asserted edges

The few things a walker cannot see, written down by hand in `annotations.json`.
Everything else in this file is derived.

- **nfl_availability_rates** — ASSERTED producer: Produced by `node scripts/fit-availability.mjs` over a shell on the machine. There is no route and no job that runs it, by design: it is a 5-season fit with a 2,000-draw bootstrap.
- **nfl_availability_role_rates** — ASSERTED producer: Same script, and only if its pre-registered gate passes on held-out 2025. A failed gate writes zero rows on purpose; an empty table is a legitimate outcome, not a bug.
- **espn_player_market** — ASSERTED producer: syncEspnMarket() exists and nothing calls it. Not a deliberate manual step — an unfinished one.
- **league_roster_snapshots** — ASSERTED producer: `node scripts/collect-roster-snapshots.mjs`, run by hand per league.
- **league_transactions_raw** — ASSERTED producer: `node scripts/collect-league-transactions.mjs`, run by hand per league.
- **negotiation_profiles** — ASSERTED producer: Lives in the CHAT database, not the app's — built by scripts/build-negotiation-profiles.mjs on the Mac and delivered inside the corpus file.
- **nfl_rebuild_progress** — ASSERTED producer: Written by the 2022-2025 rebuild script while it runs. Correctly manual.
- **nfl_rebuild_checkpoints** — ASSERTED producer: Written by the 2022-2025 rebuild script while it runs. Correctly manual.
- **manager_archetypes** — ASSERTED producer: Written by server/services/manager-archetypes.js:561-563, driven by scripts/build-manager-archetypes.mjs. No chat dependency — it replays league-seasons out of the app's own database, so it works whether or not a corpus ever arrives. PR #26 registers it as a scheduled job (scheduler.js, heavy tier, 24h).

- **league_member_identity** — ASSERTED note: The walker sees a writer (manager-identity.js) reachable from a route and calls the table fed. It is not, in the way that matters: a row only becomes usable to the Trade Brain when its `confidence` is 'confirmed' or 'exact' (TRUSTED_CONFIDENCE), and 'confirmed' is only ever set from the `confirmations` argument, which a person has to supply. Name matching cannot bootstrap it, because chat handles are not ESPN names. A value-level dependency like this one is outside what an import-and-SQL walker can prove, which is why it is written down here.
- **espn_settings** — ASSERTED note: Read once at server/db/index.js:228 to migrate a pre-multi-league row into `leagues`. Nothing writes it any more and nothing should. Kept as a legacy read.
- **the chat corpus** — ASSERTED note: messages, jev_chat_signals, manager_chat_profile, manager_player_sentiment and negotiation_profiles are NOT in the app's database. They are Nick's iMessage corpus, a separate SQLite file opened read-only via chatDbPath()/messagesDbPath(), built on his Mac and delivered whole through POST /api/league-chat/upload (which refuses a corpus with an empty messages table). The map reported all four as 'written by nothing' before it learned to tell the two handles apart; they were fine. Both consuming surfaces already refuse and name the gap when the corpus is absent.

## Tables: who fills them, who reads them, what moves

`reaches` is the blast radius: change the table and every surface listed moves,
with no code change and no pull request behind it.

| table | scope | written by | read by | reaches |
| --- | --- | --- | --- | --- |
| `ADD` | shared | **nothing** | **nothing** | — |
| `ai_usage` | shared | `claude.js`, `llm-budget.js` | `claude.js`, `llm-budget.js` | /api/accolades /api/analysis /api/betting /api/dev +2 job(s) |
| `and` | shared | **nothing** | **nothing** | — |
| `app_settings` | shared | `teaser-season.js`, `espn-connect.js`, `claude.js`, `league-chat-sync.js` | `teaser-season.js`, `espn-connect.js`, `claude.js`, `espn-draft.js` | /api/espn-connect /api/accolades /api/analysis /api/betting +2 job(s) |
| `audit_log` | shared | `002_platform_audit_log.js`, `audit.js` | `audit.js` | /api/drafts /api/news |
| `audit_registry` | shared | `audit-registry.js` | `054_audit_registry_corrected_alpha.js`, `audit-registry.js`, `gridiron-model.js`, `backfill-historical-trial-registry.mjs` | /api/betting /api/model /api/dev /api/leagues |
| `auth_invites` | shared | `062_google_identity_and_invites.js`, `account-link.js`, `google-auth.js` | `account-link.js`, `google-auth.js` | /api/auth |
| `auth_login_flows` | shared | `062_google_identity_and_invites.js`, `google-auth.js` | `google-auth.js` | /api/auth |
| `auth_pairing_codes` | shared | `017_auth_pairing_codes.js`, `local-auth.js` | `local-auth.js` | /api/auth /api/drafts /api/league-chat |
| `auth_sessions` | shared | `006_identity_and_draft_authorization.js`, `provision-auth.js`, `google-auth.js`, `local-auth.js` | `auth.js`, `local-auth.js` | /api/auth /api/betting /api/decision-inbox /api/drafts +2 job(s) |
| `cfbd_player_season` | shared | `cfbd.js` | `cfbd.js` | /api/accolades /api/aggregates /api/betting /api/drafts +2 job(s) |
| `correlation_estimates` | shared | `correlation.js` | `model.js`, `correlation.js` | /api/model /api/dev /api/leagues /api/drafts +2 job(s) |
| `db_health_checks` | shared | `index.js` | `index.js` | /api/accolades /api/aggregates /api/analysis /api/auth +8 job(s) |
| `decay_watch_runs` | shared | `decay-watch.js` | `decay-watch.js` | /api/betting /api/accolades /api/aggregates /api/espn +2 job(s) |
| `decision_basis` | shared | `decision-basis.js` | `decision-basis.js` | /api/betting |
| `decision_recommendations` | shared | `020_decision_recommendations.js`, `decision-inbox.js`, `trade-engine.js` | `decision-inbox.js` | /api/decision-inbox /api/model /api/players /api/trades +2 job(s) |
| `draft_advice` | fantasy | `drafts.js` | `drafts.js` | /api/drafts |
| `draft_capture_events` | fantasy | `draft-ingest.js` | `draft-ingest.js` | /api/drafts |
| `draft_capture_sessions` | fantasy | `draft-ingest.js` | `draft-ingest.js` | /api/drafts |
| `draft_events` | fantasy | `store.js`, `003_draft_state_machine.js` | `store.js` | /api/drafts |
| `draft_grades` | fantasy | **nothing** | **nothing** | — |
| `draft_pick_corrections` | fantasy | `draft-reconcile.js` | `draft-reconcile.js` | /api/drafts /api/espn /api/leagues /api/aggregates |
| `draft_pick_quarantine` | fantasy | `draft-reconcile.js` | `draft-reconcile.js` | /api/drafts /api/espn /api/leagues /api/aggregates |
| `draft_picks` | fantasy | `store.js`, `leagues.js`, `draft-ingest.js`, `draft-reconcile.js` | `store.js`, `drafts.js`, `leagues.js`, `draft-assist.js` | /api/drafts /api/leagues /api/espn /api/trades +2 job(s) |
| `draft_queue` | fantasy | `store.js`, `003_draft_state_machine.js` | `store.js` | /api/drafts |
| `draft_team_grades` | fantasy | `007_model_permissions_and_upgrade_guard.js`, `drafts.js` | `drafts.js` | /api/drafts |
| `draft_team_ownership` | fantasy | `006_identity_and_draft_authorization.js`, `009_authoritative_actor_and_ownership_guards.js`, `provision-auth.js`, `drafts.js` | `009_authoritative_actor_and_ownership_guards.js`, `auth.js`, `drafts.js` | /api/drafts /api/auth /api/betting /api/decision-inbox +2 job(s) |
| `drafts` | fantasy | `store.js`, `provision-auth.js`, `drafts.js`, `leagues.js` | `store.js`, `index.js`, `006_identity_and_draft_authorization.js`, `007_model_permissions_and_upgrade_guard.js` | /api/auth /api/drafts /api/leagues /api/accolades +2 job(s) |
| `dynasty_values` | shared | `aggregates.js` | `tradelab.js`, `draft-assist.js`, `trade-engine.js` | /api/tradelab /api/drafts /api/model /api/players +2 job(s) |
| `espn_cache` | fantasy | **nothing** | **nothing** | — |
| `espn_line_moves` | fantasy | `nfl-espn-line-watch.js` | `nfl-capture-dispatch.js`, `nfl-espn-line-watch.js`, `polymarket-lines.js`, `prediction-markets.js` | /api/betting /api/news /api/nfl-market /api/accolades +3 job(s) |
| `espn_player_market` | fantasy | `espn-market.js` | `aggregates.js`, `consensus-weights.js`, `espn-market.js`, `manager-archetypes.js` | /api/aggregates /api/drafts /api/leagues /api/players +2 job(s) |
| `espn_settings` | fantasy | **nothing** | `index.js` | /api/accolades /api/aggregates /api/analysis /api/auth +8 job(s) |
| `evidence_capture_windows` | shared | `evidence-daemon.js` | `evidence-daemon.js` | /api/mlb /api/nfl-market /api/accolades /api/aggregates +2 job(s) |
| `evidence_daemon_runs` | shared | `evidence-daemon.js` | `evidence-daemon.js` | /api/mlb /api/nfl-market /api/accolades /api/aggregates +2 job(s) |
| `fantasy_coordinator_fits` | shared | `fantasy-coordinator.js` | `model.js`, `decay-watch.js`, `fantasy-coordinator.js` | /api/model /api/betting /api/dev /api/leagues +2 job(s) |
| `forward_picks` | shared | `forward-ledger.js` | `forward-ledger.js`, `nfl-research-lab.js` | /api/nfl-betting /api/nfl-market /api/accolades /api/aggregates +2 job(s) |
| `game_lines` | shared | `047_backfill_open_spread_pinnacle_2022_2025.js`, `055_repair_pinnacle_placeholder_openers.js`, `gamescript.js`, `nfl-opening-lines.js` | `margin-distribution.js`, `t60-runner.js`, `teaser-leg-rates.js`, `model.js` | /api/model /api/nfl /api/analysis /api/betting +5 job(s) |
| `gamescript_model` | shared | `gamescript.js` | `model.js`, `gamescript.js` | /api/model /api/dev /api/leagues /api/nfl-market +2 job(s) |
| `jev_chat_done` | shared | **nothing** | **nothing** | — |
| `jev_chat_signals` | shared | **nothing** | `bluff-detector.js`, `manager-signals.js`, `build-negotiation-profiles.mjs` | /api/league-chat /api/trades /api/model /api/players |
| `league_member_identity` | fantasy | `manager-identity.js` | `bluff-detector.js`, `manager-identity.js`, `manager-signals.js`, `build-negotiation-profiles.mjs` | /api/league-chat /api/trades /api/model /api/players |
| `league_memberships` | fantasy | `006_identity_and_draft_authorization.js`, `provision-auth.js`, `espn-connect.js`, `leagues.js` | `006_identity_and_draft_authorization.js`, `009_authoritative_actor_and_ownership_guards.js`, `016_mock_draft_ownership_exemption.js`, `account-link.js` | /api/auth /api/drafts /api/leagues /api/news +2 job(s) |
| `league_roster_snapshots` | fantasy | `058_league_roster_snapshots.js`, `collect-roster-snapshots.mjs` | `bluff-detector.js`, `collect-roster-snapshots.mjs` | /api/trades /api/model /api/players |
| `league_season_teams` | fantasy | `backfill-league-history.mjs` | `manager-archetypes.js`, `build-manager-archetypes.mjs`, `luck-panel.mjs` | /api/trades |
| `league_transactions_raw` | fantasy | `collect-league-transactions.mjs` | `counterparty-pricing.js`, `manager-signals.js`, `trade-tactics.js`, `build-negotiation-profiles.mjs` | /api/league-chat /api/trades /api/model /api/players +2 job(s) |
| `league_week_scores` | fantasy | `backfill-league-history.mjs` | `fit-posture-calibration.mjs`, `luck-panel.mjs` | — |
| `leagues` | fantasy | `index.js`, `espn-connect.js`, `leagues.js`, `scheduler.js` | `index.js`, `006_identity_and_draft_authorization.js`, `011_league_connection_integrity.js`, `015_manager_profiles.js` | /api/aggregates /api/auth /api/espn-connect /api/leagues +8 job(s) |
| `leagues_parked` | fantasy | **nothing** | **nothing** | — |
| `legacy_draft_quarantine` | shared | `007_model_permissions_and_upgrade_guard.js`, `provision-auth.js` | **nothing** | — |
| `manager_archetype_jev` | fantasy | `manager-archetypes.js` | `manager-archetypes.js` | /api/trades |
| `manager_archetypes` | fantasy | `manager-archetypes.js` | `manager-archetypes.js`, `manager-signals.js`, `build-manager-archetypes.mjs`, `refresh-live-data.mjs` | /api/league-chat /api/trades /api/model /api/players |
| `manager_chat_profile` | fantasy | **nothing** | `manager-signals.js`, `build-negotiation-profiles.mjs` | /api/league-chat /api/trades /api/model /api/players |
| `manager_player_sentiment` | fantasy | **nothing** | `manager-signals.js` | /api/league-chat /api/trades /api/model /api/players |
| `manager_player_view` | fantasy | `manager-signals.js` | `bluff-detector.js`, `counterparty-pricing.js`, `manager-signals.js`, `talk-vs-model.js` | /api/league-chat /api/trades /api/model /api/players +2 job(s) |
| `manager_profiles` | fantasy | `015_manager_profiles.js`, `league-brain.js` | `trades.js`, `counterparty-pricing.js`, `league-brain.js`, `trade-engine.js` | /api/trades /api/model /api/players /api/accolades +2 job(s) |
| `manager_signals` | fantasy | `manager-signals.js` | `trades.js`, `manager-signals.js` | /api/trades /api/league-chat /api/model /api/players |
| `messages` | shared | **nothing** | `league-chat.js`, `bluff-detector.js`, `league-chat-sync.js`, `manager-signals.js` | /api/league-chat /api/trades /api/model /api/players |
| `mlb_batter_games` | betting | `mlb.js` | `mlb-auto-picks.js`, `mlb-calibration.js`, `mlb-experiments.js`, `mlb-projections.js` | /api/mlb /api/accolades /api/aggregates /api/betting +2 job(s) |
| `mlb_boxscore_sync` | betting | `mlb.js` | `mlb-auto-picks.js` | /api/mlb /api/accolades /api/aggregates /api/betting +2 job(s) |
| `mlb_first_party_picks` | betting | `mlb-auto-picks.js` | `mlb-auto-picks.js`, `mlb-calibration.js` | /api/mlb /api/accolades /api/aggregates /api/betting +2 job(s) |
| `mlb_games` | betting | `mlb.js` | `evidence-daemon.js`, `mlb-auto-picks.js`, `mlb-calibration.js`, `mlb-experiments.js` | /api/mlb /api/nfl-market /api/accolades /api/aggregates +2 job(s) |
| `mlb_market_quotes` | betting | `mlb-pregame.js` | `market-movement.js`, `mlb-pregame.js`, `nfl-shopping-board.js` | /api/betting /api/execution-slate /api/mlb /api/nfl-betting +2 job(s) |
| `mlb_model_experiments` | betting | `mlb-experiments.js` | `mlb-experiments.js` | /api/mlb |
| `mlb_pick_decisions` | betting | `mlb-auto-picks.js` | `mlb-research.js` | /api/mlb |
| `mlb_pitcher_games` | betting | `mlb.js` | `mlb-auto-picks.js`, `mlb-calibration.js`, `mlb-experiments.js`, `mlb-projections.js` | /api/mlb /api/accolades /api/aggregates /api/betting +2 job(s) |
| `mlb_pregame_snapshots` | betting | `mlb-pregame.js` | `mlb-experiments.js`, `mlb-pregame.js` | /api/mlb /api/nfl-market /api/accolades /api/aggregates +2 job(s) |
| `mlb_probability_calibrations` | betting | `mlb-calibration.js` | `mlb-calibration.js` | /api/mlb |
| `mlb_probable_starters` | betting | `mlb.js` | `mlb-pregame.js`, `mlb.js` | /api/mlb /api/accolades /api/aggregates /api/betting +2 job(s) |
| `model_actor_provenance_quarantine` | shared | `009_authoritative_actor_and_ownership_guards.js` | **nothing** | — |
| `model_audit_log` | shared | `005_model_registry_integrity.js`, `008_model_actor_foreign_keys.js`, `sqlite-store.js` | `sqlite-store.js` | /api/mlb /api/model /api/nfl-market /api/dev |
| `model_backtests` | shared | `005_model_registry_integrity.js`, `model.js` | `sqlite-store.js`, `model.js` | /api/model /api/dev /api/leagues /api/mlb +2 job(s) |
| `model_dataset_versions` | shared | `005_model_registry_integrity.js`, `008_model_actor_foreign_keys.js`, `model.js` | `sqlite-store.js`, `model.js` | /api/model /api/dev /api/leagues /api/mlb +2 job(s) |
| `model_evidence_manifests` | shared | `model-governance.js` | `model-governance.js` | /api/mlb /api/nfl-market /api/betting /api/nfl-betting +3 job(s) |
| `model_experiment_inputs` | shared | `005_model_registry_integrity.js`, `model.js` | `sqlite-store.js`, `model.js` | /api/model /api/dev /api/leagues /api/mlb +2 job(s) |
| `model_experiments` | shared | `004_model_lab.js`, `008_model_actor_foreign_keys.js`, `sqlite-store.js` | `009_authoritative_actor_and_ownership_guards.js`, `sqlite-store.js` | /api/mlb /api/model /api/nfl-market /api/dev |
| `model_feature_contracts` | shared | `model-governance.js` | `model-governance.js` | /api/mlb /api/nfl-market /api/betting /api/nfl-betting +3 job(s) |
| `model_feature_versions` | shared | `005_model_registry_integrity.js`, `008_model_actor_foreign_keys.js`, `model.js` | `sqlite-store.js`, `model.js` | /api/model /api/dev /api/leagues /api/mlb +2 job(s) |
| `model_gate_audits` | shared | `model-governance.js` | `model-governance.js` | /api/mlb /api/nfl-market /api/betting /api/nfl-betting +3 job(s) |
| `model_metrics` | shared | `005_model_registry_integrity.js`, `model.js` | `sqlite-store.js` | /api/mlb /api/model /api/nfl-market /api/dev |
| `model_permissions` | shared | `007_model_permissions_and_upgrade_guard.js`, `account-link.js`, `provision-auth.js`, `local-auth.js` | `authz.js`, `account-link.js`, `legacy-access.js` | /api/auth /api/betting /api/espn-connect /api/execution-slate |
| `model_predictions` | shared | `004_model_lab.js`, `sqlite-store.js`, `model.js` | `sqlite-store.js` | /api/mlb /api/model /api/nfl-market /api/dev |
| `model_production_pointer` | shared | `004_model_lab.js`, `sqlite-store.js` | `sqlite-store.js` | /api/mlb /api/model /api/nfl-market /api/dev |
| `model_promotion_history` | shared | `005_model_registry_integrity.js`, `008_model_actor_foreign_keys.js`, `sqlite-store.js` | `sqlite-store.js` | /api/mlb /api/model /api/nfl-market /api/dev |
| `model_registry` | shared | `model-governance.js` | `model-governance.js` | /api/mlb /api/nfl-market /api/betting /api/nfl-betting +3 job(s) |
| `model_registry_history` | shared | `model-governance.js` | `model-governance.js` | /api/mlb /api/nfl-market /api/betting /api/nfl-betting +3 job(s) |
| `negotiation_profiles` | shared | `build-negotiation-profiles.mjs` | `counterparty-pricing.js`, `build-negotiation-profiles.mjs` | /api/trades /api/model /api/players /api/accolades +2 job(s) |
| `news_items` | shared | `store.js`, `espn.js`, `news.js` | `store.js`, `analysis.js`, `dev.js`, `edge.js` | /api/analysis /api/dev /api/edge /api/espn +3 job(s) |
| `news_source_validation` | shared | `source-validation.js` | `nfl-news-signal.js`, `source-validation.js`, `who-plays.js` | /api/news /api/nfl-betting /api/accolades /api/aggregates +3 job(s) |
| `nfl_ai_replay_candidate_cache` | shared | `nfl-ai-replay.js` | `nfl-ai-replay.js` | /api/nfl-betting |
| `nfl_ai_replay_reviews` | shared | `nfl-ai-replay.js` | `nfl-ai-replay.js` | /api/nfl-betting |
| `nfl_ai_replay_runs` | shared | `run-nfl-ai-replay.js`, `nfl-ai-replay.js` | `nfl-ai-replay.js`, `nfl-profitability.js` | /api/nfl-betting /api/betting /api/execution-slate |
| `nfl_alt_spread_captures` | shared | `035_alt_spread_capture.js`, `alt-spread-import.js` | `035_alt_spread_capture.js`, `alt-spread-import.js` | — |
| `nfl_alt_spread_quotes` | shared | `035_alt_spread_capture.js`, `alt-spread-import.js` | `035_alt_spread_capture.js`, `alt-spread-import.js` | — |
| `nfl_audit_only_table` | shared | **nothing** | **nothing** | — |
| `nfl_auto_picks` | shared | `nfl-auto-picks.js` | `nfl-auto-picks.js`, `nfl-pick-watch.js` | /api/betting /api/nfl-betting /api/nfl-market /api/accolades +4 job(s) |
| `nfl_availability_rates` | fantasy | `fit-availability.mjs` | `contingency.js`, `availability-decision-calibration.mjs`, `fit-availability.mjs` | /api/model /api/dev /api/leagues /api/news +2 job(s) |
| `nfl_availability_role_rates` | fantasy | `fit-availability.mjs` | `contingency.js`, `availability-decision-calibration.mjs` | /api/model /api/dev /api/leagues /api/news +2 job(s) |
| `nfl_bet_log` | shared | `clv-core.js` | `clv-core.js`, `nfl-sharp.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +4 job(s) |
| `nfl_blind_audit_retries` | shared | `nfl-blind-audit.js` | `nfl-blind-audit.js` | /api/nfl-betting |
| `nfl_blind_audit_runs` | shared | `nfl-blind-audit.js` | `nfl-audit-overview.js`, `nfl-blind-audit.js`, `nfl-candidate-findings.js`, `nfl-profitability.js` | /api/nfl-betting /api/nfl-market /api/betting /api/execution-slate +4 job(s) |
| `nfl_blind_audit_week_performance` | shared | `nfl-blind-audit.js` | `nfl-blind-audit.js` | /api/nfl-betting |
| `nfl_blind_audit_weeks` | shared | `nfl-blind-audit.js` | `nfl-audit-overview.js`, `nfl-blind-audit.js`, `nfl-2022-2025-rebuild.mjs` | /api/nfl-betting |
| `nfl_blind_input_mutations` | shared | `nfl-a-to-m.js`, `053_nfl_news_signals_versioning.js` | `teaser-leg-rates.js`, `nfl-blind-audit.js` | /api/betting /api/nfl-betting /api/execution-slate /api/nfl-market +2 job(s) |
| `nfl_bootstrap_only_table` | shared | **nothing** | **nothing** | — |
| `nfl_cached_reports` | shared | `report-cache.js` | `report-cache.js` | /api/betting /api/nfl-betting /api/nfl-market /api/accolades +2 job(s) |
| `nfl_candidate_finding_seasons` | shared | `024_candidate_findings.js`, `nfl-candidate-findings.js` | `nfl-candidate-findings.js` | /api/betting /api/nfl-betting /api/nfl-market /api/accolades +4 job(s) |
| `nfl_candidate_findings` | shared | `024_candidate_findings.js`, `039_candidate_finding_rule_version.js`, `nfl-candidate-findings.js` | `039_candidate_finding_rule_version.js`, `nfl-candidate-findings.js`, `backfill-historical-trial-registry.mjs` | /api/betting /api/nfl-betting /api/nfl-market /api/accolades +4 job(s) |
| `nfl_candidate_findings_new` | shared | `039_candidate_finding_rule_version.js` | **nothing** | — |
| `nfl_candidate_findings_old` | shared | `039_candidate_finding_rule_version.js` | **nothing** | — |
| `nfl_candidate_input_audits` | shared | `nfl-replay.js` | `nfl-replay.js`, `backfill-historical-trial-registry.mjs` | /api/nfl-betting /api/nfl-market /api/betting /api/mlb +1 job(s) |
| `nfl_candidate_robustness_audits` | shared | `nfl-candidate-analysis.js` | `nfl-candidate-analysis.js`, `backfill-historical-trial-registry.mjs` | /api/nfl-betting |
| `nfl_capacity_events` | shared | `t60-runner.js`, `034_t60_runner_ledger.js` | `t60-runner.js`, `034_t60_runner_ledger.js` | /api/accolades /api/aggregates /api/betting /api/espn +4 job(s) |
| `nfl_capture_triggers` | shared | `014_profit_execution_triggers.js`, `nfl-capture-dispatch.js` | `nfl-capture-dispatch.js` | /api/betting /api/news /api/accolades /api/aggregates +2 job(s) |
| `nfl_clv_grades` | betting | `037_nfl_clv_grades.js`, `clv-core.js` | `037_nfl_clv_grades.js`, `clv-core.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +3 job(s) |
| `nfl_cover_calibrations` | shared | `nfl-cover-calibration.js` | `nfl-cover-calibration.js` | /api/betting /api/nfl-betting /api/nfl-market /api/accolades +4 job(s) |
| `nfl_decision_events` | shared | `027_decision_tape.js`, `nfl-decision-tape.js` | `027_decision_tape.js`, `031_decision_identity.js`, `nfl-decision-tape.js`, `nfl-learned-shadow-explain.js` | /api/nfl-market /api/accolades /api/aggregates /api/betting +4 job(s) |
| `nfl_decision_run_invalidations` | shared | `031_decision_identity.js`, `nfl-decision-tape.js` | `031_decision_identity.js`, `nfl-decision-tape.js` | /api/nfl-market /api/accolades /api/aggregates /api/betting +4 job(s) |
| `nfl_decision_runs` | shared | `027_decision_tape.js`, `031_decision_identity.js`, `nfl-decision-tape.js` | `027_decision_tape.js`, `031_decision_identity.js`, `nfl-decision-tape.js`, `nfl-learned-shadow-explain.js` | /api/nfl-betting /api/nfl-market /api/accolades /api/aggregates +4 job(s) |
| `nfl_definition_only_table` | shared | **nothing** | **nothing** | — |
| `nfl_depth` | fantasy | `nfl-advanced.js` | `nfl-advanced.js`, `nfl-bitemporal.js`, `nfl-evidence.js`, `nfl-player-value.js` | /api/dev /api/model /api/nfl-betting /api/nfl-market +3 job(s) |
| `nfl_engine_artifacts` | shared | `nfl-engine-registry.js` | `nfl-engine-registry.js` | /api/nfl-betting /api/betting /api/model /api/nfl-market +3 job(s) |
| `nfl_engine_backfill_checkpoints` | shared | `nfl-engine-backfill.js` | **nothing** | — |
| `nfl_engine_backfill_runs` | shared | `nfl-engine-backfill.js` | `nfl-engine-backfill.js` | /api/nfl-betting |
| `nfl_ensemble_fit_artifacts` | shared | `nfl-ensemble.js` | `nfl-engine-registry.js`, `nfl-ensemble.js`, `nfl-model-growth.js`, `run-historical-leaderboard.mjs` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +4 job(s) |
| `nfl_ensemble_rank_reports` | shared | `nfl-ensemble-rank.js` | `nfl-ensemble-rank.js` | — |
| `nfl_execution_lifecycle_events` | betting | `023_execution_lifecycle_ledger.js`, `027_decision_tape.js`, `028_settlement_corrections.js`, `nfl-execution-lifecycle.js` | `027_decision_tape.js`, `028_settlement_corrections.js`, `nfl-execution-clv.js`, `nfl-execution-lifecycle.js` | /api/nfl-market |
| `nfl_execution_lifecycle_events_new` | betting | `027_decision_tape.js`, `028_settlement_corrections.js` | **nothing** | — |
| `nfl_execution_lifecycle_events_old` | betting | `027_decision_tape.js`, `028_settlement_corrections.js` | **nothing** | — |
| `nfl_execution_log` | betting | `nfl-execution.js` | `nfl-execution.js` | /api/betting /api/execution-slate /api/nfl-market /api/accolades +2 job(s) |
| `nfl_execution_opportunities` | betting | `023_execution_lifecycle_ledger.js`, `027_decision_tape.js`, `nfl-execution-lifecycle.js` | `027_decision_tape.js`, `033_opportunity_push_probability.js`, `nfl-execution-lifecycle.js` | /api/nfl-market |
| `nfl_execution_opportunities_new` | betting | `027_decision_tape.js` | **nothing** | — |
| `nfl_execution_opportunities_old` | betting | `027_decision_tape.js` | **nothing** | — |
| `nfl_expert_forward_predictions` | shared | `nfl-expert-council.js` | `nfl-evidence-provenance.js`, `nfl-expert-coordinator.js`, `nfl-expert-council.js`, `nfl-postgame-truth.js` | /api/nfl-betting /api/nfl-market /api/accolades /api/aggregates +2 job(s) |
| `nfl_expert_forward_settlements` | shared | `nfl-expert-council.js` | `nfl-expert-coordinator.js`, `nfl-expert-council.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nfl_external_player_grades` | shared | `nfl-roster-strength.js` | `nfl-roster-strength.js`, `nfl-weekly-feature-store-v2.js`, `nfl-weekly-feature-store.js` | /api/nfl-betting /api/trades /api/accolades /api/aggregates +3 job(s) |
| `nfl_external_ratings` | shared | `nfl-external-ratings.js` | `nfl-blind-audit.js`, `nfl-external-ratings.js`, `export-teamrankings-features.mjs` | /api/nfl-betting /api/nfl-market /api/accolades /api/aggregates +2 job(s) |
| `nfl_feature_ablation_audits` | shared | `nfl-research.js` | `nfl-research.js` | /api/nfl-market |
| `nfl_feature_coverage_snapshots` | shared | `nfl-feature-coverage.js` | `nfl-feature-coverage.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nfl_feature_dictionary` | shared | `nfl-weekly-feature-store-v2.js`, `nfl-weekly-feature-store.js` | `nfl-weekly-feature-store-v2.js`, `nfl-weekly-feature-store.js` | /api/nfl-betting /api/trades /api/accolades /api/aggregates +2 job(s) |
| `nfl_feature_revisions` | shared | `nfl-bitemporal.js` | `nfl-bitemporal.js`, `nfl-t60-packet.js`, `nfl-team-card.js` | /api/nfl-betting /api/betting /api/model /api/nfl-market +4 job(s) |
| `nfl_ffopportunity_weekly` | shared | `ffopportunity.js` | `draft-assist.js`, `ffopportunity.js`, `offseason-data.js`, `opportunity-model.js` | /api/accolades /api/aggregates /api/betting /api/drafts +2 job(s) |
| `nfl_game_variance` | shared | `nfl-postgame-truth.js` | `nfl-expert-coordinator.js`, `nfl-postgame-truth.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nfl_game_weather` | shared | `nfl-weather.js` | `nfl-gbm.js`, `nfl-t60-packet.js`, `nfl-weather.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +4 job(s) |
| `nfl_game_weather_forecast_history` | shared | `nfl-weather-history.js` | `nfl-t60-packet.js`, `nfl-weather-history.js` | /api/nfl-betting /api/nfl-market /api/accolades /api/aggregates +4 job(s) |
| `nfl_historical_adp` | shared | `historical-adp.js` | `historical-adp.js`, `manager-archetypes.js`, `preseason-model.js` | /api/model /api/trades /api/dev /api/drafts +2 job(s) |
| `nfl_historical_adp_scrape` | shared | `historical-adp-scrapes.js` | `historical-adp-scrapes.js` | — |
| `nfl_historical_engine_replay` | shared | `nfl-engine-backfill.js` | `nfl-engine-backfill.js` | /api/nfl-betting |
| `nfl_historical_ffc_adp` | shared | `consensus-weights.js` | `consensus-weights.js` | — |
| `nfl_historical_signal_replay` | shared | `nfl-engine-backfill.js` | `nfl-diagnostic.js`, `nfl-engine-backfill.js` | /api/nfl-betting |
| `nfl_injuries` | fantasy | `nfl-advanced.js` | `boom-bust.js`, `contingency.js`, `draft-assist.js`, `football-context.js` | /api/drafts /api/model /api/nfl-betting /api/nfl-market +4 job(s) |
| `nfl_learning_epochs` | shared | `nfl-engine-registry.js` | `nfl-engine-registry.js` | /api/nfl-betting /api/betting /api/model /api/nfl-market +3 job(s) |
| `nfl_line_snapshots` | betting | `052_line_snapshot_receipt_clock.js`, `book-feeds-extra.js`, `book-feeds.js`, `line-shopping.js` | `teaser-scan.js`, `052_line_snapshot_receipt_clock.js`, `beat-the-close.js`, `book-feeds-extra.js` | /api/betting /api/execution-slate /api/news /api/nfl-betting +5 job(s) |
| `nfl_live_possession_predictions` | shared | `nfl-live-ledger.js` | `nfl-expert-council.js`, `nfl-live-ledger.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nfl_live_possession_settlements` | shared | `nfl-live-ledger.js` | `nfl-expert-council.js`, `nfl-live-ledger.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nfl_model_experiments` | shared | `nfl-experiments.js` | `nfl-experiments.js` | /api/nfl-betting |
| `nfl_model_growth_runs` | shared | `nfl-model-growth.js` | `nfl-model-growth.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nfl_model_watch_runs` | shared | `nfl-model-watch.js` | `nfl-model-watch.js` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `nfl_neural_replay_audits` | shared | `nfl-replay.js` | `nfl-replay.js` | /api/nfl-betting /api/nfl-market /api/betting /api/mlb +1 job(s) |
| `nfl_news_event_extraction_cache` | shared | `019_nfl_news_events.js`, `nfl-news-events.js` | `nfl-news-events.js` | /api/nfl-market |
| `nfl_news_events` | shared | `019_nfl_news_events.js`, `nfl-news-events.js` | `nfl-news-event-impact.js`, `nfl-news-events.js`, `nfl-t60-packet.js`, `run-news-event-impact.mjs` | /api/nfl-betting /api/betting /api/nfl-market /api/accolades +4 job(s) |
| `nfl_news_extraction_attempts` | shared | `nfl-news-signal.js` | `nfl-news-signal.js` | /api/news /api/nfl-betting /api/accolades /api/aggregates +3 job(s) |
| `nfl_news_signals` | shared | `053_nfl_news_signals_versioning.js`, `nfl-news-signal.js` | `053_nfl_news_signals_versioning.js`, `nfl-news-signal.js` | /api/news /api/nfl-betting /api/accolades /api/aggregates +3 job(s) |
| `nfl_news_signals_new` | shared | `053_nfl_news_signals_versioning.js` | **nothing** | — |
| `nfl_news_signals_old` | shared | `053_nfl_news_signals_versioning.js` | **nothing** | — |
| `nfl_nfelo_games` | shared | `nfelo.js` | `nfelo.js`, `nfl-blind-audit.js` | /api/nfl-betting /api/nfl-market /api/accolades /api/aggregates +3 job(s) |
| `nfl_nfelo_lines` | shared | `nfelo.js` | `nfelo.js` | /api/nfl-market /api/accolades /api/aggregates /api/betting +3 job(s) |
| `nfl_nfelo_qb` | shared | `nfelo.js` | `nfelo.js` | /api/nfl-market /api/accolades /api/aggregates /api/betting +3 job(s) |
| `nfl_ngs` | shared | `nfl-advanced.js` | `nfl-advanced.js`, `nfl-data-consistency.js`, `nfl-evidence.js`, `nfl-player-value.js` | /api/model /api/nfl-betting /api/nfl-market /api/accolades +3 job(s) |
| `nfl_no_reader_table` | shared | **nothing** | **nothing** | — |
| `nfl_odds_archive` | betting | `odds-archive.js` | `047_backfill_open_spread_pinnacle_2022_2025.js`, `052_line_snapshot_receipt_clock.js`, `055_repair_pinnacle_placeholder_openers.js`, `beat-the-close.js` | /api/nfl-betting /api/nfl-market /api/accolades /api/aggregates +2 job(s) |
| `nfl_officials` | shared | `nfl-officials.js` | `nfl-officials.js`, `nfl-team-card.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nfl_online_neural_artifacts` | shared | `nfl-online-neural.js` | `nfl-engine-registry.js`, `nfl-online-neural.js` | /api/nfl-betting /api/betting /api/mlb /api/model +4 job(s) |
| `nfl_online_neural_examples` | shared | `nfl-online-neural.js` | `nfl-online-neural.js`, `nfl-risk-lab.js` | /api/nfl-betting /api/betting /api/mlb /api/nfl-market +4 job(s) |
| `nfl_orthogonal_specialist_artifacts` | shared | `nfl-orthogonal-specialists.js` | `nfl-orthogonal-specialists.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nfl_page_explain_audits` | shared | `nfl-page-explain-audit.js` | `nfl-page-explain-audit.js` | /api/betting |
| `nfl_pfr_adv` | shared | `nfl-advanced.js` | `nfl-advanced.js`, `nfl-availability.js`, `nfl-player-value.js`, `nfl-weekly-feature-store-v2.js` | /api/model /api/nfl-betting /api/accolades /api/aggregates +3 job(s) |
| `nfl_pick_decisions` | betting | `nfl-decision-tape.js` | `nfl-expert-council.js`, `run-historical-leaderboard.mjs` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nfl_pick_explanation_audits` | betting | `nfl-pick-explanation-audit.js` | `nfl-pick-explanation-audit.js` | /api/nfl-betting |
| `nfl_pick_path_table` | betting | **nothing** | **nothing** | — |
| `nfl_pick_watch_log` | betting | `nfl-pick-watch.js` | `nfl-pick-watch.js` | /api/betting /api/accolades /api/aggregates /api/espn +2 job(s) |
| `nfl_play_by_play` | shared | `nfl-espn-pbp.js` | `live-edge.js`, `nfl-espn-pbp.js`, `nfl-live-ledger.js`, `nfl-postgame-truth.js` | /api/betting /api/nfl-betting /api/accolades /api/aggregates +3 job(s) |
| `nfl_play_charting` | shared | `nfl-formations.js` | `nfl-formations.js`, `nfl-weekly-feature-store-v2.js`, `nfl-weekly-feature-store.js`, `nfl-2022-2025-rebuild.mjs` | /api/nfl-betting /api/trades /api/accolades /api/aggregates +2 job(s) |
| `nfl_play_formations` | shared | `nfl-formations.js` | `nfl-formations.js`, `nfl-weekly-feature-store-v2.js`, `nfl-weekly-feature-store.js`, `nfl-2022-2025-rebuild.mjs` | /api/nfl-betting /api/trades /api/accolades /api/aggregates +2 job(s) |
| `nfl_player_feature_vectors` | shared | `nfl-weekly-feature-store-v2.js`, `nfl-weekly-feature-store.js` | `nfl-weekly-feature-store-v2.js`, `nfl-weekly-feature-store.js`, `grade-feature-vector.mjs` | /api/nfl-betting /api/trades /api/accolades /api/aggregates +2 job(s) |
| `nfl_player_roster_events` | shared | `nfl-player-state.js` | `nfl-player-state.js`, `nfl-roster-strength.js`, `nfl-team-card.js` | /api/nfl /api/nfl-betting /api/analysis /api/dev +3 job(s) |
| `nfl_player_state_quarantine` | shared | `nfl-player-state.js` | `nfl-player-state.js` | /api/nfl /api/analysis /api/dev /api/nfl-betting +2 job(s) |
| `nfl_player_week_features` | shared | `nfl-pbp.js`, `nflverse.js` | `football-context.js`, `nfl-expert-council.js`, `nfl-passing-specialists.js`, `nfl-pbp.js` | /api/betting /api/model /api/nfl-betting /api/props +3 job(s) |
| `nfl_policy_audits` | betting | `nfl-replay.js` | `nfl-replay.js` | /api/nfl-betting /api/nfl-market /api/betting /api/mlb +1 job(s) |
| `nfl_postgame_truth_packets` | shared | `nfl-postgame-truth.js` | `nfl-postgame-truth.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nfl_pregame_snapshot_history` | shared | `nfl-pregame.js` | `nfl-evidence.js`, `nfl-player-value.js`, `nfl-pregame.js` | /api/nfl-betting /api/nfl-market /api/accolades /api/aggregates +4 job(s) |
| `nfl_prop_calibration_fits` | betting | `nfl-prop-calibration.js` | `decay-watch.js`, `nfl-prop-calibration.js` | /api/betting /api/nfl-betting /api/accolades /api/aggregates +2 job(s) |
| `nfl_prop_clv` | betting | `nfl-prop-clv.js` | `nfl-prop-clv.js`, `scheduler.js` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `nfl_prop_policy_archive` | betting | `nfl-prop-clv.js` | `nfl-prop-clv.js` | /api/betting /api/nfl-betting /api/accolades /api/aggregates +2 job(s) |
| `nfl_prop_quote_snapshots` | betting | `nfl-props.js`, `prop-feeds.js` | `nfl-prop-clv.js`, `nfl-props.js`, `prop-feeds.js`, `audit-props-total-consistency.mjs` | /api/betting /api/nfl-betting /api/nfl-market /api/accolades +2 job(s) |
| `nfl_qbr_weekly` | shared | `nfl-qbr.js`, `flag-qbr-2026-placeholders.mjs` | `nfl-qb-quality.js`, `nfl-qbr.js`, `analyze-qbr-fantasy-signal.mjs`, `flag-qbr-2026-placeholders.mjs` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `nfl_quote_batches` | betting | `032_quote_receipt_clock.js`, `nfl-quote-tape.js` | `032_quote_receipt_clock.js`, `nfl-evidence-dataset.js`, `nfl-execution-replay.js`, `nfl-quote-tape.js` | /api/nfl-betting /api/betting /api/nfl-market /api/accolades +4 job(s) |
| `nfl_quote_tape` | betting | `nfl-quote-tape.js` | `teaser-scan.js`, `nfl-evidence-dataset.js`, `nfl-execution-clv.js`, `nfl-execution-pipeline.js` | /api/betting /api/nfl-betting /api/nfl-market /api/accolades +4 job(s) |
| `nfl_rebuild_checkpoints` | shared | `nfl-2022-2025-rebuild.mjs` | `nfl-rebuild-progress.js`, `nfl-2022-2025-rebuild.mjs` | /api/nfl-betting |
| `nfl_rebuild_progress` | shared | `nfl-2022-2025-rebuild.mjs` | `nfl-rebuild-progress.js`, `nfl-2022-2025-rebuild.mjs` | /api/nfl-betting |
| `nfl_replay_bets` | shared | `nfl-replay.js` | **nothing** | — |
| `nfl_replay_runs` | shared | `nfl-replay.js` | **nothing** | — |
| `nfl_residual_audits` | shared | `nfl-research.js` | `nfl-research.js`, `backfill-historical-trial-registry.mjs` | /api/nfl-market |
| `nfl_risk_lab_artifacts` | betting | `nfl-risk-lab.js` | `nfl-engine-registry.js`, `nfl-risk-lab.js` | /api/nfl-betting /api/betting /api/mlb /api/model +3 job(s) |
| `nfl_risk_lab_predictions` | betting | `nfl-risk-lab.js` | `nfl-risk-lab.js` | /api/nfl-betting /api/mlb /api/nfl-market /api/accolades +2 job(s) |
| `nfl_rookie_evidence` | shared | `nfl-rookies.js` | `nfl-offseason-cycle.js`, `nfl-rookie-ingest.js`, `nfl-rookies.js`, `nfl-roster-strength.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +3 job(s) |
| `nfl_roster_snapshots` | shared | `nfl-player-state.js` | `draft-assist.js`, `nfl-player-state.js`, `offseason-model.js`, `preseason-model.js` | /api/drafts /api/nfl /api/trades /api/analysis +2 job(s) |
| `nfl_rule_state` | shared | `beat-the-close.js` | `beat-the-close.js` | /api/nfl-market /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nfl_scottfree_game_features` | shared | `022_scottfree_game_features.js`, `import-scottfree.mjs` | **nothing** | — |
| `nfl_sgp_quotes` | shared | `014_profit_execution_triggers.js`, `nfl-prop-correlation.js` | `nfl-prop-correlation.js` | /api/betting |
| `nfl_signal_reliability_artifacts` | shared | `nfl-signal-reliability.js` | `nfl-engine-registry.js`, `nfl-signal-reliability.js` | /api/nfl-betting /api/betting /api/model /api/nfl-market +3 job(s) |
| `nfl_signal_snapshots` | shared | `beat-the-close.js` | `beat-the-close.js` | /api/nfl-market /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nfl_sim_calibration_artifacts` | shared | `nfl-sim-calibration.js` | `nfl-sim-calibration.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +3 job(s) |
| `nfl_snaps` | fantasy | `nfl-advanced.js` | `nfl-advanced.js`, `nfl-availability.js`, `nfl-data-consistency.js`, `nfl-evidence.js` | /api/model /api/nfl-betting /api/nfl-market /api/accolades +4 job(s) |
| `nfl_source_registry` | shared | `nfl-evidence.js` | `nfl-evidence.js` | /api/nfl-betting /api/nfl-market /api/betting /api/execution-slate |
| `nfl_stadiums` | shared | `nfelo.js` | **nothing** | — |
| `nfl_t60_observations` | shared | `t60-runner.js`, `034_t60_runner_ledger.js` | `t60-runner.js`, `034_t60_runner_ledger.js`, `036_t60_packet_body.js` | /api/accolades /api/aggregates /api/betting /api/espn +4 job(s) |
| `nfl_team_cards` | shared | `nfl-team-card.js` | `nfl-orthogonal-specialists.js`, `nfl-team-card.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nfl_team_coaches` | shared | `nfl-coaches.js` | `nfl-coaches.js`, `nfl-team-card.js`, `offseason-model.js` | /api/model /api/nfl-betting /api/accolades /api/aggregates +2 job(s) |
| `nfl_team_feature_vectors` | shared | `nfl-weekly-feature-store-v2.js`, `nfl-weekly-feature-store.js` | `nfl-weekly-feature-store-v2.js`, `nfl-weekly-feature-store.js`, `grade-feature-vector.mjs` | /api/nfl-betting /api/trades /api/accolades /api/aggregates +2 job(s) |
| `nfl_team_stadiums` | shared | `nfelo.js` | **nothing** | — |
| `nfl_team_week_features` | shared | `nfl-pbp.js` | `nfl-betting.js`, `trades.js`, `football-context.js`, `nfl-data-consistency.js` | /api/nfl-betting /api/trades /api/model /api/nfl-market +5 job(s) |
| `nfl_teams` | shared | `index.js`, `analysis.js`, `nfldata.js`, `teams.js` | `index.js`, `052_line_snapshot_receipt_clock.js`, `ingest.js`, `twitter-ingest.js` | /api/accolades /api/aggregates /api/analysis /api/drafts +5 job(s) |
| `nfl_teaser_execution_legs` | betting | `012_teaser_execution_ledger.js`, `wong.js`, `nfl-teaser-execution.js` | `nfl-profitability.js`, `nfl-teaser-execution.js` | /api/betting /api/execution-slate /api/nfl-betting |
| `nfl_teaser_executions` | betting | `012_teaser_execution_ledger.js`, `wong.js`, `nfl-teaser-execution.js` | `wong.js`, `nfl-profitability.js`, `nfl-teaser-execution.js`, `run-historical-leaderboard.mjs` | /api/betting /api/execution-slate /api/nfl-betting |
| `nfl_teaser_price_ledger` | betting | `nfl-profitability.js` | `teaser-scan.js`, `teaser-season.js`, `wong.js`, `alt-spread-import.js` | /api/betting /api/execution-slate /api/nfl-betting |
| `nfl_test_only_table` | shared | **nothing** | **nothing** | — |
| `nfl_top100` | shared | `accolades.js` | `accolades.js`, `edge.js`, `nfldata.js` | /api/accolades /api/edge /api/nfl /api/analysis +2 job(s) |
| `nfl_total_calibrations` | shared | `nfl-total-calibration.js` | `nfl-total-calibration.js` | /api/betting /api/nfl-betting /api/accolades /api/aggregates +2 job(s) |
| `nfl_total_picks` | shared | `nfl-props.js` | `nfl-pick-watch.js`, `nfl-props.js` | /api/betting /api/nfl-betting /api/accolades /api/aggregates +2 job(s) |
| `nfl_tweet_line_watch` | shared | `nfl-tweet-line-correlation.js` | `nfl-profitability.js`, `nfl-tweet-line-correlation.js` | /api/news /api/nfl-betting /api/accolades /api/aggregates +2 job(s) |
| `nfl_two_hop_table` | shared | **nothing** | **nothing** | — |
| `nfl_user_bets` | shared | `nfl-user-bets.js` | `nfl-user-bets.js` | /api/nfl-market |
| `nfl_validation_windows` | shared | `nfl-evidence.js` | `nfl-evidence.js` | /api/nfl-betting /api/nfl-market /api/betting /api/execution-slate |
| `nfl_verified_events` | shared | `nfl-event-archive.js` | `nfl-event-archive.js`, `nfl-news-market-latency.js` | /api/nfl-betting /api/nfl-market /api/accolades /api/aggregates +2 job(s) |
| `nfl_weekly_expert_examples` | shared | `nfl-expert-council.js` | `nfl-expert-coordinator.js`, `nfl-expert-council.js`, `nfl-postgame-truth.js`, `nfl-slice-diagnostic.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +2 job(s) |
| `nflverse_player_positions` | shared | `nflverse.js` | `dynasty-age-curve.js`, `nfl-pbp.js`, `nflverse.js`, `offseason-data.js` | /api/model /api/nfl-betting /api/props /api/tradelab +3 job(s) |
| `odds_cache` | shared | `odds-api.js` | `odds-api.js` | /api/betting /api/mlb /api/nfl-betting /api/accolades +3 job(s) |
| `odds_usage` | shared | `odds-api.js` | `odds-api.js` | /api/betting /api/mlb /api/nfl-betting /api/accolades +3 job(s) |
| `off_contracts` | shared | `offseason-data.js` | `offseason-data.js` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `off_depth_chart` | shared | `offseason-data.js` | `offseason-data.js` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `off_draft_picks` | shared | `offseason-data.js` | `offseason-data.js` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `off_ngs_season` | shared | `offseason-data.js` | `offseason-data.js` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `off_pfr_adv_season` | shared | `offseason-data.js` | `offseason-data.js` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `off_player_season_features` | shared | `offseason-data.js` | `offseason-data.js`, `offseason-model.js`, `preseason-model.js` | /api/drafts /api/model /api/players /api/trades +2 job(s) |
| `off_qbr_season` | shared | `offseason-data.js` | `offseason-data.js` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `off_rosters` | shared | `offseason-data.js` | `offseason-data.js` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `off_schedule_games` | shared | `offseason-data.js` | `offseason-data.js` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `off_sleeper_players` | shared | `offseason-data.js` | `offseason-data.js` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `off_team_season` | shared | `offseason-data.js` | `mlb-model-misc.js`, `nfl-team-strength.js` | /api/accolades /api/aggregates /api/analysis /api/auth +8 job(s) |
| `off_team_season_stats` | shared | `offseason-data.js` | `offseason-data.js` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `parlay_cache` | betting | `parlay-api.js` | `parlay-api.js` | /api/mlb /api/nfl-market |
| `parlay_usage` | betting | `parlay-api.js` | `parlay-api.js` | /api/mlb /api/nfl-market |
| `pick_values` | shared | `aggregates.js` | `picks.js` | /api/tradelab /api/model /api/players /api/trades +2 job(s) |
| `player_accolades` | fantasy | `accolades.js`, `nfldata.js` | `accolades.js`, `edge.js`, `nfldata.js`, `draft-assist.js` | /api/accolades /api/edge /api/nfl /api/analysis +3 job(s) |
| `player_analysis` | fantasy | `players.js` | `edge.js`, `players.js` | /api/edge /api/players /api/tradelab /api/model +2 job(s) |
| `player_gamelog` | fantasy | `edge.js` | `edge.js`, `draft-assist.js`, `lineup-brain.js`, `matchups.js` | /api/edge /api/drafts /api/model /api/nfl +2 job(s) |
| `player_head_audits` | fantasy | `player-head-validation.js` | `gridiron-model.js`, `player-head-validation.js` | /api/model /api/nfl-betting /api/dev /api/leagues +2 job(s) |
| `player_identity_repairs` | fantasy | **nothing** | **nothing** | — |
| `player_metrics` | fantasy | `aggregates.js` | `core-and-fantasy.js`, `aggregates.js`, `dev.js`, `edge.js` | /api/aggregates /api/dev /api/edge /api/leagues +8 job(s) |
| `player_season_stats` | fantasy | `stats.js` | `dev.js`, `edge.js`, `nfldata.js`, `stats.js` | /api/dev /api/edge /api/nfl /api/stats +2 job(s) |
| `player_team_changes` | fantasy | `espn.js` | `espn.js`, `nfl-preseason-blend.js`, `nfl-roster-strength.js` | /api/espn /api/aggregates /api/dev /api/nfl-betting +3 job(s) |
| `player_week_snaps` | fantasy | `nflverse.js` | `contingency.js`, `news-fantasy-impact.js`, `offseason-data.js`, `opportunity-model.js` | /api/model /api/news /api/players /api/trades +2 job(s) |
| `player_week_usage` | fantasy | `nflverse.js` | `trades.js`, `backtest.js`, `boom-bust.js`, `bottom-up-team-total.js` | /api/trades /api/betting /api/model /api/news +2 job(s) |
| `players` | fantasy | `index.js`, `aggregates.js`, `espn.js`, `players.js` | `index.js`, `store.js`, `ingest.js`, `twitter-ingest.js` | /api/aggregates /api/analysis /api/drafts /api/edge +3 job(s) |
| `polymarket_line_moves` | betting | `polymarket-lines.js` | `polymarket-lines.js`, `signal-latency.js` | /api/betting /api/nfl-market /api/accolades /api/aggregates +2 job(s) |
| `polymarket_markets` | betting | `polymarket.js` | `live-edge.js`, `polymarket-lines.js`, `polymarket.js`, `execution-fill-real-book-validation.mjs` | /api/betting /api/nfl-market /api/accolades /api/aggregates +3 job(s) |
| `polymarket_order_book_levels` | betting | `044_polymarket_order_book_levels.js`, `polymarket.js` | `polymarket.js` | /api/betting /api/accolades /api/aggregates /api/espn +3 job(s) |
| `polymarket_price_history` | betting | `polymarket.js` | `polymarket.js` | /api/betting /api/accolades /api/aggregates /api/espn +3 job(s) |
| `polymarket_quotes` | betting | `polymarket.js` | `live-edge.js`, `polymarket-lines.js`, `polymarket.js`, `execution-fill-real-book-validation.mjs` | /api/betting /api/nfl-market /api/accolades /api/aggregates +3 job(s) |
| `prediction_market_flow` | betting | `prediction-markets.js` | `prediction-markets.js` | /api/betting /api/accolades /api/aggregates /api/espn +3 job(s) |
| `prediction_market_quotes` | betting | `prediction-markets.js` | `prediction-markets.js` | /api/betting /api/accolades /api/aggregates /api/espn +3 job(s) |
| `press_availability` | shared | `press-conference.js` | `nfl-news-events.js`, `press-conference.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +3 job(s) |
| `press_conferences` | shared | `press-conference.js` | `nfl-news-events.js`, `press-conference.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +3 job(s) |
| `prop_correlation_estimates` | shared | `nfl-prop-correlation.js` | `nfl-prop-correlation.js` | /api/betting |
| `prop_head_audits` | shared | `nfl-prop-head-validation.js` | `nfl-prop-head-validation.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +2 job(s) |
| `props_auto_picks` | shared | `props.js` | `betting-hub.js`, `props.js` | /api/betting /api/props |
| `ranking_entries` | shared | `index.js`, `aggregates.js`, `rankings.js` | `drafts.js`, `edge.js`, `players.js`, `rankings.js` | /api/drafts /api/edge /api/players /api/rankings +2 job(s) |
| `ranking_sets` | shared | `index.js`, `aggregates.js`, `rankings.js` | `index.js`, `drafts.js`, `players.js`, `rankings.js` | /api/drafts /api/players /api/rankings |
| `research_hypotheses` | shared | `model-intelligence.js` | `model-intelligence.js` | /api/mlb /api/nfl-market |
| `research_trials` | shared | `038_research_trials.js`, `research-trials.js` | `research-trials.js` | /api/nfl-market |
| `roster_players` | fantasy | `nfldata.js` | `accolades.js`, `analysis.js`, `dev.js`, `edge.js` | /api/accolades /api/analysis /api/dev /api/edge +4 job(s) |
| `saved_prop_tickets` | shared | `018_saved_prop_tickets.js`, `props-tickets.js` | `props-tickets.js` | /api/props-tickets |
| `schedule_games` | shared | `nfldata.js` | `edge.js`, `nfldata.js`, `wong.js`, `draft-assist.js` | /api/betting /api/edge /api/nfl /api/analysis +2 job(s) |
| `schema_migrations` | shared | `index.js`, `migrate.js` | `index.js`, `migrate.js`, `schema-snapshot.mjs` | /api/accolades /api/aggregates /api/analysis /api/auth +8 job(s) |
| `schema_preflight` | shared | `preflight.js` | **nothing** | — |
| `scout_reports` | shared | `edge.js` | `edge.js` | /api/edge /api/tradelab /api/model /api/players +2 job(s) |
| `sgo_usage` | shared | `sportsgameodds.js` | `sportsgameodds.js` | /api/nfl-market /api/accolades /api/aggregates /api/betting +2 job(s) |
| `sh_crawl` | shared | `collect-sleeper-history.mjs` | `collect-sleeper-history.mjs` | — |
| `sh_leagues` | shared | `collect-sleeper-history.mjs` | `collect-sleeper-history.mjs` | — |
| `sh_team_seasons` | shared | `collect-sleeper-history.mjs` | **nothing** | — |
| `sh_team_weeks` | shared | `collect-sleeper-history.mjs` | **nothing** | — |
| `sh_transactions` | shared | `collect-sleeper-history.mjs` | **nothing** | — |
| `shadow_decisions` | shared | `beat-the-close.js`, `shadow-ledger.js` | `beat-the-close.js`, `nfl-blind-audit.js`, `nfl-evidence.js`, `nfl-signal-reliability.js` | /api/nfl-betting /api/nfl-market /api/accolades /api/aggregates +3 job(s) |
| `shrinkage_fits` | fantasy | `shrinkage-fit.js` | `shrinkage-fit.js` | /api/model /api/dev /api/drafts /api/leagues |
| `shrinkage_k` | fantasy | `shrinkage-fit.js` | `shrinkage-fit.js` | /api/model /api/dev /api/drafts /api/leagues |
| `slot_weakness` | shared | `accolades.js`, `nfldata.js` | `nfldata.js` | /api/nfl /api/analysis /api/dev /api/players +2 job(s) |
| `sync_log` | shared | `061_sync_log_consecutive_failures.js`, `scheduler.js`, `build-manager-signals.mjs`, `collect-league-transactions.mjs` | `scheduler.js`, `signal-latency.js` | /api/accolades /api/aggregates /api/betting /api/espn +2 job(s) |
| `team_cap` | fantasy | `nfldata.js` | `analysis.js`, `dev.js`, `nfldata.js` | /api/analysis /api/dev /api/nfl /api/players +2 job(s) |
| `trade_proposal_cache` | fantasy | `060_trade_proposal_cache.js`, `trade-proposals.js` | `trade-proposals.js` | /api/trades |
| `trend_findings` | shared | `trend-watch.js` | `trend-watch.js` | /api/trades |
| `trending_players` | shared | `tradelab.js` | `tradelab.js`, `trade-engine.js` | /api/tradelab /api/model /api/players /api/trades +2 job(s) |
| `twitterapi_io_usage` | shared | `twitterapi-io.js` | `twitter-ingest.js`, `twitterapi-io.js` | /api/news /api/accolades /api/aggregates /api/betting +2 job(s) |
| `user_identities` | shared | `062_google_identity_and_invites.js`, `account-link.js` | `account-link.js`, `google-auth.js` | /api/auth |
| `users` | shared | `006_identity_and_draft_authorization.js`, `account-link.js`, `provision-auth.js`, `google-auth.js` | `006_identity_and_draft_authorization.js`, `008_model_actor_foreign_keys.js`, `009_authoritative_actor_and_ownership_guards.js`, `062_google_identity_and_invites.js` | /api/auth /api/betting /api/decision-inbox /api/drafts +2 job(s) |
| `weekly_ensemble_fits` | shared | `weekly-weight-store.js` | `nfl-engine-registry.js`, `weekly-learning.js`, `weekly-weight-store.js` | /api/model /api/nfl-betting /api/nfl-market /api/accolades +3 job(s) |
| `weekly_prediction_snapshots` | shared | `weekly-learning.js` | `weekly-learning.js` | /api/model /api/nfl-betting /api/nfl-market /api/accolades +2 job(s) |
| `yt_channels` | shared | `press-conference.js` | `nfl-news-events.js`, `press-conference.js` | /api/nfl-betting /api/accolades /api/aggregates /api/betting +3 job(s) |

## Scheduler jobs

| job | tier | module |
| --- | --- | --- |
| `mlb_schedule` | live | `null` |
| `mlb_logs` | heavy | `null` |
| `mlb_boxscores` | live | `null` |
| `mlb_probables` | live | `null` |
| `mlb_tomorrow_picks` | heavy | `null` |
| `player_rosters` | live | `null` |
| `nflverse_crosswalk` | growth | `null` |
| `nflverse_weekly_usage` | growth | `null` |
| `nflverse_snap_counts` | growth | `null` |
| `espn_depth_chart` | growth | `null` |
| `espn_season_stats` | growth | `null` |
| `sleeper_players` | growth | `null` |
| `espn_rosters` | growth | `null` |
| `league_rosters` | live | `null` |
| `nfl_lines` | live | `null` |
| `nfl_forward_settle` | live | `null` |
| `nfl_line_snapshots` | metered | `null` |
| `nfl_sgo_snapshot` | metered | `null` |
| `nfl_prop_feeds` | metered | `null` |
| `nfl_book_feeds_extra` | live | `null` |
| `nfelo_sync` | growth | `null` |
| `nfl_external_ratings` | growth | `null` |
| `nfl_forecast_history` | heavy | `null` |
| `nfl_book_feeds_fast` | live | `null` |
| `nfl_book_feeds_slow` | live | `null` |
| `nfl_qbr_weather` | growth | `null` |
| `beat_the_close` | metered | `null` |
| `nfl_pick_watch` | live | `null` |
| `nfl_t60_runner` | live | `server/betting/nfl/strategy/t60-runner.js` |
| `nfl_learned_shadow` | growth | `server/betting/nfl/strategy/learned-shadow-runner.js` |
| `polymarket_line_watch` | live | `server/services/nfl-espn-line-watch.js` |
| `espn_line_watch` | live | `server/services/nfl-espn-line-watch.js` |
| `nfl_play_by_play` | live | `server/services/nfl-espn-pbp.js` |
| `prediction_markets` | live | `server/services/prediction-markets.js` |
| `polymarket` | live | `server/services/polymarket.js` |
| `press_conferences` | heavy | `server/services/press-conference.js` |
| `evidence_daemon` | live | `null` |
| `nfl_weekly_learning` | heavy | `null` |
| `nfl_model_growth` | growth | `null` |
| `nfl_offseason_depth_injury` | growth | `null` |
| `nfl_decision_ledger` | growth | `null` |
| `nfl_reports` | growth | `null` |
| `nfl_prop_calibration` | heavy | `null` |
| `fantasy_coordinator_refit` | heavy | `null` |
| `trade_asset_universe_warm` | growth | `null` |
| `manager_signals` | growth | `null` |
| `manager_archetypes` | heavy | `null` |
| `nfl_prop_capture` | metered | `null` |
| `nfl_prop_clv_free` | live | `null` |
| `rss_news` | live | `null` |
| `espn_news` | live | `null` |
| `nfl_news_signals` | live | `null` |
| `nfl_model_watch` | heavy | `null` |
| `decay_watch` | growth | `null` |
| `twitter_insiders` | metered | `null` |
| `nfl_injuries` | live | `null` |
| `nfl_transactions` | live | `null` |
| `nfl_rookie_public` | heavy | `null` |
| `cfbd_rookie_usage` | heavy | `null` |
| `team_analyses` | heavy | `null` |
| `nfl_coaches` | growth | `null` |
| `ffopportunity` | growth | `null` |

