# Isolated research environment; Python 3.11–3.13. No app-runtime dependency.
tpot==1.1.0
scikit-learn==1.7.2
setuptools<81  # TPOT dependency stopit still imports pkg_resources

# Package C extension (research/tree_lab.py): bounded model families beyond
# sklearn's own boosted/extra trees, used for movement regression, cover/over
# classification and quantile regression alike.
lightgbm==4.7.0
xgboost==3.4.1
catboost==1.2.10

# Package F (research/expert_selector_lab.py) imports scipy.optimize directly
# for the simplex-constrained meta-learner. scikit-learn already pulls scipy in
# transitively, but a direct import belongs in the direct dependency list —
# otherwise a future scikit-learn that dropped it would break this lab with a
# confusing ModuleNotFoundError instead of a resolver error.
scipy==1.18.1
