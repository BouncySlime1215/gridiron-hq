/** FIX-05 (RED stub): brain report + number health gate for the campaign producer. */
const notBuilt = () => { throw new Error('brain-gate: not built yet (FIX-05 RED)'); };
export const readBrainReport = notBuilt;
export const applyBrainReport = notBuilt;
export const brainReportSection = notBuilt;
export const readNumberHealth = notBuilt;
