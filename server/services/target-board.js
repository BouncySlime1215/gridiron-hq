/**
 * TM-03 target board — stub for the RED commit; the implementation lands in GREEN.
 */
export const TARGET_BOARD_THIN_N = 5;
export function targetBoard() { return { managers: new Map(), meta: null }; }
